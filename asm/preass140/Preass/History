    V1.35  --> V1.36

    IncBssBlock eingeführt.

    V1.34a --> V1.35

    Mathefunktionen um indizierte Variablen erweitert.

    V1.32 --> V1.34a

    In V1.33 kleine Fehler behoben

    In V1.34 neu Funktion eingesetzt
    
    In V1.34a alte, buggy Funktion IncFFPVar durch neue ersetzt

    V1.31 --> V1.32

    IncludeDatei für Preass eingeführt.1024/20 Ebenen können  genutzt
    werden,d.h. es können ca. 500 Interne Includes von Includes von
    Includes von Includes von Includes im Include des Includes usw...
    benutzet werden.

    Dazu gibts im Update neue GrundFiles und die Standart Includes.

    {* Optimierung an *} (Default: Aus)
    
    Ruft den externen Optimizer auf.

    V1.30a --> V1.31

    Neue Funktionen:

    1. IncArray[Type]
    2. String
    3. IncFFPVar
    4. NewProc
    5. Include          Noch in der BetaPhase

    Bugfix:     Mehrfachanweisungen nach IF laufen nun wieder.

    MEGABUGFIX: Ein cmpi.b #`"`,(XX) oder vergleichbares brachte das
                blanke Chaos! Loesung der Angelegenheit , ein vergleich
                achtete nicht auf ZeilenEnde! zwei simple Befehle und es
                ging, ich könnt mich schlagen,danach hab ich seit Monden
                gesucht!

    V1.30 --> V1.30a 

    Bugfix: Ein Fragezeichen in diversen Zeilen führte zum Abbruch des
            Compilers,ist behoben.

    V1.29  --> V1.30:

    Kleines Bugfix: Preasssourcecode ASL3.p funktioniert nun.
                    Preass läuft auch auf 68000 wieder (sollte!).

    Stringoperationen sind nun möglich .
    
    Bei der Namensgebung der Strings sind jetzt viele Sonderzeichen
    erlaubt, was vorher zu einigen Problemen geführt hat.Ausserdem 
    wird jeden String eine Nummer verpasst , was Dupes ausschliessen
    sollte.Es kann allerdings nötig sein, seinen Source zuüberholen.

    If-Do-Anweisungen führen einen "Libraryflush" nun vor dem Ausführen
    ihrer DO-Anweisung aus.So sind weniger Probleme mit LibraryBases
    vorhanden.
    {* Flush *} eingeführt 
    Im kompletten Archiv befindet sich das Programm Preass_Gui,in der
    FRAS/Mini-Bins-Ausgabe ist es, der Länge wegen, nicht vorhanden,
    falls es die Mini-Bins-Ausgabe geben wird.

    V1.28  --> V1.29:

    Kleines Fixing der Anleitung; * AutoLVO * ; PreAss zeigt im Process-
    window den Namen des Files an , das übersetzt wird , ist drin für
    die Überwachung von automatischen Compilaten (CED,GOLDED usw.).

    V1.27c --> V1.28: 

    Bugfix ; Ausgabe eingeführt ; Verbesserung der Eingabezeilenaus-
    wertung; WB2F und WB2B .

    Preass erstellt nun auch selbstständig den Zielnamen,falls dieser
    nicht angegeben wird.

    V1.27a --> V1.27b --> V1.27c:

    Bugfix : In Variablen wurde eine Zeichenfolge "IF" fuer den 
             Befehl IF gehalten.Behoben.

    V1.27 --> V1.27a:

    Bugfix

    V1.26 --> V1.27 :

    Bei If Anweisungen kann nun auch Kalkuliert werden und es koennen
    mehrere Anweisungen in einer Zeile stehen.

    V1.25 --> V1.26 :

    IF XXX=YYY Do Funktion():---> LABEL

    Bugfix: Dummy=Funktion()    ;Kommentar

            funktioniert nun wieder! 

    V1.24 --> V1.25 :

    Neue Befehle: Return und INITxxx

    V1.23 --> V1.24 :

    IntegerMathematik

    V1.22 --> V1.23 :

    IF Vergleiche eingefuehrt.

    V1.21 --> V1.22 :

    GUIenv eingefügt.

    V1.20 --> V1.21 :

    Einige Suchroutinen sind leicht optimiert worden.Einführung von
    Version.Kleiner Fehler in der Globallabel Erkennung behoben.Spart
    Sourcecode.
    
    V1.15 --> V1.20 :

    UseFD,FlashFD,Autolib,Start,Delayaus eingeführt. Speedup durch 
    bearbeiten der FD-Files.

    V1.14 --> V1.15 :

    NoLibnames eingeführt.

