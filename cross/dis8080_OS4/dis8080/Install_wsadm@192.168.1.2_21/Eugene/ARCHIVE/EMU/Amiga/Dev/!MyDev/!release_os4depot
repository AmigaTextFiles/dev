;
; $VER: dis8080
;
;=============================================================================
; Set defaults

(set #appName "dis8080")
(set #defaultDest "RAM:")
(set #destination "C:")

;=============================================================================
; Copy main executable
(complete 50)

(copyfiles
    (prompt "Install app")
	(source #appName)
	(newname #appName)
	(dest (tackon #appName #destination))
	(help @copyfiles-help)
	(confirm)
)
;=============================================================================
; Final message
(complete 100)

(message "Installation complete!")

;----------------------------------------------------------------------------
(exit (QUIET))
