(if (patmatch "(68040|68060)" (database "cpu"))
  (set #client "MakeSFX.040")
  (if (patmatch "(68020|68030)" (database "cpu"))
    (set #client "MakeSFX.020")
    (set #client "MakeSFX.000")
  )
)

(copyfiles
  (help @copyfiles-help)
  (source #client)
  (dest "LIBS:xad")
  (newname "MakeSFX")
)
