(if (patmatch "(68040|68060)" (database "cpu"))
  (set #client "MS-TNEF.040")
  (if (patmatch "(68020|68030)" (database "cpu"))
    (set #client "MS-TNEF.020")
    (set #client "MS-TNEF.000")
  )
)

(copyfiles
  (help @copyfiles-help)
  (source #client)
  (dest "LIBS:xad")
  (newname "MS-TNEF")
)
