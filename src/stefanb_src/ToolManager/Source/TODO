Library:

 -

Prefs:

 - Support save/load of group open/close state?
 - Keyboard shortcuts don't work?

Misc:

 -

Future:

 - When shift is pressed while selecting an entry in a popup dock then the
   dock is not closed.
 - Fix Listtree drag scrolling (requires bug-fixed Listtree.mcc)
 - Fix drop position in dock entry list (requires documented/improved List.mui)
 - Reimplement Network support and Access objects (is it worth the effort?)
 - Library function to get Dock as BOOPSI gadget for inclusion in a window
