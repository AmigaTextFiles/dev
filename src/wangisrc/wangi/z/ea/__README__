ea:

Similar to uuencode but only converts required characters and adds run-length
compression. Never finished. I'll probBLY continue this one on another platform...

The source has artefacts from chunker