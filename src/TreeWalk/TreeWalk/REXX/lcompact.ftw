/*
 * Take each directory/file pair, and build an lcompact command to
 * take things from isrc to INCLUDE:, compacting as we go. Note: the
 * treewalk filter should filter out directories before lcompact is
 * called.
 */
isrc = 'src:include/'

dir = arg(1)
file = arg(2)
parse var dir (isrc) out
address command 'lcompact <'dir || file '>include:'out || file
return 1
