.bra {
.ket }
.key file
FailAT 1
Stack 50000
echo "Converting..."
F2c {file}.f 
IF NOT WARN
	echo "Compiling..." 
	cc -md -mc -ms -wo -so -fm {file} >NIL:
	ln +Q {file} mktemp -lf2c -lml -lcl -lf2c >NIL:
	List {file}
ELSE
	Echo "There were errors!"
ENDIF
