// Program 8.2

PRINT Count("ab","abcdababefgab")

FUNC Count(pattern$,text$)
  LOCAL Cnt, pos
  Cnt:=0
  pos:=pattern$ IN text$
  IF pos>0 THEN
    Cnt:=1+Count(pattern$,text$(pos+1..))
  ENDIF
  RETURN Cnt
ENDFUNC Count
