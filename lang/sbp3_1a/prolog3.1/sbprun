stack 32767

;	check if environment is setup for the search paths.
;	if not issue the setenv to set it up.


;if not exists ENV:SIMPATH then
   echo "setting up the environment paths."
   setenv SIMPATH=.!PLg:Prolog3.1/modlib!Plg:Prolog3.1/lib!Plg:Prolog3.1/cmplib!
;   setenv SIMPATH=!.!modlib!lib!/cmplib!
;   setenv SIMPATH=:modlib:lib:cmplib:
;endif


PLg:Prolog3.1/sbprolog -s -p 1000000 -m 400000 modlib/$amyrdloop

