/* get command line args */

PROC main(ARG)
  MCHAR argadr+argl-1,0           /* now terminate argstring with a 0 */
  IF argl=1
    WRITE 'No Args!\n'
  ELSE
    WRITE 'You wrote: \s\n',argadr
  ENDIF
ENDPROC
