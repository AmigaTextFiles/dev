	- Can't be RUN ... can't see how to fix this without prejudice
	to ability to accept command line redirection.

	- Should have used CATCH and THROW in place of very old-style
	ABORT and ABORT" techniques. Using CATCH and THROW at system
	level allows user apps to trap system errors. Jax4th for
	Windows NT uses the new technology correctly.

	- ( "paren" doesn't span lines in files.

	- UNUSED is hard-wired, and to the wrong value.
