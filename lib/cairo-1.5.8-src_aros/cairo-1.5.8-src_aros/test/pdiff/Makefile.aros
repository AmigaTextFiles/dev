
SUBMAKEFILE = 1

include ../../Makefile.aros

CFLAGS += -I../../src

SOURCES = \
	lpyramid.c \
	pdiff.c \
	$(NULL)

OBJECTS = $(subst .c,.o,$(SOURCES))

all: libpdiff.a

libpdiff.a: $(OBJECTS)
	$(AR) rs $@ $(OBJECTS)
