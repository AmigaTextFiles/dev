Short:        MP3 LAME encoding engine
Author:       Various authors
Uploader:     Fredrik Wikstrom <fredrik@a500.org>
Type:         dev/lib
Version:      3.98.2
Architecture: ppc-amigaos

This archive contains libmp3lame compiled both as a static library (.a) and a
shared object (.so). Both are compiled using the source code from Stephan
Rupprecht's lame 3.98.2 port.

Installation:

Copy SDK/#? all SDK:

You might also want to copy libmp3lame.so into SYS:SObjs.
