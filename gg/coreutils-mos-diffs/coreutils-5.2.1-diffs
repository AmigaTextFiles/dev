diff -ruN coreutils-5.2.1-orig/config/mdate-sh coreutils-5.2.1/config/mdate-sh
--- coreutils-5.2.1-orig/config/mdate-sh	Mon Jan 12 20:48:37 2004
+++ coreutils-5.2.1/config/mdate-sh	Tue Mar 22 23:08:49 2005
@@ -62,9 +62,9 @@
 
 # Find out how to get the extended ls output of a file or directory.
 if ls -L /dev/null 1>/dev/null 2>&1; then
-  ls_command='ls -L -l -d'
+  ls_command='/bin/ls -L -l -d'
 else
-  ls_command='ls -l -d'
+  ls_command='/bin/ls -l -d'
 fi
 
 # A `ls -l' line looks as follows on OS/2.
diff -ruN coreutils-5.2.1-orig/configure coreutils-5.2.1/configure
--- coreutils-5.2.1-orig/configure	Thu Mar 11 08:58:11 2004
+++ coreutils-5.2.1/configure	Sun Feb 27 01:09:17 2005
@@ -312,7 +312,8 @@
 #endif"
 
 ac_config_libobj_dir=lib
-ac_subst_vars='SHELL PATH_SEPARATOR PACKAGE_NAME PACKAGE_TARNAME PACKAGE_VERSION PACKAGE_STRING PACKAGE_BUGREPORT exec_prefix prefix program_transform_name bindir sbindir libexecdir datadir sysconfdir sharedstatedir localstatedir libdir includedir oldincludedir infodir mandir build_alias host_alias target_alias DEFS ECHO_C ECHO_N ECHO_T LIBS INSTALL_PROGRAM INSTALL_SCRIPT INSTALL_DATA CYGPATH_W PACKAGE VERSION ACLOCAL AUTOCONF AUTOMAKE AUTOHEADER MAKEINFO AMTAR install_sh STRIP ac_ct_STRIP INSTALL_STRIP_PROGRAM mkdir_p AWK SET_MAKE am__leading_dot DEFAULT_POSIX2_VERSION CC CFLAGS LDFLAGS CPPFLAGS ac_ct_CC EXEEXT OBJEXT DEPDIR am__include am__quote AMDEP_TRUE AMDEP_FALSE AMDEPBACKSLASH CCDEPMODE am__fastdepCC_TRUE am__fastdepCC_FALSE CPP EGREP PERL RANLIB ac_ct_RANLIB LN_S build build_cpu build_vendor build_os host host_cpu host_vendor host_os GNU_PACKAGE HELP2MAN OPTIONAL_BIN_PROGS MAN DF_PROG U ANSI2KNR LIBOBJS STDBOOL_H HAVE__BOOL ALLOCA ALLOCA_H FNMATCH_H NEED_SETGID KMEM_GROUP GETLOADAVG_LIBS POW_LIB YACC LIB_CLOCK_GETTIME LIB_NANOSLEEP SEQ_LIBM GLIBC21 LIBICONV LTLIBICONV SQRT_LIBM FESETROUND_LIBM LIB_CRYPT MKINSTALLDIRS USE_NLS MSGFMT GMSGFMT XGETTEXT MSGMERGE INTLLIBS LIBINTL LTLIBINTL POSUB LTLIBOBJS'
+ac_subst_vars='SHELL PATH_SEPARATOR PACKAGE_NAME PACKAGE_TARNAME PACKAGE_VERSION PACKAGE_STRING PACKAGE_BUGREPORT exec_prefix prefix program_transform_name bindir sbindir libexecdir datadir sysconfdir sharedstatedir localstatedir libdir includedir oldincludedir infodir mandir build_alias host_alias target_alias DEFS ECHO_C ECHO_N ECHO_T LIBS INSTALL_PROGRAM INSTALL_SCRIPT INSTALL_DATA CYGPATH_W PACKAGE VERSION ACLOCAL AUTOCONF AUTOMAKE AUTOHEADER MAKEINFO AMTAR install_sh STRIP ac_ct_STRIP INSTALL_STRIP_PROGRAM mkdir_p AWK SET_MAKE am__leading_dot DEFAULT_POSIX2_VERSION CC CFLAGS LDFLAGS CPPFLAGS ac_ct_CC EXEEXT OBJEXT DEPDIR am__include am__quote AMDEP_TRUE AMDEP_FALSE AMDEPBACKSLASH CCDEPMODE am__fastdepCC_TRUE am__fastdepCC_FALSE CPP EGREP PERL RANLIB ac_ct_RANLIB LN_S build build_cpu build_vendor build_os host host_cpu host_vendor host_os GNU_PACKAGE HELP2MAN OPTIONAL_BIN_PROGS MAN DF_PROG U ANSI2KNR LIBOBJS STDBOOL_H HAVE__BOOL ALLOCA ALLOCA_H FNMATCH_H NEED_SETGID KMEM_GROUP GET
+LOADAVG_LIBS POW_LIB YACC LIB_CLOCK_GETTIME LIB_NANOSLEEP SEQ_LIBM GLIBC21 LIBICONV LTLIBICONV SQRT_LIBM FESETROUND_LIBM LIB_CRYPT MKINSTALLDIRS USE_NLS MSGFMT GMSGFMT XGETTEXT MSGMERGE INTLLIBS LIBINTL LTLIBINTL POSUB LTLIBOBJS'
 ac_subst_files=''
 
 # Initialize some variables set by options.
@@ -32005,7 +32006,7 @@
 int
 main ()
 {
-static struct timespec x; x.tv_sec = x.tv_nsec;
+static struct timespec x; x.ts_sec = x.ts_nsec;
   ;
   return 0;
 }
@@ -50865,7 +50866,8 @@
 
 
 
-                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              ac_config_files="$ac_config_files Makefile doc/Makefile lib/Makefile man/Makefile m4/Makefile po/Makefile.in src/Makefile tests/Makefile tests/basename/Makefile tests/chgrp/Makefile tests/chmod/Makefile tests/chown/Makefile tests/cp/Makefile tests/cut/Makefile tests/date/Makefile tests/dd/Makefile tests/dircolors/Makefile tests/du/Makefile tests/expr/Makefile tests/factor/Makefile tests/fmt/Makefile tests/head/Makefile tests/install/Makefile tests/join/Makefile tests/ln/Makefile tests/ls-2/Makefile tests/ls/Makefile tests/md5sum/Makefile tests/misc/Makefile tests/mkdir/Makefile tests/mv/Makefile tests/od/Makefile tests/pr/Makefile tests/rm/Makefile tests/rmdir/Makefile tests/seq/Makefile tests/sha1sum/Makefile tests/shred/Makefile tests/sort/Makefile tests/stty/Makefile tests/sum/Makefile tests/tac/Makefile tests/tail-2/Makefile tests/tail/Makefile tests/test/Makefile tests/touch/Makefile tests/tr/Makefile tests/tsort/Makefile tests/unexpand/Makefile tests/uniq/Makefile tests/wc/Makefile"
+                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              ac_config_files="$ac_config_files Makefile doc/Makefile lib/Makefile man/Makefile m4/Makefile po/Makefile.in src/Makefile tests/Makefile tests/basename/Makefile tests/chgrp/Makefile tests/chmod/Makefile tests/chown/Makefile tests/cp/Makefile tests/cut/Makefile tests/date/Makefile tests/dd/Makefile tests/dircolors/Makefile tests/du/Makefile tests/expr/Makefile tests/factor/Makefile tests/fmt/Makefile tests/head/Makefile tests/install/Makefile tests/join/Makefile tests/ln/Makefile tests
+/ls-2/Makefile tests/ls/Makefile tests/md5sum/Makefile tests/misc/Makefile tests/mkdir/Makefile tests/mv/Makefile tests/od/Makefile tests/pr/Makefile tests/rm/Makefile tests/rmdir/Makefile tests/seq/Makefile tests/sha1sum/Makefile tests/shred/Makefile tests/sort/Makefile tests/stty/Makefile tests/sum/Makefile tests/tac/Makefile tests/tail-2/Makefile tests/tail/Makefile tests/test/Makefile tests/touch/Makefile tests/tr/Makefile tests/tsort/Makefile tests/unexpand/Makefile tests/uniq/Makefile tests/wc/Makefile"
 
 cat >confcache <<\_ACEOF
 # This file is a shell script that caches the results of configure
diff -ruN coreutils-5.2.1-orig/doc/coreutils.info coreutils-5.2.1/doc/coreutils.info
--- coreutils-5.2.1-orig/doc/coreutils.info	Wed Mar 10 18:06:16 2004
+++ coreutils-5.2.1/doc/coreutils.info	Tue Mar 22 23:43:52 2005
@@ -1,5 +1,5 @@
-This is coreutils.info, produced by makeinfo version 4.5 from
-coreutils.texi.
+This is Info file coreutils.info, produced by Makeinfo version 1.68
+from the input file coreutils.texi.
 
 INFO-DIR-SECTION Basics
 START-INFO-DIR-ENTRY
@@ -99,26 +99,13 @@
 * yes: (coreutils)yes invocation.               Print a string indefinitely.
 END-INFO-DIR-ENTRY
 
-This manual documents version 5.2.1 of the GNU core utilities,
-including the standard programs for text and file manipulation.
-
-   Copyright (C) 1994, 1995, 1996, 2000, 2001, 2002, 2003, 2004 Free
-Software Foundation, Inc.
-
-     Permission is granted to copy, distribute and/or modify this
-     document under the terms of the GNU Free Documentation License,
-     Version 1.1 or any later version published by the Free Software
-     Foundation; with no Invariant Sections, with no Front-Cover Texts,
-     and with no Back-Cover Texts.  A copy of the license is included
-     in the section entitled "GNU Free Documentation License".
-   
 
 File: coreutils.info,  Node: Top,  Next: Introduction,  Up: (dir)
 
 GNU Coreutils
 *************
 
-This manual documents version 5.2.1 of the GNU core utilities,
+   This manual documents version 5.2.1 of the GNU core utilities,
 including the standard programs for text and file manipulation.
 
    Copyright (C) 1994, 1995, 1996, 2000, 2001, 2002, 2003, 2004 Free
@@ -130,7 +117,7 @@
      Foundation; with no Invariant Sections, with no Front-Cover Texts,
      and with no Back-Cover Texts.  A copy of the license is included
      in the section entitled "GNU Free Documentation License".
-   
+
 * Menu:
 
 * Introduction::                       Caveats, overview, and authors.
@@ -164,7 +151,7 @@
 * GNU Free Documentation License::     The license for this documentation.
 * Index::                              General index.
 
- --- The Detailed Node Listing ---
+ -- The Detailed Node Listing --
 
 Common Options
 
@@ -467,7 +454,7 @@
    Normally options and operands can appear in any order, and programs
 act as if all the options appear before any operands.  For example,
 `sort -r passwd -t :' acts like `sort -r -t : passwd', since `:' is an
-option-argument of `-t'.  However, if the `POSIXLY_CORRECT' environment
+option-argument of `-t'.  However, if the POSIXLY_CORRECT environment
 variable is set, options must appear before operands, unless otherwise
 specified for a particular command.
 
@@ -486,7 +473,6 @@
      operands even if they begin with `-'.  For example, `sort -- -r'
      reads from the file named `-r'.
 
-
    A single `-' is not really an option, though it looks like one.  It
 stands for standard input, or for standard output if that is clear from
 the context, and it can be used either as an operand or as an
@@ -499,11 +485,11 @@
 
 * Exit status::                 Indicating program success or failure.
 * Backup options::              -b -S -V, in some programs.
-* Block size::                  BLOCK_SIZE and --block-size, in some programs.
-* Target directory::            --target-directory, in some programs.
-* Trailing slashes::            --strip-trailing-slashes, in some programs.
+* Block size::                  BLOCK_SIZE and -block-size, in some programs.
+* Target directory::            -target-directory, in some programs.
+* Trailing slashes::            -strip-trailing-slashes, in some programs.
 * Traversing symlinks::         -H, -L, or -P, in some programs.
-* Treating / specially::        --preserve-root and --no-preserve-root.
+* Treating / specially::        -preserve-root and -no-preserve-root.
 * Standards conformance::       Conformance to the POSIX standard.
 
 
@@ -540,9 +526,8 @@
      removed.  Without this option, the original versions are destroyed.
      Use METHOD to determine the type of backups to make.  When this
      option is used but METHOD is not specified, then the value of the
-     `VERSION_CONTROL' environment variable is used.  And if
-     `VERSION_CONTROL' is not set, the default backup type is
-     `existing'.
+     VERSION_CONTROL environment variable is used.  And if
+     VERSION_CONTROL is not set, the default backup type is `existing'.
 
      Note that the short form of this option, `-b' does not accept any
      argument.  Using `-b' is equivalent to using `--backup=existing'.
@@ -570,19 +555,17 @@
           Always make simple backups.  Please note `never' is not to be
           confused with `none'.
 
-
 `-S SUFFIX'
 `--suffix=SUFFIX'
      Append SUFFIX to each backup file made with `-b'.  If this option
-     is not specified, the value of the `SIMPLE_BACKUP_SUFFIX'
-     environment variable is used.  And if `SIMPLE_BACKUP_SUFFIX' is not
+     is not specified, the value of the SIMPLE_BACKUP_SUFFIX
+     environment variable is used.  And if SIMPLE_BACKUP_SUFFIX is not
      set, the default is `~', just as in Emacs.
 
 `--version-control=METHOD'
      This option is obsolete and will be removed in a future release.
      It has been replaced with `--backup'.
 
-
 
 File: coreutils.info,  Node: Block size,  Next: Target directory,  Prev: Backup options,  Up: Common options
 
@@ -601,17 +584,16 @@
 
 `DF_BLOCK_SIZE'
      This specifies the default block size for the `df' command.
-     Similarly, `DU_BLOCK_SIZE' specifies the default for `du' and
-     `LS_BLOCK_SIZE' for `ls'.
+     Similarly, DU_BLOCK_SIZE specifies the default for `du' and
+     LS_BLOCK_SIZE for `ls'.
 
 `BLOCK_SIZE'
      This specifies the default block size for all three commands, if
      the above command-specific environment variables are not set.
 
 `POSIXLY_CORRECT'
-     If neither the `COMMAND_BLOCK_SIZE' nor the `BLOCK_SIZE' variables
-     are set, but this variable is set, the block size defaults to 512.
-
+     If neither the COMMAND_BLOCK_SIZE nor the BLOCK_SIZE variables are
+     set, but this variable is set, the block size defaults to 512.
 
    If none of the above environment variables are set, the block size
 currently defaults to 1024 bytes in most contexts, but this number may
@@ -621,10 +603,9 @@
    A block size specification can be a positive integer specifying the
 number of bytes per block, or it can be `human-readable' or `si' to
 select a human-readable format.  Integers may be followed by suffixes
-that are upward compatible with the SI prefixes
-(http://www.bipm.fr/enus/3_SI/si-prefixes.html) for decimal multiples
-and with the IEC 60027-2 prefixes for binary multiples
-(http://physics.nist.gov/cuu/Units/binary.html).
+that are upward compatible with the
+SI prefixes (http://www.bipm.fr/enus/3_SI/si-prefixes.html) for decimal
+multiples and with the IEC 60027-2 prefixes for binary multiples (http://physics.nist.gov/cuu/Units/binary.html).
 
    With human-readable formats, output sizes are followed by a size
 letter such as `M' for megabytes.  `BLOCK_SIZE=human-readable' uses
@@ -633,7 +614,7 @@
 1,000,000 bytes.
 
    A block size specification preceded by `'' causes output sizes to be
-displayed with thousands separators.  The `LC_NUMERIC' locale specifies
+displayed with thousands separators.  The LC_NUMERIC locale specifies
 the thousands separator and grouping.  For example, in an American
 English locale, `--block-size="'1kB"' would cause a size of 1234000
 bytes to be displayed as `1,234'.  In the default C locale, there is no
@@ -715,7 +696,7 @@
 
    Block size defaults can be overridden by an explicit
 `--block-size=SIZE' option.  The `-k' option is equivalent to
-`--block-size=1K', which is the default unless the `POSIXLY_CORRECT'
+`--block-size=1K', which is the default unless the POSIXLY_CORRECT
 environment variable is set.  The `-h' or `--human-readable' option is
 equivalent to `--block-size=human-readable'.  The `--si' option is
 equivalent to `--block-size=si'.
@@ -740,11 +721,11 @@
      this convention.
 
      The commands in the `mv'-family are unusual in that they take a
-     variable number of arguments with a special case at the _end_
+     variable number of arguments with a special case at the *end*
      (namely, the target directory).  This makes it nontrivial to
      perform some operations, e.g., "move all files from here to
      ../d/", because `mv * ../d/' might exhaust the argument space, and
-     `ls | xargs ...' doesn't have a clean way to specify an extra
+     `ls | xargs ...'  doesn't have a clean way to specify an extra
      final argument for each invocation of the subject command.  (It
      can be done by going through a shell command, but that requires
      more human labor and brain power than it should.)
@@ -757,7 +738,7 @@
 
           ls |xargs mv --target-directory=../d
 
-     If you use the GNU `find' program, you can move _all_ files with
+     If you use the GNU `find' program, you can move *all* files with
      this command:
           find . -mindepth 1 -maxdepth 1 \
             | xargs mv --target-directory=../d
@@ -770,7 +751,6 @@
             | xargs --null --no-run-if-empty \
                 mv --target-directory=../d
 
-
 
 File: coreutils.info,  Node: Trailing slashes,  Next: Traversing symlinks,  Prev: Target directory,  Up: Common options
 
@@ -787,7 +767,7 @@
 when performing file name completion on such symbolic links.  Without
 this option, `mv', for example, (via the system's rename function) must
 interpret a trailing slash as a request to dereference the symbolic link
-and so must rename the indirectly referenced _directory_ and not the
+and so must rename the indirectly referenced *directory* and not the
 symbolic link.  Although it may seem surprising that such behavior be
 the default, it is required by POSIX and is consistent with other parts
 of that standard.
@@ -821,7 +801,6 @@
      Do not traverse any symbolic links.  This is the default if none
      of `-H', `-L', or `-P' is specified.
 
-
 
 File: coreutils.info,  Node: Treating / specially,  Next: Standards conformance,  Prev: Traversing symlinks,  Up: Common options
 
@@ -858,9 +837,8 @@
 
    In a few cases, the GNU utilities' default behavior is incompatible
 with the POSIX standard.  To suppress these incompatibilities, define
-the `POSIXLY_CORRECT' environment variable.  Unless you are checking
-for POSIX conformance, you probably do not need to define
-`POSIXLY_CORRECT'.
+the POSIXLY_CORRECT environment variable.  Unless you are checking for
+POSIX conformance, you probably do not need to define POSIXLY_CORRECT.
 
    Newer versions of POSIX are occasionally incompatible with older
 versions.  For example, older versions of POSIX required the command
@@ -871,9 +849,9 @@
 
    The GNU utilities normally conform to the version of POSIX that is
 standard for your system.  To cause them to conform to a different
-version of POSIX, define the `_POSIX2_VERSION' environment variable to
-a value of the form YYYYMM specifying the year and month the standard
-was adopted.  Two values are currently supported for `_POSIX2_VERSION':
+version of POSIX, define the _POSIX2_VERSION environment variable to a
+value of the form YYYYMM specifying the year and month the standard was
+adopted.  Two values are currently supported for _POSIX2_VERSION:
 `199209' stands for POSIX 1003.2-1992, and `200112' stands for POSIX
 1003.1-2001.  For example, if you are running older software that
 assumes an older version of POSIX and uses `sort +1', `head -1', or
@@ -908,7 +886,7 @@
      cat [OPTION] [FILE]...
 
    The program accepts the following options.  Also see *Note Common
-options::.
+options::..
 
 `-A'
 `--show-all'
@@ -976,7 +954,6 @@
      files and standard input in DOS binary mode, so the CR characters
      at the end of each line are also visible.
 
-
    An exit status of zero indicates success, and a nonzero value
 indicates failure.
 
@@ -997,7 +974,7 @@
 the record that it follows in the file.
 
    The program accepts the following options.  Also see *Note Common
-options::.
+options::..
 
 `-b'
 `--before'
@@ -1015,7 +992,6 @@
 `--separator=SEPARATOR'
      Use SEPARATOR as the record separator, instead of newline.
 
-
    An exit status of zero indicates success, and a nonzero value
 indicates failure.
 
@@ -1062,7 +1038,7 @@
 contains no section delimiters as a single body section.
 
    The program accepts the following options.  Also see *Note Common
-options::.
+options::..
 
 `-b STYLE'
 `--body-numbering=STYLE'
@@ -1143,7 +1119,6 @@
 `--number-width=NUMBER'
      Use NUMBER characters for line numbers (default 6).
 
-
    An exit status of zero indicates success, and a nonzero value
 indicates failure.
 
@@ -1165,7 +1140,7 @@
 single octal number.
 
    The program accepts the following options.  Also see *Note Common
-options::.
+options::..
 
 `-A RADIX'
 `--address-radix=RADIX'
@@ -1208,7 +1183,7 @@
      If N is omitted with `--strings', the default is 3.  On older
      systems, GNU `od' instead supports an obsolete option `-s[N]',
      where N also defaults to 3.  POSIX 1003.1-2001 (*note Standards
-     conformance::) does not allow `-s' without an argument; use
+     conformance::.) does not allow `-s' without an argument; use
      `--strings' instead.
 
 `-t TYPE'
@@ -1296,9 +1271,9 @@
      If this option is not given at all, the default is 16.  If N is
      omitted with `--width', the default is 32.  On older systems, GNU
      `od' instead supports an obsolete option `-w[N]', where N also
-     defaults to 32.  POSIX 1003.1-2001 (*note Standards conformance::)
-     does not allow `-w' without an argument; use `--width' instead.
-
+     defaults to 32.  POSIX 1003.1-2001 (*note Standards
+     conformance::.) does not allow `-w' without an argument; use
+     `--width' instead.
 
    The next several options are shorthands for format specifications.
 GNU `od' accepts any combination of shorthands and format specification
@@ -1354,7 +1329,6 @@
      pseudo-address.  The pseudo-addresses are displayed in parentheses
      following any normal address.
 
-
    An exit status of zero indicates success, and a nonzero value
 indicates failure.
 
@@ -1402,7 +1376,7 @@
 Experience', 11 (1981), 1119-1184).
 
    The program accepts the following options.  Also see *Note Common
-options::.
+options::..
 
 `-c'
 `--crown-margin'
@@ -1443,7 +1417,6 @@
      formatted output line.  One use is to format certain kinds of
      program comments, while leaving the code unchanged.
 
-
    An exit status of zero indicates success, and a nonzero value
 indicates failure.
 
@@ -1470,7 +1443,7 @@
 STRING PAGE', with spaces inserted around STRING so that the line takes
 up the full PAGE_WIDTH.  Here, DATE is the date (see the `-D' or
 `--date-format' option for details), STRING is the centered header
-string, and PAGE identifies the page number.  The `LC_MESSAGES' locale
+string, and PAGE identifies the page number.  The LC_MESSAGES locale
 category affects the spelling of PAGE; in the default C locale, it is
 `Page NUMBER' where NUMBER is the decimal page number.
 
@@ -1503,7 +1476,7 @@
      option letter (already stated in the POSIX specification).
 
    The program accepts the following options.  Also see *Note Common
-options::.
+options::..
 
 `+FIRST_PAGE[:LAST_PAGE]'
 `--pages=FIRST_PAGE[:LAST_PAGE]'
@@ -1554,9 +1527,9 @@
      arbitrary string in place of the header date, e.g.,
      `--date-format="Monday morning"'.
 
-     If the `POSIXLY_CORRECT' environment variable is not set, the date
+     If the POSIXLY_CORRECT environment variable is not set, the date
      format defaults to `%Y-%m-%d %H:%M' (for example, `2001-12-04
-     23:59'); otherwise, the format depends on the `LC_TIME' locale
+     23:59'); otherwise, the format depends on the LC_TIME locale
      category, with the default being `%b %e %H:%M %Y' (for example,
      `Dec  4 23:59 2001'.
 
@@ -1677,7 +1650,7 @@
 
      On older systems, `pr' instead supports an obsolete option
      `-S[STRING]', where STRING is optional.  POSIX 1003.1-2001 (*note
-     Standards conformance::) does not allow this older usage.  To
+     Standards conformance::.) does not allow this older usage.  To
      specify an empty STRING portably, use `--sep-string'.
 
 `-t'
@@ -1721,7 +1694,6 @@
      downward compatibility and to meet most frequent tasks).  That's
      equivalent to `-W 72 -J'.  The header line is never truncated.
 
-
    An exit status of zero indicates success, and a nonzero value
 indicates failure.
 
@@ -1745,7 +1717,7 @@
 return sets the column to zero.
 
    The program accepts the following options.  Also see *Note Common
-options::.
+options::..
 
 `-b'
 `--bytes'
@@ -1764,10 +1736,9 @@
      Use a maximum line length of WIDTH columns instead of 80.
 
      On older systems, `fold' supports an obsolete option `-WIDTH'.
-     POSIX 1003.1-2001 (*note Standards conformance::) does not allow
+     POSIX 1003.1-2001 (*note Standards conformance::.) does not allow
      this; use `-w WIDTH' instead.
 
-
    An exit status of zero indicates success, and a nonzero value
 indicates failure.
 
@@ -1805,7 +1776,7 @@
 before the output for each FILE.
 
    The program accepts the following options.  Also see *Note Common
-options::.
+options::..
 
 `-c N'
 `--bytes=N'
@@ -1827,12 +1798,11 @@
 `--verbose'
      Always print file name headers.
 
-
    On older systems, `head' supports an obsolete option
 `-COUNTOPTIONS', which is recognized only if it is specified first.
 COUNT is a decimal number optionally followed by a size letter (`b',
 `k', `m') as in `-c', or `l' to mean count by lines, or other option
-letters (`cqv').  POSIX 1003.1-2001 (*note Standards conformance::)
+letters (`cqv').  POSIX 1003.1-2001 (*note Standards conformance::.)
 does not allow this; use `-c COUNT' or `-n COUNT' instead.
 
    An exit status of zero indicates success, and a nonzero value
@@ -1869,7 +1839,7 @@
 of from the end.
 
    The program accepts the following options.  Also see *Note Common
-options::.
+options::..
 
 `-c BYTES'
 `--bytes=BYTES'
@@ -1974,14 +1944,13 @@
 `--verbose'
      Always print file name headers.
 
-
    On older systems, `tail' supports an obsolete option
 `-COUNTOPTIONS', which is recognized only if it is specified first.
 COUNT is a decimal number optionally followed by a size letter (`b',
 `k', `m') as in `-c', or `l' to mean count by lines, or other option
 letters (`cfqv').  Some older `tail' implementations also support an
 obsolete option `+COUNT' with the same meaning as `-+COUNT'.  POSIX
-1003.1-2001 (*note Standards conformance::) does not allow these
+1003.1-2001 (*note Standards conformance::.) does not allow these
 options; use `-c COUNT' or `-n COUNT' instead.
 
    An exit status of zero indicates success, and a nonzero value
@@ -2009,7 +1978,7 @@
 that it did create.
 
    The program accepts the following options.  Also see *Note Common
-options::.
+options::..
 
 `-a LENGTH'
 `--suffix-length=LENGTH'
@@ -2020,7 +1989,7 @@
      Put LINES lines of INPUT into each output file.
 
      On older systems, `split' supports an obsolete option `-LINES'.
-     POSIX 1003.1-2001 (*note Standards conformance::) does not allow
+     POSIX 1003.1-2001 (*note Standards conformance::.) does not allow
      this; use `-l LINES' instead.
 
 `-b BYTES'
@@ -2045,7 +2014,6 @@
      Write a diagnostic to standard error just before each output file
      is opened.
 
-
    An exit status of zero indicates success, and a nonzero value
 indicates failure.
 
@@ -2095,7 +2063,6 @@
      meaning repeat as many times as necessary until the input is
      exhausted.
 
-
    The output files' names consist of a prefix (`xx' by default)
 followed by a suffix.  By default, the suffix is an ascending sequence
 of two-digit decimal numbers from `00' to `99'.  In any case,
@@ -2107,7 +2074,7 @@
 it has created so far before it exits.
 
    The program accepts the following options.  Also see *Note Common
-options::.
+options::..
 
 `-f PREFIX'
 `--prefix=PREFIX'
@@ -2151,7 +2118,6 @@
 `--quiet'
      Do not print counts of output file sizes.
 
-
    An exit status of zero indicates success, and a nonzero value
 indicates failure.
 
@@ -2208,7 +2174,7 @@
 maximum (not the sum) of those lengths.
 
    The program accepts the following options.  Also see *Note Common
-options::.
+options::..
 
 `-c'
 `--bytes'
@@ -2230,7 +2196,6 @@
 `--max-line-length'
      Print only the maximum line lengths.
 
-
    An exit status of zero indicates success, and a nonzero value
 indicates failure.
 
@@ -2256,7 +2221,7 @@
 blocks.
 
    The program accepts the following options.  Also see *Note Common
-options::.
+options::..
 
 `-r'
      Use the default (BSD compatible) algorithm.  This option is
@@ -2268,7 +2233,6 @@
      Compute checksums using an algorithm compatible with System V
      `sum''s default, and print file sizes in units of 512-byte blocks.
 
-
    `sum' is provided for compatibility; the `cksum' program (see next
 section) is preferable in new applications.
 
@@ -2325,7 +2289,7 @@
 specified as `-', standard input is read.
 
    The program accepts the following options.  Also see *Note Common
-options::.
+options::..
 
 `-b'
 `--binary'
@@ -2377,7 +2341,6 @@
      checksum lines.  This option is useful only if all but a few lines
      in the checked input are valid.
 
-
    An exit status of zero indicates success, and a nonzero value
 indicates failure.
 
@@ -2426,13 +2389,12 @@
      instead of merge; merging is provided because it is faster, in the
      case where it works.
 
-
    A pair of lines is compared as follows: if any key fields have been
 specified, `sort' compares each pair of fields, in the order specified
 on the command line, according to the associated ordering options,
 until a difference is found or no fields are left.  Unless otherwise
 specified, all comparisons use the character collating sequence
-specified by the `LC_COLLATE' locale.  (1)
+specified by the LC_COLLATE locale.  (1)
 
    If any of the global options `bdfgiMnr' are given but no key fields
 are specified, `sort' compares the entire lines according to the global
@@ -2458,8 +2420,8 @@
      1 if invoked with `-c' and the input is not properly sorted
      2 if an error occurred
 
-   If the environment variable `TMPDIR' is set, `sort' uses its value
-as the directory for temporary files instead of `/tmp'.  The
+   If the environment variable TMPDIR is set, `sort' uses its value as
+the directory for temporary files instead of `/tmp'.  The
 `--temporary-directory' (`-T') option in turn overrides the environment
 variable.
 
@@ -2474,26 +2436,26 @@
 `-b'
 `--ignore-leading-blanks'
      Ignore leading blanks when finding sort keys in each line.  The
-     `LC_CTYPE' locale determines character types.
+     LC_CTYPE locale determines character types.
 
 `-d'
 `--dictionary-order'
      Sort in "phone directory" order: ignore all characters except
-     letters, digits and blanks when sorting.  The `LC_CTYPE' locale
+     letters, digits and blanks when sorting.  The LC_CTYPE locale
      determines character types.
 
 `-f'
 `--ignore-case'
      Fold lowercase characters into the equivalent uppercase characters
      when comparing so that, for example, `b' and `B' sort as equal.
-     The `LC_CTYPE' locale determines character types.
+     The LC_CTYPE locale determines character types.
 
 `-g'
 `--general-numeric-sort'
      Sort numerically, using the standard C function `strtod' to convert
      a prefix of each line to a double-precision floating point number.
      This allows floating point numbers to be specified in scientific
-     notation, like `1.0e-34' and `10e100'.  The `LC_NUMERIC' locale
+     notation, like `1.0e-34' and `10e100'.  The LC_NUMERIC locale
      determines the decimal-point character.  Do not report overflow,
      underflow, or conversion errors.  Use the following collating
      sequence:
@@ -2517,7 +2479,7 @@
 
 `-i'
 `--ignore-nonprinting'
-     Ignore nonprinting characters.  The `LC_CTYPE' locale determines
+     Ignore nonprinting characters.  The LC_CTYPE locale determines
      character types.  This option has no effect if the stronger
      `--dictionary-order' (`-d') option is also given.
 
@@ -2526,8 +2488,8 @@
      An initial string, consisting of any amount of blanks, followed by
      a month name abbreviation, is folded to UPPER case and compared in
      the order `JAN' < `FEB' < ... < `DEC'.  Invalid names compare low
-     to valid names.  The `LC_TIME' locale category determines the
-     month spellings.
+     to valid names.  The LC_TIME locale category determines the month
+     spellings.
 
 `-n'
 `--numeric-sort'
@@ -2535,7 +2497,7 @@
      consists of optional blanks, an optional `-' sign, and zero or more
      digits possibly separated by thousands separators, optionally
      followed by a decimal-point character and zero or more digits.
-     The `LC_NUMERIC' locale specifies the decimal-point character and
+     The LC_NUMERIC locale specifies the decimal-point character and
      thousands separator.
 
      Numeric sort uses what might be considered an unconventional
@@ -2560,7 +2522,6 @@
      Reverse the result of comparison, so that lines with greater key
      values appear earlier in the output instead of later.
 
-
    Other options are:
 
 `-o OUTPUT-FILE'
@@ -2571,7 +2532,7 @@
      F' and `cat F | sort -o F'.
 
      On newer systems, `-o' cannot appear after an input file if
-     `POSIXLY_CORRECT' is set, e.g., `sort F -o F'.  Portable scripts
+     POSIXLY_CORRECT is set, e.g., `sort F -o F'.  Portable scripts
      should specify `-o OUTPUT-FILE' before any input files.
 
 `-s'
@@ -2621,7 +2582,7 @@
 `-T TEMPDIR'
 `--temporary-directory=TEMPDIR'
      Use directory TEMPDIR to store temporary files, overriding the
-     `TMPDIR' environment variable.  If this option is given more than
+     TMPDIR environment variable.  If this option is given more than
      once, temporary files are stored in all the directories given.  If
      you have a large sort or merge that is I/O-bound, you can often
      improve performance by using this option to specify directories on
@@ -2637,7 +2598,7 @@
 `--key=POS1[,POS2]'
      Specify a sort field that consists of the part of the line between
      POS1 and POS2 (or the end of the line, if POS2 is omitted),
-     _inclusive_.  Fields and character positions are numbered starting
+     *inclusive*.  Fields and character positions are numbered starting
      with 1.  So to sort on the second field, you'd use `--key=2,2'
      (`-k 2,2').  See below for more examples.
 
@@ -2650,7 +2611,6 @@
      handle arbitrary pathnames (even those which contain Line Feed
      characters.)
 
-
    Historical (BSD and System V) implementations of `sort' have
 differed in their interpretation of some options, particularly `-b',
 `-f', and `-n'.  GNU sort follows the POSIX behavior, which is usually
@@ -2678,7 +2638,7 @@
 
    On older systems, `sort' supports an obsolete origin-zero syntax
 `+POS1 [-POS2]' for specifying sort keys.  POSIX 1003.1-2001 (*note
-Standards conformance::) does not allow this; use `-k' instead.
+Standards conformance::.) does not allow this; use `-k' instead.
 
    Here are some examples to illustrate various combinations of options.
 
@@ -2700,14 +2660,14 @@
 
      Note that if you had written `-k 2' instead of `-k 2,2' `sort'
      would have used all characters beginning in the second field and
-     extending to the end of the line as the primary _numeric_ key.
+     extending to the end of the line as the primary *numeric* key.
      For the large majority of applications, treating keys spanning
      more than one field as numeric will not do what you expect.
 
      Also note that the `n' modifier was applied to the field-end
      specifier for the first key.  It would have been equivalent to
      specify `-k 2n,2' or `-k 2n,2n'.  All modifiers except `b' apply
-     to the associated _field_, regardless of whether the modifier
+     to the associated *field*, regardless of whether the modifier
      character is attached to the field-start and/or the field-end part
      of the key specifier.
 
@@ -2740,17 +2700,16 @@
 
           sort -t : -b -k 5,5 -k 3,3n /etc/passwd
 
-
    ---------- Footnotes ----------
 
-   (1) If you use a non-POSIX locale (e.g., by setting `LC_ALL' to
+   (1) If you use a non-POSIX locale (e.g., by setting LC_ALL to
 `en_US'), then `sort' may produce output that is sorted differently
-than you're accustomed to.  In that case, set the `LC_ALL' environment
-variable to `C'.  Note that setting only `LC_COLLATE' has two problems.
-First, it is ineffective if `LC_ALL' is also set.  Second, it has
-undefined behavior if `LC_CTYPE' (or `LANG', if `LC_CTYPE' is unset) is
-set to an incompatible value.  For example, you get undefined behavior
-if `LC_CTYPE' is `ja_JP.PCK' but `LC_COLLATE' is `en_US.UTF-8'.
+than you're accustomed to.  In that case, set the LC_ALL environment
+variable to `C'.  Note that setting only LC_COLLATE has two problems.
+First, it is ineffective if LC_ALL is also set.  Second, it has
+undefined behavior if LC_CTYPE (or LANG, if LC_CTYPE is unset) is set
+to an incompatible value.  For example, you get undefined behavior if
+LC_CTYPE is `ja_JP.PCK' but LC_COLLATE is `en_US.UTF-8'.
 
 
 File: coreutils.info,  Node: uniq invocation,  Next: comm invocation,  Prev: sort invocation,  Up: Operating on sorted files
@@ -2773,12 +2732,12 @@
 duplicate lines, perhaps you want to use `sort -u'.
 
    Comparisons use the character collating sequence specified by the
-`LC_COLLATE' locale category.
+LC_COLLATE locale category.
 
    If no OUTPUT file is specified, `uniq' writes to standard output.
 
    The program accepts the following options.  Also see *Note Common
-options::.
+options::..
 
 `-f N'
 `--skip-fields=N'
@@ -2788,7 +2747,7 @@
      separated from each other by at least one space or tab.
 
      On older systems, `uniq' supports an obsolete option `-N'.  POSIX
-     1003.1-2001 (*note Standards conformance::) does not allow this;
+     1003.1-2001 (*note Standards conformance::.)  does not allow this;
      use `-f N' instead.
 
 `-s N'
@@ -2799,7 +2758,7 @@
      skipped over first.
 
      On older systems, `uniq' supports an obsolete option `+N'.  POSIX
-     1003.1-2001 (*note Standards conformance::) does not allow this;
+     1003.1-2001 (*note Standards conformance::.)  does not allow this;
      use `-s N' instead.
 
 `-c'
@@ -2856,7 +2815,6 @@
      specified fields and characters).  By default the entire rest of
      the lines are compared.
 
-
    An exit status of zero indicates success, and a nonzero value
 indicates failure.
 
@@ -2873,7 +2831,7 @@
      comm [OPTION]... FILE1 FILE2
 
    Before `comm' can be used, the input files must be sorted using the
-collating sequence specified by the `LC_COLLATE' locale.  If an input
+collating sequence specified by the LC_COLLATE locale.  If an input
 file ends in a non-newline character, a newline is silently appended.
 The `sort' command with no options always outputs a file that is
 suitable input to `comm'.
@@ -2884,7 +2842,7 @@
 are separated by a single TAB character.
 
    The options `-1', `-2', and `-3' suppress printing of the
-corresponding columns.  Also see *Note Common options::.
+corresponding columns.  Also see *Note Common options::..
 
    Unlike some other comparison utilities, `comm' has an exit status
 that does not depend on the result of the comparison.  Upon normal
@@ -2899,7 +2857,7 @@
 
    `tsort' performs a topological sort on the given FILE, or standard
 input if no input file is given or for a FILE of `-'.  For more details
-and some history, see *Note tsort background::.  Synopsis:
+and some history, see *Note tsort background::..  Synopsis:
 
      tsort [OPTION] [FILE]
 
@@ -3067,19 +3025,19 @@
 line numbers refer to individual text input files.  In all cases, the
 program outputs the permuted index to the standard output.
 
-   When GNU extensions are _not_ enabled, that is, when the program
+   When GNU extensions are *not* enabled, that is, when the program
 operates in traditional mode, there may be zero, one or two parameters
 besides the options.  If there are no parameters, the program reads the
 standard input and outputs the permuted index to the standard output.
 If there is only one parameter, it names the text INPUT to be read
 instead of the standard input.  If two parameters are given, they give
 respectively the name of the INPUT file to read and the name of the
-OUTPUT file to produce.  _Be very careful_ to note that, in this case,
+OUTPUT file to produce.  *Be very careful* to note that, in this case,
 the contents of file given by the second parameter is destroyed.  This
 behavior is dictated by System V `ptx' compatibility; GNU Standards
 normally discourage output parameters not introduced by an option.
 
-   Note that for _any_ file named as the value of an option or as an
+   Note that for *any* file named as the value of an option or as an
 input text file, a single dash `-' may be used, in which case standard
 input is assumed.  However, it would not make sense to use this
 convention more than once per program invocation.
@@ -3116,7 +3074,6 @@
      Print the program version on standard output, then exit without
      further processing.
 
-
    An exit status of zero indicates success, and a nonzero value
 indicates failure.
 
@@ -3128,7 +3085,7 @@
 
    As it is set up now, the program assumes that the input file is coded
 using 8-bit ISO 8859-1 code, also known as Latin-1 character set,
-_unless_ it is compiled for MS-DOS, in which case it uses the character
+*unless* it is compiled for MS-DOS, in which case it uses the character
 set of the IBM-PC.  (GNU `ptx' is not known to work on smaller MS-DOS
 machines anymore.)  Compared to 7-bit ASCII, the set of characters
 which are letters is different; this alters the behavior of regular
@@ -3141,7 +3098,6 @@
 `--ignore-case'
      Fold lower case letters to upper case for sorting.
 
-
 
 File: coreutils.info,  Node: Input processing in ptx,  Next: Output formatting in ptx,  Prev: Charset selection in ptx,  Up: ptx invocation
 
@@ -3153,7 +3109,7 @@
 `--break-file=FILE'
      This option provides an alternative (to `-W') method of describing
      which characters make up words.  It introduces the name of a file
-     which contains a list of characters which can_not_ be part of one
+     which contains a list of characters which can*not* be part of one
      word; this file is called the "Break file".  Any character which
      is not part of the Break file is a word constituent.  If both
      options `-b' and `-W' are specified, then `-W' has precedence and
@@ -3203,7 +3159,7 @@
 
      Using this option, the program does not try very hard to remove
      references from contexts in output, but it succeeds in doing so
-     _when_ the context ends exactly at the newline.  If option `-r' is
+     *when* the context ends exactly at the newline.  If option `-r' is
      used with `-S' default value, or when GNU extensions are disabled,
      this condition is always met and references are completely
      excluded from the output contexts.
@@ -3262,7 +3218,6 @@
      escape sequences, as found in the C language, are recognized and
      converted to the corresponding characters by `ptx' itself.
 
-
 
 File: coreutils.info,  Node: Output formatting in ptx,  Next: Compatibility in ptx,  Prev: Input processing in ptx,  Up: ptx invocation
 
@@ -3277,7 +3232,7 @@
 field is properly justified, so the concordance output can be readily
 observed.  As a special feature, if automatic references are selected
 by option `-A' and are output before the left context, that is, if
-option `-R' is _not_ selected, then a colon is added after the
+option `-R' is *not* selected, then a colon is added after the
 reference; this nicely interfaces with GNU Emacs `next-error'
 processing.  In this default output format, each white space character,
 like newline and tab, is merely changed to exactly one space, with no
@@ -3324,7 +3279,7 @@
      references are rather placed at the beginning of each output line,
      before the left context.  For any other output format, option `-R'
      is ignored, with one exception:  with `-R' the width of references
-     is _not_ taken into account in total output width given by `-w'.
+     is *not* taken into account in total output width given by `-w'.
 
      This option is automatically selected whenever GNU extensions are
      disabled.
@@ -3400,7 +3355,6 @@
      compress consecutive spaces.  Let me know how to improve this
      special character processing for TeX.
 
-
 
 File: coreutils.info,  Node: Compatibility in ptx,  Prev: Output formatting in ptx,  Up: ptx invocation
 
@@ -3466,7 +3420,6 @@
    * The user can specify both an Ignore file and an Only file.  This
      is not allowed with System V `ptx'.
 
-
 
 File: coreutils.info,  Node: Operating on fields within a line,  Next: Operating on characters,  Prev: Operating on sorted files,  Up: Top
 
@@ -3501,7 +3454,7 @@
 read, and is written exactly once.
 
    The program accepts the following options.  Also see *Note Common
-options::.
+options::..
 
 `-b BYTE-LIST'
 `--bytes=BYTE-LIST'
@@ -3548,7 +3501,6 @@
      opposed to ranges of fields), output OUTPUT_DELIM_STRING between
      ranges of selected bytes.
 
-
    An exit status of zero indicates success, and a nonzero value
 indicates failure.
 
@@ -3582,7 +3534,7 @@
      paste [OPTION]... [FILE]...
 
    The program accepts the following options.  Also see *Note Common
-options::.
+options::..
 
 `-s'
 `--serial'
@@ -3604,7 +3556,6 @@
           2%b_2
           %c_
 
-
    An exit status of zero indicates success, and a nonzero value
 indicates failure.
 
@@ -3623,7 +3574,7 @@
 input.  FILE1 and FILE2 should be sorted on the join fields.
 
    Normally, the sort order is that of the collating sequence specified
-by the `LC_COLLATE' locale.  Unless the `-t' option is given, the sort
+by the LC_COLLATE locale.  Unless the `-t' option is given, the sort
 comparison ignores blanks at the start of the join field, as in `sort
 -b'.  If the `--ignore-case' option is given, the sort comparison
 ignores the case of characters in the join field, as in `sort -f'.
@@ -3653,7 +3604,7 @@
 fields from FILE1, then the remaining fields from FILE2.
 
    The program accepts the following options.  Also see *Note Common
-options::.
+options::..
 
 `-a FILE-NUMBER'
      Print a line for each unpairable line in file FILE-NUMBER (either
@@ -3705,7 +3656,6 @@
      Print a line for each unpairable line in file FILE-NUMBER (either
      `1' or `2'), instead of the normal output.
 
-
    In addition, when GNU `join' is invoked with exactly one argument,
 the `--help' and `--version' options are recognized.  *Note Common
 options::.
@@ -3897,7 +3847,6 @@
      implemented in GNU `tr'; each character's equivalence class
      consists only of that character, which is of no particular use.
 
-
 
 File: coreutils.info,  Node: Translating,  Next: Squeezing,  Prev: Character sets,  Up: tr invocation
 
@@ -4030,14 +3979,13 @@
      Note how single quotes are used in the above example to protect the
      square brackets from interpretation by a shell.
 
-
 
 File: coreutils.info,  Node: Warnings in tr,  Prev: Squeezing,  Up: tr invocation
 
 Warning messages
 ----------------
 
-   Setting the environment variable `POSIXLY_CORRECT' turns off the
+   Setting the environment variable POSIXLY_CORRECT turns off the
 following warning and error messages, for strict compliance with POSIX.
 Otherwise, the following diagnostics are issued:
 
@@ -4076,7 +4024,7 @@
 every 8 columns).
 
    The program accepts the following options.  Also see *Note Common
-options::.
+options::..
 
 `-t TAB1[,TAB2]...'
 `--tabs=TAB1[,TAB2]...'
@@ -4088,7 +4036,7 @@
 
      On older systems, `expand' supports an obsolete option
      `-TAB1[,TAB2]...', where tabstops must be separated by commas.
-     POSIX 1003.1-2001 (*note Standards conformance::) does not allow
+     POSIX 1003.1-2001 (*note Standards conformance::.) does not allow
      this; use `-t TAB1[,TAB2]...' instead.
 
 `-i'
@@ -4096,7 +4044,6 @@
      Only convert initial tabs (those that precede all non-space or
      non-tab characters) on each line to spaces.
 
-
    An exit status of zero indicates success, and a nonzero value
 indicates failure.
 
@@ -4120,7 +4067,7 @@
 column.
 
    The program accepts the following options.  Also see *Note Common
-options::.
+options::..
 
 `-t TAB1[,TAB2]...'
 `--tabs=TAB1[,TAB2]...'
@@ -4133,7 +4080,7 @@
      On older systems, `unexpand' supports an obsolete option
      `-TAB1[,TAB2]...', where tabstops must be separated by commas.
      (Unlike `-t', this obsolete option does not imply `-a'.)  POSIX
-     1003.1-2001 (*note Standards conformance::) does not allow this;
+     1003.1-2001 (*note Standards conformance::.) does not allow this;
      use `--first-only -t TAB1[,TAB2]...' instead.
 
 `-a'
@@ -4141,7 +4088,6 @@
      Convert all strings of two or more spaces or tabs, not just initial
      ones, to tabs.
 
-
    An exit status of zero indicates success, and a nonzero value
 indicates failure.
 
@@ -4193,7 +4139,7 @@
    An exit status of zero indicates success, and a nonzero value
 indicates failure.
 
-   Also see *Note Common options::.
+   Also see *Note Common options::..
 
 * Menu:
 
@@ -4207,9 +4153,9 @@
 
    ---------- Footnotes ----------
 
-   (1) If you use a non-POSIX locale (e.g., by setting `LC_ALL' to
+   (1) If you use a non-POSIX locale (e.g., by setting LC_ALL to
 `en_US'), then `ls' may produce output that is sorted differently than
-you're accustomed to.  In that case, set the `LC_ALL' environment
+you're accustomed to.  In that case, set the LC_ALL environment
 variable to `C'.
 
 
@@ -4283,7 +4229,6 @@
 `--recursive'
      List the contents of all directories recursively.
 
-
 
 File: coreutils.info,  Node: What information is listed,  Next: Sorting the output,  Prev: Which files are listed,  Up: ls invocation
 
@@ -4318,7 +4263,7 @@
      Finally, output a line of the form:
           //DIRED-OPTIONS// --quoting-style=WORD
      where WORD is the quoting style (*note Formatting the file
-     names::).
+     names::.).
 
      Here is an actual example:
 
@@ -4364,7 +4309,7 @@
      trailing slash.  However, if you invoke `ls' with `--dired' along
      with an option like `--escape' (aka `-b') and operate on a file
      whose name contains special characters, notice that the backslash
-     _is_ included:
+     *is* included:
 
           $ touch 'a b'
           $ ls -blog --dired 'a b'
@@ -4375,7 +4320,7 @@
      If you use a quoting style that adds quote marks (e.g.,
      `--quoting-style=c'), then the offsets include the quote marks.
      So beware that the user may select the quoting style via the
-     environment variable `QUOTING_STYLE'.  Hence, applications using
+     environment variable QUOTING_STYLE.  Hence, applications using
      `--dired' should either specify an explicit
      `--quoting-style=literal' option (aka `-N' or `--literal') on the
      command line, or else be prepared to parse the escaped names.
@@ -4383,7 +4328,7 @@
 `--full-time'
      Produce long format directory listings, and list times in full.
      It is equivalent to using `--format=long' with
-     `--time-style=full-iso' (*note Formatting file timestamps::).
+     `--time-style=full-iso' (*note Formatting file timestamps::.).
 
 `-g'
      Produce long format directory listings, but don't display owner
@@ -4413,25 +4358,25 @@
 `--format=verbose'
      In addition to the name of each file, print the file type,
      permissions, number of hard links, owner name, group name, size,
-     and timestamp (*note Formatting file timestamps::), normally the
+     and timestamp (*note Formatting file timestamps::.), normally the
      modification time.
 
      Normally the size is printed as a byte count without punctuation,
-     but this can be overridden (*note Block size::).  For example, `-h'
-     prints an abbreviated, human-readable count, and
+     but this can be overridden (*note Block size::.).  For example,
+     `-h' prints an abbreviated, human-readable count, and
      `--block-size="'1"' prints a byte count with the thousands
      separator of the current locale.
 
      For each directory that is listed, preface the files with a line
      `total BLOCKS', where BLOCKS is the total disk allocation for all
      files in that directory.  The block size currently defaults to 1024
-     bytes, but this can be overridden (*note Block size::).  The
+     bytes, but this can be overridden (*note Block size::.).  The
      BLOCKS computed counts each hard link separately; this is arguably
      a deficiency.
 
      The permissions listed are similar to symbolic mode specifications
-     (*note Symbolic Modes::).  But `ls' combines multiple bits into the
-     third character of each set of permissions as follows:
+     (*note Symbolic Modes::.).  But `ls' combines multiple bits into
+     the third character of each set of permissions as follows:
     `s'
           If the setuid or setgid bit and the corresponding executable
           bit are both set.
@@ -4476,7 +4421,7 @@
      file has holes.
 
      Normally the disk allocation is printed in units of 1024 bytes,
-     but this can be overridden (*note Block size::).
+     but this can be overridden (*note Block size::.).
 
      For files that are NFS-mounted from an HP-UX system to a BSD
      system, this option reports sizes that are half the correct
@@ -4492,7 +4437,6 @@
      Use the `-h' or `--human-readable' option if you prefer powers of
      1024.
 
-
 
 File: coreutils.info,  Node: Sorting the output,  Next: More details about version sort,  Prev: What information is listed,  Up: ls invocation
 
@@ -4560,7 +4504,6 @@
      (characters after the last `.'); files with no extension are
      sorted first.
 
-
 
 File: coreutils.info,  Node: More details about version sort,  Next: General output formatting,  Prev: Sorting the output,  Up: ls invocation
 
@@ -4595,8 +4538,8 @@
    This functionality is implemented using the `strverscmp' function.
 *Note String/Array Comparison: (libc)String/Array Comparison.  One
 result of that implementation decision is that `ls -v' does not use the
-locale category, `LC_COLLATE'.  As a result, non-numeric prefixes are
-sorted as if `LC_COLLATE' were set to `C'.
+locale category, LC_COLLATE.  As a result, non-numeric prefixes are
+sorted as if LC_COLLATE were set to `C'.
 
 
 File: coreutils.info,  Node: General output formatting,  Next: Formatting file timestamps,  Prev: More details about version sort,  Up: ls invocation
@@ -4625,11 +4568,10 @@
 
         * auto - Only use color if standard output is a terminal.
 
-        * always - Always use color.
-     Specifying `--color' and no WHEN is equivalent to `--color=always'.
-     Piping a colorized listing through a pager like `more' or `less'
-     usually produces unreadable results.  However, using `more -f'
-     does seem to work.
+        * always - Always use color.  Specifying `--color' and no WHEN
+     is equivalent to `--color=always'.  Piping a colorized listing
+     through a pager like `more' or `less' usually produces unreadable
+     results.  However, using `more -f' does seem to work.
 
 `-F'
 `--classify'
@@ -4660,7 +4602,7 @@
 
 `-k'
      Print file sizes in 1024-byte blocks, overriding the default block
-     size (*note Block size::).  This option is equivalent to
+     size (*note Block size::.).  This option is equivalent to
      `--block-size=1K'.
 
 `-m'
@@ -4689,9 +4631,7 @@
 `--width=COLS'
      Assume the screen is COLS columns wide.  The default is taken from
      the terminal settings if possible; otherwise the environment
-     variable `COLUMNS' is used if it is set; otherwise the default is
-     80.
-
+     variable COLUMNS is used if it is set; otherwise the default is 80.
 
 
 File: coreutils.info,  Node: Formatting file timestamps,  Next: Formatting the file names,  Prev: General output formatting,  Up: ls invocation
@@ -4719,10 +4659,10 @@
 
     `+FORMAT'
           List timestamps using FORMAT, where FORMAT is interpreted
-          like the format argument of `date' (*note date invocation::).
+          like the format argument of `date' (*note date invocation::.).
           For example, `--time-style="+%Y-%m-%d %H:%M:%S"' causes `ls'
           to list timestamps like `2002-03-30 23:45:56'.  As with
-          `date', FORMAT's interpretation is affected by the `LC_TIME'
+          `date', FORMAT's interpretation is affected by the LC_TIME
           locale category.
 
           If FORMAT contains two format strings separated by a newline,
@@ -4771,7 +4711,7 @@
           locale conventions vary so widely, but they are easier for
           many people to read.
 
-          The `LC_TIME' locale category specifies the timestamp format.
+          The LC_TIME locale category specifies the timestamp format.
           The default POSIX locale uses timestamps like `Mar 30  2002'
           and `Mar 30 23:45'; in this locale, the following two `ls'
           invocations are equivalent:
@@ -4784,18 +4724,18 @@
           Other locales behave differently.  For example, in a German
           locale, `--time-style="locale"' might be equivalent to
           `--time-style="+%e. %b %Y $newline%e. %b %H:%M"' and might
-          generate timestamps like `30. Ma"r 2002 ' and `30. Ma"r
+          generate timestamps like `30. M"ar 2002 ' and `30. M"ar
           23:45'.
 
     `posix-STYLE'
-          List POSIX-locale timestamps if the `LC_TIME' locale category
+          List POSIX-locale timestamps if the LC_TIME locale category
           is POSIX, STYLE timestamps otherwise.  For example, the
           default style, which is `posix-long-iso', lists timestamps
           like `Mar 30  2002' and `Mar 30 23:45' when in the POSIX
           locale, and like `2002-03-30 23:45' otherwise.
 
    You can specify the default value of the `--time-style' option with
-the environment variable `TIME_STYLE'; if `TIME_STYLE' is not set the
+the environment variable TIME_STYLE; if TIME_STYLE is not set the
 default style is `posix-long-iso'.  GNU Emacs 21 and later can parse
 ISO dates, but older Emacs versions do not, so if you are using an
 older version of Emacs and specify a non-POSIX locale, you may need to
@@ -4871,7 +4811,7 @@
           locale.  This looks nicer on many displays.
 
      You can specify the default value of the `--quoting-style' option
-     with the environment variable `QUOTING_STYLE'.  If that environment
+     with the environment variable QUOTING_STYLE.  If that environment
      variable is not set, the default value is `literal', but this
      default may change to `shell' in a future version of this package.
 
@@ -4879,7 +4819,6 @@
      Print nongraphic characters as-is in file names.  This is the
      default unless the output is a terminal and the program is `ls'.
 
-
 
 File: coreutils.info,  Node: dir invocation,  Next: vdir invocation,  Prev: ls invocation,  Up: Directory listing
 
@@ -4918,18 +4857,18 @@
 database is used.  For details on the format of these files, run
 `dircolors --print-database'.
 
-   The output is a shell command to set the `LS_COLORS' environment
+   The output is a shell command to set the LS_COLORS environment
 variable.  You can specify the shell syntax to use on the command line,
-or `dircolors' will guess it from the value of the `SHELL' environment
+or `dircolors' will guess it from the value of the SHELL environment
 variable.
 
    The program accepts the following options.  Also see *Note Common
-options::.
+options::..
 
 `-b'
 `--sh'
 `--bourne-shell'
-     Output Bourne shell commands.  This is the default if the `SHELL'
+     Output Bourne shell commands.  This is the default if the SHELL
      environment variable is set and does not end with `csh' or `tcsh'.
 
 `-c'
@@ -4944,7 +4883,6 @@
      output is itself a valid configuration file, and is fairly
      descriptive of the possibilities.
 
-
    An exit status of zero indicates success, and a nonzero value
 indicates failure.
 
@@ -5008,11 +4946,11 @@
 following exception: if `--force --backup' is specified with SOURCE and
 DEST identical, and referring to a regular file, `cp' will make a
 backup file, either regular or numbered, as specified in the usual ways
-(*note Backup options::).  This is useful when you simply want to make
+(*note Backup options::.).  This is useful when you simply want to make
 a backup of an existing file before changing it.
 
    The program accepts the following options.  Also see *Note Common
-options::.
+options::..
 
 `-a'
 `--archive'
@@ -5232,10 +5170,9 @@
 `-x'
 `--one-file-system'
      Skip subdirectories that are on different filesystems from the one
-     that the copy started on.  However, mount point directories _are_
+     that the copy started on.  However, mount point directories *are*
      copied.
 
-
    An exit status of zero indicates success, and a nonzero value
 indicates failure.
 
@@ -5252,11 +5189,11 @@
      dd [OPTION]...
 
    The program accepts the following options.  Also see *Note Common
-options::.
+options::..
 
    The numeric-valued options below (BYTES and BLOCKS) can be followed
 by a multiplier: `b'=512, `c'=1, `w'=2, `xM'=M, or any of the standard
-block size suffixes like `k'=1024 (*note Block size::).
+block size suffixes like `k'=1024 (*note Block size::.).
 
    Use different `dd' invocations to use different block sizes for
 skipping and I/O.  For example, the following shell commands copy data
@@ -5363,7 +5300,6 @@
           bytes.  When used with `block' or `unblock', pad with spaces
           instead of zero bytes.
 
-
    An exit status of zero indicates success, and a nonzero value
 indicates failure.
 
@@ -5391,7 +5327,7 @@
 files onto themselves.
 
    The program accepts the following options.  Also see *Note Common
-options::.
+options::..
 
 `-b'
 `--backup[=METHOD]'
@@ -5421,7 +5357,7 @@
      Set the permissions for the installed file or directory to MODE,
      which can be either an octal number, or a symbolic mode as in
      `chmod', with 0 as the point of departure (*note File
-     permissions::).  The default mode is `u=rwx,go=rx'--read, write,
+     permissions::.).  The default mode is `u=rwx,go=rx'--read, write,
      and execute for the owner, and read and execute for group and
      other.
 
@@ -5463,7 +5399,6 @@
      can be `none' (or `off'), `numbered' (or `t'), `existing' (or
      `nil'), or `never' (or `simple').  *Note Backup options::.
 
-
    An exit status of zero indicates success, and a nonzero value
 indicates failure.
 
@@ -5503,13 +5438,13 @@
 the file, or have write permission on its directory.)  If the response
 does not begin with `y' or `Y', the file is skipped.
 
-   _Warning_: If you try to move a symlink that points to a directory,
+   *Warning*: If you try to move a symlink that points to a directory,
 and you specify the symlink with a trailing slash, then `mv' doesn't
 move the symlink but instead moves the directory referenced by the
 symlink.  *Note Trailing slashes::.
 
    The program accepts the following options.  Also see *Note Common
-options::.
+options::..
 
 `-b'
 `--backup[=METHOD]'
@@ -5560,7 +5495,6 @@
      can be `none' (or `off'), `numbered' (or `t'), `existing' (or
      `nil'), or `never' (or `simple').  *Note Backup options::.
 
-
    An exit status of zero indicates success, and a nonzero value
 indicates failure.
 
@@ -5577,15 +5511,15 @@
 
    If a file is unwritable, standard input is a terminal, and the `-f'
 or `--force' option is not given, or the `-i' or `--interactive' option
-_is_ given, `rm' prompts the user for whether to remove the file.  If
+*is* given, `rm' prompts the user for whether to remove the file.  If
 the response does not begin with `y' or `Y', the file is skipped.
 
-   _Warning_: If you use `rm' to remove a file, it is usually possible
+   *Warning*: If you use `rm' to remove a file, it is usually possible
 to recover the contents of that file.  If you want more assurance that
 the contents are truly unrecoverable, consider using `shred'.
 
    The program accepts the following options.  Also see *Note Common
-options::.
+options::..
 
 `-d'
 `--directory'
@@ -5626,7 +5560,6 @@
 `--verbose'
      Print the name of each file before removing it.
 
-
    One common question is how to remove files whose names begin with a
 `-'.  GNU `rm', like every program that uses the `getopt' function to
 parse its arguments, lets you use the `--' option to indicate that all
@@ -5654,7 +5587,7 @@
    `shred' overwrites devices or files, to help prevent even very
 expensive hardware from recovering the data.
 
-   Ordinarily when you remove a file (*note rm invocation::), the data
+   Ordinarily when you remove a file (*note rm invocation::.), the data
 is not actually destroyed.  Only the index listing where the file is
 stored is destroyed, and the storage is made available for reuse.
 There are undelete utilities that will attempt to reconstruct the index
@@ -5684,7 +5617,7 @@
 Deletion of Data from Magnetic and Solid-State Memory', from the
 proceedings of the Sixth USENIX Security Symposium (San Jose,
 California, 22-25 July, 1996).  The paper is also available online
-<http://www.cs.auckland.ac.nz/~pgut001/pubs/secure_del.html>.
+`http://www.cs.auckland.ac.nz/~pgut001/pubs/secure_del.html'.
 
    *Please note* that `shred' relies on a very important assumption:
 that the filesystem overwrites data in place.  This is the traditional
@@ -5731,7 +5664,7 @@
      shred [OPTION]... FILE[...]
 
    The program accepts the following options.  Also see *Note Common
-options::.
+options::..
 
 `-f'
 `--force'
@@ -5802,7 +5735,6 @@
      Use the command `shred file' or (if using a Bourne-compatible
      shell) the command `shred - 1<>file' instead.
 
-
    You might use the following command to erase all trace of the
 filesystem you'd created on the floppy disk in your first drive.  That
 command takes about 20 minutes to erase a "1.44MB" (actually 1440 KiB)
@@ -5828,7 +5760,7 @@
 (and `rmdir', which removes directories, one special file type).
 
    Although Unix-like operating systems have markedly fewer special file
-types than others, not _everything_ can be treated only as the
+types than others, not *everything* can be treated only as the
 undifferentiated byte stream of "normal files".  For example, when a
 file is created or removed, the system must record this information,
 which it does in a "directory"--a special type of file.  Although you
@@ -5897,11 +5829,10 @@
      will not remove an existing file.  Use the `--backup' option to
      make `ln' rename existing files.
 
-
    A "hard link" is another name for an existing file; the link and the
 original are indistinguishable.  Technically speaking, they share the
 same inode, and the inode contains all the information about a
-file--indeed, it is not incorrect to say that the inode _is_ the file.
+file--indeed, it is not incorrect to say that the inode *is* the file.
 On all existing implementations, you cannot make a hard link to a
 directory, and hard links cannot cross filesystem boundaries.  (These
 restrictions are not mandated by POSIX, however.)
@@ -5917,7 +5848,7 @@
 (libc)Symbolic Links.
 
    The program accepts the following options.  Also see *Note Common
-options::.
+options::..
 
 `-b'
 `--backup[=METHOD]'
@@ -5979,7 +5910,6 @@
      can be `none' (or `off'), `numbered' (or `t'), `existing' (or
      `nil'), or `never' (or `simple').  *Note Backup options::.
 
-
    Examples:
 
      ln -s /some/name  # creates link ./name pointing to /some/name
@@ -6009,7 +5939,7 @@
 move on to processing any remaining NAMEs.
 
    The program accepts the following options.  Also see *Note Common
-options::.
+options::..
 
 `-m MODE'
 `--mode=MODE'
@@ -6050,7 +5980,7 @@
 anonymous pipe in shells or elsewhere.
 
    The program accepts the following option.  Also see *Note Common
-options::.
+options::..
 
 `-m MODE'
 `--mode=MODE'
@@ -6059,7 +5989,6 @@
      the bits set in the umask for the point of departure.  *Note File
      permissions::.
 
-
    An exit status of zero indicates success, and a nonzero value
 indicates failure.
 
@@ -6094,7 +6023,6 @@
 `c'
      for a character special file
 
-
    When making a block or character special file, the major and minor
 device numbers must be given after the file type.  If a major or minor
 device number begins with `0x' or `0X', it is interpreted as
@@ -6102,7 +6030,7 @@
 decimal.
 
    The program accepts the following option.  Also see *Note Common
-options::.
+options::..
 
 `-m MODE'
 `--mode=MODE'
@@ -6110,7 +6038,6 @@
      `chmod' and uses `a=rw' minus the bits set in the umask as the
      point of departure.  *Note File permissions::.
 
-
    An exit status of zero indicates success, and a nonzero value
 indicates failure.
 
@@ -6135,13 +6062,12 @@
      unavailable, it produces no output and exits with a nonzero exit
      code.
 
-
      readlink [OPTION] FILE
 
    By default, `readlink' operates in readlink mode.
 
    The program accepts the following options.  Also see *Note Common
-options::.
+options::..
 
 `-f'
 `--canonicalize'
@@ -6161,7 +6087,6 @@
 `--verbose'
      Report error messages.
 
-
    The `readlink' utility first appeared in OpenBSD 2.1.
 
    An exit status of zero indicates success, and a nonzero value
@@ -6181,7 +6106,7 @@
 directory, it is an error.
 
    The program accepts the following option.  Also see *Note Common
-options::.
+options::..
 
 `--ignore-fail-on-non-empty'
      Ignore each failure to remove a directory that is solely because
@@ -6202,7 +6127,6 @@
      Give a diagnostic for each successful removal.  DIRECTORY is
      removed.
 
-
    *Note rm invocation::, for how to remove non-empty directories
 (recursively).
 
@@ -6228,7 +6152,7 @@
 
    By default, `unlink' honors the `--help' and `--version' options.
 That makes it a little harder to remove files named `--help' and
-`--version', so when the environment variable `POSIXLY_CORRECT' is set,
+`--version', so when the environment variable POSIXLY_CORRECT is set,
 `unlink' treats such a command line arguments not as an option, but as
 an operand.
 
@@ -6242,8 +6166,8 @@
 ************************
 
    A file is not merely its contents, a name, and a file type (*note
-Special file types::).  A file also has an owner (a userid), a group (a
-group id), permissions (what the owner can do with the file, what
+Special file types::.).  A file also has an owner (a userid), a group
+(a group id), permissions (what the owner can do with the file, what
 people in the group can do, and what everyone else can do), various
 timestamps, and other information.  Collectively, we call these a file's
 "attributes".
@@ -6296,9 +6220,8 @@
      omitted, only the group of the files is changed; in this case,
      `chown' performs the same function as `chgrp'.
 
-
    Some older scripts may still use `.' in place of the `:' separator.
-POSIX 1003.1-2001 (*note Standards conformance::) does not require
+POSIX 1003.1-2001 (*note Standards conformance::.) does not require
 support for that, but for backward compatibility GNU `chown' supports
 `.' so long as no ambiguity results.  New scripts should avoid the use
 of `.' because it is not portable, and because it has undesirable
@@ -6318,7 +6241,7 @@
 underlying system behavior.
 
    The program accepts the following options.  Also see *Note Common
-options::.
+options::..
 
 `-c'
 `--changes'
@@ -6407,7 +6330,6 @@
      Do not traverse any symbolic links.  This is the default if none
      of `-H', `-L', or `-P' is specified.  *Note Traversing symlinks::.
 
-
    An exit status of zero indicates success, and a nonzero value
 indicates failure.
 
@@ -6424,7 +6346,7 @@
      chgrp [OPTION]... {GROUP | --reference=REF_FILE} FILE...
 
    The program accepts the following options.  Also see *Note Common
-options::.
+options::..
 
 `-c'
 `--changes'
@@ -6490,7 +6412,6 @@
      Do not traverse any symbolic links.  This is the default if none
      of `-H', `-L', or `-P' is specified.  *Note Traversing symlinks::.
 
-
    An exit status of zero indicates success, and a nonzero value
 indicates failure.
 
@@ -6513,10 +6434,10 @@
 traversals.
 
    If used, MODE specifies the new permissions.  For details, see the
-section on *Note File permissions::.
+section on *Note File permissions::..
 
    The program accepts the following options.  Also see *Note Common
-options::.
+options::..
 
 `-c'
 `--changes'
@@ -6552,7 +6473,6 @@
 `--recursive'
      Recursively change permissions of directories and their contents.
 
-
    An exit status of zero indicates success, and a nonzero value
 indicates failure.
 
@@ -6572,7 +6492,7 @@
 timestamp is given with any of the `-d', `-r', or `-t' options and the
 `--' argument is not given, that argument is interpreted as the time
 for the other files instead of as a file name.  POSIX 1003.1-2001
-(*note Standards conformance::) does not allow this; use `-t' instead.
+(*note Standards conformance::.)  does not allow this; use `-t' instead.
 
    Any FILE that does not exist is created empty.
 
@@ -6598,7 +6518,7 @@
 a user-specified value.
 
    The program accepts the following options.  Also see *Note Common
-options::.
+options::..
 
 `-a'
 `--time=atime'
@@ -6640,7 +6560,6 @@
      If no digits of the year are specified, the argument is
      interpreted as a date in the current year.
 
-
    An exit status of zero indicates success, and a nonzero value
 indicates failure.
 
@@ -6652,7 +6571,7 @@
 
    No disk can hold an infinite amount of data.  These commands report
 on how much disk storage is in use or available.  (This has nothing
-much to do with how much _main memory_, i.e., RAM, a program is using
+much to do with how much *main memory*, i.e., RAM, a program is using
 when it runs; for that, you want `ps' or `pstat' or `swap' or some such
 command.)
 
@@ -6679,7 +6598,7 @@
 on the filesystem containing each argument FILE.
 
    Normally the disk space is printed in units of 1024 bytes, but this
-can be overridden (*note Block size::).  Non-integer quantities are
+can be overridden (*note Block size::.).  Non-integer quantities are
 rounded up to the next higher unit.
 
    If an argument FILE is a disk device file containing a mounted
@@ -6691,7 +6610,7 @@
 structures.
 
    The program accepts the following options.  Also see *Note Common
-options::.
+options::..
 
 `-a'
 `--all'
@@ -6703,7 +6622,7 @@
 
 `-B SIZE'
 `--block-size=SIZE'
-     Scale sizes by SIZE before printing them (*note Block size::).
+     Scale sizes by SIZE before printing them (*note Block size::.).
      For example, `-BG' prints sizes in units of 1,073,741,824 bytes.
 
 `-h'
@@ -6723,7 +6642,7 @@
 
 `-k'
      Print sizes in 1024-byte blocks, overriding the default block size
-     (*note Block size::).  This option is equivalent to
+     (*note Block size::.).  This option is equivalent to
      `--block-size=1K'.
 
 `-l'
@@ -6793,7 +6712,6 @@
     `pcfs'
           An MS-DOS filesystem, usually on a diskette.
 
-
 `-x FSTYPE'
 `--exclude-type=FSTYPE'
      Limit the listing to filesystems not of type FSTYPE.  Multiple
@@ -6803,7 +6721,6 @@
 `-v'
      Ignored; for compatibility with System V versions of `df'.
 
-
    An exit status of zero indicates success, and a nonzero value
 indicates failure.
 
@@ -6820,11 +6737,11 @@
 
    With no arguments, `du' reports the disk space for the current
 directory.  Normally the disk space is printed in units of 1024 bytes,
-but this can be overridden (*note Block size::).  Non-integer
+but this can be overridden (*note Block size::.).  Non-integer
 quantities are rounded up to the next higher unit.
 
    The program accepts the following options.  Also see *Note Common
-options::.
+options::..
 
 `-a'
 `--all'
@@ -6850,7 +6767,7 @@
 
 `-B SIZE'
 `--block-size=SIZE'
-     Scale sizes by SIZE before printing them (*note Block size::).
+     Scale sizes by SIZE before printing them (*note Block size::.).
      For example, `-BG' prints sizes in units of 1,073,741,824 bytes.
 
 `-c'
@@ -6879,7 +6796,7 @@
 
 `-k'
      Print sizes in 1024-byte blocks, overriding the default block size
-     (*note Block size::).  This option is equivalent to
+     (*note Block size::.).  This option is equivalent to
      `--block-size=1K'.
 
 `-l'
@@ -6941,7 +6858,6 @@
      one per line.  If FILE is `-', take the patterns from standard
      input.
 
-
    On BSD systems, `du' reports sizes that are half the correct values
 for files that are NFS-mounted from HP-UX systems.  On HP-UX systems,
 it reports sizes that are twice the correct values for files that are
@@ -7080,7 +6996,7 @@
 `sync' ensures everything in memory is written to disk.
 
    Any arguments are ignored, except for a lone `--help' or `--version'
-(*note Common options::).
+(*note Common options::.).
 
    An exit status of zero indicates success, and a nonzero value
 indicates failure.
@@ -7111,7 +7027,7 @@
      echo [OPTION]... [STRING]...
 
    The program accepts the following options.  Also see *Note Common
-options::.
+options::..
 
 `-n'
      Do not output the trailing newline.
@@ -7151,7 +7067,6 @@
           the character whose ASCII code is NNN (octal); if NNN is not
           a valid octal number, it is printed literally.
 
-
    An exit status of zero indicates success, and a nonzero value
 indicates failure.
 
@@ -7170,7 +7085,7 @@
 argument is re-used as necessary to convert all of the given ARGUMENTs.
 
    A floating-point argument must use a period before any fractional
-digits, but is printed according to the `LC_NUMERIC' category of the
+digits, but is printed according to the LC_NUMERIC category of the
 current locale.  For example, in a locale whose radix character is a
 comma, the command `printf %g 3.14' outputs `3,14' whereas the command
 `printf %g 3,14' is an error.
@@ -7190,7 +7105,7 @@
 `\U' for 32-bit Unicode characters, specified as 8 hex digits HHHHHHHH.
 `printf' outputs the Unicode characters according to the LC_CTYPE part
 of the current locale, i.e. depending on the values of the environment
-variables `LC_ALL', `LC_CTYPE', `LANG'.
+variables LC_ALL, LC_CTYPE, LANG.
 
    The processing of `\u' and `\U' requires a full-featured `iconv'
 facility. It is activated on systems with glibc 2.2 (or newer), or when
@@ -7281,7 +7196,7 @@
 
    By default, `false' honors the `--help' and `--version' options.
 However, that is contrary to POSIX, so when the environment variable
-`POSIXLY_CORRECT' is set, `false' ignores _all_ command line arguments,
+POSIXLY_CORRECT is set, `false' ignores *all* command line arguments,
 including `--help' and `--version'.
 
    This version of `false' is implemented as a C program, and is thus
@@ -7306,7 +7221,7 @@
 
    By default, `true' honors the `--help' and `--version' options.
 However, that is contrary to POSIX, so when the environment variable
-`POSIXLY_CORRECT' is set, `true' ignores _all_ command line arguments,
+POSIXLY_CORRECT is set, `true' ignores *all* command line arguments,
 including `--help' and `--version'.
 
    Note, however, that it is possible to cause `true' to exit with
@@ -7404,7 +7319,6 @@
 `-t FD'
      True if FD is a file descriptor that is associated with a terminal.
 
-
 
 File: coreutils.info,  Node: Access permission tests,  Next: File characteristic tests,  Prev: File type tests,  Up: test invocation
 
@@ -7437,7 +7351,6 @@
 `-G FILE'
      True if FILE exists and is owned by the current effective group id.
 
-
 
 File: coreutils.info,  Node: File characteristic tests,  Next: String tests,  Prev: Access permission tests,  Up: test invocation
 
@@ -7464,7 +7377,6 @@
      True if FILE1 and FILE2 have the same device and inode numbers,
      i.e., if they are hard links to each other.
 
-
 
 File: coreutils.info,  Node: String tests,  Next: Numeric tests,  Prev: File characteristic tests,  Up: test invocation
 
@@ -7488,7 +7400,6 @@
 `STRING1 != STRING2'
      True if the strings are not equal.
 
-
 
 File: coreutils.info,  Node: Numeric tests,  Next: Connectives for test,  Prev: String tests,  Up: test invocation
 
@@ -7509,7 +7420,6 @@
      not-equal, less-than, less-than-or-equal, greater-than, or
      greater-than-or-equal than ARG2, respectively.
 
-
    For example:
 
      test -1 -gt -2 && echo yes
@@ -7536,7 +7446,6 @@
 `EXPR1 -o EXPR2'
      True if either EXPR1 or EXPR2 is true.
 
-
 
 File: coreutils.info,  Node: expr invocation,  Prev: test invocation,  Up: Conditions
 
@@ -7620,7 +7529,7 @@
      alternatives.  SunOS and other `expr''s treat these as regular
      characters.  (POSIX allows either behavior.)  *Note Regular
      Expression Library: (regex)Top, for details of regular expression
-     syntax.  Some examples are in *Note Examples of expr::.
+     syntax.  Some examples are in *Note Examples of expr::..
 
 `match STRING REGEX'
      An alternative way to do pattern matching.  This is the same as
@@ -7647,7 +7556,6 @@
      `index'.  This operator is a GNU extension.  Portable shell
      scripts should use `" $token" : ' \(.*\)'' instead of `+ "$token"'.
 
-
    To make `expr' interpret keywords as strings, you must use the
 `quote' operator.
 
@@ -7669,7 +7577,6 @@
      Multiplication, division, remainder.  Both arguments are converted
      to integers; an error occurs if this cannot be done.
 
-
 
 File: coreutils.info,  Node: Relations for expr,  Next: Examples of expr,  Prev: Numeric expressions,  Up: expr invocation
 
@@ -7697,10 +7604,9 @@
      otherwise.  `==' is a synonym for `='.  `expr' first tries to
      convert both arguments to integers and do a numeric comparison; if
      either conversion fails, it does a lexicographic comparison using
-     the character collating sequence specified by the `LC_COLLATE'
+     the character collating sequence specified by the LC_COLLATE
      locale.
 
-
 
 File: coreutils.info,  Node: Examples of expr,  Prev: Relations for expr,  Up: expr invocation
 
@@ -7761,7 +7667,7 @@
 contained is overwritten unless the `-a' option is used.
 
    The program accepts the following options.  Also see *Note Common
-options::.
+options::..
 
 `-a'
 `--append'
@@ -7772,7 +7678,6 @@
 `--ignore-interrupts'
      Ignore interrupt signals.
 
-
    An exit status of zero indicates success, and a nonzero value
 indicates failure.
 
@@ -7822,7 +7727,7 @@
 
      dirname NAME
 
-   If NAME is a single component, `dirname' prints `.' (meaning the
+   If NAME is a single component, `dirname' prints `.'  (meaning the
 current directory).
 
    The only options are `--help' and `--version'.  *Note Common
@@ -7854,7 +7759,7 @@
      a file name component.
 
    The program accepts the following option.  Also see *Note Common
-options::.
+options::..
 
 `-p'
 `--portability'
@@ -7864,7 +7769,6 @@
      name contains no characters not in the portable file name
      character set.
 
-
    Exit status:
 
      0 if all specified file names passed all of the tests,
@@ -7929,7 +7833,7 @@
 terminal line operation, as described below.
 
    The program accepts the following options.  Also see *Note Common
-options::.
+options::..
 
 `-a'
 `--all'
@@ -7953,11 +7857,10 @@
      settings.  This option may not be used in combination with any
      line settings.
 
-
    Many settings can be turned off by preceding them with a `-'.  Such
 arguments are marked below with "May be negated" in their description.
 The descriptions themselves refer to the positive case, that is, when
-_not_ negated (unless stated otherwise, of course).
+*not* negated (unless stated otherwise, of course).
 
    Some settings are not available on all POSIX systems, since they use
 extensions.  Such arguments are marked below with "Non-POSIX" in their
@@ -8370,9 +8273,9 @@
 `size'
      Print the number of rows and columns that the kernel thinks the
      terminal has.  (Systems that don't support rows and columns in the
-     kernel typically use the environment variables `LINES' and
-     `COLUMNS' instead; however, GNU `stty' does not know anything
-     about them.)  Non-POSIX.
+     kernel typically use the environment variables LINES and COLUMNS
+     instead; however, GNU `stty' does not know anything about them.)
+     Non-POSIX.
 
 `line N'
      Use line discipline N.  Non-POSIX.
@@ -8422,14 +8325,13 @@
      tty [OPTION]...
 
    The program accepts the following option.  Also see *Note Common
-options::.
+options::..
 
 `-s'
 `--silent'
 `--quiet'
      Print nothing; only return an exit status.
 
-
    Exit status:
 
      0 if standard input is a terminal
@@ -8474,7 +8376,7 @@
 followed by the corresponding user or group name in parentheses.
 
    The options cause `id' to print only part of the above information.
-Also see *Note Common options::.
+Also see *Note Common options::..
 
 `-g'
 `--group'
@@ -8498,7 +8400,6 @@
 `--user'
      Print only the user id.
 
-
    An exit status of zero indicates success, and a nonzero value
 indicates failure.
 
@@ -8605,7 +8506,7 @@
 `who am i'.
 
    The program accepts the following options.  Also see *Note Common
-options::.
+options::..
 
 `-a'
 `--all'
@@ -8673,7 +8574,6 @@
           `-' disallowing `write' messages
           `?' cannot find terminal device
 
-
    An exit status of zero indicates success, and a nonzero value
 indicates failure.
 
@@ -8767,7 +8667,7 @@
 
 `%s'
      seconds since the epoch, i.e., 1 January 1970 00:00:00 UTC (a GNU
-     extension).  For examples, *Note %s-examples::.
+     extension).
 
 `%S'
      second (00...60).  The range is [00...60], and not [00...59], in
@@ -8782,11 +8682,11 @@
 `%z'
      RFC-2822 style numeric time zone (e.g., -0600 or +0100), or
      nothing if no time zone is determinable.  This value reflects the
-     _current_ time zone.  It isn't changed by the `--date' option.
+     *current* time zone.  It isn't changed by the `--date' option.
 
 `%Z'
      time zone (e.g., EDT), or nothing if no time zone is determinable.
-     Note that this value reflects the _current_ time zone.  It isn't
+     Note that this value reflects the *current* time zone.  It isn't
      changed by the `--date' option.
 
 
@@ -8973,7 +8873,7 @@
 ------------------
 
    The program accepts the following options.  Also see *Note Common
-options::.
+options::..
 
 `-d DATESTR'
 `--date=DATESTR'
@@ -9014,7 +8914,7 @@
      If TIMESPEC is omitted with `--iso-8601', the default is `auto'.
      On older systems, GNU `date' instead supports an obsolete option
      `-I[TIMESPEC]', where TIMESPEC defaults to `auto'.  POSIX
-     1003.1-2001 (*note Standards conformance::) does not allow `-I'
+     1003.1-2001 (*note Standards conformance::.) does not allow `-I'
      without an argument; use `--iso-8601' instead.
 
 `-R'
@@ -9026,10 +8926,10 @@
 
           Fri,  1 Aug 2003 23:05:56 -0700
 
-     This format conforms to RFC 2822
-     (ftp://ftp.rfc-editor.org/in-notes/rfc2822.txt) and RFC 822
-     (ftp://ftp.rfc-editor.org/in-notes/rfc822.txt), the current and
-     previous standards for Internet email.
+     This format conforms to
+     RFC 2822 (ftp://ftp.rfc-editor.org/in-notes/rfc2822.txt) and
+     RFC 822 (ftp://ftp.rfc-editor.org/in-notes/rfc822.txt), the
+     current and previous standards for Internet email.
 
 `-r FILE'
 `--reference=FILE'
@@ -9043,10 +8943,10 @@
 `-u'
 `--utc'
 `--universal'
-     Use Coordinated Universal Time (UTC) by operating as if the `TZ'
+     Use Coordinated Universal Time (UTC) by operating as if the TZ
      environment variable were set to the string `UTC0'.  Normally,
-     `date' operates in the time zone indicated by `TZ', or the system
-     default if `TZ' is not set.  Coordinated Universal Time is often
+     `date' operates in the time zone indicated by TZ, or the system
+     default if TZ is not set.  Coordinated Universal Time is often
      called "Greenwich Mean Time" (GMT) for historical reasons.
 
 
@@ -9141,7 +9041,6 @@
           date -u -d '1970-01-01 946684800 seconds' +"%Y-%m-%d %T %z"
           2000-01-01 00:00:00 +0000
 
-
 
 File: coreutils.info,  Node: uname invocation,  Next: hostname invocation,  Prev: date invocation,  Up: System context
 
@@ -9167,7 +9066,7 @@
      => Linux dum 2.2.18ss.e820-bda652a #4 SMP Tue Jun 5 11:24:08 PDT 2001 i686 unknown unknown GNU/Linux
 
    The program accepts the following options.  Also see *Note Common
-options::.
+options::..
 
 `-a'
 `--all'
@@ -9203,7 +9102,7 @@
 `-s'
 `--kernel-name'
      Print the kernel name.  POSIX 1003.1-2001 (*note Standards
-     conformance::) calls this "the implementation of the operating
+     conformance::.) calls this "the implementation of the operating
      system", because the POSIX specification itself has no notion of
      "kernel".  The kernel name might be the same as the operating
      system name printed by the `-o' or `--operating-system' option,
@@ -9215,7 +9114,6 @@
 `--kernel-version'
      Print the kernel version.
 
-
    An exit status of zero indicates success, and a nonzero value
 indicates failure.
 
@@ -9293,8 +9191,8 @@
 directory structure, i.e., `/'.  `chroot' changes the root to the
 directory NEWROOT (which must exist) and then runs COMMAND with
 optional ARGS.  If COMMAND is not specified, the default is the value
-of the `SHELL' environment variable or `/bin/sh' if not set, invoked
-with the `-i' option.
+of the SHELL environment variable or `/bin/sh' if not set, invoked with
+the `-i' option.
 
    The only options are `--help' and `--version'.  *Note Common
 options::.
@@ -9346,7 +9244,7 @@
 variable to an empty value is different from unsetting it.
 
    The first remaining argument specifies the program name to invoke;
-it is searched for according to the `PATH' environment variable.  Any
+it is searched for according to the PATH environment variable.  Any
 remaining arguments are passed as arguments to that program.
 
    If no command name is specified following the environment
@@ -9354,7 +9252,7 @@
 specifying a command name of `printenv'.
 
    The program accepts the following options.  Also see *Note Common
-options::.
+options::..
 
 `-u NAME'
 `--unset=NAME'
@@ -9367,7 +9265,6 @@
      Start with an empty environment, ignoring the inherited
      environment.
 
-
    Exit status:
 
      0   if no COMMAND is specified and the environment is output
@@ -9400,17 +9297,16 @@
 different functionality than that described here.
 
    The program accepts the following option.  Also see *Note Common
-options::.
+options::..
 
 `-n ADJUSTMENT'
 `--adjustment=ADJUSTMENT'
      Add ADJUSTMENT instead of 10 to the command's priority.
 
      On older systems, `nice' supports an obsolete option
-     `-ADJUSTMENT'.  POSIX 1003.1-2001 (*note Standards conformance::)
+     `-ADJUSTMENT'.  POSIX 1003.1-2001 (*note Standards conformance::.)
      does not allow this; use `-n ADJUSTMENT' instead.
 
-
    Exit status:
 
      0   if no COMMAND is specified and the current priority is output
@@ -9513,9 +9409,9 @@
 super-user).
 
    By default, `su' does not change the current directory.  It sets the
-environment variables `HOME' and `SHELL' from the password entry for
-USER, and if USER is not the super-user, sets `USER' and `LOGNAME' to
-USER.  By default, the shell is not a login shell.
+environment variables HOME and SHELL from the password entry for USER,
+and if USER is not the super-user, sets USER and LOGNAME to USER.  By
+default, the shell is not a login shell.
 
    Any additional ARGs are passed as additional arguments to the shell.
 
@@ -9529,7 +9425,7 @@
 the `wheel' group; see below.
 
    The program accepts the following options.  Also see *Note Common
-options::.
+options::..
 
 `-c COMMAND'
 `--command=COMMAND'
@@ -9548,22 +9444,22 @@
 `-l'
 `--login'
      Make the shell a login shell.  This means the following.  Unset all
-     environment variables except `TERM', `HOME', and `SHELL' (which
-     are set as described above), and `USER' and `LOGNAME' (which are
-     set, even for the super-user, as described above), and set `PATH'
-     to a compiled-in default value.  Change to USER's home directory.
-     Prepend `-' to the shell's name, intended to make it read its
-     login startup file(s).
+     environment variables except TERM, HOME, and SHELL (which are set
+     as described above), and USER and LOGNAME (which are set, even for
+     the super-user, as described above), and set PATH to a compiled-in
+     default value.  Change to USER's home directory.  Prepend `-' to
+     the shell's name, intended to make it read its login startup
+     file(s).
 
 `-m'
 `-p'
 `--preserve-environment'
-     Do not change the environment variables `HOME', `USER', `LOGNAME',
-     or `SHELL'.  Run the shell given in the environment variable
-     `SHELL' instead of the shell from USER's passwd entry, unless the
-     user running `su' is not the superuser and USER's shell is
-     restricted.  A "restricted shell" is one that is not listed in the
-     file `/etc/shells', or in a compiled-in list if that file does not
+     Do not change the environment variables HOME, USER, LOGNAME, or
+     SHELL.  Run the shell given in the environment variable SHELL
+     instead of the shell from USER's passwd entry, unless the user
+     running `su' is not the superuser and USER's shell is restricted.
+     A "restricted shell" is one that is not listed in the file
+     `/etc/shells', or in a compiled-in list if that file does not
      exist.  Parts of what this option does can be overridden by
      `--login' and `--shell'.
 
@@ -9573,7 +9469,6 @@
      the user running `su' is not the superuser and USER's shell is
      restricted (see `-m' just above).
 
-
    Exit status:
 
      1   if `su' itself fails
@@ -9890,7 +9785,7 @@
 (using a period before any fractional digits).
 
    The program accepts the following options.  Also see *Note Common
-options::.
+options::..
 
 `-f FORMAT'
 `--format=FORMAT'
@@ -9908,7 +9803,6 @@
      Print all numbers with the same width, by padding with leading
      zeroes.  (To have other kinds of padding, use `--format').
 
-
    If you want to use `seq' to print sequences of large integer values,
 don't use the default `%g' format since it can result in loss of
 precision:
@@ -10072,9 +9966,9 @@
    Although a file's permission "bits" allow an operation on that file,
 that operation may still fail, because:
 
-   * the filesystem-specific permissions do not permit it;
+   - the filesystem-specific permissions do not permit it;
 
-   * the filesystem is mounted as read-only.
+   - the filesystem is mounted as read-only.
 
    For example, if the immutable attribute is set on a file, it cannot
 be modified, regardless of the fact that you may have just run `chmod
@@ -10090,7 +9984,7 @@
 operations on single-character symbols.  They allow you to modify either
 all or selected parts of files' permissions, optionally based on their
 previous values, and perhaps on the current `umask' as well (*note
-Umask and Protection::).
+Umask and Protection::.).
 
    The format of symbolic modes is:
 
@@ -10126,7 +10020,7 @@
 
    The USERS part tells which users' access to the file is changed.  It
 consists of one or more of the following letters (or it can be empty;
-*note Umask and Protection::, for a description of what happens then).
+*note Umask and Protection::., for a description of what happens then).
 When more than one of these letters is given, the order that they are
 in does not matter.
 
@@ -10254,7 +10148,7 @@
 
    Remember that the special permissions only affect files that are
 executable, plus, on some systems, directories (on which they have
-different meanings; *note Mode Structure::).  Also, the combinations
+different meanings; *note Mode Structure::.).  Also, the combinations
 `u+t', `g+t', and `o+s' have no effect.
 
    The `=' operator is not very useful with special permissions; for
@@ -10292,7 +10186,7 @@
 -----------------------
 
    The format of symbolic modes is actually more complex than described
-above (*note Setting Permissions::).  It provides two ways to make
+above (*note Setting Permissions::.).  It provides two ways to make
 multiple changes to files' permissions.
 
    The first way is to specify multiple OPERATION and PERMISSIONS parts
@@ -10343,8 +10237,8 @@
 ------------------------
 
    If the USERS part of a symbolic mode is omitted, it defaults to `a'
-(affect all users), except that any permissions that are _set_ in the
-system variable `umask' are _not affected_.  The value of `umask' can
+(affect all users), except that any permissions that are *set* in the
+system variable `umask' are *not affected*.  The value of `umask' can
 be set using the `umask' command.  Its default value varies from system
 to system.
 
@@ -10359,12 +10253,12 @@
      +w
 
 adds permission to write to the file to its owner and to other users who
-are in the file's group, but _not_ to other users.  In contrast, the
+are in the file's group, but *not* to other users.  In contrast, the
 mode:
 
      a+w
 
-ignores `umask', and _does_ give write permission for the file to all
+ignores `umask', and *does* give write permission for the file to all
 users.
 
 
@@ -10498,7 +10392,7 @@
 
    * pure numbers.
 
-We describe each of these item types in turn, below.
+ We describe each of these item types in turn, below.
 
    A few numbers may be written out in words in most contexts.  This is
 most useful for specifying day of the week items or relative items (see
@@ -10614,7 +10508,7 @@
 Alternatively, `:SECOND' can be omitted, in which case it is taken to
 be zero.
 
-   If the time is followed by `am' or `pm' (or `a.m.' or `p.m.'), HOUR
+   If the time is followed by `am' or `pm' (or `a.m.'  or `p.m.'), HOUR
 is restricted to run from 1 to 12, and `:MINUTE' may be omitted (taken
 to be zero).  `am' indicates the first half of the day, `pm' indicates
 the second half of the day.  In this notation, 12 is the predecessor of
@@ -10738,8 +10632,8 @@
    Also, take care when manipulating dates around clock changes such as
 daylight saving leaps.  In a few cases these have added or subtracted
 as much as 24 hours from the clock, so it is often wise to adopt
-universal time by setting the `TZ' environment variable to `UTC0'
-before embarking on calendrical calculations.
+universal time by setting the TZ environment variable to `UTC0' before
+embarking on calendrical calculations.
 
 
 File: coreutils.info,  Node: Pure numbers in date strings,  Next: Authors of getdate,  Prev: Relative items in date strings,  Up: Date input formats
@@ -10751,7 +10645,7 @@
 context in the date string.
 
    If the decimal number is of the form YYYYMMDD and no other calendar
-date item (*note Calendar date items::) appears before it in the date
+date item (*note Calendar date items::.) appears before it in the date
 string, then YYYY is read as the year, MM as the month number and DD as
 the day of the month, for the specified calendar date.
 
@@ -11063,7 +10957,7 @@
 feat.
 
    Second, it is also important to emphasize that with the
-_combination_ of the tools, it is possible to do a special purpose job
+*combination* of the tools, it is possible to do a special purpose job
 never imagined by the authors of the individual programs.
 
    Third, it is also valuable to build up your pipeline in stages, as
@@ -11107,7 +11001,7 @@
 in three columns.  The output columns are the data lines unique to the
 first file, the data lines unique to the second file, and the data
 lines that are common to both.  The `-1', `-2', and `-3' command line
-options _omit_ the respective columns. (This is non-intuitive and takes
+options *omit* the respective columns. (This is non-intuitive and takes
 a little getting used to.)  For example:
 
      $ cat f1
@@ -11225,7 +11119,7 @@
      $ tr '[A-Z]' '[a-z]' < whats.gnu | tr -cd '[A-Za-z0-9_ \012]' |
      > tr -s '[ ]' '\012' | sort -u | ...
 
-   Now, all we need is a list of words that are _not_ in the
+   Now, all we need is a list of words that are *not* in the
 dictionary.  Here is where the `comm' command comes in.
 
      $ tr '[A-Z]' '[a-z]' < whats.gnu | tr -cd '[A-Za-z0-9_ \012]' |
@@ -11302,13 +11196,14 @@
 
    Initially, the programs in both books were available (on 9-track
 tape) from Addison-Wesley.  Unfortunately, this is no longer the case,
-although the `ratfor' versions are available from Brian Kernighan's
-home page (http://cm.bell-labs.come/who/bwk), and you might be able to
-find copies of the Pascal versions floating around the Internet.  For a
-number of years, there was an active Software Tools Users Group, whose
-members had ported the original `ratfor' programs to essentially every
-computer system with a FORTRAN compiler.  The popularity of the group
-waned in the middle 1980s as Unix began to spread beyond universities.
+although the `ratfor' versions are available from
+Brian Kernighan's home page (http://cm.bell-labs.come/who/bwk), and you
+might be able to find copies of the Pascal versions floating around the
+Internet.  For a number of years, there was an active Software Tools
+Users Group, whose members had ported the original `ratfor' programs to
+essentially every computer system with a FORTRAN compiler.  The
+popularity of the group waned in the middle 1980s as Unix began to
+spread beyond universities.
 
    With the current proliferation of GNU code and other clones of Unix
 programs, these programs now receive little attention; modern C
@@ -11723,815 +11618,819 @@
 * - <1>:                                 su invocation.
 * - <2>:                                 env invocation.
 * - <3>:                                 Numeric expressions.
+* - <4>:                                 Common options.
 * -:                                     shred invocation.
 * - and Unix rm:                         rm invocation.
 * -, removing files beginning with:      rm invocation.
-* --:                                    Common options.
-* --across:                              pr invocation.
-* --address-radix:                       od invocation.
-* --adjustment:                          nice invocation.
-* --all <1>:                             uname invocation.
-* --all <2>:                             who invocation.
-* --all <3>:                             stty invocation.
-* --all <4>:                             du invocation.
-* --all <5>:                             df invocation.
-* --all <6>:                             Which files are listed.
-* --all:                                 unexpand invocation.
-* --all-repeated:                        uniq invocation.
-* --almost-all:                          Which files are listed.
-* --apparent-size:                       du invocation.
-* --append:                              tee invocation.
-* --archive:                             cp invocation.
-* --author:                              What information is listed.
-* --backup <1>:                          ln invocation.
-* --backup <2>:                          mv invocation.
-* --backup <3>:                          install invocation.
-* --backup <4>:                          cp invocation.
-* --backup:                              Backup options.
-* --before:                              tac invocation.
-* --binary <1>:                          md5sum invocation.
-* --binary:                              cat invocation.
-* --block-size <1>:                      du invocation.
-* --block-size <2>:                      df invocation.
-* --block-size:                          Block size.
-* --block-size=SIZE:                     Block size.
-* --body-numbering:                      nl invocation.
-* --boot:                                who invocation.
-* --bourne-shell:                        dircolors invocation.
-* --buffer-size:                         sort invocation.
-* --bytes <1>:                           du invocation.
-* --bytes <2>:                           cut invocation.
-* --bytes <3>:                           wc invocation.
-* --bytes <4>:                           split invocation.
-* --bytes <5>:                           tail invocation.
-* --bytes <6>:                           head invocation.
-* --bytes:                               fold invocation.
-* --c-shell:                             dircolors invocation.
-* --canonicalize:                        readlink invocation.
-* --changes <1>:                         chmod invocation.
-* --changes <2>:                         chgrp invocation.
-* --changes:                             chown invocation.
-* --characters:                          cut invocation.
-* --chars:                               wc invocation.
-* --check:                               sort invocation.
-* --check-chars:                         uniq invocation.
-* --classify:                            General output formatting.
-* --color:                               General output formatting.
-* --columns:                             pr invocation.
-* --command:                             su invocation.
-* --count <1>:                           who invocation.
-* --count:                               uniq invocation.
-* --count-links:                         du invocation.
-* --crown-margin:                        fmt invocation.
-* --csh:                                 dircolors invocation.
-* --date <1>:                            Options for date.
-* --date:                                touch invocation.
-* --dead:                                who invocation.
-* --delimiter:                           cut invocation.
-* --delimiters:                          paste invocation.
-* --dereference <1>:                     stat invocation.
-* --dereference <2>:                     du invocation.
-* --dereference <3>:                     chgrp invocation.
-* --dereference <4>:                     chown invocation.
-* --dereference <5>:                     cp invocation.
-* --dereference:                         Which files are listed.
-* --dereference-args:                    du invocation.
-* --dereference-command-line:            Which files are listed.
-* --dereference-command-line-symlink-to-dir: Which files are listed.
-* --dictionary-order:                    sort invocation.
-* --digits:                              csplit invocation.
-* --directory <1>:                       ln invocation.
-* --directory <2>:                       rm invocation.
-* --directory <3>:                       install invocation.
-* --directory:                           Which files are listed.
-* --dired:                               What information is listed.
-* --double-space:                        pr invocation.
-* --elide-empty-files:                   csplit invocation.
-* --escape:                              Formatting the file names.
-* --exact:                               shred invocation.
-* --exclude-from=FILE:                   du invocation.
-* --exclude-type:                        df invocation.
-* --exclude=PATTERN:                     du invocation.
-* --expand-tabs:                         pr invocation.
-* --fast:                                su invocation.
-* --field-separator:                     sort invocation.
-* --fields:                              cut invocation.
-* --file <1>:                            Options for date.
-* --file:                                stty invocation.
-* --file-type:                           General output formatting.
-* --filesystem:                          stat invocation.
-* --first-line-number:                   pr invocation.
-* --follow:                              tail invocation.
-* --footer-numbering:                    nl invocation.
-* --force <1>:                           ln invocation.
-* --force <2>:                           shred invocation.
-* --force <3>:                           rm invocation.
-* --force <4>:                           mv invocation.
-* --force:                               cp invocation.
-* --form-feed:                           pr invocation.
-* --format <1>:                          stat invocation.
-* --format <2>:                          General output formatting.
-* --format <3>:                          What information is listed.
-* --format:                              od invocation.
-* --format=FORMAT:                       seq invocation.
-* --from:                                chown invocation.
-* --full-time:                           What information is listed.
-* --general-numeric-sort:                sort invocation.
-* --group <1>:                           id invocation.
-* --group:                               install invocation.
-* --groups:                              id invocation.
-* --hardware-platform:                   uname invocation.
-* --header:                              pr invocation.
-* --header-numbering:                    nl invocation.
-* --heading:                             who invocation.
-* --help:                                Common options.
-* --hide-control-chars:                  Formatting the file names.
-* --human-readable <1>:                  du invocation.
-* --human-readable <2>:                  df invocation.
-* --human-readable <3>:                  What information is listed.
-* --human-readable:                      Block size.
-* --idle:                                who invocation.
-* --ignore-backups:                      Which files are listed.
-* --ignore-case <1>:                     join invocation.
-* --ignore-case <2>:                     uniq invocation.
-* --ignore-case:                         sort invocation.
-* --ignore-environment:                  env invocation.
-* --ignore-fail-on-non-empty:            rmdir invocation.
-* --ignore-interrupts:                   tee invocation.
-* --ignore-leading-blanks:               sort invocation.
-* --ignore-nonprinting:                  sort invocation.
-* --ignore=PATTERN:                      Which files are listed.
-* --indent:                              pr invocation.
-* --indicator-style:                     General output formatting.
-* --initial:                             expand invocation.
-* --inode:                               What information is listed.
-* --inodes:                              df invocation.
-* --interactive <1>:                     ln invocation.
-* --interactive <2>:                     rm invocation.
-* --interactive <3>:                     mv invocation.
-* --interactive:                         cp invocation.
-* --iso-8601[=TIMESPEC]:                 Options for date.
-* --iterations=NUMBER:                   shred invocation.
-* --join-blank-lines:                    nl invocation.
-* --join-lines:                          pr invocation.
-* --keep-files:                          csplit invocation.
-* --kernel-name:                         uname invocation.
-* --kernel-release:                      uname invocation.
-* --kernel-version:                      uname invocation.
-* --key:                                 sort invocation.
-* --length:                              pr invocation.
-* --line-bytes:                          split invocation.
-* --lines <1>:                           wc invocation.
-* --lines <2>:                           split invocation.
-* --lines <3>:                           tail invocation.
-* --lines:                               head invocation.
-* --link:                                cp invocation.
-* --literal:                             Formatting the file names.
-* --local:                               df invocation.
-* --login <1>:                           su invocation.
-* --login:                               who invocation.
-* --lookup:                              who invocation.
-* --machine:                             uname invocation.
-* --max-depth=DEPTH:                     du invocation.
-* --max-line-length:                     wc invocation.
-* --max-unchanged-stats:                 tail invocation.
-* --merge <1>:                           sort invocation.
-* --merge:                               pr invocation.
-* --mesg:                                who invocation.
-* --message:                             who invocation.
-* --mode <1>:                            mknod invocation.
-* --mode <2>:                            mkfifo invocation.
-* --mode <3>:                            mkdir invocation.
-* --mode:                                install invocation.
-* --month-sort:                          sort invocation.
-* --name:                                id invocation.
-* --no-create:                           touch invocation.
-* --no-dereference <1>:                  du invocation.
-* --no-dereference <2>:                  chgrp invocation.
-* --no-dereference <3>:                  chown invocation.
-* --no-dereference <4>:                  ln invocation.
-* --no-dereference:                      cp invocation.
-* --no-file-warnings:                    pr invocation.
-* --no-group:                            What information is listed.
-* --no-newline:                          readlink invocation.
-* --no-preserve-root <1>:                chmod invocation.
-* --no-preserve-root <2>:                chgrp invocation.
-* --no-preserve-root <3>:                chown invocation.
-* --no-preserve-root:                    rm invocation.
-* --no-renumber:                         nl invocation.
-* --no-sync:                             df invocation.
-* --nodename:                            uname invocation.
-* --null:                                du invocation.
-* --number:                              cat invocation.
-* --number-format:                       nl invocation.
-* --number-lines:                        pr invocation.
-* --number-nonblank:                     cat invocation.
-* --number-separator:                    nl invocation.
-* --number-width:                        nl invocation.
-* --numeric-sort:                        sort invocation.
-* --numeric-suffixes:                    split invocation.
-* --numeric-uid-gid:                     What information is listed.
-* --omit-header:                         pr invocation.
-* --omit-pagination:                     pr invocation.
-* --one-file-system <1>:                 du invocation.
-* --one-file-system:                     cp invocation.
-* --only-delimited:                      cut invocation.
-* --operating-system:                    uname invocation.
-* --output:                              sort invocation.
-* --output-delimiter:                    cut invocation.
-* --output-duplicates:                   od invocation.
-* --output-tabs:                         pr invocation.
-* --owner:                               install invocation.
-* --page-increment:                      nl invocation.
-* --page_width:                          pr invocation.
-* --pages=PAGE_RANGE:                    pr invocation.
-* --parents <1>:                         rmdir invocation.
-* --parents <2>:                         mkdir invocation.
-* --parents:                             cp invocation.
-* --pid:                                 tail invocation.
-* --portability <1>:                     pathchk invocation.
-* --portability:                         df invocation.
-* --prefix:                              csplit invocation.
-* --preserve:                            cp invocation.
-* --preserve-environment:                su invocation.
-* --preserve-root <1>:                   chmod invocation.
-* --preserve-root <2>:                   chgrp invocation.
-* --preserve-root <3>:                   chown invocation.
-* --preserve-root:                       rm invocation.
-* --preserve-timestamps:                 install invocation.
-* --print-database:                      dircolors invocation.
-* --print-type:                          df invocation.
-* --processor:                           uname invocation.
-* --quiet <1>:                           tty invocation.
-* --quiet <2>:                           chmod invocation.
-* --quiet <3>:                           chgrp invocation.
-* --quiet <4>:                           chown invocation.
-* --quiet <5>:                           readlink invocation.
-* --quiet <6>:                           csplit invocation.
-* --quiet <7>:                           tail invocation.
-* --quiet:                               head invocation.
-* --quote-name:                          Formatting the file names.
-* --quoting-style:                       Formatting the file names.
-* --read-bytes:                          od invocation.
-* --real:                                id invocation.
-* --recursive <1>:                       chmod invocation.
-* --recursive <2>:                       chgrp invocation.
-* --recursive <3>:                       chown invocation.
-* --recursive <4>:                       rm invocation.
-* --recursive <5>:                       cp invocation.
-* --recursive:                           Which files are listed.
-* --reference <1>:                       Options for date.
-* --reference <2>:                       touch invocation.
-* --reference <3>:                       chmod invocation.
-* --reference <4>:                       chgrp invocation.
-* --reference:                           chown invocation.
-* --regex:                               tac invocation.
-* --remove:                              shred invocation.
-* --remove-destination:                  cp invocation.
-* --repeated:                            uniq invocation.
-* --reply <1>:                           mv invocation.
-* --reply:                               cp invocation.
-* --retry:                               tail invocation.
-* --reverse <1>:                         Sorting the output.
-* --reverse:                             sort invocation.
-* --rfc-2822:                            Options for date.
-* --rfc-822:                             Options for date.
-* --save:                                stty invocation.
-* --section-delimiter:                   nl invocation.
-* --sep-string:                          pr invocation.
-* --separate-dirs:                       du invocation.
-* --separator <1>:                       pr invocation.
-* --separator:                           tac invocation.
-* --serial:                              paste invocation.
-* --set:                                 Options for date.
-* --sh:                                  dircolors invocation.
-* --shell:                               su invocation.
-* --show-all:                            cat invocation.
-* --show-control-chars <1>:              Formatting the file names.
-* --show-control-chars:                  pr invocation.
-* --show-ends:                           cat invocation.
-* --show-nonprinting <1>:                pr invocation.
-* --show-nonprinting:                    cat invocation.
-* --show-tabs:                           cat invocation.
-* --si <1>:                              du invocation.
-* --si <2>:                              df invocation.
-* --si <3>:                              What information is listed.
-* --si:                                  Block size.
-* --silent <1>:                          tty invocation.
-* --silent <2>:                          chmod invocation.
-* --silent <3>:                          chgrp invocation.
-* --silent <4>:                          chown invocation.
-* --silent <5>:                          readlink invocation.
-* --silent <6>:                          csplit invocation.
-* --silent <7>:                          tail invocation.
-* --silent:                              head invocation.
-* --size:                                What information is listed.
-* --size=BYTES:                          shred invocation.
-* --skip-bytes:                          od invocation.
-* --skip-chars:                          uniq invocation.
-* --skip-fields:                         uniq invocation.
-* --sleep-interval:                      tail invocation.
-* --sort:                                Sorting the output.
-* --spaces:                              fold invocation.
-* --sparse=WHEN:                         cp invocation.
-* --split-only:                          fmt invocation.
-* --squeeze-blank:                       cat invocation.
-* --stable:                              sort invocation.
-* --starting-line-number:                nl invocation.
-* --status:                              md5sum invocation.
-* --strings:                             od invocation.
-* --strip:                               install invocation.
-* --strip-trailing-slashes <1>:          mv invocation.
-* --strip-trailing-slashes:              cp invocation.
-* --suffix <1>:                          ln invocation.
-* --suffix <2>:                          mv invocation.
-* --suffix <3>:                          install invocation.
-* --suffix <4>:                          cp invocation.
-* --suffix <5>:                          csplit invocation.
-* --suffix:                              Backup options.
-* --suffix-length:                       split invocation.
-* --summarize:                           du invocation.
-* --symbolic:                            ln invocation.
-* --symbolic-link:                       cp invocation.
-* --sync:                                df invocation.
-* --sysv:                                sum invocation.
-* --tabs <1>:                            unexpand invocation.
-* --tabs:                                expand invocation.
-* --tabsize:                             General output formatting.
-* --tagged-paragraph:                    fmt invocation.
-* --target-directory <1>:                ln invocation.
-* --target-directory <2>:                mv invocation.
-* --target-directory <3>:                install invocation.
-* --target-directory <4>:                cp invocation.
-* --target-directory:                    Target directory.
-* --temporary-directory:                 sort invocation.
-* --terse:                               stat invocation.
-* --text:                                md5sum invocation.
-* --time <1>:                            touch invocation.
-* --time:                                Sorting the output.
-* --time-style:                          Formatting file timestamps.
-* --total:                               du invocation.
-* --traditional:                         od invocation.
-* --type:                                df invocation.
-* --uniform-spacing:                     fmt invocation.
-* --unique <1>:                          uniq invocation.
-* --unique:                              sort invocation.
-* --universal:                           Options for date.
-* --update:                              mv invocation.
-* --user:                                id invocation.
-* --utc:                                 Options for date.
-* --verbose <1>:                         chmod invocation.
-* --verbose <2>:                         chgrp invocation.
-* --verbose <3>:                         chown invocation.
-* --verbose <4>:                         rmdir invocation.
-* --verbose <5>:                         readlink invocation.
-* --verbose <6>:                         mkdir invocation.
-* --verbose <7>:                         ln invocation.
-* --verbose <8>:                         shred invocation.
-* --verbose <9>:                         rm invocation.
-* --verbose <10>:                        mv invocation.
-* --verbose <11>:                        install invocation.
-* --verbose <12>:                        cp invocation.
-* --verbose <13>:                        split invocation.
-* --verbose <14>:                        tail invocation.
-* --verbose:                             head invocation.
-* --version:                             Common options.
-* --version-control <1>:                 ln invocation.
-* --version-control <2>:                 mv invocation.
-* --version-control <3>:                 install invocation.
-* --version-control <4>:                 cp invocation.
-* --version-control:                     Backup options.
-* --warn:                                md5sum invocation.
-* --width <1>:                           General output formatting.
-* --width <2>:                           fold invocation.
-* --width <3>:                           pr invocation.
-* --width <4>:                           fmt invocation.
-* --width:                               od invocation.
-* --words:                               wc invocation.
-* --writable:                            who invocation.
-* --zero:                                shred invocation.
-* --zero-terminated:                     sort invocation.
 * -0:                                    du invocation.
-* -1 <1>:                                General output formatting.
-* -1 <2>:                                join invocation.
-* -1:                                    comm invocation.
+* -1 <1>:                                comm invocation.
+* -1 <2>:                                General output formatting.
+* -1:                                    join invocation.
 * -2 <1>:                                join invocation.
 * -2:                                    comm invocation.
 * -3:                                    comm invocation.
-* -a <1>:                                uname invocation.
-* -a <2>:                                who invocation.
-* -a <3>:                                stty invocation.
-* -a <4>:                                tee invocation.
-* -a <5>:                                Connectives for test.
-* -a <6>:                                du invocation.
-* -a <7>:                                df invocation.
-* -a <8>:                                touch invocation.
-* -a:                                    cp invocation.
-* -A:                                    Which files are listed.
 * -a <1>:                                Which files are listed.
-* -a <2>:                                unexpand invocation.
-* -a <3>:                                join invocation.
-* -a <4>:                                split invocation.
-* -a <5>:                                pr invocation.
-* -a:                                    od invocation.
-* -A <1>:                                od invocation.
+* -a <2>:                                Connectives for test.
+* -a <3>:                                touch invocation.
+* -a <4>:                                join invocation.
+* -a:                                    du invocation.
 * -A:                                    cat invocation.
+* -a <1>:                                pr invocation.
+* -a:                                    uname invocation.
+* -A:                                    od invocation.
+* -a <1>:                                od invocation.
+* -a <2>:                                df invocation.
+* -a <3>:                                unexpand invocation.
+* -a:                                    tee invocation.
+* -A:                                    Which files are listed.
+* -a <1>:                                cp invocation.
+* -a <2>:                                stty invocation.
+* -a <3>:                                who invocation.
+* -a:                                    split invocation.
+* -across:                               pr invocation.
+* -address-radix:                        od invocation.
+* -adjustment:                           nice invocation.
+* -all <1>:                              unexpand invocation.
+* -all <2>:                              uname invocation.
+* -all <3>:                              df invocation.
+* -all <4>:                              Which files are listed.
+* -all <5>:                              du invocation.
+* -all <6>:                              stty invocation.
+* -all:                                  who invocation.
+* -all-repeated:                         uniq invocation.
+* -almost-all:                           Which files are listed.
+* -apparent-size:                        du invocation.
+* -append:                               tee invocation.
+* -archive:                              cp invocation.
+* -author:                               What information is listed.
+* -b <1>:                                Backup options.
+* -b <2>:                                cat invocation.
+* -b:                                    nl invocation.
+* -B:                                    cat invocation.
 * -b <1>:                                who invocation.
-* -b:                                    File type tests.
-* -B:                                    du invocation.
-* -b:                                    du invocation.
-* -B:                                    df invocation.
-* -b <1>:                                ln invocation.
-* -b <2>:                                mv invocation.
-* -b <3>:                                install invocation.
-* -b <4>:                                cp invocation.
-* -b <5>:                                dircolors invocation.
-* -b:                                    Formatting the file names.
+* -b <2>:                                tac invocation.
+* -b:                                    md5sum invocation.
 * -B:                                    Which files are listed.
-* -b <1>:                                cut invocation.
-* -b <2>:                                sort invocation.
-* -b <3>:                                md5sum invocation.
-* -b <4>:                                csplit invocation.
-* -b <5>:                                split invocation.
-* -b <6>:                                fold invocation.
-* -b <7>:                                od invocation.
-* -b <8>:                                nl invocation.
-* -b <9>:                                tac invocation.
-* -b:                                    cat invocation.
-* -B:                                    cat invocation.
-* -b:                                    Backup options.
-* -c <1>:                                su invocation.
-* -c <2>:                                File type tests.
-* -c <3>:                                stat invocation.
-* -c <4>:                                du invocation.
-* -c <5>:                                touch invocation.
-* -c <6>:                                chmod invocation.
-* -c <7>:                                chgrp invocation.
-* -c <8>:                                chown invocation.
-* -c <9>:                                install invocation.
-* -c:                                    dircolors invocation.
-* -C:                                    General output formatting.
-* -c <1>:                                Sorting the output.
-* -c <2>:                                cut invocation.
-* -c <3>:                                uniq invocation.
-* -c <4>:                                sort invocation.
-* -c:                                    wc invocation.
-* -C:                                    split invocation.
-* -c <1>:                                tail invocation.
-* -c <2>:                                head invocation.
-* -c <3>:                                pr invocation.
+* -b:                                    sort invocation.
+* -B:                                    df invocation.
+* -b <1>:                                Formatting the file names.
+* -b <2>:                                fold invocation.
+* -b <3>:                                split invocation.
+* -b <4>:                                dircolors invocation.
+* -b <5>:                                File type tests.
+* -b <6>:                                od invocation.
+* -b <7>:                                cut invocation.
+* -b <8>:                                cp invocation.
+* -b <9>:                                ln invocation.
+* -b <10>:                               install invocation.
+* -b:                                    du invocation.
+* -B:                                    du invocation.
+* -b <1>:                                mv invocation.
+* -b:                                    csplit invocation.
+* -backup <1>:                           mv invocation.
+* -backup <2>:                           Backup options.
+* -backup <3>:                           cp invocation.
+* -backup <4>:                           ln invocation.
+* -backup:                               install invocation.
+* -before:                               tac invocation.
+* -binary <1>:                           md5sum invocation.
+* -binary:                               cat invocation.
+* -block-size <1>:                       df invocation.
+* -block-size <2>:                       du invocation.
+* -block-size:                           Block size.
+* -block-size=SIZE:                      Block size.
+* -body-numbering:                       nl invocation.
+* -boot:                                 who invocation.
+* -bourne-shell:                         dircolors invocation.
+* -buffer-size:                          sort invocation.
+* -bytes <1>:                            split invocation.
+* -bytes <2>:                            wc invocation.
+* -bytes <3>:                            tail invocation.
+* -bytes <4>:                            head invocation.
+* -bytes <5>:                            fold invocation.
+* -bytes <6>:                            cut invocation.
+* -bytes:                                du invocation.
+* -c <1>:                                dircolors invocation.
+* -c <2>:                                wc invocation.
+* -c <3>:                                od invocation.
 * -c <4>:                                fmt invocation.
-* -c:                                    od invocation.
+* -c <5>:                                cut invocation.
+* -c:                                    chown invocation.
+* -C:                                    split invocation.
+* -c <1>:                                uniq invocation.
+* -c <2>:                                stat invocation.
+* -c <3>:                                du invocation.
+* -c <4>:                                chmod invocation.
+* -c <5>:                                File type tests.
+* -c:                                    install invocation.
+* -C:                                    General output formatting.
+* -c <1>:                                head invocation.
+* -c <2>:                                chgrp invocation.
+* -c <3>:                                Sorting the output.
+* -c <4>:                                pr invocation.
+* -c <5>:                                su invocation.
+* -c <6>:                                tail invocation.
+* -c <7>:                                touch invocation.
+* -c:                                    sort invocation.
+* -c-shell:                              dircolors invocation.
+* -canonicalize:                         readlink invocation.
+* -changes <1>:                          chown invocation.
+* -changes <2>:                          chmod invocation.
+* -changes:                              chgrp invocation.
+* -characters:                           cut invocation.
+* -chars:                                wc invocation.
+* -check:                                sort invocation.
+* -check-chars:                          uniq invocation.
+* -classify:                             General output formatting.
+* -color:                                General output formatting.
 * -COLUMN:                               pr invocation.
-* -d <1>:                                Options for date.
-* -d <2>:                                who invocation.
-* -d:                                    File type tests.
+* -columns:                              pr invocation.
+* -command:                              su invocation.
+* -count <1>:                            who invocation.
+* -count:                                uniq invocation.
+* -count-links:                          du invocation.
+* -crown-margin:                         fmt invocation.
+* -csh:                                  dircolors invocation.
+* -d <1>:                                sort invocation.
+* -d <2>:                                split invocation.
+* -d <3>:                                od invocation.
+* -d <4>:                                who invocation.
+* -d:                                    pr invocation.
+* -D <1>:                                uniq invocation.
 * -D:                                    du invocation.
-* -d <1>:                                touch invocation.
-* -d <2>:                                ln invocation.
-* -d <3>:                                rm invocation.
-* -d <4>:                                install invocation.
-* -d:                                    cp invocation.
-* -D:                                    What information is listed.
-* -d <1>:                                Which files are listed.
+* -d <1>:                                install invocation.
 * -d <2>:                                paste invocation.
+* -d <3>:                                nl invocation.
 * -d:                                    cut invocation.
-* -D:                                    uniq invocation.
-* -d <1>:                                uniq invocation.
-* -d <2>:                                sort invocation.
-* -d <3>:                                split invocation.
-* -d <4>:                                pr invocation.
-* -d <5>:                                od invocation.
-* -d:                                    nl invocation.
-* -e <1>:                                File characteristic tests.
-* -e <2>:                                echo invocation.
-* -e <3>:                                join invocation.
+* -D:                                    What information is listed.
+* -d <1>:                                File type tests.
+* -d <2>:                                Options for date.
+* -d <3>:                                touch invocation.
+* -d <4>:                                Which files are listed.
+* -d <5>:                                cp invocation.
+* -d <6>:                                uniq invocation.
+* -d <7>:                                ln invocation.
+* -d:                                    rm invocation.
+* -date <1>:                             Options for date.
+* -date:                                 touch invocation.
+* -dead:                                 who invocation.
+* -delimiter:                            cut invocation.
+* -delimiters:                           paste invocation.
+* -dereference <1>:                      chgrp invocation.
+* -dereference <2>:                      cp invocation.
+* -dereference <3>:                      stat invocation.
+* -dereference <4>:                      chown invocation.
+* -dereference <5>:                      du invocation.
+* -dereference:                          Which files are listed.
+* -dereference-args:                     du invocation.
+* -dereference-command-line:             Which files are listed.
+* -dereference-command-line-symlink-to-dir: Which files are listed.
+* -dictionary-order:                     sort invocation.
+* -digits:                               csplit invocation.
+* -directory <1>:                        Which files are listed.
+* -directory <2>:                        rm invocation.
+* -directory <3>:                        ln invocation.
+* -directory:                            install invocation.
+* -dired:                                What information is listed.
+* -double-space:                         pr invocation.
+* -e <1>:                                echo invocation.
 * -e:                                    pr invocation.
 * -E:                                    cat invocation.
-* -e:                                    cat invocation.
+* -e <1>:                                cat invocation.
+* -e <2>:                                join invocation.
+* -e:                                    File characteristic tests.
 * -ef:                                   File characteristic tests.
+* -elide-empty-files:                    csplit invocation.
 * -eq:                                   Numeric tests.
-* -f <1>:                                su invocation.
-* -f:                                    Options for date.
-* -F:                                    stty invocation.
-* -f <1>:                                File type tests.
-* -f <2>:                                stat invocation.
-* -f <3>:                                touch invocation.
-* -f <4>:                                chmod invocation.
-* -f <5>:                                chgrp invocation.
-* -f <6>:                                chown invocation.
-* -f <7>:                                readlink invocation.
-* -f:                                    ln invocation.
-* -F:                                    ln invocation.
-* -f <1>:                                shred invocation.
-* -f <2>:                                rm invocation.
-* -f <3>:                                mv invocation.
-* -f:                                    cp invocation.
-* -F:                                    General output formatting.
+* -escape:                               Formatting the file names.
+* -exact:                                shred invocation.
+* -exclude-from=FILE:                    du invocation.
+* -exclude-type:                         df invocation.
+* -exclude=PATTERN:                      du invocation.
+* -expand-tabs:                          pr invocation.
 * -f <1>:                                Sorting the output.
-* -f <2>:                                cut invocation.
-* -f <3>:                                uniq invocation.
+* -f:                                    readlink invocation.
+* -F:                                    General output formatting.
+* -f <1>:                                chown invocation.
+* -f <2>:                                pr invocation.
+* -f <3>:                                csplit invocation.
 * -f <4>:                                sort invocation.
-* -f:                                    csplit invocation.
-* -F:                                    tail invocation.
-* -f <1>:                                tail invocation.
-* -f:                                    pr invocation.
+* -f <5>:                                mv invocation.
+* -f <6>:                                chgrp invocation.
+* -f <7>:                                touch invocation.
+* -f <8>:                                od invocation.
+* -f <9>:                                Options for date.
+* -f:                                    uniq invocation.
 * -F:                                    pr invocation.
-* -f <1>:                                od invocation.
+* -f <1>:                                File type tests.
+* -f <2>:                                shred invocation.
+* -f <3>:                                chmod invocation.
+* -f <4>:                                su invocation.
+* -f <5>:                                rm invocation.
+* -f <6>:                                tail invocation.
+* -f <7>:                                cp invocation.
 * -f:                                    nl invocation.
+* -F <1>:                                stty invocation.
+* -F:                                    tail invocation.
+* -f <1>:                                cut invocation.
+* -f:                                    ln invocation.
+* -F:                                    ln invocation.
+* -f:                                    stat invocation.
 * -f FORMAT:                             seq invocation.
+* -fast:                                 su invocation.
+* -field-separator:                      sort invocation.
+* -fields:                               cut invocation.
+* -file <1>:                             stty invocation.
+* -file:                                 Options for date.
+* -file-type:                            General output formatting.
+* -filesystem:                           stat invocation.
+* -first-line-number:                    pr invocation.
+* -follow:                               tail invocation.
+* -footer-numbering:                     nl invocation.
+* -force <1>:                            rm invocation.
+* -force <2>:                            shred invocation.
+* -force <3>:                            mv invocation.
+* -force <4>:                            cp invocation.
+* -force:                                ln invocation.
+* -form-feed:                            pr invocation.
+* -format <1>:                           What information is listed.
+* -format <2>:                           General output formatting.
+* -format <3>:                           od invocation.
+* -format <4>:                           General output formatting.
+* -format:                               stat invocation.
+* -format=FORMAT:                        seq invocation.
+* -from:                                 chown invocation.
+* -full-time:                            What information is listed.
 * -G:                                    id invocation.
 * -g <1>:                                id invocation.
-* -g:                                    stty invocation.
-* -G:                                    Access permission tests.
-* -g <1>:                                Access permission tests.
+* -g <2>:                                What information is listed.
+* -g <3>:                                Access permission tests.
 * -g:                                    install invocation.
-* -G:                                    What information is listed.
-* -g <1>:                                What information is listed.
+* -G <1>:                                What information is listed.
+* -G:                                    Access permission tests.
+* -g <1>:                                stty invocation.
 * -g:                                    sort invocation.
 * -ge:                                   Numeric tests.
+* -general-numeric-sort:                 sort invocation.
+* -group <1>:                            id invocation.
+* -group:                                install invocation.
+* -groups:                               id invocation.
 * -gt:                                   Numeric tests.
-* -H:                                    who invocation.
-* -h:                                    File type tests.
-* -H:                                    du invocation.
-* -h:                                    du invocation.
-* -H:                                    df invocation.
-* -h:                                    df invocation.
-* -H:                                    chgrp invocation.
-* -h:                                    chgrp invocation.
-* -H:                                    chown invocation.
-* -h:                                    chown invocation.
-* -H:                                    cp invocation.
-* -h:                                    What information is listed.
+* -h <1>:                                What information is listed.
+* -h <2>:                                du invocation.
+* -h:                                    Block size.
 * -H:                                    Which files are listed.
-* -h <1>:                                pr invocation.
-* -h <2>:                                od invocation.
-* -h:                                    nl invocation.
+* -h:                                    pr invocation.
+* -H <1>:                                du invocation.
+* -H:                                    chown invocation.
+* -h <1>:                                File type tests.
+* -h:                                    od invocation.
 * -H:                                    Traversing symlinks.
-* -h:                                    Block size.
-* -i <1>:                                env invocation.
-* -i <2>:                                uname invocation.
-* -i <3>:                                who invocation.
-* -i <4>:                                tee invocation.
-* -i <5>:                                df invocation.
-* -i <6>:                                ln invocation.
-* -i <7>:                                rm invocation.
-* -i <8>:                                mv invocation.
-* -i <9>:                                cp invocation.
-* -i:                                    What information is listed.
-* -I:                                    Which files are listed.
-* -i <1>:                                expand invocation.
-* -i <2>:                                join invocation.
-* -i <3>:                                uniq invocation.
-* -i <4>:                                sort invocation.
-* -i <5>:                                pr invocation.
+* -h:                                    chgrp invocation.
+* -H <1>:                                chgrp invocation.
+* -H <2>:                                cp invocation.
+* -H <3>:                                who invocation.
+* -H:                                    df invocation.
+* -h <1>:                                chown invocation.
+* -h <2>:                                df invocation.
+* -h:                                    nl invocation.
+* -H:                                    who invocation.
+* -hardware-platform:                    uname invocation.
+* -header:                               pr invocation.
+* -header-numbering:                     nl invocation.
+* -heading:                              who invocation.
+* -help:                                 Common options.
+* -hide-control-chars:                   Formatting the file names.
+* -human-readable <1>:                   du invocation.
+* -human-readable <2>:                   Block size.
+* -human-readable <3>:                   What information is listed.
+* -human-readable:                       df invocation.
+* -i <1>:                                ln invocation.
+* -i <2>:                                cp invocation.
+* -i <3>:                                tee invocation.
+* -i <4>:                                uniq invocation.
+* -i <5>:                                who invocation.
 * -i <6>:                                od invocation.
-* -i:                                    nl invocation.
+* -i <7>:                                uname invocation.
+* -i <8>:                                pr invocation.
+* -i <9>:                                who invocation.
+* -i <10>:                               expand invocation.
+* -i <11>:                               nl invocation.
+* -i <12>:                               join invocation.
+* -i <13>:                               What information is listed.
+* -i <14>:                               env invocation.
+* -i:                                    df invocation.
+* -I:                                    Which files are listed.
+* -i <1>:                                rm invocation.
+* -i <2>:                                sort invocation.
+* -i:                                    mv invocation.
 * -I TIMESPEC:                           Options for date.
+* -idle:                                 who invocation.
+* -ignore-backups:                       Which files are listed.
+* -ignore-case <1>:                      sort invocation.
+* -ignore-case <2>:                      uniq invocation.
+* -ignore-case:                          join invocation.
+* -ignore-environment:                   env invocation.
+* -ignore-fail-on-non-empty:             rmdir invocation.
+* -ignore-interrupts:                    tee invocation.
+* -ignore-leading-blanks:                sort invocation.
+* -ignore-nonprinting:                   sort invocation.
+* -ignore=PATTERN:                       Which files are listed.
+* -indent:                               pr invocation.
+* -indicator-style:                      General output formatting.
+* -initial:                              expand invocation.
+* -inode:                                What information is listed.
+* -inodes:                               df invocation.
+* -interactive <1>:                      ln invocation.
+* -interactive <2>:                      mv invocation.
+* -interactive <3>:                      rm invocation.
+* -interactive:                          cp invocation.
+* -iso-8601[=TIMESPEC]:                  Options for date.
+* -iterations=NUMBER:                    shred invocation.
 * -J:                                    pr invocation.
 * -j:                                    od invocation.
-* -k <1>:                                Access permission tests.
+* -join-blank-lines:                     nl invocation.
+* -join-lines:                           pr invocation.
+* -k <1>:                                sort invocation.
 * -k <2>:                                du invocation.
-* -k <3>:                                df invocation.
-* -k <4>:                                General output formatting.
-* -k <5>:                                sort invocation.
-* -k <6>:                                csplit invocation.
-* -k:                                    Block size.
-* -l <1>:                                su invocation.
-* -l:                                    who invocation.
+* -k <3>:                                General output formatting.
+* -k <4>:                                Block size.
+* -k <5>:                                Access permission tests.
+* -k <6>:                                df invocation.
+* -k:                                    csplit invocation.
+* -keep-files:                           csplit invocation.
+* -kernel-name:                          uname invocation.
+* -kernel-release:                       uname invocation.
+* -kernel-version:                       uname invocation.
+* -key:                                  sort invocation.
+* -L:                                    Traversing symlinks.
+* -l <1>:                                What information is listed.
+* -l:                                    nl invocation.
 * -L <1>:                                File type tests.
-* -L <2>:                                stat invocation.
 * -L:                                    du invocation.
-* -l <1>:                                du invocation.
+* -l <1>:                                pr invocation.
+* -l <2>:                                who invocation.
+* -l <3>:                                split invocation.
+* -l:                                    su invocation.
+* -L:                                    wc invocation.
+* -l <1>:                                od invocation.
+* -l <2>:                                du invocation.
+* -l <3>:                                cp invocation.
+* -l <4>:                                wc invocation.
 * -l:                                    df invocation.
 * -L <1>:                                chgrp invocation.
-* -L <2>:                                chown invocation.
-* -L:                                    cp invocation.
-* -l <1>:                                cp invocation.
-* -l:                                    What information is listed.
-* -L <1>:                                Which files are listed.
-* -L:                                    wc invocation.
-* -l <1>:                                wc invocation.
-* -l <2>:                                split invocation.
-* -l <3>:                                pr invocation.
-* -l <4>:                                od invocation.
-* -l:                                    nl invocation.
-* -L:                                    Traversing symlinks.
+* -L <2>:                                stat invocation.
+* -L <3>:                                cp invocation.
+* -L <4>:                                chown invocation.
+* -L:                                    Which files are listed.
 * -le:                                   Numeric tests.
+* -length:                               pr invocation.
+* -line-bytes:                           split invocation.
+* -lines <1>:                            head invocation.
+* -lines <2>:                            tail invocation.
+* -lines <3>:                            wc invocation.
+* -lines:                                split invocation.
+* -link:                                 cp invocation.
+* -literal:                              Formatting the file names.
+* -local:                                df invocation.
+* -login <1>:                            who invocation.
+* -login:                                su invocation.
+* -lookup:                               who invocation.
 * -lt:                                   Numeric tests.
+* -m <1>:                                wc invocation.
+* -m <2>:                                who invocation.
+* -m <3>:                                General output formatting.
+* -m <4>:                                sort invocation.
+* -m:                                    uname invocation.
+* -M:                                    sort invocation.
 * -m <1>:                                su invocation.
-* -m <2>:                                uname invocation.
-* -m <3>:                                who invocation.
-* -m <4>:                                touch invocation.
-* -m <5>:                                mknod invocation.
+* -m <2>:                                touch invocation.
+* -m <3>:                                install invocation.
+* -m <4>:                                mknod invocation.
+* -m <5>:                                pr invocation.
 * -m <6>:                                mkfifo invocation.
-* -m <7>:                                mkdir invocation.
-* -m <8>:                                install invocation.
-* -m:                                    General output formatting.
-* -M:                                    sort invocation.
-* -m <1>:                                sort invocation.
-* -m <2>:                                wc invocation.
-* -m:                                    pr invocation.
-* -n <1>:                                nice invocation.
-* -n <2>:                                uname invocation.
-* -n <3>:                                id invocation.
-* -n <4>:                                String tests.
-* -n <5>:                                echo invocation.
-* -n <6>:                                readlink invocation.
-* -n:                                    ln invocation.
-* -N:                                    Formatting the file names.
-* -n <1>:                                What information is listed.
-* -n <2>:                                cut invocation.
-* -n <3>:                                sort invocation.
-* -n <4>:                                csplit invocation.
-* -n <5>:                                tail invocation.
-* -n:                                    head invocation.
+* -m:                                    mkdir invocation.
+* -machine:                              uname invocation.
+* -max-depth=DEPTH:                      du invocation.
+* -max-line-length:                      wc invocation.
+* -max-unchanged-stats:                  tail invocation.
+* -merge <1>:                            sort invocation.
+* -merge:                                pr invocation.
+* -mesg:                                 who invocation.
+* -message:                              who invocation.
+* -mode <1>:                             mkfifo invocation.
+* -mode <2>:                             mkdir invocation.
+* -mode <3>:                             install invocation.
+* -mode:                                 mknod invocation.
+* -month-sort:                           sort invocation.
+* -n <1>:                                nl invocation.
+* -n <2>:                                tail invocation.
+* -n <3>:                                uname invocation.
+* -n <4>:                                What information is listed.
+* -n <5>:                                readlink invocation.
+* -n <6>:                                cut invocation.
+* -n <7>:                                head invocation.
+* -n <8>:                                nice invocation.
+* -n <9>:                                String tests.
+* -n:                                    csplit invocation.
 * -N:                                    pr invocation.
-* -n:                                    pr invocation.
+* -n <1>:                                ln invocation.
+* -n:                                    id invocation.
 * -N:                                    od invocation.
-* -n <1>:                                nl invocation.
-* -n:                                    cat invocation.
+* -n <1>:                                cat invocation.
+* -n <2>:                                pr invocation.
+* -n:                                    sort invocation.
+* -N:                                    Formatting the file names.
+* -n:                                    echo invocation.
 * -n NUMBER:                             shred invocation.
+* -name:                                 id invocation.
 * -ne:                                   Numeric tests.
+* -no-create:                            touch invocation.
+* -no-dereference <1>:                   cp invocation.
+* -no-dereference <2>:                   chgrp invocation.
+* -no-dereference <3>:                   ln invocation.
+* -no-dereference <4>:                   du invocation.
+* -no-dereference:                       chown invocation.
+* -no-file-warnings:                     pr invocation.
+* -no-group:                             What information is listed.
+* -no-newline:                           readlink invocation.
+* -no-preserve-root <1>:                 chown invocation.
+* -no-preserve-root <2>:                 rm invocation.
+* -no-preserve-root <3>:                 chmod invocation.
+* -no-preserve-root:                     chgrp invocation.
+* -no-renumber:                          nl invocation.
+* -no-sync:                              df invocation.
+* -nodename:                             uname invocation.
 * -nt:                                   File characteristic tests.
-* -o <1>:                                uname invocation.
-* -o:                                    Connectives for test.
+* -null:                                 du invocation.
+* -number:                               cat invocation.
+* -number-format:                        nl invocation.
+* -number-lines:                         pr invocation.
+* -number-nonblank:                      cat invocation.
+* -number-separator:                     nl invocation.
+* -number-width:                         nl invocation.
+* -numeric-sort:                         sort invocation.
+* -numeric-suffixes:                     split invocation.
+* -numeric-uid-gid:                      What information is listed.
 * -O:                                    Access permission tests.
-* -o <1>:                                install invocation.
-* -o <2>:                                What information is listed.
+* -o <1>:                                od invocation.
+* -o <2>:                                pr invocation.
 * -o <3>:                                sort invocation.
-* -o <4>:                                pr invocation.
-* -o:                                    od invocation.
+* -o <4>:                                install invocation.
+* -o <5>:                                uname invocation.
+* -o <6>:                                What information is listed.
+* -o:                                    Connectives for test.
+* -omit-header:                          pr invocation.
+* -omit-pagination:                      pr invocation.
+* -one-file-system <1>:                  cp invocation.
+* -one-file-system:                      du invocation.
+* -only-delimited:                       cut invocation.
+* -operating-system:                     uname invocation.
 * -ot:                                   File characteristic tests.
-* -p <1>:                                su invocation.
-* -p <2>:                                uname invocation.
-* -p <3>:                                pathchk invocation.
-* -p:                                    File type tests.
-* -P <1>:                                du invocation.
-* -P <2>:                                df invocation.
-* -P <3>:                                chgrp invocation.
-* -P:                                    chown invocation.
-* -p <1>:                                rmdir invocation.
-* -p <2>:                                mkdir invocation.
-* -p <3>:                                install invocation.
-* -p:                                    cp invocation.
+* -output:                               sort invocation.
+* -output-delimiter:                     cut invocation.
+* -output-duplicates:                    od invocation.
+* -output-tabs:                          pr invocation.
+* -owner:                                install invocation.
+* -P <1>:                                df invocation.
+* -P <2>:                                chgrp invocation.
 * -P:                                    cp invocation.
-* -p <1>:                                dircolors invocation.
-* -p:                                    nl invocation.
+* -p <1>:                                cp invocation.
+* -p <2>:                                install invocation.
+* -p <3>:                                File type tests.
+* -p <4>:                                mkdir invocation.
+* -p:                                    dircolors invocation.
+* -P <1>:                                chown invocation.
 * -P:                                    Traversing symlinks.
-* -q <1>:                                who invocation.
-* -q:                                    readlink invocation.
+* -p <1>:                                uname invocation.
+* -p <2>:                                rmdir invocation.
+* -p <3>:                                nl invocation.
+* -p:                                    su invocation.
+* -P:                                    du invocation.
+* -p:                                    pathchk invocation.
+* -page-increment:                       nl invocation.
+* -page_width:                           pr invocation.
+* -pages=PAGE_RANGE:                     pr invocation.
+* -parents <1>:                          mkdir invocation.
+* -parents <2>:                          rmdir invocation.
+* -parents:                              cp invocation.
+* -pid:                                  tail invocation.
+* -portability <1>:                      pathchk invocation.
+* -portability:                          df invocation.
+* -prefix:                               csplit invocation.
+* -preserve:                             cp invocation.
+* -preserve-environment:                 su invocation.
+* -preserve-root <1>:                    rm invocation.
+* -preserve-root <2>:                    chmod invocation.
+* -preserve-root <3>:                    chgrp invocation.
+* -preserve-root:                        chown invocation.
+* -preserve-timestamps:                  install invocation.
+* -print-database:                       dircolors invocation.
+* -print-type:                           df invocation.
+* -processor:                            uname invocation.
+* -q:                                    tail invocation.
 * -Q:                                    Formatting the file names.
-* -q <1>:                                Formatting the file names.
-* -q <2>:                                csplit invocation.
-* -q <3>:                                tail invocation.
+* -q <1>:                                readlink invocation.
+* -q <2>:                                Formatting the file names.
+* -q <3>:                                csplit invocation.
+* -q <4>:                                who invocation.
 * -q:                                    head invocation.
-* -r <1>:                                uname invocation.
-* -r:                                    Options for date.
-* -R:                                    Options for date.
-* -r <1>:                                id invocation.
-* -r <2>:                                Access permission tests.
-* -r:                                    touch invocation.
-* -R <1>:                                chmod invocation.
-* -R <2>:                                chgrp invocation.
-* -R <3>:                                chown invocation.
-* -R:                                    rm invocation.
+* -quiet <1>:                            tail invocation.
+* -quiet <2>:                            head invocation.
+* -quiet <3>:                            csplit invocation.
+* -quiet <4>:                            tty invocation.
+* -quiet <5>:                            readlink invocation.
+* -quiet <6>:                            chown invocation.
+* -quiet <7>:                            chgrp invocation.
+* -quiet:                                chmod invocation.
+* -quote-name:                           Formatting the file names.
+* -quoting-style:                        Formatting the file names.
+* -r <1>:                                pr invocation.
+* -r:                                    id invocation.
+* -R <1>:                                chown invocation.
+* -R:                                    Which files are listed.
 * -r <1>:                                rm invocation.
+* -r:                                    tac invocation.
+* -R:                                    rm invocation.
+* -r <1>:                                touch invocation.
+* -r:                                    sort invocation.
+* -R:                                    chmod invocation.
+* -r <1>:                                Access permission tests.
+* -r:                                    sum invocation.
+* -R:                                    chgrp invocation.
 * -r:                                    cp invocation.
+* -R:                                    Options for date.
+* -r:                                    uname invocation.
 * -R:                                    cp invocation.
-* -r:                                    Sorting the output.
-* -R:                                    Which files are listed.
-* -r <1>:                                sort invocation.
-* -r <2>:                                sum invocation.
-* -r <3>:                                pr invocation.
-* -r:                                    tac invocation.
-* -s <1>:                                su invocation.
-* -s <2>:                                uname invocation.
-* -s <3>:                                Options for date.
-* -s <4>:                                who invocation.
-* -s <5>:                                tty invocation.
-* -s:                                    File characteristic tests.
-* -S <1>:                                File type tests.
+* -r <1>:                                Sorting the output.
+* -r:                                    Options for date.
+* -read-bytes:                           od invocation.
+* -real:                                 id invocation.
+* -recursive <1>:                        chmod invocation.
+* -recursive <2>:                        chgrp invocation.
+* -recursive <3>:                        cp invocation.
+* -recursive <4>:                        chown invocation.
+* -recursive <5>:                        Which files are listed.
+* -recursive:                            rm invocation.
+* -reference <1>:                        Options for date.
+* -reference <2>:                        chmod invocation.
+* -reference <3>:                        chown invocation.
+* -reference <4>:                        touch invocation.
+* -reference:                            chgrp invocation.
+* -regex:                                tac invocation.
+* -remove:                               shred invocation.
+* -remove-destination:                   cp invocation.
+* -repeated:                             uniq invocation.
+* -reply <1>:                            cp invocation.
+* -reply:                                mv invocation.
+* -retry:                                tail invocation.
+* -reverse <1>:                          sort invocation.
+* -reverse:                              Sorting the output.
+* -rfc-2822:                             Options for date.
+* -rfc-822:                              Options for date.
+* -s <1>:                                who invocation.
+* -s <2>:                                tty invocation.
+* -s <3>:                                What information is listed.
+* -s <4>:                                uniq invocation.
+* -s:                                    paste invocation.
+* -S <1>:                                sort invocation.
+* -S:                                    Sorting the output.
+* -s <1>:                                Options for date.
+* -s <2>:                                csplit invocation.
+* -s <3>:                                sum invocation.
+* -s <4>:                                sort invocation.
+* -s <5>:                                cut invocation.
+* -s:                                    cp invocation.
 * -S:                                    du invocation.
 * -s <1>:                                du invocation.
-* -s:                                    readlink invocation.
-* -S:                                    ln invocation.
+* -s <2>:                                fmt invocation.
+* -s <3>:                                su invocation.
 * -s:                                    ln invocation.
-* -S <1>:                                mv invocation.
+* -S <1>:                                cp invocation.
+* -S:                                    ln invocation.
+* -s <1>:                                od invocation.
+* -s:                                    readlink invocation.
+* -S <1>:                                Backup options.
+* -S:                                    File type tests.
+* -s <1>:                                nl invocation.
+* -s:                                    tac invocation.
+* -S:                                    mv invocation.
+* -s:                                    pr invocation.
+* -S <1>:                                pr invocation.
 * -S:                                    install invocation.
-* -s:                                    install invocation.
-* -S:                                    cp invocation.
-* -s:                                    cp invocation.
-* -S:                                    Sorting the output.
-* -s <1>:                                What information is listed.
-* -s <2>:                                paste invocation.
-* -s <3>:                                cut invocation.
-* -s:                                    uniq invocation.
-* -S:                                    sort invocation.
-* -s <1>:                                sort invocation.
-* -s <2>:                                sum invocation.
-* -s <3>:                                csplit invocation.
-* -s:                                    fold invocation.
-* -S:                                    pr invocation.
-* -s <1>:                                pr invocation.
-* -s <2>:                                fmt invocation.
-* -s <3>:                                od invocation.
-* -s <4>:                                nl invocation.
-* -s <5>:                                tac invocation.
-* -s:                                    cat invocation.
-* -S:                                    Backup options.
+* -s <1>:                                fold invocation.
+* -s <2>:                                install invocation.
+* -s <3>:                                uname invocation.
+* -s <4>:                                cat invocation.
+* -s:                                    File characteristic tests.
 * -s BYTES:                              shred invocation.
+* -save:                                 stty invocation.
+* -section-delimiter:                    nl invocation.
+* -sep-string:                           pr invocation.
+* -separate-dirs:                        du invocation.
+* -separator <1>:                        tac invocation.
+* -separator:                            pr invocation.
+* -serial:                               paste invocation.
+* -set:                                  Options for date.
+* -sh:                                   dircolors invocation.
+* -shell:                                su invocation.
+* -show-all:                             cat invocation.
+* -show-control-chars <1>:               pr invocation.
+* -show-control-chars:                   Formatting the file names.
+* -show-ends:                            cat invocation.
+* -show-nonprinting <1>:                 cat invocation.
+* -show-nonprinting:                     pr invocation.
+* -show-tabs:                            cat invocation.
+* -si <1>:                               du invocation.
+* -si <2>:                               df invocation.
+* -si <3>:                               What information is listed.
+* -si:                                   Block size.
+* -silent <1>:                           chmod invocation.
+* -silent <2>:                           csplit invocation.
+* -silent <3>:                           readlink invocation.
+* -silent <4>:                           chown invocation.
+* -silent <5>:                           tail invocation.
+* -silent <6>:                           head invocation.
+* -silent <7>:                           tty invocation.
+* -silent:                               chgrp invocation.
+* -size:                                 What information is listed.
+* -size=BYTES:                           shred invocation.
+* -skip-bytes:                           od invocation.
+* -skip-chars:                           uniq invocation.
+* -skip-fields:                          uniq invocation.
+* -sleep-interval:                       tail invocation.
+* -sort:                                 Sorting the output.
+* -spaces:                               fold invocation.
+* -sparse=WHEN:                          cp invocation.
+* -split-only:                           fmt invocation.
+* -squeeze-blank:                        cat invocation.
+* -stable:                               sort invocation.
+* -starting-line-number:                 nl invocation.
+* -status:                               md5sum invocation.
+* -strings:                              od invocation.
+* -strip:                                install invocation.
+* -strip-trailing-slashes <1>:           mv invocation.
+* -strip-trailing-slashes:               cp invocation.
 * -su:                                   su invocation.
-* -T:                                    who invocation.
-* -t <1>:                                File type tests.
-* -t:                                    stat invocation.
-* -T:                                    df invocation.
-* -t:                                    df invocation.
-* -T:                                    General output formatting.
-* -t <1>:                                Sorting the output.
+* -suffix <1>:                           install invocation.
+* -suffix <2>:                           cp invocation.
+* -suffix <3>:                           ln invocation.
+* -suffix <4>:                           mv invocation.
+* -suffix <5>:                           csplit invocation.
+* -suffix:                               Backup options.
+* -suffix-length:                        split invocation.
+* -summarize:                            du invocation.
+* -symbolic:                             ln invocation.
+* -symbolic-link:                        cp invocation.
+* -sync:                                 df invocation.
+* -sysv:                                 sum invocation.
+* -t <1>:                                pr invocation.
 * -t <2>:                                unexpand invocation.
-* -t:                                    expand invocation.
+* -t <3>:                                md5sum invocation.
+* -t <4>:                                cat invocation.
+* -t:                                    sort invocation.
 * -T:                                    sort invocation.
-* -t <1>:                                sort invocation.
-* -t:                                    md5sum invocation.
-* -T:                                    pr invocation.
-* -t <1>:                                pr invocation.
-* -t <2>:                                fmt invocation.
-* -t:                                    od invocation.
+* -t:                                    expand invocation.
 * -T:                                    cat invocation.
-* -t:                                    cat invocation.
+* -t:                                    File type tests.
+* -T <1>:                                pr invocation.
+* -T <2>:                                General output formatting.
+* -T:                                    who invocation.
+* -t <1>:                                fmt invocation.
+* -t <2>:                                df invocation.
+* -t <3>:                                od invocation.
+* -t:                                    stat invocation.
+* -T:                                    df invocation.
+* -t:                                    Sorting the output.
+* -tabs <1>:                             unexpand invocation.
+* -tabs:                                 expand invocation.
+* -tabsize:                              General output formatting.
+* -tagged-paragraph:                     fmt invocation.
+* -target-directory <1>:                 Target directory.
+* -target-directory <2>:                 ln invocation.
+* -target-directory <3>:                 mv invocation.
+* -target-directory <4>:                 install invocation.
+* -target-directory:                     cp invocation.
+* -temporary-directory:                  sort invocation.
+* -terse:                                stat invocation.
+* -text:                                 md5sum invocation.
+* -time <1>:                             touch invocation.
+* -time <2>:                             Sorting the output.
+* -time:                                 touch invocation.
+* -time-style:                           Formatting file timestamps.
+* -total:                                du invocation.
+* -traditional:                          od invocation.
+* -type:                                 df invocation.
 * -u <1>:                                env invocation.
-* -u <2>:                                Options for date.
-* -u <3>:                                who invocation.
-* -u <4>:                                id invocation.
-* -u <5>:                                Access permission tests.
-* -u <6>:                                shred invocation.
-* -u:                                    mv invocation.
+* -u <2>:                                Access permission tests.
+* -u <3>:                                shred invocation.
+* -u <4>:                                sort invocation.
+* -u <5>:                                fmt invocation.
+* -u <6>:                                cat invocation.
+* -u <7>:                                mv invocation.
+* -u:                                    who invocation.
 * -U:                                    Sorting the output.
 * -u <1>:                                Sorting the output.
-* -u <2>:                                uniq invocation.
-* -u <3>:                                sort invocation.
-* -u <4>:                                fmt invocation.
-* -u:                                    cat invocation.
+* -u <2>:                                Options for date.
+* -u <3>:                                id invocation.
+* -u:                                    uniq invocation.
+* -uniform-spacing:                      fmt invocation.
+* -unique <1>:                           sort invocation.
+* -unique:                               uniq invocation.
+* -universal:                            Options for date.
 * -unset:                                env invocation.
+* -update:                               mv invocation.
+* -user:                                 id invocation.
+* -utc:                                  Options for date.
 * -v <1>:                                uname invocation.
-* -v <2>:                                chmod invocation.
-* -v <3>:                                chgrp invocation.
-* -v <4>:                                chown invocation.
-* -v <5>:                                rmdir invocation.
-* -v <6>:                                readlink invocation.
-* -v:                                    mkdir invocation.
-* -V:                                    ln invocation.
-* -v <1>:                                ln invocation.
-* -v <2>:                                shred invocation.
-* -v:                                    rm invocation.
-* -V:                                    mv invocation.
 * -v:                                    mv invocation.
-* -V:                                    install invocation.
-* -v:                                    install invocation.
+* -V:                                    mv invocation.
+* -v <1>:                                pr invocation.
+* -v <2>:                                tail invocation.
+* -v <3>:                                head invocation.
+* -v <4>:                                install invocation.
+* -v:                                    shred invocation.
 * -V:                                    cp invocation.
 * -v <1>:                                cp invocation.
-* -v <2>:                                Sorting the output.
-* -v <3>:                                tail invocation.
-* -v <4>:                                head invocation.
-* -v <5>:                                pr invocation.
-* -v <6>:                                od invocation.
-* -v <7>:                                nl invocation.
-* -v:                                    cat invocation.
-* -w <1>:                                who invocation.
-* -w <2>:                                Access permission tests.
-* -w <3>:                                General output formatting.
-* -w <4>:                                uniq invocation.
-* -w <5>:                                md5sum invocation.
-* -w <6>:                                wc invocation.
-* -w:                                    fold invocation.
+* -v:                                    rm invocation.
+* -V:                                    install invocation.
+* -v <1>:                                chown invocation.
+* -v <2>:                                cat invocation.
+* -v <3>:                                chmod invocation.
+* -v <4>:                                rmdir invocation.
+* -v <5>:                                readlink invocation.
+* -v <6>:                                mkdir invocation.
+* -v:                                    Sorting the output.
+* -V:                                    ln invocation.
+* -v <1>:                                nl invocation.
+* -v <2>:                                chgrp invocation.
+* -v <3>:                                ln invocation.
+* -v:                                    od invocation.
+* -verbose <1>:                          chgrp invocation.
+* -verbose <2>:                          rm invocation.
+* -verbose <3>:                          chown invocation.
+* -verbose <4>:                          tail invocation.
+* -verbose <5>:                          head invocation.
+* -verbose <6>:                          install invocation.
+* -verbose <7>:                          chmod invocation.
+* -verbose <8>:                          rmdir invocation.
+* -verbose <9>:                          cp invocation.
+* -verbose <10>:                         readlink invocation.
+* -verbose <11>:                         split invocation.
+* -verbose <12>:                         mv invocation.
+* -verbose <13>:                         mkdir invocation.
+* -verbose <14>:                         ln invocation.
+* -verbose:                              shred invocation.
+* -version:                              Common options.
+* -version-control <1>:                  mv invocation.
+* -version-control <2>:                  cp invocation.
+* -version-control <3>:                  install invocation.
+* -version-control <4>:                  Backup options.
+* -version-control:                      ln invocation.
+* -w <1>:                                fold invocation.
+* -w <2>:                                nl invocation.
+* -w <3>:                                who invocation.
+* -w <4>:                                Access permission tests.
+* -w:                                    od invocation.
 * -W:                                    pr invocation.
-* -w <1>:                                pr invocation.
-* -w <2>:                                fmt invocation.
-* -w <3>:                                od invocation.
-* -w:                                    nl invocation.
+* -w <1>:                                General output formatting.
+* -w <2>:                                uniq invocation.
+* -w <3>:                                pr invocation.
+* -w <4>:                                wc invocation.
+* -w <5>:                                md5sum invocation.
+* -w:                                    fmt invocation.
+* -warn:                                 md5sum invocation.
+* -width <1>:                            fold invocation.
+* -width:                                od invocation.
 * -WIDTH:                                fmt invocation.
-* -x <1>:                                Access permission tests.
-* -x <2>:                                du invocation.
-* -x <3>:                                df invocation.
-* -x <4>:                                shred invocation.
-* -x <5>:                                cp invocation.
-* -x:                                    General output formatting.
+* -width <1>:                            pr invocation.
+* -width <2>:                            fmt invocation.
+* -width:                                General output formatting.
+* -words:                                wc invocation.
+* -writable:                             who invocation.
+* -x:                                    df invocation.
 * -X:                                    Sorting the output.
+* -x <1>:                                General output formatting.
+* -x <2>:                                Access permission tests.
+* -x <3>:                                shred invocation.
+* -x <4>:                                du invocation.
+* -x <5>:                                cp invocation.
 * -x:                                    od invocation.
 * -X FILE:                               du invocation.
 * -z <1>:                                String tests.
-* -z <2>:                                shred invocation.
-* -z <3>:                                sort invocation.
-* -z:                                    csplit invocation.
+* -z <2>:                                sort invocation.
+* -z <3>:                                csplit invocation.
+* -z:                                    shred invocation.
+* -zero:                                 shred invocation.
+* -zero-terminated:                      sort invocation.
 * .cshrc:                                su invocation.
 * /:                                     Numeric expressions.
 * /bin/sh:                               su invocation.
 * /etc/passwd:                           su invocation.
 * /etc/shells:                           su invocation.
-* /etc/utmp <1>:                         who invocation.
-* /etc/utmp <2>:                         users invocation.
-* /etc/utmp:                             logname invocation.
+* /etc/utmp <1>:                         logname invocation.
+* /etc/utmp <2>:                         who invocation.
+* /etc/utmp:                             users invocation.
 * /etc/wtmp <1>:                         who invocation.
 * /etc/wtmp:                             users invocation.
 * 128-bit checksum:                      md5sum invocation.
@@ -12539,8 +12438,8 @@
 * 4.2 filesystem type:                   df invocation.
 * <:                                     Relations for expr.
 * <=:                                    Relations for expr.
-* = <1>:                                 Relations for expr.
-* =:                                     String tests.
+* = <1>:                                 String tests.
+* =:                                     Relations for expr.
 * ==:                                    Relations for expr.
 * >:                                     Relations for expr.
 * >=:                                    Relations for expr.
@@ -12571,12 +12470,12 @@
 * always color option:                   General output formatting.
 * am i:                                  who invocation.
 * am in date strings:                    Time of day items.
-* and operator <1>:                      Relations for expr.
-* and operator:                          Connectives for test.
-* appropriate privileges <1>:            nice invocation.
-* appropriate privileges <2>:            hostname invocation.
-* appropriate privileges <3>:            Setting the time.
-* appropriate privileges:                install invocation.
+* and operator <1>:                      Connectives for test.
+* and operator:                          Relations for expr.
+* appropriate privileges <1>:            install invocation.
+* appropriate privileges <2>:            Setting the time.
+* appropriate privileges <3>:            hostname invocation.
+* appropriate privileges:                nice invocation.
 * arbitrary date strings, parsing:       Options for date.
 * arbitrary text, displaying:            echo invocation.
 * arithmetic tests:                      Numeric tests.
@@ -12590,17 +12489,17 @@
 * automounter filesystems:               df invocation.
 * b for block special file:              mknod invocation.
 * background jobs, stopping at terminal write: Local.
-* backslash escapes <1>:                 echo invocation.
-* backslash escapes:                     Character sets.
+* backslash escapes <1>:                 Character sets.
+* backslash escapes:                     echo invocation.
 * backslash sequences for file names:    Formatting the file names.
 * backup files, ignoring:                Which files are listed.
 * backup options:                        Backup options.
 * backup suffix:                         Backup options.
-* backups, making <1>:                   ln invocation.
-* backups, making <2>:                   mv invocation.
-* backups, making <3>:                   install invocation.
-* backups, making <4>:                   cp invocation.
-* backups, making:                       Backup options.
+* backups, making <1>:                   mv invocation.
+* backups, making <2>:                   install invocation.
+* backups, making <3>:                   cp invocation.
+* backups, making <4>:                   Backup options.
+* backups, making:                       ln invocation.
 * backups, making only:                  cp invocation.
 * basename:                              basename invocation.
 * baud rate, setting:                    Special.
@@ -12617,8 +12516,8 @@
 * blank lines, numbering:                nl invocation.
 * blanks, ignoring leading:              sort invocation.
 * block (space-padding):                 dd invocation.
-* block size <1>:                        dd invocation.
-* block size:                            Block size.
+* block size <1>:                        Block size.
+* block size:                            dd invocation.
 * block size of conversion:              dd invocation.
 * block size of input:                   dd invocation.
 * block size of output:                  dd invocation.
@@ -12637,8 +12536,8 @@
 * BSD touch compatibility:               touch invocation.
 * bsN:                                   Output.
 * bugs, reporting:                       Introduction.
-* built-in shell commands, conflicts with <1>: nice invocation.
-* built-in shell commands, conflicts with <2>: pwd invocation.
+* built-in shell commands, conflicts with <1>: pwd invocation.
+* built-in shell commands, conflicts with <2>: nice invocation.
 * built-in shell commands, conflicts with: test invocation.
 * byte count:                            wc invocation.
 * byte-swapping:                         dd invocation.
@@ -12661,8 +12560,8 @@
 * changing file attributes:              Changing file attributes.
 * changing file ownership:               chown invocation.
 * changing file timestamps:              touch invocation.
-* changing group ownership <1>:          chgrp invocation.
-* changing group ownership:              chown invocation.
+* changing group ownership <1>:          chown invocation.
+* changing group ownership:              chgrp invocation.
 * changing special permissions:          Changing Special Permissions.
 * character classes:                     Character sets.
 * character count:                       wc invocation.
@@ -12686,8 +12585,8 @@
 * color setup:                           dircolors invocation.
 * color, distinguishing file types with: General output formatting.
 * cols:                                  Special.
-* COLUMNS:                               Special.
 * columns:                               Special.
+* COLUMNS <1>:                           Special.
 * COLUMNS:                               General output formatting.
 * combination settings:                  Combination.
 * comm:                                  comm invocation.
@@ -12713,8 +12612,8 @@
 * conditional executability:             Conditional Executability.
 * conditions:                            Conditions.
 * conflicts with shell built-ins <1>:    nice invocation.
-* conflicts with shell built-ins <2>:    pwd invocation.
-* conflicts with shell built-ins:        test invocation.
+* conflicts with shell built-ins <2>:    test invocation.
+* conflicts with shell built-ins:        pwd invocation.
 * connectives, logical <1>:              Relations for expr.
 * connectives, logical:                  Connectives for test.
 * context splitting:                     csplit invocation.
@@ -12775,11 +12674,11 @@
 * deleting characters:                   Squeezing.
 * dereferencing symbolic links:          ln invocation.
 * descriptor follow option:              tail invocation.
-* destination directory <1>:             ln invocation.
-* destination directory <2>:             mv invocation.
-* destination directory <3>:             install invocation.
-* destination directory <4>:             cp invocation.
-* destination directory:                 Target directory.
+* destination directory <1>:             cp invocation.
+* destination directory <2>:             Target directory.
+* destination directory <3>:             mv invocation.
+* destination directory <4>:             install invocation.
+* destination directory:                 ln invocation.
 * destinations, multiple output:         tee invocation.
 * device file, disk:                     df invocation.
 * df:                                    df invocation.
@@ -12844,8 +12743,8 @@
 * effective UID, printing:               whoami invocation.
 * efs filesystem type:                   df invocation.
 * Eggert, Paul:                          Authors of getdate.
-* eight-bit characters <1>:              Combination.
-* eight-bit characters:                  Control.
+* eight-bit characters <1>:              Control.
+* eight-bit characters:                  Combination.
 * eight-bit input:                       Input.
 * ek:                                    Combination.
 * empty files, creating:                 touch invocation.
@@ -12865,8 +12764,8 @@
 * equivalence classes:                   Character sets.
 * erase:                                 Characters.
 * erasing data:                          shred invocation.
-* error messages, omitting <1>:          chmod invocation.
-* error messages, omitting <2>:          chgrp invocation.
+* error messages, omitting <1>:          chgrp invocation.
+* error messages, omitting <2>:          chmod invocation.
 * error messages, omitting:              chown invocation.
 * evaluation of expressions:             expr invocation.
 * even parity:                           Control.
@@ -12898,8 +12797,8 @@
 * exit status of tty:                    tty invocation.
 * expand:                                expand invocation.
 * expr:                                  expr invocation.
-* expression evaluation <1>:             expr invocation.
-* expression evaluation:                 test invocation.
+* expression evaluation <1>:             test invocation.
+* expression evaluation:                 expr invocation.
 * expressions, numeric:                  Numeric expressions.
 * expressions, string:                   String expressions.
 * extension, sorting files by:           Sorting the output.
@@ -12957,8 +12856,8 @@
 * force deletion:                        shred invocation.
 * formatting file contents:              Formatting file contents.
 * formatting of numbers in seq:          seq invocation.
-* formatting times <1>:                  date invocation.
-* formatting times:                      pr invocation.
+* formatting times <1>:                  pr invocation.
+* formatting times:                      date invocation.
 * fortnight in date strings:             Relative items in date strings.
 * fsck:                                  rm invocation.
 * general date syntax:                   General date syntax.
@@ -12985,8 +12884,8 @@
 * hard link, defined:                    ln invocation.
 * hard links to directories:             ln invocation.
 * hard links, counting in du:            du invocation.
-* hard links, creating <1>:              ln invocation.
-* hard links, creating:                  link invocation.
+* hard links, creating <1>:              link invocation.
+* hard links, creating:                  ln invocation.
 * hard links, preserving:                cp invocation.
 * hardware class:                        uname invocation.
 * hardware flow control:                 Control.
@@ -13008,8 +12907,8 @@
 * hour in date strings:                  Relative items in date strings.
 * hsfs filesystem type:                  df invocation.
 * human-readable output <1>:             du invocation.
-* human-readable output <2>:             df invocation.
-* human-readable output <3>:             What information is listed.
+* human-readable output <2>:             What information is listed.
+* human-readable output <3>:             df invocation.
 * human-readable output:                 Block size.
 * hup[cl]:                               Control.
 * hurd, author, printing:                What information is listed.
@@ -13064,32 +12963,33 @@
 * kibibyte, definition of:               Block size.
 * kibibytes for file sizes:              du invocation.
 * kibibytes for filesystem sizes:        df invocation.
-* kill <1>:                              kill invocation.
-* kill:                                  Characters.
+* kill <1>:                              Characters.
+* kill:                                  kill invocation.
 * kilobyte, definition of:               Block size.
 * Knuth, Donald E.:                      fmt invocation.
 * language, in dates:                    General date syntax.
-* last DAY <1>:                          Day of week items.
-* last DAY:                              Options for date.
+* last DAY <1>:                          Options for date.
+* last DAY:                              Day of week items.
 * last in date strings:                  General date syntax.
 * last part of files, outputting:        tail invocation.
 * LC_ALL <1>:                            ls invocation.
 * LC_ALL:                                sort invocation.
 * LC_COLLATE <1>:                        Relations for expr.
 * LC_COLLATE <2>:                        join invocation.
-* LC_COLLATE <3>:                        comm invocation.
-* LC_COLLATE <4>:                        uniq invocation.
-* LC_COLLATE:                            sort invocation.
+* LC_COLLATE <3>:                        sort invocation.
+* LC_COLLATE <4>:                        comm invocation.
+* LC_COLLATE:                            uniq invocation.
 * LC_CTYPE:                              sort invocation.
 * LC_MESSAGES:                           pr invocation.
-* LC_NUMERIC <1>:                        printf invocation.
-* LC_NUMERIC <2>:                        sort invocation.
+* LC_NUMERIC <1>:                        sort invocation.
+* LC_NUMERIC <2>:                        printf invocation.
+* LC_NUMERIC <3>:                        sort invocation.
 * LC_NUMERIC:                            Block size.
 * LC_TIME <1>:                           Formatting file timestamps.
-* LC_TIME <2>:                           sort invocation.
-* LC_TIME:                               pr invocation.
-* LCASE:                                 Combination.
+* LC_TIME <2>:                           pr invocation.
+* LC_TIME:                               sort invocation.
 * lcase:                                 Combination.
+* LCASE:                                 Combination.
 * lcase, converting to:                  dd invocation.
 * lchown <1>:                            chgrp invocation.
 * lchown:                                chown invocation.
@@ -13129,8 +13029,8 @@
 * login shell:                           su invocation.
 * login shell, creating:                 su invocation.
 * login time:                            who invocation.
-* LOGNAME:                               su invocation.
 * logname:                               logname invocation.
+* LOGNAME:                               su invocation.
 * long ls format:                        What information is listed.
 * lower:                                 Character sets.
 * lowercase, translating to output:      Output.
@@ -13195,12 +13095,12 @@
 * newline, echoing:                      Local.
 * newline, translating to crlf:          Output.
 * newline, translating to return:        Input.
-* next DAY <1>:                          Day of week items.
-* next DAY:                              Options for date.
+* next DAY <1>:                          Options for date.
+* next DAY:                              Day of week items.
 * next in date strings:                  General date syntax.
 * NFS filesystem type:                   df invocation.
-* NFS mounts from BSD to HP-UX <1>:      du invocation.
-* NFS mounts from BSD to HP-UX:          What information is listed.
+* NFS mounts from BSD to HP-UX <1>:      What information is listed.
+* NFS mounts from BSD to HP-UX:          du invocation.
 * nice:                                  nice invocation.
 * nl <1>:                                Combination.
 * nl:                                    nl invocation.
@@ -13301,8 +13201,8 @@
 * passwd entry, and su shell:            su invocation.
 * paste:                                 paste invocation.
 * Paterson, R.:                          Introduction.
-* PATH <1>:                              su invocation.
-* PATH:                                  env invocation.
+* PATH <1>:                              env invocation.
+* PATH:                                  su invocation.
 * pathchk:                               pathchk invocation.
 * pattern matching:                      String expressions.
 * PC filesystem:                         df invocation.
@@ -13317,8 +13217,8 @@
 * petabyte, definition of:               Block size.
 * phone directory order:                 sort invocation.
 * pieces, splitting a file into:         split invocation.
-* Pinard, F. <1>:                        Authors of getdate.
-* Pinard, F.:                            Introduction.
+* Pinard, F. <1>:                        Introduction.
+* Pinard, F.:                            Authors of getdate.
 * pipe fitting:                          tee invocation.
 * Plass, Michael F.:                     fmt invocation.
 * platform, hardware:                    uname invocation.
@@ -13328,10 +13228,10 @@
 * POSIX:                                 Introduction.
 * POSIX output format:                   df invocation.
 * POSIXLY_CORRECT <1>:                   Warnings in tr.
-* POSIXLY_CORRECT <2>:                   sort invocation.
+* POSIXLY_CORRECT <2>:                   Common options.
 * POSIXLY_CORRECT <3>:                   pr invocation.
-* POSIXLY_CORRECT <4>:                   Standards conformance.
-* POSIXLY_CORRECT:                       Common options.
+* POSIXLY_CORRECT <4>:                   sort invocation.
+* POSIXLY_CORRECT:                       Standards conformance.
 * POSIXLY_CORRECT, and block size:       Block size.
 * pr:                                    pr invocation.
 * prime factors:                         factor invocation.
@@ -13420,8 +13320,8 @@
 * root directory, allow recursive modification: chown invocation.
 * root directory, disallow recursive destruction: rm invocation.
 * root directory, disallow recursive modification <1>: chmod invocation.
-* root directory, disallow recursive modification <2>: chgrp invocation.
-* root directory, disallow recursive modification: chown invocation.
+* root directory, disallow recursive modification <2>: chown invocation.
+* root directory, disallow recursive modification: chgrp invocation.
 * root directory, running a program in a specified: chroot invocation.
 * root, becoming:                        su invocation.
 * rows:                                  Special.
@@ -13457,10 +13357,10 @@
 * SHELL environment variable, and color: dircolors invocation.
 * shell utilities:                       Top.
 * shred:                                 shred invocation.
-* SI output <1>:                         du invocation.
+* SI output <1>:                         Block size.
 * SI output <2>:                         df invocation.
-* SI output <3>:                         What information is listed.
-* SI output:                             Block size.
+* SI output <3>:                         du invocation.
+* SI output:                             What information is listed.
 * simple backup method:                  Backup options.
 * SIMPLE_BACKUP_SUFFIX:                  Backup options.
 * single-column output of files:         General output formatting.
@@ -13533,9 +13433,9 @@
 * symbolic (soft) links, creating:       ln invocation.
 * symbolic link check:                   File type tests.
 * symbolic link to directory, controlling traversal of: Traversing symlinks.
-* symbolic link to directory, never traverse <1>: chgrp invocation.
+* symbolic link to directory, never traverse <1>: Traversing symlinks.
 * symbolic link to directory, never traverse <2>: chown invocation.
-* symbolic link to directory, never traverse: Traversing symlinks.
+* symbolic link to directory, never traverse: chgrp invocation.
 * symbolic link to directory, traverse each that is encountered <1>: chgrp invocation.
 * symbolic link to directory, traverse each that is encountered <2>: chown invocation.
 * symbolic link to directory, traverse each that is encountered: Traversing symlinks.
@@ -13570,11 +13470,12 @@
 * tagged paragraphs:                     fmt invocation.
 * tail:                                  tail invocation.
 * tandem:                                Input.
-* target directory <1>:                  ln invocation.
-* target directory <2>:                  mv invocation.
-* target directory <3>:                  install invocation.
-* target directory <4>:                  cp invocation.
-* target directory:                      Target directory.
+* target directory <1>:                  cp invocation.
+* target directory <2>:                  install invocation.
+* target directory <3>:                  Target directory.
+* target directory <4>:                  ln invocation.
+* target directory <5>:                  Target directory.
+* target directory:                      mv invocation.
 * tebibyte, definition of:               Block size.
 * tee:                                   tee invocation.
 * telephone directory order:             sort invocation.
@@ -13665,9 +13566,9 @@
 * version-control Emacs variable:        Backup options.
 * VERSION_CONTROL <1>:                   ln invocation.
 * VERSION_CONTROL <2>:                   mv invocation.
-* VERSION_CONTROL <3>:                   install invocation.
+* VERSION_CONTROL <3>:                   Backup options.
 * VERSION_CONTROL <4>:                   cp invocation.
-* VERSION_CONTROL:                       Backup options.
+* VERSION_CONTROL:                       install invocation.
 * vertical sorted files in columns:      General output formatting.
 * vtN:                                   Output.
 * wc:                                    wc invocation.
@@ -13701,201 +13602,196 @@
 
 
 Tag Table:
-Node: Top7430
-Node: Introduction19986
-Node: Common options21543
-Node: Exit status23780
-Node: Backup options24504
-Node: Block size26564
-Node: Target directory31076
-Node: Trailing slashes33310
-Node: Traversing symlinks34326
-Node: Treating / specially35392
-Ref: Treating / specially-Footnote-136647
-Node: Standards conformance36716
-Node: Output of entire files38212
-Node: cat invocation38744
-Node: tac invocation41442
-Node: nl invocation42699
-Node: od invocation46572
-Node: Formatting file contents52890
-Node: fmt invocation53340
-Node: pr invocation56130
-Node: fold invocation68980
-Node: Output of parts of files70483
-Node: head invocation70990
-Node: tail invocation72552
-Node: split invocation79119
-Node: csplit invocation81244
-Node: Summarizing files85345
-Node: wc invocation85872
-Node: sum invocation87869
-Node: cksum invocation89269
-Node: md5sum invocation90403
-Node: Operating on sorted files93740
-Node: sort invocation94343
-Ref: sort invocation-Footnote-1109106
-Node: uniq invocation109658
-Node: comm invocation113528
-Node: tsort invocation114851
-Node: tsort background117880
-Node: ptx invocation119638
-Node: General options in ptx122437
-Node: Charset selection in ptx123137
-Node: Input processing in ptx124030
-Node: Output formatting in ptx129712
-Node: Compatibility in ptx136269
-Node: Operating on fields within a line139490
-Node: cut invocation139890
-Node: paste invocation142765
-Node: join invocation144088
-Node: Operating on characters147824
-Node: tr invocation148259
-Node: Character sets149468
-Node: Translating153687
-Node: Squeezing155713
-Node: Warnings in tr158707
-Node: expand invocation159837
-Node: unexpand invocation161402
-Node: Directory listing163161
-Node: ls invocation163644
-Ref: ls invocation-Footnote-1165397
-Node: Which files are listed165619
-Node: What information is listed168257
-Node: Sorting the output176112
-Node: More details about version sort178421
-Node: General output formatting179981
-Node: Formatting file timestamps183385
-Node: Formatting the file names188387
-Node: dir invocation191117
-Node: vdir invocation191545
-Node: dircolors invocation191940
-Node: Basic operations193411
-Node: cp invocation194028
-Node: dd invocation205006
-Node: install invocation208862
-Node: mv invocation212465
-Node: rm invocation216118
-Node: shred invocation218850
-Node: Special file types225866
-Node: link invocation227359
-Node: ln invocation228013
-Node: mkdir invocation232493
-Node: mkfifo invocation234066
-Node: mknod invocation235048
-Node: readlink invocation236784
-Node: rmdir invocation238146
-Node: unlink invocation239413
-Node: Changing file attributes240391
-Node: chown invocation241201
-Node: chgrp invocation247027
-Node: chmod invocation249771
-Node: touch invocation251761
-Node: Disk usage255207
-Node: df invocation255883
-Node: du invocation261139
-Node: stat invocation266127
-Node: sync invocation268937
-Node: Printing text269844
-Node: echo invocation270215
-Node: printf invocation271286
-Node: yes invocation274639
-Node: Conditions275154
-Node: false invocation275742
-Node: true invocation276669
-Node: test invocation278161
-Node: File type tests280003
-Node: Access permission tests280764
-Node: File characteristic tests281530
-Node: String tests282284
-Node: Numeric tests282866
-Node: Connectives for test283652
-Node: expr invocation283994
-Node: String expressions286119
-Node: Numeric expressions288691
-Node: Relations for expr289318
-Node: Examples of expr290507
-Node: Redirection291221
-Node: tee invocation291663
-Node: File name manipulation292571
-Node: basename invocation293016
-Node: dirname invocation293651
-Node: pathchk invocation294279
-Node: Working context295484
-Node: pwd invocation296125
-Node: stty invocation296811
-Node: Control299563
-Node: Input300312
-Node: Output301700
-Node: Local302945
-Node: Combination304513
-Node: Characters306662
-Node: Special308205
-Node: printenv invocation309557
-Node: tty invocation310313
-Node: User information311012
-Node: id invocation311661
-Node: logname invocation312887
-Node: whoami invocation313476
-Node: groups invocation313956
-Node: users invocation314674
-Node: who invocation315566
-Node: System context318159
-Node: date invocation318646
-Node: Time directives319895
-Node: Date directives321222
-Node: Literal directives323480
-Node: Padding323756
-Node: Setting the time324563
-Node: Options for date325542
-Node: Examples of date328381
-Ref: %s-examples329824
-Node: uname invocation331811
-Node: hostname invocation334053
-Node: hostid invocation334658
-Node: Modified command invocation335338
-Node: chroot invocation335972
-Node: env invocation337964
-Node: nice invocation339444
-Node: nohup invocation341910
-Node: su invocation343465
-Node: Process control347917
-Node: kill invocation348134
-Node: Delaying352480
-Node: sleep invocation352671
-Node: Numeric operations353475
-Node: factor invocation353804
-Node: seq invocation355134
-Node: File permissions358901
-Node: Mode Structure359492
-Node: Symbolic Modes362613
-Node: Setting Permissions363615
-Node: Copying Permissions366155
-Node: Changing Special Permissions366944
-Node: Conditional Executability368569
-Node: Multiple Changes369191
-Node: Umask and Protection370844
-Node: Numeric Modes371938
-Node: Date input formats373759
-Node: General date syntax376547
-Node: Calendar date items378963
-Node: Time of day items380961
-Node: Time zone items382788
-Node: Day of week items383639
-Node: Relative items in date strings384629
-Node: Pure numbers in date strings387432
-Node: Authors of getdate388413
-Node: Opening the software toolbox389162
-Node: Toolbox introduction389870
-Node: I/O redirection392596
-Node: The who command395432
-Node: The cut command396332
-Node: The sort command397398
-Node: The uniq command398105
-Node: Putting the tools together398797
-Ref: Putting the tools together-Footnote-1410973
-Node: GNU Free Documentation License411047
-Node: How to use this License for your documents429540
-Node: Index430950
+Node: Top6819
+Node: Introduction19373
+Node: Common options20930
+Node: Exit status23159
+Node: Backup options23883
+Node: Block size25928
+Node: Target directory30427
+Node: Trailing slashes32661
+Node: Traversing symlinks33677
+Node: Treating / specially34742
+Node: Standards conformance36066
+Node: Output of entire files37554
+Node: cat invocation38086
+Node: tac invocation40784
+Node: nl invocation42041
+Node: od invocation45914
+Node: Formatting file contents52238
+Node: fmt invocation52688
+Node: pr invocation55478
+Node: fold invocation68323
+Node: Output of parts of files69827
+Node: head invocation70334
+Node: tail invocation71897
+Node: split invocation78465
+Node: csplit invocation80591
+Node: Summarizing files84691
+Node: wc invocation85218
+Node: sum invocation87215
+Node: cksum invocation88615
+Node: md5sum invocation89749
+Node: Operating on sorted files93086
+Node: sort invocation93689
+Node: uniq invocation108961
+Node: comm invocation112833
+Node: tsort invocation114155
+Node: tsort background117185
+Node: ptx invocation118943
+Node: General options in ptx121742
+Node: Charset selection in ptx122441
+Node: Input processing in ptx123333
+Node: Output formatting in ptx129014
+Node: Compatibility in ptx135570
+Node: Operating on fields within a line138790
+Node: cut invocation139190
+Node: paste invocation142065
+Node: join invocation143388
+Node: Operating on characters147122
+Node: tr invocation147557
+Node: Character sets148766
+Node: Translating152984
+Node: Squeezing155010
+Node: Warnings in tr158003
+Node: expand invocation159131
+Node: unexpand invocation160697
+Node: Directory listing162457
+Node: ls invocation162940
+Node: Which files are listed164912
+Node: What information is listed167549
+Node: Sorting the output175408
+Node: More details about version sort177716
+Node: General output formatting179272
+Node: Formatting file timestamps182666
+Node: Formatting the file names187659
+Node: dir invocation190386
+Node: vdir invocation190814
+Node: dircolors invocation191209
+Node: Basic operations192674
+Node: cp invocation193291
+Node: dd invocation204270
+Node: install invocation208127
+Node: mv invocation211731
+Node: rm invocation215384
+Node: shred invocation218116
+Node: Special file types225133
+Node: link invocation226626
+Node: ln invocation227280
+Node: mkdir invocation231759
+Node: mkfifo invocation233333
+Node: mknod invocation234315
+Node: readlink invocation236050
+Node: rmdir invocation237411
+Node: unlink invocation238678
+Node: Changing file attributes239654
+Node: chown invocation240465
+Node: chgrp invocation246291
+Node: chmod invocation249035
+Node: touch invocation251026
+Node: Disk usage254474
+Node: df invocation255150
+Node: du invocation260408
+Node: stat invocation265399
+Node: sync invocation268209
+Node: Printing text269117
+Node: echo invocation269488
+Node: printf invocation270559
+Node: yes invocation273904
+Node: Conditions274419
+Node: false invocation275007
+Node: true invocation275932
+Node: test invocation277422
+Node: File type tests279264
+Node: Access permission tests280024
+Node: File characteristic tests280789
+Node: String tests281542
+Node: Numeric tests282123
+Node: Connectives for test282908
+Node: expr invocation283249
+Node: String expressions285374
+Node: Numeric expressions287946
+Node: Relations for expr288572
+Node: Examples of expr289758
+Node: Redirection290472
+Node: tee invocation290914
+Node: File name manipulation291822
+Node: basename invocation292267
+Node: dirname invocation292902
+Node: pathchk invocation293531
+Node: Working context294736
+Node: pwd invocation295377
+Node: stty invocation296063
+Node: Control298815
+Node: Input299564
+Node: Output300952
+Node: Local302197
+Node: Combination303765
+Node: Characters305914
+Node: Special307457
+Node: printenv invocation308804
+Node: tty invocation309560
+Node: User information310259
+Node: id invocation310908
+Node: logname invocation312134
+Node: whoami invocation312723
+Node: groups invocation313203
+Node: users invocation313921
+Node: who invocation314813
+Node: System context317406
+Node: date invocation317893
+Node: Time directives319142
+Node: Date directives320433
+Node: Literal directives322691
+Node: Padding322967
+Node: Setting the time323774
+Node: Options for date324753
+Node: Examples of date327588
+Node: uname invocation331017
+Node: hostname invocation333260
+Node: hostid invocation333865
+Node: Modified command invocation334545
+Node: chroot invocation335179
+Node: env invocation337169
+Node: nice invocation338647
+Node: nohup invocation341114
+Node: su invocation342669
+Node: Process control347091
+Node: kill invocation347308
+Node: Delaying351654
+Node: sleep invocation351845
+Node: Numeric operations352649
+Node: factor invocation352978
+Node: seq invocation354308
+Node: File permissions358075
+Node: Mode Structure358666
+Node: Symbolic Modes361787
+Node: Setting Permissions362790
+Node: Copying Permissions365331
+Node: Changing Special Permissions366120
+Node: Conditional Executability367746
+Node: Multiple Changes368368
+Node: Umask and Protection370022
+Node: Numeric Modes371116
+Node: Date input formats372937
+Node: General date syntax375725
+Node: Calendar date items378142
+Node: Time of day items380140
+Node: Time zone items381968
+Node: Day of week items382819
+Node: Relative items in date strings383809
+Node: Pure numbers in date strings386610
+Node: Authors of getdate387592
+Node: Opening the software toolbox388341
+Node: Toolbox introduction389049
+Node: I/O redirection391775
+Node: The who command394611
+Node: The cut command395511
+Node: The sort command396577
+Node: The uniq command397284
+Node: Putting the tools together397976
+Node: GNU Free Documentation License410226
+Node: How to use this License for your documents428719
+Node: Index430129
 
 End Tag Table
diff -ruN coreutils-5.2.1-orig/doc/coreutils.texi coreutils-5.2.1/doc/coreutils.texi
--- coreutils-5.2.1-orig/doc/coreutils.texi	Wed Mar 10 17:50:52 2004
+++ coreutils-5.2.1/doc/coreutils.texi	Tue Mar 22 23:43:40 2005
@@ -118,7 +118,7 @@
 * yes: (coreutils)yes invocation.               Print a string indefinitely.
 @end direntry
 
-@copying
+@macro insertcopying{}
 This manual documents version @value{VERSION} of the @sc{gnu} core
 utilities, including the standard programs for text and file manipulation.
 
@@ -133,7 +133,7 @@
 Texts.  A copy of the license is included in the section entitled ``GNU
 Free Documentation License''.
 @end quotation
-@end copying
+@end macro
 
 @titlepage
 @title @sc{gnu} @code{Coreutils}
@@ -143,16 +143,15 @@
 
 @page
 @vskip 0pt plus 1filll
+
 @insertcopying
 @end titlepage
 
 
-@ifnottex
 @node Top
 @top GNU Coreutils
 
 @insertcopying
-@end ifnottex
 
 @cindex core utilities
 @cindex text utilities
@@ -240,13 +239,13 @@
 * ptx invocation::              Produce a permuted index of file contents.
 * tsort invocation::            Topological sort.
 
-@command{ptx}: Produce permuted indexes
+@file{ptx}: Produce permuted indexes
 
 * General options in ptx::      Options which affect general program behavior.
 * Charset selection in ptx::    Underlying character set considerations.
 * Input processing in ptx::     Input fields, contexts, and keyword selection.
 * Output formatting in ptx::    Types of output format, and sizing the fields.
-* Compatibility in ptx::        The GNU extensions to @command{ptx}
+* Compatibility in ptx::        The GNU extensions to @file{ptx}
 
 Operating on fields within a line
 
@@ -260,7 +259,7 @@
 * expand invocation::           Convert tabs to spaces.
 * unexpand invocation::         Convert spaces to tabs.
 
-@command{tr}: Translate, squeeze, and/or delete characters
+@file{tr}: Translate, squeeze, and/or delete characters
 
 * Character sets::              Specifying sets of characters.
 * Translating::                 Changing one set of characters to another.
@@ -272,9 +271,9 @@
 * ls invocation::               List directory contents
 * dir invocation::              Briefly list directory contents
 * vdir invocation::             Verbosely list directory contents
-* dircolors invocation::        Color setup for @command{ls}
+* dircolors invocation::        Color setup for @file{ls}
 
-@command{ls}:  List directory contents
+@file{ls}:  List directory contents
 
 * Which files are listed::      Which files are listed
 * What information is listed::  What information is listed
@@ -330,7 +329,7 @@
 * test invocation::               Check file types and compare values
 * expr invocation::               Evaluate expressions
 
-@command{test}: Check file types and compare values
+@file{test}: Check file types and compare values
 
 * File type tests::               File type tests
 * Access permission tests::       Access permission tests
@@ -338,12 +337,12 @@
 * String tests::                  String tests
 * Numeric tests::                 Numeric tests
 
-@command{expr}: Evaluate expression
+@file{expr}: Evaluate expression
 
 * String expressions::         + : match substr index length
 * Numeric expressions::        + - * / %
 * Relations for expr::         | & < <= = == != >= >
-* Examples of expr::           Examples of using @command{expr}
+* Examples of expr::           Examples of using @file{expr}
 
 Redirection
 
@@ -362,7 +361,7 @@
 * printenv invocation::          Print all or some environment variables
 * tty invocation::               Print file name of terminal on standard input
 
-@command{stty}: Print or change terminal characteristics
+@file{stty}: Print or change terminal characteristics
 
 * Control::                     Control settings
 * Input::                       Input settings
@@ -388,15 +387,15 @@
 * hostname invocation::          Print or set system name
 * hostid invocation::            Print numeric host identifier.
 
-@command{date}: Print or set system date and time
+@file{date}: Print or set system date and time
 
 * Time directives::              Time directives
 * Date directives::              Date directives
 * Literal directives::           Literal directives
 * Padding::                      Padding
 * Setting the time::             Setting the time
-* Options for date::             Options for @command{date}
-* Examples of date::             Examples of @command{date}
+* Options for date::             Options for @file{date}
+* Examples of date::             Examples of @file{date}
 
 Modified command invocation
 
@@ -440,10 +439,10 @@
 
 * Toolbox introduction::         Toolbox introduction
 * I/O redirection::              I/O redirection
-* The who command::              The @command{who} command
-* The cut command::              The @command{cut} command
-* The sort command::             The @command{sort} command
-* The uniq command::             The @command{uniq} command
+* The who command::              The @file{who} command
+* The cut command::              The @file{cut} command
+* The sort command::             The @file{sort} command
+* The uniq command::             The @file{uniq} command
 * Putting the tools together::   Putting the tools together
 
 GNU Free Documentation License
@@ -462,9 +461,9 @@
 please get involved in improving this manual.  The entire @sc{gnu} community
 will benefit.
 
-@cindex @acronym{POSIX}
+@cindex @b{POSIX}
 The @sc{gnu} utilities documented here are mostly compatible with the
-@acronym{POSIX} standard.
+@b{POSIX} standard.
 @cindex bugs, reporting
 Please report bugs to @email{bug-coreutils@@gnu.org}.  Remember
 to include the version number, machine architecture, input files, and
@@ -484,7 +483,7 @@
 distributions, which were written by David MacKenzie and updated by Jim
 Meyering.  What you are reading now is the authoritative documentation
 for these utilities; the man pages are no longer being maintained.  The
-original @command{fmt} man page was written by Ross Paterson.  Fran@,{c}ois
+original @file{fmt} man page was written by Ross Paterson.  Fran@,{c}ois
 Pinard did the initial conversion to Texinfo format.  Karl Berry did the
 indexing, some reorganization, and editing of the results.  Brian
 Youmans of the Free Software Foundation office staff combined the
@@ -511,7 +510,7 @@
 @itemx --suffix=@var{suffix}
 @opindex -S
 @opindex --suffix
-Append @var{suffix} to each backup file made with @option{-b}.
+Append @var{suffix} to each backup file made with @code{-b}.
 @xref{Backup options}.
 @end macro
 
@@ -531,8 +530,8 @@
 Append an SI-style abbreviation to each size, such as @samp{MB} for
 megabytes.  Powers of 1000 are used, not 1024; @samp{MB} stands for
 1,000,000 bytes.  This option is equivalent to
-@option{--block-size=si}.  Use the @option{-h} or
-@option{--human-readable} option if
+@code{--block-size=si}.  Use the @code{-h} or
+@code{--human-readable} option if
 you prefer powers of 1024.
 @end macro
 
@@ -544,7 +543,7 @@
 @cindex human-readable output
 Append a size letter to each size, such as @samp{M} for mebibytes.
 Powers of 1024 are used, not 1000; @samp{M} stands for 1,048,576 bytes.
-Use the @option{--si} option if you prefer powers of 1000.
+Use the @code{--si} option if you prefer powers of 1000.
 @end macro
 
 @macro optStripTrailingSlashes
@@ -560,7 +559,7 @@
 @itemx --version-control=@var{method}
 @opindex -V
 @opindex --version-control
-Change the type of backups made with @option{-b}.  The @var{method}
+Change the type of backups made with @code{-b}.  The @var{method}
 argument can be @samp{none} (or @samp{off}), @samp{numbered} (or
 @samp{t}), @samp{existing} (or @samp{nil}), or @samp{never} (or
 @samp{simple}).  @xref{Backup options}.
@@ -577,11 +576,11 @@
 Normally options and operands can appear in any order, and programs act
 as if all the options appear before any operands.  For example,
 @samp{sort -r passwd -t :} acts like @samp{sort -r -t : passwd}, since
-@samp{:} is an option-argument of @option{-t}.  However, if the
-@env{POSIXLY_CORRECT} environment variable is set, options must appear
+@samp{:} is an option-argument of @code{-t}.  However, if the
+@var{POSIXLY_CORRECT} environment variable is set, options must appear
 before operands, unless otherwise specified for a particular command.
 
-Some of these programs recognize the @option{--help} and @option{--version}
+Some of these programs recognize the @code{--help} and @code{--version}
 options only when one of them is the sole command line argument.
 
 @table @samp
@@ -623,7 +622,7 @@
 * Trailing slashes::            --strip-trailing-slashes, in some programs.
 * Traversing symlinks::         -H, -L, or -P, in some programs.
 * Treating / specially::        --preserve-root and --no-preserve-root.
-* Standards conformance::       Conformance to the @acronym{POSIX} standard.
+* Standards conformance::       Conformance to the @b{POSIX} standard.
 @end menu
 
 
@@ -639,16 +638,16 @@
 that can be used to change how other commands work.
 For the vast majority of commands, an exit status of zero indicates
 success.  Failure is indicated by a nonzero value---typically
-@samp{1}, though it may differ on unusual platforms as @acronym{POSIX}
+@samp{1}, though it may differ on unusual platforms as @b{POSIX}
 requires only that it be nonzero.
 
 However, some of the programs documented here do produce
 other exit status values and a few associate different
 meanings with the values @samp{0} and @samp{1}.
 Here are some of the exceptions:
-@command{chroot}, @command{env}, @command{expr},
-@command{nice}, @command{nohup}, @command{printenv},
-@command{sort}, @command{su}, @command{test}, @command{tty}.
+@file{chroot}, @file{env}, @file{expr},
+@file{nice}, @file{nohup}, @file{printenv},
+@file{sort}, @file{su}, @file{test}, @file{tty}.
 
 
 @node Backup options
@@ -656,8 +655,8 @@
 
 @cindex backup options
 
-Some @sc{gnu} programs (at least @command{cp}, @command{install},
-@command{ln}, and @command{mv}) optionally make backups of files
+Some @sc{gnu} programs (at least @file{cp}, @file{install},
+@file{ln}, and @file{mv}) optionally make backups of files
 before writing new versions.
 These options control the details of these backups.  The options are also
 briefly mentioned in the descriptions of the particular programs.
@@ -674,12 +673,12 @@
 Without this option, the original versions are destroyed.
 Use @var{method} to determine the type of backups to make.
 When this option is used but @var{method} is not specified,
-then the value of the @env{VERSION_CONTROL}
-environment variable is used.  And if @env{VERSION_CONTROL} is not set,
+then the value of the @var{VERSION_CONTROL}
+environment variable is used.  And if @var{VERSION_CONTROL} is not set,
 the default backup type is @samp{existing}.
 
-Note that the short form of this option, @option{-b} does not accept any
-argument.  Using @option{-b} is equivalent to using @option{--backup=existing}.
+Note that the short form of this option, @code{-b} does not accept any
+argument.  Using @code{-b} is equivalent to using @code{--backup=existing}.
 
 @vindex version-control @r{Emacs variable}
 This option corresponds to the Emacs variable @samp{version-control};
@@ -718,9 +717,9 @@
 @opindex --suffix
 @cindex backup suffix
 @vindex SIMPLE_BACKUP_SUFFIX
-Append @var{suffix} to each backup file made with @option{-b}.  If this
-option is not specified, the value of the @env{SIMPLE_BACKUP_SUFFIX}
-environment variable is used.  And if @env{SIMPLE_BACKUP_SUFFIX} is not
+Append @var{suffix} to each backup file made with @code{-b}.  If this
+option is not specified, the value of the @var{SIMPLE_BACKUP_SUFFIX}
+environment variable is used.  And if @var{SIMPLE_BACKUP_SUFFIX} is not
 set, the default is @samp{~}, just as in Emacs.
 
 @itemx --version-control=@var{method}
@@ -737,8 +736,8 @@
 
 @cindex block size
 
-Some @sc{gnu} programs (at least @command{df}, @command{du}, and
-@command{ls}) display sizes in ``blocks''.  You can adjust the block size
+Some @sc{gnu} programs (at least @file{df}, @file{du}, and
+@file{ls}) display sizes in ``blocks''.  You can adjust the block size
 and method of display to make sizes easier to read.  The block size
 used for display is independent of any filesystem block size.
 Fractional block counts are rounded up to the nearest integer.
@@ -756,23 +755,23 @@
 @table @code
 
 @item DF_BLOCK_SIZE
-This specifies the default block size for the @command{df} command.
-Similarly, @env{DU_BLOCK_SIZE} specifies the default for @command{du} and
-@env{LS_BLOCK_SIZE} for @command{ls}.
+This specifies the default block size for the @file{df} command.
+Similarly, @var{DU_BLOCK_SIZE} specifies the default for @file{du} and
+@var{LS_BLOCK_SIZE} for @file{ls}.
 
 @item BLOCK_SIZE
 This specifies the default block size for all three commands, if the
 above command-specific environment variables are not set.
 
 @item POSIXLY_CORRECT
-If neither the @env{@var{command}_BLOCK_SIZE} nor the @env{BLOCK_SIZE}
+If neither the @var{@var{command}_BLOCK_SIZE} nor the @var{BLOCK_SIZE}
 variables are set, but this variable is set, the block size defaults to 512.
 
 @end table
 
 If none of the above environment variables are set, the block size
 currently defaults to 1024 bytes in most contexts, but this number may
-change in the future.  For @command{ls} file sizes, the block size
+change in the future.  For @file{ls} file sizes, the block size
 defaults to 1 byte.
 
 @cindex human-readable output
@@ -795,7 +794,7 @@
 
 @vindex LC_NUMERIC
 A block size specification preceded by @samp{'} causes output sizes to
-be displayed with thousands separators.  The @env{LC_NUMERIC} locale
+be displayed with thousands separators.  The @var{LC_NUMERIC} locale
 specifies the thousands separator and grouping.  For example, in an
 American English locale, @samp{--block-size="'1kB"} would cause a size
 of 1234000 bytes to be displayed as @samp{1,234}.  In the default C
@@ -828,7 +827,7 @@
 @cindex kibibyte, definition of
 kibibyte: @math{2^10 = 1024}.  @samp{K} is special: the SI prefix is
 @samp{k} and the IEC 60027-2 prefix is @samp{Ki}, but tradition and
-@acronym{POSIX} use @samp{k} to mean @samp{KiB}.
+@b{POSIX} use @samp{k} to mean @samp{KiB}.
 @item MB
 @cindex megabyte, definition of
 megabyte: @math{10^6 = 1,000,000}.
@@ -887,20 +886,20 @@
 @opindex --si
 
 Block size defaults can be overridden by an explicit
-@option{--block-size=@var{size}} option.  The @option{-k}
-option is equivalent to @option{--block-size=1K}, which
-is the default unless the @env{POSIXLY_CORRECT} environment variable is
-set.  The @option{-h} or @option{--human-readable} option is equivalent to
-@option{--block-size=human-readable}.  The @option{--si} option is
-equivalent to @option{--block-size=si}.
+@code{--block-size=@var{size}} option.  The @code{-k}
+option is equivalent to @code{--block-size=1K}, which
+is the default unless the @var{POSIXLY_CORRECT} environment variable is
+set.  The @code{-h} or @code{--human-readable} option is equivalent to
+@code{--block-size=human-readable}.  The @code{--si} option is
+equivalent to @code{--block-size=si}.
 
 @node Target directory
 @section Target directory
 
 @cindex target directory
 
-Some @sc{gnu} programs (at least @command{cp}, @command{install},
-@command{ln}, and @command{mv}) allow you to specify the target directory
+Some @sc{gnu} programs (at least @file{cp}, @file{install},
+@file{ln}, and @file{mv}) allow you to specify the target directory
 via this option:
 
 @table @samp
@@ -914,10 +913,10 @@
 The interface for most programs is that after processing options and a
 finite (possibly zero) number of fixed-position arguments, the remaining
 argument list is either expected to be empty, or is a list of items
-(usually files) that will all be handled identically.  The @command{xargs}
+(usually files) that will all be handled identically.  The @file{xargs}
 program is designed to work well with this convention.
 
-The commands in the @command{mv}-family are unusual in that they take
+The commands in the @file{mv}-family are unusual in that they take
 a variable number of arguments with a special case at the @emph{end}
 (namely, the target directory).  This makes it nontrivial to perform some
 operations, e.g., ``move all files from here to ../d/'', because
@@ -927,9 +926,9 @@
 shell command, but that requires more human labor and brain power than
 it should.)
 
-The @w{@kbd{--target-directory}} option allows the @command{cp},
-@command{install}, @command{ln}, and @command{mv} programs to be used
-conveniently with @command{xargs}.  For example, you can move the files
+The @w{@kbd{--target-directory}} option allows the @file{cp},
+@file{install}, @file{ln}, and @file{mv} programs to be used
+conveniently with @file{xargs}.  For example, you can move the files
 from the current directory to a sibling directory, @code{d} like this:
 (However, this doesn't move files whose names begin with @samp{.}.)
 
@@ -937,7 +936,7 @@
 ls |xargs mv --target-directory=../d
 @end smallexample
 
-If you use the @sc{gnu} @command{find} program, you can move @emph{all}
+If you use the @sc{gnu} @file{find} program, you can move @emph{all}
 files with this command:
 @example
 find . -mindepth 1 -maxdepth 1 \
@@ -947,7 +946,7 @@
 But that will fail if there are no files in the current directory
 or if any file has a name containing a newline character.
 The following example removes those limitations and requires both
-@sc{gnu} @command{find} and @sc{gnu} @command{xargs}:
+@sc{gnu} @file{find} and @sc{gnu} @file{xargs}:
 @example
 find . -mindepth 1 -maxdepth 1 -print0 \
   | xargs --null --no-run-if-empty \
@@ -961,7 +960,7 @@
 
 @cindex trailing slashes
 
-Some @sc{gnu} programs (at least @command{cp} and @command{mv}) allow you to
+Some @sc{gnu} programs (at least @file{cp} and @file{mv}) allow you to
 remove any trailing slashes from each @var{source} argument before
 operating on it.  The @w{@kbd{--strip-trailing-slashes}} option enables
 this behavior.
@@ -971,11 +970,11 @@
 specify a symbolic link to a directory.  This scenario is in fact rather
 common because some shells can automatically append a trailing slash when
 performing file name completion on such symbolic links.  Without this
-option, @command{mv}, for example, (via the system's rename function) must
+option, @file{mv}, for example, (via the system's rename function) must
 interpret a trailing slash as a request to dereference the symbolic link
 and so must rename the indirectly referenced @emph{directory} and not
 the symbolic link.  Although it may seem surprising that such behavior
-be the default, it is required by @acronym{POSIX} and is consistent with
+be the default, it is required by @b{POSIX} and is consistent with
 other parts of that standard.
 
 @node Traversing symlinks
@@ -983,10 +982,10 @@
 
 @cindex symbolic link to directory, controlling traversal of
 
-The following options modify how @command{chown} and @command{chgrp}
+The following options modify how @file{chown} and @file{chgrp}
 @c FIXME: note that `du' has these options, too, but they have slightly
 @c different meaning.
-traverse a hierarchy when the @option{--recursive} (@option{-R})
+traverse a hierarchy when the @code{--recursive} (@code{-R})
 option is also specified.
 If more than one of the following options is specified, only the final
 one takes effect.
@@ -994,8 +993,8 @@
 entails operating on just the symbolic link or on all files in the
 hierarchy rooted at that directory.
 
-These options are independent of @option{--dereference} and
-@option{--no-dereference} (@option{-h}), which control whether to modify
+These options are independent of @code{--dereference} and
+@code{--no-dereference} (@code{-h}), which control whether to modify
 a symlink or its referent.
 
 @table @samp
@@ -1004,7 +1003,7 @@
 @item -H
 @opindex -H
 @cindex symbolic link to directory, traverse each that is specified on the command line
-If @option{--recursive} (@option{-R}) is specified and
+If @code{--recursive} (@code{-R}) is specified and
 a command line argument is a symbolic link to a directory, traverse it.
 @end macro
 @choptH
@@ -1023,8 +1022,8 @@
 @opindex -P
 @cindex symbolic link to directory, never traverse
 Do not traverse any symbolic links.
-This is the default if none of @option{-H}, @option{-L},
-or @option{-P} is specified.
+This is the default if none of @code{-H}, @code{-L},
+or @code{-P} is specified.
 @end macro
 @choptP
 
@@ -1040,18 +1039,18 @@
 all files on the entire system.  Since there are so few
 @footnote{If you know of one, please write to @email{bug-coreutils@@gnu.org}.}
 legitimate uses for such a command,
-@sc{gnu} @command{rm} provides the @option{--preserve-root} option
-to make it so @command{rm} declines to operate on any directory
+@sc{gnu} @file{rm} provides the @code{--preserve-root} option
+to make it so @file{rm} declines to operate on any directory
 that resolves to @file{/}.  The default is still to allow
 @samp{rm -rf /} to operate unimpeded.
-Another new option, @option{--no-preserve-root}, cancels the
-effect of any preceding @option{--preserve-root} option.
-Note that the @option{--preserve-root} behavior may become the default
-for @command{rm}.
+Another new option, @code{--no-preserve-root}, cancels the
+effect of any preceding @code{--preserve-root} option.
+Note that the @code{--preserve-root} behavior may become the default
+for @file{rm}.
 
-The commands @command{chgrp}, @command{chmod} and @command{chown}
+The commands @file{chgrp}, @file{chmod} and @file{chown}
 can also operate destructively on entire hierarchies, so they too
-support these options.  Although, unlike @command{rm}, they don't
+support these options.  Although, unlike @file{rm}, they don't
 actually unlink files, these commands are arguably more dangerous
 when operating recursively on @file{/}, since they often work much
 more quickly, and hence damage more files before an alert user can
@@ -1062,29 +1061,29 @@
 
 @vindex POSIXLY_CORRECT
 In a few cases, the @sc{gnu} utilities' default behavior is
-incompatible with the @acronym{POSIX} standard.  To suppress these
-incompatibilities, define the @env{POSIXLY_CORRECT} environment
-variable.  Unless you are checking for @acronym{POSIX} conformance, you
-probably do not need to define @env{POSIXLY_CORRECT}.
+incompatible with the @b{POSIX} standard.  To suppress these
+incompatibilities, define the @var{POSIXLY_CORRECT} environment
+variable.  Unless you are checking for @b{POSIX} conformance, you
+probably do not need to define @var{POSIXLY_CORRECT}.
 
-Newer versions of @acronym{POSIX} are occasionally incompatible with older
-versions.  For example, older versions of @acronym{POSIX} required the
+Newer versions of @b{POSIX} are occasionally incompatible with older
+versions.  For example, older versions of @b{POSIX} required the
 command @samp{sort +1} to sort based on the second and succeeding
-fields in each input line, but starting with @acronym{POSIX} 1003.1-2001
+fields in each input line, but starting with @b{POSIX} 1003.1-2001
 the same command is required to sort the file named @file{+1}, and you
 must instead use the command @samp{sort -k 2} to get the field-based
 sort.
 
 @vindex _POSIX2_VERSION
-The @sc{gnu} utilities normally conform to the version of @acronym{POSIX}
+The @sc{gnu} utilities normally conform to the version of @b{POSIX}
 that is standard for your system.  To cause them to conform to a
-different version of @acronym{POSIX}, define the @env{_POSIX2_VERSION}
+different version of @b{POSIX}, define the @var{_POSIX2_VERSION}
 environment variable to a value of the form @var{yyyymm} specifying
 the year and month the standard was adopted.  Two values are currently
-supported for @env{_POSIX2_VERSION}: @samp{199209} stands for
-@acronym{POSIX} 1003.2-1992, and @samp{200112} stands for @acronym{POSIX}
+supported for @var{_POSIX2_VERSION}: @samp{199209} stands for
+@b{POSIX} 1003.2-1992, and @samp{200112} stands for @b{POSIX}
 1003.1-2001.  For example, if you are running older software that
-assumes an older version of @acronym{POSIX} and uses @samp{sort +1},
+assumes an older version of @b{POSIX} and uses @samp{sort +1},
 @samp{head -1}, or @samp{tail +1}, you
 can work around the compatibility problems by setting
 @samp{_POSIX2_VERSION=199209} in your environment.
@@ -1106,13 +1105,13 @@
 @end menu
 
 @node cat invocation
-@section @command{cat}: Concatenate and write files
+@section @file{cat}: Concatenate and write files
 
 @pindex cat
 @cindex concatenate and write files
 @cindex copying files
 
-@command{cat} copies each @var{file} (@samp{-} means standard input), or
+@file{cat} copies each @var{file} (@samp{-} means standard input), or
 standard input if none are given, to standard output.  Synopsis:
 
 @example
@@ -1127,7 +1126,7 @@
 @itemx --show-all
 @opindex -A
 @opindex --show-all
-Equivalent to @option{-vET}.
+Equivalent to @code{-vET}.
 
 @item -B
 @itemx --binary
@@ -1135,17 +1134,17 @@
 @opindex --binary
 @cindex binary and text I/O in cat
 On MS-DOS and MS-Windows only, read and write the files in binary mode.
-By default, @command{cat} on MS-DOS/MS-Windows uses binary mode only when
+By default, @file{cat} on MS-DOS/MS-Windows uses binary mode only when
 standard output is redirected to a file or a pipe; this option overrides
 that.  Binary file I/O is used so that the files retain their format
-(Unix text as opposed to DOS text and binary), because @command{cat} is
+(Unix text as opposed to DOS text and binary), because @file{cat} is
 frequently used as a file-copying program.  Some options (see below)
-cause @command{cat} to read and write files in text mode because in those
+cause @file{cat} to read and write files in text mode because in those
 cases the original file contents aren't important (e.g., when lines are
-numbered by @command{cat}, or when line endings should be marked).  This is
+numbered by @file{cat}, or when line endings should be marked).  This is
 so these options work as DOS/Windows users would expect; for example,
 DOS-style text files have their lines end with the CR-LF pair of
-characters, which won't be processed as an empty line by @option{-b} unless
+characters, which won't be processed as an empty line by @code{-b} unless
 the file is read in text mode.
 
 @item -b
@@ -1153,19 +1152,19 @@
 @opindex -b
 @opindex --number-nonblank
 Number all nonblank output lines, starting with 1.  On MS-DOS and
-MS-Windows, this option causes @command{cat} to read and write files in
+MS-Windows, this option causes @file{cat} to read and write files in
 text mode.
 
 @item -e
 @opindex -e
-Equivalent to @option{-vE}.
+Equivalent to @code{-vE}.
 
 @item -E
 @itemx --show-ends
 @opindex -E
 @opindex --show-ends
 Display a @samp{$} after the end of each line.  On MS-DOS and
-MS-Windows, this option causes @command{cat} to read and write files in
+MS-Windows, this option causes @file{cat} to read and write files in
 text mode.
 
 @item -n
@@ -1173,7 +1172,7 @@
 @opindex -n
 @opindex --number
 Number all output lines, starting with 1.  On MS-DOS and MS-Windows,
-this option causes @command{cat} to read and write files in text mode.
+this option causes @file{cat} to read and write files in text mode.
 
 @item -s
 @itemx --squeeze-blank
@@ -1181,12 +1180,12 @@
 @opindex --squeeze-blank
 @cindex squeezing blank lines
 Replace multiple adjacent blank lines with a single blank line.  On
-MS-DOS and MS-Windows, this option causes @command{cat} to read and write
+MS-DOS and MS-Windows, this option causes @file{cat} to read and write
 files in text mode.
 
 @item -t
 @opindex -t
-Equivalent to @option{-vT}.
+Equivalent to @code{-vT}.
 
 @item -T
 @itemx --show-tabs
@@ -1204,7 +1203,7 @@
 @opindex --show-nonprinting
 Display control characters except for LFD and TAB using
 @samp{^} notation and precede characters that have the high bit set with
-@samp{M-}.  On MS-DOS and MS-Windows, this option causes @command{cat} to
+@samp{M-}.  On MS-DOS and MS-Windows, this option causes @file{cat} to
 read files and standard input in DOS binary mode, so the CR
 characters at the end of each line are also visible.
 
@@ -1214,12 +1213,12 @@
 
 
 @node tac invocation
-@section @command{tac}: Concatenate and write files in reverse
+@section @file{tac}: Concatenate and write files in reverse
 
 @pindex tac
 @cindex reversing files
 
-@command{tac} copies each @var{file} (@samp{-} means standard input), or
+@file{tac} copies each @var{file} (@samp{-} means standard input), or
 standard input if none are given, to standard output, reversing the
 records (lines by default) in each separately.  Synopsis:
 
@@ -1246,8 +1245,8 @@
 @itemx --regex
 @opindex -r
 @opindex --regex
-Treat the separator string as a regular expression.  Users of @command{tac}
-on MS-DOS/MS-Windows should note that, since @command{tac} reads files in
+Treat the separator string as a regular expression.  Users of @file{tac}
+on MS-DOS/MS-Windows should note that, since @file{tac} reads files in
 binary mode, each line of a text file might end with a CR/LF pair
 instead of the Unix-style LF.
 
@@ -1263,13 +1262,13 @@
 
 
 @node nl invocation
-@section @command{nl}: Number lines and write files
+@section @file{nl}: Number lines and write files
 
 @pindex nl
 @cindex numbering lines
 @cindex line numbering
 
-@command{nl} writes each @var{file} (@samp{-} means standard input), or
+@file{nl} writes each @var{file} (@samp{-} means standard input), or
 standard input if none are given, to standard output, with line numbers
 added to some or all of the lines.  Synopsis:
 
@@ -1278,8 +1277,8 @@
 @end example
 
 @cindex logical pages, numbering on
-@command{nl} decomposes its input into (logical) pages; by default, the
-line number is reset to 1 at the top of each logical page.  @command{nl}
+@file{nl} decomposes its input into (logical) pages; by default, the
+line number is reset to 1 at the top of each logical page.  @file{nl}
 treats all of the input files as a single document; it does not reset
 line numbers or logical pages between files.
 
@@ -1308,7 +1307,7 @@
 
 A section delimiter is replaced by an empty line on output.  Any text
 that comes before the first section delimiter string in the input file
-is considered to be part of a body section, so @command{nl} treats a
+is considered to be part of a body section, so @file{nl} treats a
 file that contains no section delimiters as a single body section.
 
 The program accepts the following options.  Also see @ref{Common options}.
@@ -1351,13 +1350,13 @@
 @itemx --footer-numbering=@var{style}
 @opindex -f
 @opindex --footer-numbering
-Analogous to @option{--body-numbering}.
+Analogous to @code{--body-numbering}.
 
 @item -h @var{style}
 @itemx --header-numbering=@var{style}
 @opindex -h
 @opindex --header-numbering
-Analogous to @option{--body-numbering}.
+Analogous to @code{--body-numbering}.
 
 @item -i @var{number}
 @itemx --page-increment=@var{number}
@@ -1385,13 +1384,13 @@
 
 @table @samp
 @item ln
-@opindex ln @r{format for @command{nl}}
+@opindex ln @r{format for @file{nl}}
 left justified, no leading zeros;
 @item rn
-@opindex rn @r{format for @command{nl}}
+@opindex rn @r{format for @file{nl}}
 right justified, no leading zeros;
 @item rz
-@opindex rz @r{format for @command{nl}}
+@opindex rz @r{format for @file{nl}}
 right justified, leading zeros.
 @end table
 
@@ -1426,7 +1425,7 @@
 
 
 @node od invocation
-@section @command{od}: Write files in octal or other formats
+@section @file{od}: Write files in octal or other formats
 
 @pindex od
 @cindex octal dump of files
@@ -1434,7 +1433,7 @@
 @cindex ASCII dump of files
 @cindex file contents, dumping unambiguously
 
-@command{od} writes an unambiguous representation of each @var{file}
+@file{od} writes an unambiguous representation of each @var{file}
 (@samp{-} means standard input), or standard input if none are given.
 Synopses:
 
@@ -1444,7 +1443,7 @@
 @end example
 
 Each line of output consists of the offset in the input, followed by
-groups of data from the file. By default, @command{od} prints the offset in
+groups of data from the file. By default, @file{od} prints the offset in
 octal, and each group of file data is two bytes of input printed as a
 single octal number.
 
@@ -1489,7 +1488,7 @@
 @opindex -N
 @opindex --read-bytes
 Output at most @var{bytes} bytes of the input.  Prefixes and suffixes on
-@code{bytes} are interpreted as for the @option{-j} option.
+@code{bytes} are interpreted as for the @code{-j} option.
 
 @item -s @var{n}
 @itemx --strings[=@var{n}]
@@ -1497,14 +1496,14 @@
 @opindex --strings
 @cindex string constants, outputting
 Instead of the normal output, output only @dfn{string constants}: at
-least @var{n} consecutive @acronym{ASCII} graphic characters,
+least @var{n} consecutive @b{ASCII} graphic characters,
 followed by a null (zero) byte.
 
-If @var{n} is omitted with @option{--strings}, the default is 3.  On
-older systems, @sc{gnu} @command{od} instead supports an obsolete
-option @option{-s[@var{n}]}, where @var{n} also defaults to 3.
-@acronym{POSIX} 1003.1-2001 (@pxref{Standards conformance}) does not allow
-@option{-s} without an argument; use @option{--strings} instead.
+If @var{n} is omitted with @code{--strings}, the default is 3.  On
+older systems, @sc{gnu} @file{od} instead supports an obsolete
+option @code{-s[@var{n}]}, where @var{n} also defaults to 3.
+@b{POSIX} 1003.1-2001 (@pxref{Standards conformance}) does not allow
+@code{-s} without an argument; use @code{--strings} instead.
 
 @item -t @var{type}
 @itemx --format=@var{type}
@@ -1513,19 +1512,19 @@
 Select the format in which to output the file data.  @var{type} is a
 string of one or more of the below type indicator characters.  If you
 include more than one type indicator character in a single @var{type}
-string, or use this option more than once, @command{od} writes one copy
+string, or use this option more than once, @file{od} writes one copy
 of each output line using each of the data types that you specified,
 in the order that you specified.
 
 Adding a trailing ``z'' to any type specification appends a display
-of the @acronym{ASCII} character representation of the printable characters
+of the @b{ASCII} character representation of the printable characters
 to the output line generated by the type specification.
 
 @table @samp
 @item a
 named character
 @item c
-@acronym{ASCII} character or backslash escape,
+@b{ASCII} character or backslash escape,
 @item d
 signed decimal
 @item f
@@ -1578,7 +1577,7 @@
 @opindex -v
 @opindex --output-duplicates
 Output consecutive lines that are identical.  By default, when two or
-more consecutive output lines would be identical, @command{od} outputs only
+more consecutive output lines would be identical, @file{od} outputs only
 the first line, and puts just an asterisk on the following line to
 indicate the elision.
 
@@ -1591,64 +1590,64 @@
 output types.
 
 If this option is not given at all, the default is 16.  If @var{n} is
-omitted with @option{--width}, the default is 32.  On older systems,
-@sc{gnu} @command{od} instead supports an obsolete option
-@option{-w[@var{n}]}, where @var{n} also defaults to 32.  @acronym{POSIX}
-1003.1-2001 (@pxref{Standards conformance}) does not allow @option{-w}
-without an argument; use @option{--width} instead.
+omitted with @code{--width}, the default is 32.  On older systems,
+@sc{gnu} @file{od} instead supports an obsolete option
+@code{-w[@var{n}]}, where @var{n} also defaults to 32.  @b{POSIX}
+1003.1-2001 (@pxref{Standards conformance}) does not allow @code{-w}
+without an argument; use @code{--width} instead.
 
 @end table
 
 The next several options are shorthands for format specifications.
-@sc{gnu} @command{od} accepts any combination of shorthands and format
+@sc{gnu} @file{od} accepts any combination of shorthands and format
 specification options.  These options accumulate.
 
 @table @samp
 
 @item -a
 @opindex -a
-Output as named characters.  Equivalent to @option{-ta}.
+Output as named characters.  Equivalent to @code{-ta}.
 
 @item -b
 @opindex -b
-Output as octal bytes.  Equivalent to @option{-toC}.
+Output as octal bytes.  Equivalent to @code{-toC}.
 
 @item -c
 @opindex -c
-Output as @acronym{ASCII} characters or backslash escapes.  Equivalent to
-@option{-tc}.
+Output as @b{ASCII} characters or backslash escapes.  Equivalent to
+@code{-tc}.
 
 @item -d
 @opindex -d
-Output as unsigned decimal shorts.  Equivalent to @option{-tu2}.
+Output as unsigned decimal shorts.  Equivalent to @code{-tu2}.
 
 @item -f
 @opindex -f
-Output as floats.  Equivalent to @option{-tfF}.
+Output as floats.  Equivalent to @code{-tfF}.
 
 @item -h
 @opindex -h
-Output as hexadecimal shorts.  Equivalent to @option{-tx2}.
+Output as hexadecimal shorts.  Equivalent to @code{-tx2}.
 
 @item -i
 @opindex -i
-Output as decimal shorts.  Equivalent to @option{-td2}.
+Output as decimal shorts.  Equivalent to @code{-td2}.
 
 @item -l
 @opindex -l
-Output as decimal longs.  Equivalent to @option{-td4}.
+Output as decimal longs.  Equivalent to @code{-td4}.
 
 @item -o
 @opindex -o
-Output as octal shorts.  Equivalent to @option{-to2}.
+Output as octal shorts.  Equivalent to @code{-to2}.
 
 @item -x
 @opindex -x
-Output as hexadecimal shorts.  Equivalent to @option{-tx2}.
+Output as hexadecimal shorts.  Equivalent to @code{-tx2}.
 
 @item --traditional
 @opindex --traditional
-Recognize the non-option arguments that traditional @command{od}
+Recognize the non-option arguments that traditional @file{od}
 accepted.  The following syntax:
 
 @smallexample
@@ -1689,21 +1688,21 @@
 
 
 @node fmt invocation
-@section @command{fmt}: Reformat paragraph text
+@section @file{fmt}: Reformat paragraph text
 
 @pindex fmt
 @cindex reformatting paragraph text
 @cindex paragraphs, reformatting
 @cindex text, reformatting
 
-@command{fmt} fills and joins lines to produce output lines of (at most)
+@file{fmt} fills and joins lines to produce output lines of (at most)
 a given number of characters (75 by default).  Synopsis:
 
 @example
 fmt [@var{option}]@dots{} [@var{file}]@dots{}
 @end example
 
-@command{fmt} reads from the specified @var{file} arguments (or standard
+@file{fmt} reads from the specified @var{file} arguments (or standard
 input if none are given), and writes to standard output.
 
 By default, blank lines, spaces between words, and indentation are
@@ -1715,12 +1714,12 @@
 @cindex sentences and line-breaking
 @cindex Knuth, Donald E.
 @cindex Plass, Michael F.
-@command{fmt} prefers breaking lines at the end of a sentence, and tries to
+@file{fmt} prefers breaking lines at the end of a sentence, and tries to
 avoid line breaks after the first word of a sentence or before the last
 word of a sentence.  A @dfn{sentence break} is defined as either the end
 of a paragraph or a word ending in any of @samp{.?!}, followed by two
 spaces or end of line, ignoring any intervening parentheses or quotes.
-Like @TeX{}, @command{fmt} reads entire ``paragraphs'' before choosing line
+Like @TeX{}, @file{fmt} reads entire ``paragraphs'' before choosing line
 breaks; the algorithm is a variant of that in ``Breaking Paragraphs Into
 Lines'' (Donald E. Knuth and Michael F. Plass, @cite{Software---Practice
 and Experience}, 11 (1981), 1119--1184).
@@ -1769,7 +1768,7 @@
 @opindex -@var{width}
 @opindex -w
 @opindex --width
-Fill output lines up to @var{width} characters (default 75).  @command{fmt}
+Fill output lines up to @var{width} characters (default 75).  @file{fmt}
 initially tries to make lines about 7% shorter than this, to give it
 room to balance line lengths.
 
@@ -1787,14 +1786,14 @@
 
 
 @node pr invocation
-@section @command{pr}: Paginate or columnate files for printing
+@section @file{pr}: Paginate or columnate files for printing
 
 @pindex pr
 @cindex printing, preparing files for
 @cindex multicolumn output, generating
 @cindex merging files in parallel
 
-@command{pr} writes each @var{file} (@samp{-} means standard input), or
+@file{pr} writes each @var{file} (@samp{-} means standard input), or
 standard input if none are given, to standard output, paginating and
 optionally outputting in multicolumn format; optionally merges all
 @var{file}s, printing all in parallel, one per column.  Synopsis:
@@ -1807,16 +1806,16 @@
 By default, a 5-line header is printed at each page: two blank lines;
 a line with the date, the filename, and the page count; and two more
 blank lines.  A footer of five blank lines is also printed.
-With the @option{-F}
+With the @code{-F}
 option, a 3-line header is printed: the leading two blank lines are
 omitted; no footer is used.  The default @var{page_length} in both cases is 66
-lines.  The default number of text lines changes from 56 (without @option{-F})
-to 63 (with @option{-F}).  The text line of the header takes the form
+lines.  The default number of text lines changes from 56 (without @code{-F})
+to 63 (with @code{-F}).  The text line of the header takes the form
 @samp{@var{date} @var{string} @var{page}}, with spaces inserted around
 @var{string} so that the line takes up the full @var{page_width}.  Here,
-@var{date} is the date (see the @option{-D} or @option{--date-format}
+@var{date} is the date (see the @code{-D} or @code{--date-format}
 option for details), @var{string} is the centered header string, and
-@var{page} identifies the page number.  The @env{LC_MESSAGES} locale
+@var{page} identifies the page number.  The @var{LC_MESSAGES} locale
 category affects the spelling of @var{page}; in the default C locale, it
 is @samp{Page @var{number}} where @var{number} is the decimal page
 number.
@@ -1826,13 +1825,13 @@
 
 Columns are of equal width, separated by an optional string (default
 is @samp{space}).  For multicolumn output, lines will always be truncated to
-@var{page_width} (default 72), unless you use the @option{-J} option.
+@var{page_width} (default 72), unless you use the @code{-J} option.
 For single
-column output no line truncation occurs by default.  Use @option{-W} option to
+column output no line truncation occurs by default.  Use @code{-W} option to
 truncate lines in that case.
 
 The following changes were made in version 1.22i and apply to later
-versions of @command{pr}:
+versions of @file{pr}:
 @c FIXME: this whole section here sounds very awkward to me. I
 @c made a few small changes, but really it all needs to be redone. - Brian
 @c OK, I fixed another sentence or two, but some of it I just don't understand.
@@ -1840,25 +1839,25 @@
 @itemize @bullet
 
 @item
-Some small @var{letter options} (@option{-s}, @option{-w}) have been
-redefined for better @acronym{POSIX} compliance.  The output of some further
+Some small @var{letter options} (@code{-s}, @code{-w}) have been
+redefined for better @b{POSIX} compliance.  The output of some further
 cases has been adapted to other Unix systems.  These changes are not
 compatible with earlier versions of the program.
 
 @item
-Some @var{new capital letter} options (@option{-J}, @option{-S}, @option{-W})
+Some @var{new capital letter} options (@code{-J}, @code{-S}, @code{-W})
 have been introduced to turn off unexpected interferences of small letter
-options.  The @option{-N} option and the second argument @var{last_page}
+options.  The @code{-N} option and the second argument @var{last_page}
 of @samp{+FIRST_PAGE} offer more flexibility.  The detailed handling of
-form feeds set in the input files requires the @option{-T} option.
+form feeds set in the input files requires the @code{-T} option.
 
 @item
 Capital letter options override small letter ones.
 
 @item
-Some of the option-arguments (compare @option{-s}, @option{-e},
-@option{-i}, @option{-n}) cannot be specified as separate arguments from the
-preceding option letter (already stated in the @acronym{POSIX} specification).
+Some of the option-arguments (compare @code{-s}, @code{-e},
+@code{-i}, @code{-n}) cannot be specified as separate arguments from the
+preceding option letter (already stated in the @b{POSIX} specification).
 @end itemize
 
 The program accepts the following options.  Also see @ref{Common options}.
@@ -1879,7 +1878,7 @@
 the number of skipped pages each form feed in the input file results
 in a new page.  Page counting with and without @samp{+@var{first_page}}
 is identical.  By default, counting starts with the first page of input
-file (not first page printed).  Line numbering may be altered by @option{-N}
+file (not first page printed).  Line numbering may be altered by @code{-N}
 option.
 
 @item -@var{column}
@@ -1888,16 +1887,16 @@
 @opindex --columns
 @cindex down columns
 With each single @var{file}, produce @var{column} columns of output
-(default is 1) and print columns down, unless @option{-a} is used.  The
+(default is 1) and print columns down, unless @code{-a} is used.  The
 column width is automatically decreased as @var{column} increases; unless
-you use the @option{-W/-w} option to increase @var{page_width} as well.
+you use the @code{-W/-w} option to increase @var{page_width} as well.
 This option might well cause some lines to be truncated.  The number of
-lines in the columns on each page are balanced.  The options @option{-e}
-and @option{-i} are on for multiple text-column output.  Together with
-@option{-J} option column alignment and line truncation is turned off.
-Lines of full length are joined in a free field format and @option{-S}
-option may set field separators.  @option{-@var{column}} may not be used
-with @option{-m} option.
+lines in the columns on each page are balanced.  The options @code{-e}
+and @code{-i} are on for multiple text-column output.  Together with
+@code{-J} option column alignment and line truncation is turned off.
+Lines of full length are joined in a free field format and @code{-S}
+option may set field separators.  @code{-@var{column}} may not be used
+with @code{-m} option.
 
 @item -a
 @itemx --across
@@ -1905,7 +1904,7 @@
 @opindex --across
 @cindex across columns
 With each single @var{file}, print columns across rather than down.  The
-@option{-@var{column}} option must be given with @var{column} greater than one.
+@code{-@var{column}} option must be given with @var{column} greater than one.
 If a line is too long to fit in a column, it is truncated.
 
 @item -c
@@ -1932,13 +1931,13 @@
 Except for directives, which start with
 @samp{%}, characters in @var{format} are printed unchanged.  You can use
 this option to specify an arbitrary string in place of the header date,
-e.g., @option{--date-format="Monday morning"}.
+e.g., @code{--date-format="Monday morning"}.
 
 @vindex POSIXLY_CORRECT
 @vindex LC_TIME
-If the @env{POSIXLY_CORRECT} environment variable is not set, the date
+If the @var{POSIXLY_CORRECT} environment variable is not set, the date
 format defaults to @samp{%Y-%m-%d %H:%M} (for example, @samp{2001-12-04
-23:59}); otherwise, the format depends on the @env{LC_TIME} locale
+23:59}); otherwise, the format depends on the @var{LC_TIME} locale
 category, with the default being @samp{%b %e %H:%M %Y} (for example,
 @samp{Dec@ @ 4 23:59 2001}.
 
@@ -1968,7 +1967,7 @@
 @opindex --header
 Replace the filename in the header with the centered string @var{header}.
 When using the shell, @var{header} should be quoted and should be
-separated from @option{-h} by a space.
+separated from @code{-h} by a space.
 
 @item -i[@var{out-tabchar}[@var{out-tabwidth}]]
 @itemx --output-tabs[=@var{out-tabchar}[@var{out-tabwidth}]]
@@ -1985,13 +1984,13 @@
 @opindex -J
 @opindex --join-lines
 Merge lines of full length.  Used together with the column options
-@option{-@var{column}}, @option{-a -@var{column}} or @option{-m}.  Turns off
-@option{-W/-w} line truncation;
+@code{-@var{column}}, @code{-a -@var{column}} or @code{-m}.  Turns off
+@code{-W/-w} line truncation;
 no column alignment used; may be used with
-@option{--sep-string[=@var{string}]}.  @option{-J} has been introduced
-(together with @option{-W} and @option{--sep-string})
-to disentangle the old (@acronym{POSIX}-compliant) options @option{-w} and
-@option{-s} along with the three column options.
+@code{--sep-string[=@var{string}]}.  @code{-J} has been introduced
+(together with @code{-W} and @code{--sep-string})
+to disentangle the old (@b{POSIX}-compliant) options @code{-w} and
+@code{-s} along with the three column options.
 
 
 @item -l @var{page_length}
@@ -2000,24 +1999,24 @@
 @opindex --length
 Set the page length to @var{page_length} (default 66) lines, including
 the lines of the header [and the footer].  If @var{page_length} is less
-than or equal to 10 (or <= 3 with @option{-F}), the header and footer are
+than or equal to 10 (or <= 3 with @code{-F}), the header and footer are
 omitted, and all form feeds set in input files are eliminated, as if
-the @option{-T} option had been given.
+the @code{-T} option had been given.
 
 @item -m
 @itemx --merge
 @opindex -m
 @opindex --merge
 Merge and print all @var{file}s in parallel, one in each column.  If a
-line is too long to fit in a column, it is truncated, unless the @option{-J}
-option is used.  @option{--sep-string[=@var{string}]} may be used.
+line is too long to fit in a column, it is truncated, unless the @code{-J}
+option is used.  @code{--sep-string[=@var{string}]} may be used.
 Empty pages in
 some @var{file}s (form feeds set) produce empty columns, still marked
 by @var{string}.  The result is a continuous line numbering and column
 marking throughout the whole merged file.  Completely empty merged pages
 show no separators or line numbers.  The default header becomes
 @samp{@var{date} @var{page}} with spaces inserted in the middle; this
-may be used with the @option{-h} or @option{--header} option to fill up
+may be used with the @code{-h} or @code{--header} option to fill up
 the middle blank part.
 
 @item -n[@var{number-separator}[@var{digits}]]
@@ -2026,18 +2025,18 @@
 @opindex --number-lines
 Provide @var{digits} digit line numbering (default for @var{digits} is
 5).  With multicolumn output the number occupies the first @var{digits}
-column positions of each text column or only each line of @option{-m}
+column positions of each text column or only each line of @code{-m}
 output.  With single column output the number precedes each line just as
-@option{-m} does.  Default counting of the line numbers starts with the
+@code{-m} does.  Default counting of the line numbers starts with the
 first line of the input file (not the first line printed, compare the
-@option{--page} option and @option{-N} option).
+@code{--page} option and @code{-N} option).
 Optional argument @var{number-separator} is the character appended to
 the line number to separate it from the text followed.  The default
 separator is the TAB character.  In a strict sense a TAB is always
 printed with single column output only.  The @var{TAB}-width varies
 with the @var{TAB}-position, e.g. with the left @var{margin} specified
-by @option{-o} option.  With multicolumn output priority is given to
-@samp{equal width of output columns} (a @acronym{POSIX} specification).
+by @code{-o} option.  With multicolumn output priority is given to
+@samp{equal width of output columns} (a @b{POSIX} specification).
 The @var{TAB}-width is fixed to the value of the first column and does
 not change with different values of left @var{margin}.  That means a
 fixed number of spaces is always printed in the place of the
@@ -2059,8 +2058,8 @@
 @cindex left margin
 Indent each line with a margin @var{margin} spaces wide (default is zero).
 The total page width is the size of the margin plus the @var{page_width}
-set with the @option{-W/-w} option.  A limited overflow may occur with
-numbered single column output (compare @option{-n} option).
+set with the @code{-W/-w} option.  A limited overflow may occur with
+numbered single column output (compare @code{-n} option).
 
 @item -r
 @itemx --no-file-warnings
@@ -2074,32 +2073,32 @@
 @opindex -s
 @opindex --separator
 Separate columns by a single character @var{char}.  The default for
-@var{char} is the TAB character without @option{-w} and @samp{no
-character} with @option{-w}.  Without @option{-s} the default separator
-@samp{space} is set.  @option{-s[char]} turns off line truncation of all
-three column options (@option{-COLUMN}|@option{-a -COLUMN}|@option{-m}) unless
-@option{-w} is set.  This is a @acronym{POSIX}-compliant formulation.
+@var{char} is the TAB character without @code{-w} and @samp{no
+character} with @code{-w}.  Without @code{-s} the default separator
+@samp{space} is set.  @code{-s[char]} turns off line truncation of all
+three column options (@code{-COLUMN}|@code{-a -COLUMN}|@code{-m}) unless
+@code{-w} is set.  This is a @b{POSIX}-compliant formulation.
 
 
 @item -S @var{string}
 @itemx --sep-string[=@var{string}]
 @opindex -S
 @opindex --sep-string
-Use @var{string} to separate output columns.  The @option{-S} option doesn't
-affect the @option{-W/-w} option, unlike the @option{-s} option which does.  It
+Use @var{string} to separate output columns.  The @code{-S} option doesn't
+affect the @code{-W/-w} option, unlike the @code{-s} option which does.  It
 does not affect line truncation or column alignment.
-Without @option{-S}, and with @option{-J}, @command{pr} uses the default output
+Without @code{-S}, and with @code{-J}, @file{pr} uses the default output
 separator, TAB.
-Without @option{-S} or @option{-J}, @command{pr} uses a @samp{space}
-(same as @option{-S"@w{ }"}).  With @option{-S@var{string}},
-@var{string} must be nonempty; @option{--sep-string} with no
-@var{string} is equivalent to @option{--sep-string=""}.
+Without @code{-S} or @code{-J}, @file{pr} uses a @samp{space}
+(same as @code{-S"@w{ }"}).  With @code{-S@var{string}},
+@var{string} must be nonempty; @code{--sep-string} with no
+@var{string} is equivalent to @code{--sep-string=""}.
 
-On older systems, @command{pr} instead supports an obsolete option
-@option{-S[@var{string}]}, where @var{string} is optional.  @acronym{POSIX}
+On older systems, @file{pr} instead supports an obsolete option
+@code{-S[@var{string}]}, where @var{string} is optional.  @b{POSIX}
 1003.1-2001 (@pxref{Standards conformance}) does not allow this older
 usage.  To specify an empty @var{string} portably, use
-@option{--sep-string}.
+@code{--sep-string}.
 
 @item -t
 @itemx --omit-header
@@ -2108,10 +2107,10 @@
 Do not print the usual header [and footer] on each page, and do not fill
 out the bottom of pages (with blank lines or a form feed).  No page
 structure is produced, but form feeds set in the input files are retained.
-The predefined pagination is not changed.  @option{-t} or @option{-T} may be
-useful together with other options; e.g.: @option{-t -e4}, expand TAB characters
+The predefined pagination is not changed.  @code{-t} or @code{-T} may be
+useful together with other options; e.g.: @code{-t -e4}, expand TAB characters
 in the input file to 4 spaces but don't make any other changes.  Use of
-@option{-t} overrides @option{-h}.
+@code{-t} overrides @code{-h}.
 
 @item -T
 @itemx --omit-pagination
@@ -2131,25 +2130,25 @@
 @opindex -w
 @opindex --width
 Set page width to @var{page_width} characters for multiple text-column
-output only (default for @var{page_width} is 72).  @option{-s[CHAR]} turns
+output only (default for @var{page_width} is 72).  @code{-s[CHAR]} turns
 off the default page width and any line truncation and column alignment.
 Lines of full length are merged, regardless of the column options
 set.  No @var{page_width} setting is possible with single column output.
-A @acronym{POSIX}-compliant formulation.
+A @b{POSIX}-compliant formulation.
 
 @item -W @var{page_width}
 @itemx --page_width=@var{page_width}
 @opindex -W
 @opindex --page_width
 Set the page width to @var{page_width} characters.  That's valid with and
-without a column option.  Text lines are truncated, unless @option{-J}
+without a column option.  Text lines are truncated, unless @code{-J}
 is used.  Together with one of the three column options
-(@option{-@var{column}}, @option{-a -@var{column}} or @option{-m}) column
-alignment is always used.  The separator options @option{-S} or @option{-s}
-don't affect the @option{-W} option.  Default is 72 characters.  Without
-@option{-W @var{page_width}} and without any of the column options NO line
+(@code{-@var{column}}, @code{-a -@var{column}} or @code{-m}) column
+alignment is always used.  The separator options @code{-S} or @code{-s}
+don't affect the @code{-W} option.  Default is 72 characters.  Without
+@code{-W @var{page_width}} and without any of the column options NO line
 truncation is used (defined to keep downward compatibility and to meet
-most frequent tasks).  That's equivalent to @option{-W 72 -J}.  The header
+most frequent tasks).  That's equivalent to @code{-W 72 -J}.  The header
 line is never truncated.
 
 @end table
@@ -2158,13 +2157,13 @@
 
 
 @node fold invocation
-@section @command{fold}: Wrap input lines to fit in specified width
+@section @file{fold}: Wrap input lines to fit in specified width
 
 @pindex fold
 @cindex wrapping long input lines
 @cindex folding long input lines
 
-@command{fold} writes each @var{file} (@option{-} means standard input), or
+@file{fold} writes each @var{file} (@code{-} means standard input), or
 standard input if none are given, to standard output, breaking long
 lines.  Synopsis:
 
@@ -2172,11 +2171,11 @@
 fold [@var{option}]@dots{} [@var{file}]@dots{}
 @end example
 
-By default, @command{fold} breaks lines wider than 80 columns.  The output
+By default, @file{fold} breaks lines wider than 80 columns.  The output
 is split into as many lines as necessary.
 
 @cindex screen columns
-@command{fold} counts screen columns by default; thus, a tab may count more
+@file{fold} counts screen columns by default; thus, a tab may count more
 than one column, backspace decreases the column count, and carriage
 return sets the column to zero.
 
@@ -2206,9 +2205,9 @@
 @opindex --width
 Use a maximum line length of @var{width} columns instead of 80.
 
-On older systems, @command{fold} supports an obsolete option
-@option{-@var{width}}.  @acronym{POSIX} 1003.1-2001 (@pxref{Standards
-conformance}) does not allow this; use @option{-w @var{width}}
+On older systems, @file{fold} supports an obsolete option
+@code{-@var{width}}.  @b{POSIX} 1003.1-2001 (@pxref{Standards
+conformance}) does not allow this; use @code{-w @var{width}}
 instead.
 
 @end table
@@ -2232,21 +2231,21 @@
 @end menu
 
 @node head invocation
-@section @command{head}: Output the first part of files
+@section @file{head}: Output the first part of files
 
 @pindex head
 @cindex initial part of files, outputting
 @cindex first part of files, outputting
 
-@command{head} prints the first part (10 lines by default) of each
+@file{head} prints the first part (10 lines by default) of each
 @var{file}; it reads from standard input if no files are given or
-when given a @var{file} of @option{-}.  Synopsis:
+when given a @var{file} of @code{-}.  Synopsis:
 
 @example
 head [@var{option}]@dots{} [@var{file}]@dots{}
 @end example
 
-If more than one @var{file} is specified, @command{head} prints a
+If more than one @var{file} is specified, @file{head} prints a
 one-line header consisting of
 @example
 ==> @var{file name} <==
@@ -2292,24 +2291,24 @@
 
 @end table
 
-On older systems, @command{head} supports an obsolete option
-@option{-@var{count}@var{options}}, which is recognized only if it is
+On older systems, @file{head} supports an obsolete option
+@code{-@var{count}@var{options}}, which is recognized only if it is
 specified first.  @var{count} is a decimal number optionally followed
-by a size letter (@samp{b}, @samp{k}, @samp{m}) as in @option{-c}, or
+by a size letter (@samp{b}, @samp{k}, @samp{m}) as in @code{-c}, or
 @samp{l} to mean count by lines, or other option letters (@samp{cqv}).
-@acronym{POSIX} 1003.1-2001 (@pxref{Standards conformance}) does not allow
-this; use @option{-c @var{count}} or @option{-n @var{count}} instead.
+@b{POSIX} 1003.1-2001 (@pxref{Standards conformance}) does not allow
+this; use @code{-c @var{count}} or @code{-n @var{count}} instead.
 
 @exitstatus
 
 
 @node tail invocation
-@section @command{tail}: Output the last part of files
+@section @file{tail}: Output the last part of files
 
 @pindex tail
 @cindex last part of files, outputting
 
-@command{tail} prints the last part (10 lines by default) of each
+@file{tail} prints the last part (10 lines by default) of each
 @var{file}; it reads from standard input if no files are given or
 when given a @var{file} of @samp{-}.  Synopsis:
 
@@ -2317,7 +2316,7 @@
 tail [@var{option}]@dots{} [@var{file}]@dots{}
 @end example
 
-If more than one @var{file} is specified, @command{tail} prints a
+If more than one @var{file} is specified, @file{tail} prints a
 one-line header consisting of
 @example
 ==> @var{file name} <==
@@ -2325,17 +2324,17 @@
 @noindent
 before the output for each @var{file}.
 
-@cindex BSD @command{tail}
-@sc{gnu} @command{tail} can output any amount of data (some other versions of
-@command{tail} cannot).  It also has no @option{-r} option (print in
+@cindex BSD @file{tail}
+@sc{gnu} @file{tail} can output any amount of data (some other versions of
+@file{tail} cannot).  It also has no @code{-r} option (print in
 reverse), since reversing a file is really a different job from printing
-the end of a file; BSD @command{tail} (which is the one with @option{-r}) can
+the end of a file; BSD @file{tail} (which is the one with @code{-r}) can
 only reverse files that are at most as large as its buffer, which is
 typically 32 KiB.  A more reliable and versatile way to reverse files is
-the @sc{gnu} @command{tac} command.
+the @sc{gnu} @file{tac} command.
 
 If any option-argument is a number @var{n} starting with a @samp{+},
-@command{tail} begins printing with the @var{n}th item from the start of
+@file{tail} begins printing with the @var{n}th item from the start of
 each file, instead of from the end.
 
 The program accepts the following options.  Also see @ref{Common options}.
@@ -2360,7 +2359,7 @@
 Loop forever trying to read more characters at the end of the file,
 presumably because the file is growing.  This option is ignored when
 reading from a pipe.
-If more than one file is given, @command{tail} prints a header whenever it
+If more than one file is given, @file{tail} prints a header whenever it
 gets output from a different file, to indicate which file that output is
 from.
 
@@ -2368,20 +2367,20 @@
 but that difference is noticeable only when a followed file is removed or
 renamed.
 If you'd like to continue to track the end of a growing file even after
-it has been unlinked, use @option{--follow=descriptor}.  This is the default
+it has been unlinked, use @code{--follow=descriptor}.  This is the default
 behavior, but it is not useful if you're tracking a log file that may be
 rotated (removed or renamed, then reopened).  In that case, use
-@option{--follow=name} to track the named file by reopening it periodically
+@code{--follow=name} to track the named file by reopening it periodically
 to see if it has been removed and recreated by some other program.
 
 No matter which method you use, if the tracked file is determined to have
-shrunk, @command{tail} prints a message saying the file has been truncated
+shrunk, @file{tail} prints a message saying the file has been truncated
 and resumes tracking the end of the file from the newly-determined endpoint.
 
-When a file is removed, @command{tail}'s behavior depends on whether it is
+When a file is removed, @file{tail}'s behavior depends on whether it is
 following the name or the descriptor.  When following by name, tail can
 detect that a file has been removed and gives a message to that effect,
-and if @option{--retry} has been specified it will continue checking
+and if @code{--retry} has been specified it will continue checking
 periodically to see if the file reappears.
 When following a descriptor, tail does not detect that the file has
 been unlinked or renamed and issues no message;  even though the file
@@ -2389,11 +2388,11 @@
 growing.
 
 The option values @samp{descriptor} and @samp{name} may be specified only
-with the long form of the option, not with @option{-f}.
+with the long form of the option, not with @code{-f}.
 
 @item -F
 @opindex -F
-This option is the same as @option{--follow=name --retry}.  That is, tail
+This option is the same as @code{--follow=name --retry}.  That is, tail
 will attempt to reopen a file when it is removed.  Should this fail, tail
 will keep trying until it becomes accessible again.
 
@@ -2409,8 +2408,8 @@
 Change the number of seconds to wait between iterations (the default is 1.0).
 During one iteration, every specified file is checked to see if it has
 changed size.
-Historical implementations of @command{tail} have required that
-@var{number} be an integer.  However, GNU @command{tail} accepts
+Historical implementations of @file{tail} have required that
+@var{number} be an integer.  However, GNU @file{tail} accepts
 an arbitrary floating point number (using a period before any
 fractional digits).
 
@@ -2421,7 +2420,7 @@
 after that process terminates, tail will also terminate.  This will
 work properly only if the writer and the tailing process are running on
 the same machine.  For example, to save the output of a build in a file
-and to watch the file grow, if you invoke @command{make} and @command{tail}
+and to watch the file grow, if you invoke @file{make} and @file{tail}
 like this then the tail process will stop when your build completes.
 Without this option, you would have had to kill the @code{tail -f}
 process yourself.
@@ -2429,10 +2428,10 @@
 $ make >& makerr & tail --pid=$! -f makerr
 @end example
 If you specify a @var{pid} that is not in use or that does not correspond
-to the process that is writing to the tailed files, then @command{tail}
+to the process that is writing to the tailed files, then @file{tail}
 may terminate long before any @var{file}s stop growing or it may not
 terminate until long after the real writer has terminated.
-Note that @option{--pid} cannot be supported on some systems; @command{tail}
+Note that @code{--pid} cannot be supported on some systems; @file{tail}
 will print a warning if this is the case.
 
 @itemx --max-unchanged-stats=@var{n}
@@ -2469,28 +2468,28 @@
 
 @end table
 
-On older systems, @command{tail} supports an obsolete option
-@option{-@var{count}@var{options}}, which is recognized only if it is
+On older systems, @file{tail} supports an obsolete option
+@code{-@var{count}@var{options}}, which is recognized only if it is
 specified first.  @var{count} is a decimal number optionally followed
-by a size letter (@samp{b}, @samp{k}, @samp{m}) as in @option{-c}, or
+by a size letter (@samp{b}, @samp{k}, @samp{m}) as in @code{-c}, or
 @samp{l} to mean count by lines, or other option letters
-(@samp{cfqv}).  Some older @command{tail} implementations also support
-an obsolete option @option{+@var{count}} with the same meaning as
-@option{-+@var{count}}.  @acronym{POSIX} 1003.1-2001 (@pxref{Standards
-conformance}) does not allow these options; use @option{-c
-@var{count}} or @option{-n @var{count}} instead.
+(@samp{cfqv}).  Some older @file{tail} implementations also support
+an obsolete option @code{+@var{count}} with the same meaning as
+@code{-+@var{count}}.  @b{POSIX} 1003.1-2001 (@pxref{Standards
+conformance}) does not allow these options; use @code{-c
+@var{count}} or @code{-n @var{count}} instead.
 
 @exitstatus
 
 
 @node split invocation
-@section @command{split}: Split a file into fixed-size pieces
+@section @file{split}: Split a file into fixed-size pieces
 
 @pindex split
 @cindex splitting a file into pieces
 @cindex pieces, splitting a file into
 
-@command{split} creates output files containing consecutive sections of
+@file{split} creates output files containing consecutive sections of
 @var{input} (standard input if none is given or @var{input} is
 @samp{-}).  Synopsis:
 
@@ -2498,7 +2497,7 @@
 split [@var{option}] [@var{input} [@var{prefix}]]
 @end example
 
-By default, @command{split} puts 1000 lines of @var{input} (or whatever is
+By default, @file{split} puts 1000 lines of @var{input} (or whatever is
 left over for the last section), into each output file.
 
 @cindex output file name prefix
@@ -2507,7 +2506,7 @@
 default), such that concatenating the output files in traditional
 sorted order by file name produces
 the original input file.  If the output file names are exhausted,
-@command{split} reports an error without deleting the output files
+@file{split} reports an error without deleting the output files
 that it did create.
 
 The program accepts the following options.  Also see @ref{Common options}.
@@ -2526,9 +2525,9 @@
 @opindex --lines
 Put @var{lines} lines of @var{input} into each output file.
 
-On older systems, @command{split} supports an obsolete option
-@option{-@var{lines}}.  @acronym{POSIX} 1003.1-2001 (@pxref{Standards
-conformance}) does not allow this; use @option{-l @var{lines}}
+On older systems, @file{split} supports an obsolete option
+@code{-@var{lines}}.  @b{POSIX} 1003.1-2001 (@pxref{Standards
+conformance}) does not allow this; use @code{-l @var{lines}}
 instead.
 
 @item -b @var{bytes}
@@ -2547,7 +2546,7 @@
 possible without exceeding @var{bytes} bytes.  For lines longer than
 @var{bytes} bytes, put @var{bytes} bytes into each output file until
 less than @var{bytes} bytes of the line are left, then continue
-normally.  @var{bytes} has the same format as for the @option{--bytes}
+normally.  @var{bytes} has the same format as for the @code{--bytes}
 option.
 
 @item -d
@@ -2566,13 +2565,13 @@
 
 
 @node csplit invocation
-@section @command{csplit}: Split a file into context-determined pieces
+@section @file{csplit}: Split a file into context-determined pieces
 
 @pindex csplit
 @cindex context splitting
 @cindex splitting a file into pieces by context
 
-@command{csplit} creates zero or more output files containing sections of
+@file{csplit} creates zero or more output files containing sections of
 @var{input} (standard input if @var{input} is @samp{-}).  Synopsis:
 
 @example
@@ -2586,7 +2585,7 @@
 @var{pattern} has been matched, any remaining input is copied into one
 last output file.
 
-By default, @command{csplit} prints the number of bytes written to each
+By default, @file{csplit} prints the number of bytes written to each
 output file after it has been created.
 
 The types of pattern arguments are:
@@ -2625,7 +2624,7 @@
 concatenating the output files in sorted order by filename produces the
 original input file.
 
-By default, if @command{csplit} encounters an error or receives a hangup,
+By default, if @file{csplit} encounters an error or receives a hangup,
 interrupt, quit, or terminate signal, it removes any output files
 that it has created so far before it exits.
 
@@ -2655,7 +2654,7 @@
 entire @var{suffix} is given (with the current output file number) to
 @code{sprintf(3)} to form the file name suffixes for each of the
 individual output files in turn.  If this option is used, the
-@option{--digits} option is ignored.
+@code{--digits} option is ignored.
 
 @item -n @var{digits}
 @itemx --digits=@var{digits}
@@ -2713,7 +2712,7 @@
 
 
 @node wc invocation
-@section @command{wc}: Print newline, word, and byte counts
+@section @file{wc}: Print newline, word, and byte counts
 
 @pindex wc
 @cindex byte count
@@ -2721,7 +2720,7 @@
 @cindex word count
 @cindex line count
 
-@command{wc} counts the number of bytes, characters, whitespace-separated
+@file{wc} counts the number of bytes, characters, whitespace-separated
 words, and newlines in each given @var{file}, or standard input if none
 are given or for a @var{file} of @samp{-}.  Synopsis:
 
@@ -2730,19 +2729,19 @@
 @end example
 
 @cindex total counts
-@command{wc} prints one line of counts for each file, and if the file was
+@file{wc} prints one line of counts for each file, and if the file was
 given as an argument, it prints the file name following the counts.  If
-more than one @var{file} is given, @command{wc} prints a final line
+more than one @var{file} is given, @file{wc} prints a final line
 containing the cumulative counts, with the file name @file{total}.  The
 counts are printed in this order: newlines, words, characters, bytes.
 Each count is printed right-justified in a field with at least one
 space between fields so that the numbers and file names normally line
 up nicely in columns.  The width of the count fields varies depending
 on the inputs, so you should not depend on a particular field width.
-However, as a @acronym{GNU} extension, if only one count is printed,
+However, as a @b{GNU} extension, if only one count is printed,
 it is guaranteed to be printed without leading spaces.
 
-By default, @command{wc} prints three counts: the newline, words, and byte
+By default, @file{wc} prints three counts: the newline, words, and byte
 counts.  Options can specify that only certain counts be printed.
 Options do not undo others previously given, so
 
@@ -2753,7 +2752,7 @@
 @noindent
 prints both the byte counts and the word counts.
 
-With the @option{--max-line-length} option, @command{wc} prints the length
+With the @code{--max-line-length} option, @file{wc} prints the length
 of the longest line per file, and if there is more than one file it
 prints the maximum (not the sum) of those lengths.
 
@@ -2797,27 +2796,27 @@
 
 
 @node sum invocation
-@section @command{sum}: Print checksum and block counts
+@section @file{sum}: Print checksum and block counts
 
 @pindex sum
 @cindex 16-bit checksum
 @cindex checksum, 16-bit
 
-@command{sum} computes a 16-bit checksum for each given @var{file}, or
+@file{sum} computes a 16-bit checksum for each given @var{file}, or
 standard input if none are given or for a @var{file} of @samp{-}.  Synopsis:
 
 @example
 sum [@var{option}]@dots{} [@var{file}]@dots{}
 @end example
 
-@command{sum} prints the checksum for each @var{file} followed by the
+@file{sum} prints the checksum for each @var{file} followed by the
 number of blocks in the file (rounded up).  If more than one @var{file}
 is given, file names are also printed (by default).  (With the
-@option{--sysv} option, corresponding file names are printed when there is
+@code{--sysv} option, corresponding file names are printed when there is
 at least one file argument.)
 
-By default, @sc{gnu} @command{sum} computes checksums using an algorithm
-compatible with BSD @command{sum} and prints file sizes in units of
+By default, @sc{gnu} @file{sum} computes checksums using an algorithm
+compatible with BSD @file{sum} and prints file sizes in units of
 1024-byte blocks.
 
 The program accepts the following options.  Also see @ref{Common options}.
@@ -2826,35 +2825,35 @@
 
 @item -r
 @opindex -r
-@cindex BSD @command{sum}
+@cindex BSD @file{sum}
 Use the default (BSD compatible) algorithm.  This option is included for
-compatibility with the System V @command{sum}.  Unless @option{-s} was also
+compatibility with the System V @file{sum}.  Unless @code{-s} was also
 given, it has no effect.
 
 @item -s
 @itemx --sysv
 @opindex -s
 @opindex --sysv
-@cindex System V @command{sum}
+@cindex System V @file{sum}
 Compute checksums using an algorithm compatible with System V
-@command{sum}'s default, and print file sizes in units of 512-byte blocks.
+@file{sum}'s default, and print file sizes in units of 512-byte blocks.
 
 @end table
 
-@command{sum} is provided for compatibility; the @command{cksum} program (see
+@file{sum} is provided for compatibility; the @file{cksum} program (see
 next section) is preferable in new applications.
 
 @exitstatus
 
 
 @node cksum invocation
-@section @command{cksum}: Print CRC checksum and byte counts
+@section @file{cksum}: Print CRC checksum and byte counts
 
 @pindex cksum
 @cindex cyclic redundancy check
 @cindex CRC checksum
 
-@command{cksum} computes a cyclic redundancy check (CRC) checksum for each
+@file{cksum} computes a cyclic redundancy check (CRC) checksum for each
 given @var{file}, or standard input if none are given or for a
 @var{file} of @samp{-}.  Synopsis:
 
@@ -2862,27 +2861,27 @@
 cksum [@var{option}]@dots{} [@var{file}]@dots{}
 @end example
 
-@command{cksum} prints the CRC checksum for each file along with the number
+@file{cksum} prints the CRC checksum for each file along with the number
 of bytes in the file, and the filename unless no arguments were given.
 
-@command{cksum} is typically used to ensure that files
+@file{cksum} is typically used to ensure that files
 transferred by unreliable means (e.g., netnews) have not been corrupted,
-by comparing the @command{cksum} output for the received files with the
-@command{cksum} output for the original files (typically given in the
+by comparing the @file{cksum} output for the received files with the
+@file{cksum} output for the original files (typically given in the
 distribution).
 
-The CRC algorithm is specified by the @acronym{POSIX} standard.  It is not
-compatible with the BSD or System V @command{sum} algorithms (see the
+The CRC algorithm is specified by the @b{POSIX} standard.  It is not
+compatible with the BSD or System V @file{sum} algorithms (see the
 previous section); it is more robust.
 
-The only options are @option{--help} and @option{--version}.  @xref{Common
+The only options are @code{--help} and @code{--version}.  @xref{Common
 options}.
 
 @exitstatus
 
 
 @node md5sum invocation
-@section @command{md5sum}: Print or check message-digests
+@section @file{md5sum}: Print or check message-digests
 
 @pindex md5sum
 @cindex 128-bit checksum
@@ -2890,11 +2889,11 @@
 @cindex fingerprint, 128-bit
 @cindex message-digest, 128-bit
 
-@command{md5sum} computes a 128-bit checksum (or @dfn{fingerprint} or
+@file{md5sum} computes a 128-bit checksum (or @dfn{fingerprint} or
 @dfn{message-digest}) for each specified @var{file}.
 If a @var{file} is specified as @samp{-} or if no files are given
-@command{md5sum} computes the checksum for the standard input.
-@command{md5sum} can also determine whether a file and checksum are
+@file{md5sum} computes the checksum for the standard input.
+@file{md5sum} can also determine whether a file and checksum are
 consistent. Synopses:
 
 @example
@@ -2926,12 +2925,12 @@
 Read filenames and checksum information from the single @var{file}
 (or from stdin if no @var{file} was specified) and report whether
 each named file and the corresponding checksum data are consistent.
-The input to this mode of @command{md5sum} is usually the output of
+The input to this mode of @file{md5sum} is usually the output of
 a prior, checksum-generating run of @samp{md5sum}.
 Each valid line of input consists of an MD5 checksum, a binary/text
 flag, and then a filename.
 Binary files are marked with @samp{*}, text with @samp{ }.
-For each such line, @command{md5sum} reads the named file and computes its
+For each such line, @file{md5sum} reads the named file and computes its
 MD5 checksum.  Then, if the computed message digest does not match the
 one on the line with the filename, the file is noted as having
 failed the test.  Otherwise, the file passes the test.
@@ -2939,10 +2938,10 @@
 output indicating whether the named file passed the test.
 After all checks have been performed, if there were any failures,
 a warning is issued to standard error.
-Use the @option{--status} option to inhibit that output.
+Use the @code{--status} option to inhibit that output.
 If any listed file cannot be opened or read, if any valid line has
 an MD5 checksum inconsistent with the associated file, or if no valid
-line is found, @command{md5sum} exits with nonzero status.  Otherwise,
+line is found, @file{md5sum} exits with nonzero status.  Otherwise,
 it exits successfully.
 
 @itemx --status
@@ -2963,7 +2962,7 @@
 @opindex --text
 @cindex text input files
 Treat all input files as text files.  This is the reverse of
-@option{--binary}.
+@code{--binary}.
 
 @item -w
 @itemx --warn
@@ -2998,21 +2997,21 @@
 
 
 @node sort invocation
-@section @command{sort}: Sort text files
+@section @file{sort}: Sort text files
 
 @pindex sort
 @cindex sorting files
 
-@command{sort} sorts, merges, or compares all the lines from the given
+@file{sort} sorts, merges, or compares all the lines from the given
 files, or standard input if none are given or for a @var{file} of
-@samp{-}.  By default, @command{sort} writes the results to standard
+@samp{-}.  By default, @file{sort} writes the results to standard
 output.  Synopsis:
 
 @example
 sort [@var{option}]@dots{} [@var{file}]@dots{}
 @end example
 
-@command{sort} has three modes of operation: sort (the default), merge,
+@file{sort} has three modes of operation: sort (the default), merge,
 and check for sortedness.  The following options change the operation
 mode:
 
@@ -3042,52 +3041,52 @@
 @vindex LC_ALL
 @vindex LC_COLLATE
 A pair of lines is compared as follows: if any key fields have
-been specified, @command{sort} compares each pair of fields, in the
+been specified, @file{sort} compares each pair of fields, in the
 order specified on the command line, according to the associated
 ordering options, until a difference is found or no fields are left.
 Unless otherwise specified, all comparisons use the character collating
-sequence specified by the @env{LC_COLLATE} locale.  @footnote{If you
-use a non-@acronym{POSIX} locale (e.g., by setting @env{LC_ALL}
-to @samp{en_US}), then @command{sort} may produce output that is sorted
-differently than you're accustomed to.  In that case, set the @env{LC_ALL}
-environment variable to @samp{C}.  Note that setting only @env{LC_COLLATE}
-has two problems.  First, it is ineffective if @env{LC_ALL} is also set.
-Second, it has undefined behavior if @env{LC_CTYPE} (or @env{LANG}, if
-@env{LC_CTYPE} is unset) is set to an incompatible value.  For example,
-you get undefined behavior if @env{LC_CTYPE} is @code{ja_JP.PCK} but
-@env{LC_COLLATE} is @code{en_US.UTF-8}. }
+sequence specified by the @var{LC_COLLATE} locale.  @footnote{If you
+use a non-@b{POSIX} locale (e.g., by setting @var{LC_ALL}
+to @samp{en_US}), then @file{sort} may produce output that is sorted
+differently than you're accustomed to.  In that case, set the @var{LC_ALL}
+environment variable to @samp{C}.  Note that setting only @var{LC_COLLATE}
+has two problems.  First, it is ineffective if @var{LC_ALL} is also set.
+Second, it has undefined behavior if @var{LC_CTYPE} (or @var{LANG}, if
+@var{LC_CTYPE} is unset) is set to an incompatible value.  For example,
+you get undefined behavior if @var{LC_CTYPE} is @code{ja_JP.PCK} but
+@var{LC_COLLATE} is @code{en_US.UTF-8}. }
 
 If any of the global options @samp{bdfgiMnr} are given but no key fields
-are specified, @command{sort} compares the entire lines according to the
+are specified, @file{sort} compares the entire lines according to the
 global options.
 
 Finally, as a last resort when all keys compare equal (or if no ordering
-options were specified at all), @command{sort} compares the entire lines.
-The last resort comparison honors the @option{--reverse} (@option{-r})
-global option.  The @option{--stable} (@option{-s}) option disables this
+options were specified at all), @file{sort} compares the entire lines.
+The last resort comparison honors the @code{--reverse} (@code{-r})
+global option.  The @code{--stable} (@code{-s}) option disables this
 last-resort comparison so that lines in which all fields compare equal
 are left in their original relative order.  If no fields or global
-options are specified, @option{--stable} (@option{-s}) has no effect.
+options are specified, @code{--stable} (@code{-s}) has no effect.
 
-@sc{gnu} @command{sort} (as specified for all @sc{gnu} utilities) has no
+@sc{gnu} @file{sort} (as specified for all @sc{gnu} utilities) has no
 limit on input line length or restrictions on bytes allowed within lines.
 In addition, if the final byte of an input file is not a newline, @sc{gnu}
-@command{sort} silently supplies one.  A line's trailing newline is not
+@file{sort} silently supplies one.  A line's trailing newline is not
 part of the line for comparison purposes.
 
-@cindex exit status of @command{sort}
+@cindex exit status of @file{sort}
 Exit status:
 
 @display
 0 if no error occurred
-1 if invoked with @option{-c} and the input is not properly sorted
+1 if invoked with @code{-c} and the input is not properly sorted
 2 if an error occurred
 @end display
 
 @vindex TMPDIR
-If the environment variable @env{TMPDIR} is set, @command{sort} uses its
+If the environment variable @var{TMPDIR} is set, @file{sort} uses its
 value as the directory for temporary files instead of @file{/tmp}.  The
-@option{--temporary-directory} (@option{-T}) option in turn overrides
+@code{--temporary-directory} (@code{-T}) option in turn overrides
 the environment variable.
 
 
@@ -3095,8 +3094,8 @@
 specified globally or as part of a specific key field.  If no key
 fields are specified, global options apply to comparison of entire
 lines; otherwise the global options are inherited by key fields that do
-not specify any special options of their own.  In pre-@acronym{POSIX}
-versions of @command{sort}, global options affect only later key fields,
+not specify any special options of their own.  In pre-@b{POSIX}
+versions of @file{sort}, global options affect only later key fields,
 so portable shell scripts should specify global options first.
 
 @table @samp
@@ -3108,7 +3107,7 @@
 @cindex blanks, ignoring leading
 @vindex LC_CTYPE
 Ignore leading blanks when finding sort keys in each line.
-The @env{LC_CTYPE} locale determines character types.
+The @var{LC_CTYPE} locale determines character types.
 
 @item -d
 @itemx --dictionary-order
@@ -3120,7 +3119,7 @@
 @vindex LC_CTYPE
 Sort in @dfn{phone directory} order: ignore all characters except
 letters, digits and blanks when sorting.
-The @env{LC_CTYPE} locale determines character types.
+The @var{LC_CTYPE} locale determines character types.
 
 @item -f
 @itemx --ignore-case
@@ -3131,7 +3130,7 @@
 @vindex LC_CTYPE
 Fold lowercase characters into the equivalent uppercase characters when
 comparing so that, for example, @samp{b} and @samp{B} sort as equal.
-The @env{LC_CTYPE} locale determines character types.
+The @var{LC_CTYPE} locale determines character types.
 
 @item -g
 @itemx --general-numeric-sort
@@ -3143,7 +3142,7 @@
 a prefix of each line to a double-precision floating point number.
 This allows floating point numbers to be specified in scientific notation,
 like @code{1.0e-34} and @code{10e100}.
-The @env{LC_NUMERIC} locale determines the decimal-point character.
+The @var{LC_NUMERIC} locale determines the decimal-point character.
 Do not report overflow, underflow, or conversion errors.
 Use the following collating sequence:
 
@@ -3162,7 +3161,7 @@
 @end itemize
 
 Use this option only if there is no alternative; it is much slower than
-@option{--numeric-sort} (@option{-n}) and it can lose information when
+@code{--numeric-sort} (@code{-n}) and it can lose information when
 converting to floating point.
 
 @item -i
@@ -3173,9 +3172,9 @@
 @cindex unprintable characters, ignoring
 @vindex LC_CTYPE
 Ignore nonprinting characters.
-The @env{LC_CTYPE} locale determines character types.
-This option has no effect if the stronger @option{--dictionary-order}
-(@option{-d}) option is also given.
+The @var{LC_CTYPE} locale determines character types.
+This option has no effect if the stronger @code{--dictionary-order}
+(@code{-d}) option is also given.
 
 @item -M
 @itemx --month-sort
@@ -3186,7 +3185,7 @@
 An initial string, consisting of any amount of blanks, followed
 by a month name abbreviation, is folded to UPPER case and
 compared in the order @samp{JAN} < @samp{FEB} < @dots{} < @samp{DEC}.
-Invalid names compare low to valid names.  The @env{LC_TIME} locale
+Invalid names compare low to valid names.  The @var{LC_TIME} locale
 category determines the month spellings.
 
 @item -n
@@ -3198,13 +3197,13 @@
 Sort numerically: the number begins each line; specifically, it consists
 of optional blanks, an optional @samp{-} sign, and zero or more
 digits possibly separated by thousands separators, optionally followed
-by a decimal-point character and zero or more digits.  The @env{LC_NUMERIC}
+by a decimal-point character and zero or more digits.  The @var{LC_NUMERIC}
 locale specifies the decimal-point character and thousands separator.
 
 Numeric sort uses what might be considered an unconventional method to
 compare strings representing floating point numbers.  Rather than first
 converting each string to the C @code{double} type and then comparing
-those values, @command{sort} aligns the decimal-point characters in the
+those values, @file{sort} aligns the decimal-point characters in the
 two strings and compares the strings a character at a time.  One benefit
 of using this approach is its speed.  In practice this is much more
 efficient than performing the two corresponding string-to-double (or
@@ -3215,7 +3214,7 @@
 
 Neither a leading @samp{+} nor exponential notation is recognized.
 To compare such strings numerically, use the
-@option{--general-numeric-sort} (@option{-g}) option.
+@code{--general-numeric-sort} (@code{-g}) option.
 
 @item -r
 @itemx --reverse
@@ -3237,14 +3236,14 @@
 @opindex --output
 @cindex overwriting of input, allowed
 Write output to @var{output-file} instead of standard output.
-If necessary, @command{sort} reads input before opening
+If necessary, @file{sort} reads input before opening
 @var{output-file}, so you can safely sort a file in place by using
 commands like @code{sort -o F F} and @code{cat F | sort -o F}.
 
 @vindex POSIXLY_CORRECT
-On newer systems, @option{-o} cannot appear after an input file if
-@env{POSIXLY_CORRECT} is set, e.g., @samp{sort F -o F}.  Portable
-scripts should specify @option{-o @var{output-file}} before any input
+On newer systems, @code{-o} cannot appear after an input file if
+@var{POSIXLY_CORRECT} is set, e.g., @samp{sort F -o F}.  Portable
+scripts should specify @code{-o @var{output-file}} before any input
 files.
 
 @item -s
@@ -3254,16 +3253,16 @@
 @cindex sort stability
 @cindex disabling sort's last-resort comparison
 
-Make @command{sort} stable by disabling the last-resort
+Make @file{sort} stable by disabling the last-resort
 comparison that is performed in some cases.
 By default, when lines compare equal based on command line options
 that affect ordering, those lines are ordered using
 a @dfn{last-resort comparison} that takes the entire
 line as the key and acts as if no ordering options were specified.
-But if @option{--reverse} (@option{-r}) was specified along with other
-ordering options, then the last-resort comparison does use @option{--reverse}.
+But if @code{--reverse} (@code{-r}) was specified along with other
+ordering options, then the last-resort comparison does use @code{--reverse}.
 In any case, when no ordering option is specified or when only
-@option{--reverse} is specified, the last-resort comparison is not performed
+@code{--reverse} is specified, the last-resort comparison is not performed
 
 @item -S @var{size}
 @itemx --buffer-size=@var{size}
@@ -3279,10 +3278,10 @@
 @samp{b} causes @var{size} to be interpreted as a byte count, with no
 multiplication.
 
-This option can improve the performance of @command{sort} by causing it
+This option can improve the performance of @file{sort} by causing it
 to start with a larger or smaller sort buffer than the default.
 However, this option affects only the initial buffer size.  The buffer
-grows beyond @var{size} if @command{sort} encounters input lines larger
+grows beyond @var{size} if @file{sort} encounters input lines larger
 than @var{size}.
 
 @item -t @var{separator}
@@ -3293,14 +3292,14 @@
 Use character @var{separator} as the field separator when finding the
 sort keys in each line.  By default, fields are separated by the empty
 string between a non-blank character and a blank character.
-That is, given the input line @w{@samp{ foo bar}}, @command{sort} breaks it
+That is, given the input line @w{@samp{ foo bar}}, @file{sort} breaks it
 into fields @w{@samp{ foo}} and @w{@samp{ bar}}.  The field separator is
 not considered to be part of either the field preceding or the field
 following.  But note that sort fields that extend to the end of the line,
-as @option{-k 2}, or sort fields consisting of a range, as @option{-k 2,3},
+as @code{-k 2}, or sort fields consisting of a range, as @code{-k 2,3},
 retain the field separators present between the endpoints of the range.
 
-To specify a zero byte (@acronym{ASCII} @sc{nul} (Null) character) as
+To specify a zero byte (@b{ASCII} @sc{nul} (Null) character) as
 the field separator, use the two-character string @samp{\0}, e.g.,
 @samp{sort -t '\0'}.
 
@@ -3311,7 +3310,7 @@
 @cindex temporary directory
 @vindex TMPDIR
 Use directory @var{tempdir} to store temporary files, overriding the
-@env{TMPDIR} environment variable.  If this option is given more than
+@var{TMPDIR} environment variable.  If this option is given more than
 once, temporary files are stored in all the directories given.  If you
 have a large sort or merge that is I/O-bound, you can often improve
 performance by using this option to specify directories on different
@@ -3324,7 +3323,7 @@
 @cindex uniquifying output
 
 Normally, output only the first of a sequence of lines that compare
-equal.  For the @option{--check} (@option{-c}) option,
+equal.  For the @code{--check} (@code{-c}) option,
 check that no pair of consecutive lines compares equal.
 
 @item -k @var{pos1}[,@var{pos2}]
@@ -3336,7 +3335,7 @@
 @var{pos1} and @var{pos2} (or the end of the line, if @var{pos2} is
 omitted), @emph{inclusive}.  Fields and character positions are numbered
 starting with 1.  So to sort on the second field, you'd use
-@option{--key=2,2} (@option{-k 2,2}).  See below for more examples.
+@code{--key=2,2} (@code{-k 2,2}).  See below for more examples.
 
 @item -z
 @itemx --zero-terminated
@@ -3344,8 +3343,8 @@
 @opindex --zero-terminated
 @cindex sort zero-terminated lines
 Treat the input as a set of lines, each terminated by a zero byte
-(@acronym{ASCII} @sc{nul} (Null) character) instead of an
-@acronym{ASCII} @sc{lf} (Line Feed).
+(@b{ASCII} @sc{nul} (Null) character) instead of an
+@b{ASCII} @sc{lf} (Line Feed).
 This option can be useful in conjunction with @samp{perl -0} or
 @samp{find -print0} and @samp{xargs -0} which do the same in order to
 reliably handle arbitrary pathnames (even those which contain Line Feed
@@ -3353,37 +3352,37 @@
 
 @end table
 
-Historical (BSD and System V) implementations of @command{sort} have
+Historical (BSD and System V) implementations of @file{sort} have
 differed in their interpretation of some options, particularly
-@option{-b}, @option{-f}, and @option{-n}.  @sc{gnu} sort follows the @acronym{POSIX}
+@code{-b}, @code{-f}, and @code{-n}.  @sc{gnu} sort follows the @b{POSIX}
 behavior, which is usually (but not always!) like the System V behavior.
-According to @acronym{POSIX}, @option{-n} no longer implies @option{-b}.  For
-consistency, @option{-M} has been changed in the same way.  This may
+According to @b{POSIX}, @code{-n} no longer implies @code{-b}.  For
+consistency, @code{-M} has been changed in the same way.  This may
 affect the meaning of character positions in field specifications in
-obscure cases.  The only fix is to add an explicit @option{-b}.
+obscure cases.  The only fix is to add an explicit @code{-b}.
 
-A position in a sort field specified with the @option{-k}
+A position in a sort field specified with the @code{-k}
 option has the form @samp{@var{f}.@var{c}}, where @var{f} is the number
 of the field to use and @var{c} is the number of the first character
 from the beginning of the field.  In a start position, an omitted
 @samp{.@var{c}} stands for the field's first character.  In an end
 position, an omitted or zero @samp{.@var{c}} stands for the field's
 last character.  If the
-@option{-b} option was specified, the @samp{.@var{c}} part of a field
+@code{-b} option was specified, the @samp{.@var{c}} part of a field
 specification is counted from the first nonblank character of the field.
 
 A sort key position may also have any of the option letters @samp{Mbdfinr}
 appended to it, in which case the global ordering options are not used
-for that particular field.  The @option{-b} option may be independently
+for that particular field.  The @code{-b} option may be independently
 attached to either or both of the start and
 end positions of a field specification, and if it is inherited
 from the global options it will be attached to both.
 Keys may span multiple fields.
 
-On older systems, @command{sort} supports an obsolete origin-zero
+On older systems, @file{sort} supports an obsolete origin-zero
 syntax @samp{+@var{pos1} [-@var{pos2}]} for specifying sort keys.
-@acronym{POSIX} 1003.1-2001 (@pxref{Standards conformance}) does not allow
-this; use @option{-k} instead.
+@b{POSIX} 1003.1-2001 (@pxref{Standards conformance}) does not allow
+this; use @code{-k} instead.
 
 Here are some examples to illustrate various combinations of options.
 
@@ -3414,15 +3413,15 @@
 sort -t : -k 2,2n -k 5.3,5.4
 @end example
 
-Note that if you had written @option{-k 2} instead of @option{-k 2,2}
-@command{sort} would have used all characters beginning in the second field
+Note that if you had written @code{-k 2} instead of @code{-k 2,2}
+@file{sort} would have used all characters beginning in the second field
 and extending to the end of the line as the primary @emph{numeric}
 key.  For the large majority of applications, treating keys spanning
 more than one field as numeric will not do what you expect.
 
 Also note that the @samp{n} modifier was applied to the field-end
 specifier for the first key.  It would have been equivalent to
-specify @option{-k 2n,2} or @option{-k 2n,2n}.  All modifiers except
+specify @code{-k 2n,2} or @code{-k 2n,2n}.  All modifiers except
 @samp{b} apply to the associated @emph{field}, regardless of whether
 the modifier character is attached to the field-start and/or the
 field-end part of the key specifier.
@@ -3436,7 +3435,7 @@
 sort -t : -k 5b,5 -k 3,3n /etc/passwd
 @end example
 
-An alternative is to use the global numeric modifier @option{-n}.
+An alternative is to use the global numeric modifier @code{-n}.
 
 @example
 sort -t : -n -k 5b,5 -k 3,3 /etc/passwd
@@ -3449,7 +3448,7 @@
 find src -type f -print0 | sort -t / -z -f | xargs -0 etags --append
 @end smallexample
 
-The use of @option{-print0}, @option{-z}, and @option{-0} in this case means
+The use of @code{-print0}, @code{-z}, and @code{-0} in this case means
 that pathnames that contain Line Feed characters will not get broken up
 by the sort operation.
 
@@ -3461,7 +3460,7 @@
 sort -t : -n -k 5b,5b -k 3,3 /etc/passwd
 @end example
 
-or by using the global @option{-b} modifier instead of @option{-n}
+or by using the global @code{-b} modifier instead of @code{-n}
 and an explicit @samp{n} with the second key specifier.
 
 @example
@@ -3482,12 +3481,12 @@
 
 
 @node uniq invocation
-@section @command{uniq}: Uniquify files
+@section @file{uniq}: Uniquify files
 
 @pindex uniq
 @cindex uniquify files
 
-@command{uniq} writes the unique lines in the given @file{input}, or
+@file{uniq} writes the unique lines in the given @file{input}, or
 standard input if nothing is given or for an @var{input} name of
 @samp{-}.  Synopsis:
 
@@ -3495,7 +3494,7 @@
 uniq [@var{option}]@dots{} [@var{input} [@var{output}]]
 @end example
 
-By default, @command{uniq} prints its input lines, except that
+By default, @file{uniq} prints its input lines, except that
 it discards all but the first of adjacent repeated lines, so that
 no output lines are repeated.  Optionally, it can instead discard
 lines that are not repeated, or all repeated lines.
@@ -3506,9 +3505,9 @@
 
 @vindex LC_COLLATE
 Comparisons use the character collating sequence specified by the
-@env{LC_COLLATE} locale category.
+@var{LC_COLLATE} locale category.
 
-If no @var{output} file is specified, @command{uniq} writes to standard
+If no @var{output} file is specified, @file{uniq} writes to standard
 output.
 
 The program accepts the following options.  Also see @ref{Common options}.
@@ -3524,9 +3523,9 @@
 are sequences of non-space non-tab characters that are separated from
 each other by at least one space or tab.
 
-On older systems, @command{uniq} supports an obsolete option
-@option{-@var{n}}.  @acronym{POSIX} 1003.1-2001 (@pxref{Standards conformance})
-does not allow this; use @option{-f @var{n}} instead.
+On older systems, @file{uniq} supports an obsolete option
+@code{-@var{n}}.  @b{POSIX} 1003.1-2001 (@pxref{Standards conformance})
+does not allow this; use @code{-f @var{n}} instead.
 
 @item -s @var{n}
 @itemx --skip-chars=@var{n}
@@ -3536,9 +3535,9 @@
 for comparison if a line has fewer than @var{n} characters.  If you use both
 the field and character skipping options, fields are skipped over first.
 
-On older systems, @command{uniq} supports an obsolete option
-@option{+@var{n}}.  @acronym{POSIX} 1003.1-2001 (@pxref{Standards conformance})
-does not allow this; use @option{-s @var{n}} instead.
+On older systems, @file{uniq} supports an obsolete option
+@code{+@var{n}}.  @b{POSIX} 1003.1-2001 (@pxref{Standards conformance})
+does not allow this; use @code{-s @var{n}} instead.
 
 @item -c
 @itemx --count
@@ -3558,7 +3557,7 @@
 @opindex --repeated
 @cindex repeated lines, outputting
 Discard lines that are not repeated.  When used by itself, this option
-causes @command{uniq} to print the first copy of each repeated line,
+causes @file{uniq} to print the first copy of each repeated line,
 and nothing else.
 
 @item -D
@@ -3577,7 +3576,7 @@
 
 @item none
 Do not delimit groups of repeated lines.
-This is equivalent to @option{--all-repeated} (@option{-D}).
+This is equivalent to @code{--all-repeated} (@code{-D}).
 
 @item prepend
 Output a newline before each group of repeated lines.
@@ -3603,7 +3602,7 @@
 @opindex --unique
 @cindex unique lines, outputting
 Discard the first repeated line.  When used by itself, this option
-causes @command{uniq} to print unique lines, and nothing else.
+causes @file{uniq} to print unique lines, and nothing else.
 
 @item -w @var{n}
 @itemx --check-chars=@var{n}
@@ -3619,13 +3618,13 @@
 
 
 @node comm invocation
-@section @command{comm}: Compare two sorted files line by line
+@section @file{comm}: Compare two sorted files line by line
 
 @pindex comm
 @cindex line-by-line comparison
 @cindex comparing sorted files
 
-@command{comm} writes to standard output lines that are common, and lines
+@file{comm} writes to standard output lines that are common, and lines
 that are unique, to two input files; a file name of @samp{-} means
 standard input.  Synopsis:
 
@@ -3634,15 +3633,15 @@
 @end example
 
 @vindex LC_COLLATE
-Before @command{comm} can be used, the input files must be sorted using the
-collating sequence specified by the @env{LC_COLLATE} locale.
+Before @file{comm} can be used, the input files must be sorted using the
+collating sequence specified by the @var{LC_COLLATE} locale.
 If an input file ends in a non-newline
-character, a newline is silently appended.  The @command{sort} command with
-no options always outputs a file that is suitable input to @command{comm}.
+character, a newline is silently appended.  The @file{sort} command with
+no options always outputs a file that is suitable input to @file{comm}.
 
 @cindex differing lines
 @cindex common lines
-With no options, @command{comm} produces three-column output.  Column one
+With no options, @file{comm} produces three-column output.  Column one
 contains lines unique to @var{file1}, column two contains lines unique
 to @var{file2}, and column three contains lines common to both files.
 Columns are separated by a single TAB character.
@@ -3652,22 +3651,22 @@
 @opindex -1
 @opindex -2
 @opindex -3
-The options @option{-1}, @option{-2}, and @option{-3} suppress printing of
+The options @code{-1}, @code{-2}, and @code{-3} suppress printing of
 the corresponding columns.  Also see @ref{Common options}.
 
-Unlike some other comparison utilities, @command{comm} has an exit
+Unlike some other comparison utilities, @file{comm} has an exit
 status that does not depend on the result of the comparison.
-Upon normal completion @command{comm} produces an exit code of zero.
+Upon normal completion @file{comm} produces an exit code of zero.
 If there is an error it exits with nonzero status.
 
 
 @node tsort invocation
-@section @command{tsort}: Topological sort
+@section @file{tsort}: Topological sort
 
 @pindex tsort
 @cindex topological sort
 
-@command{tsort} performs a topological sort on the given @var{file}, or
+@file{tsort} performs a topological sort on the given @var{file}, or
 standard input if no input file is given or for a @var{file} of
 @samp{-}.  For more details and some history, see @ref{tsort background}.
 Synopsis:
@@ -3676,7 +3675,7 @@
 tsort [@var{option}] [@var{file}]
 @end example
 
-@command{tsort} reads its input as pairs of strings, separated by blanks,
+@file{tsort} reads its input as pairs of strings, separated by blanks,
 indicating a partial ordering.  The output is a total ordering that
 corresponds to the given partial ordering.
 
@@ -3742,7 +3741,7 @@
 recheck pretty_name
 @end example
 
-then you can use @command{tsort} to produce an ordering of those
+then you can use @file{tsort} to produce an ordering of those
 functions that satisfies your requirement.
 
 @example
@@ -3766,7 +3765,7 @@
 main
 @end example
 
-@command{tsort} detects any cycles in the input and writes the first cycle
+@file{tsort} detects any cycles in the input and writes the first cycle
 encountered to standard error.
 
 Note that for a given partial ordering, generally there is no unique
@@ -3774,14 +3773,14 @@
 @code{parse_options} may be placed anywhere in the list as long as it
 precedes @code{main}.
 
-The only options are @option{--help} and @option{--version}.  @xref{Common
+The only options are @code{--help} and @code{--version}.  @xref{Common
 options}.
 
 @node tsort background
-@section @command{tsort}: Background
+@section @file{tsort}: Background
 
-@command{tsort} exists because very early versions of the Unix linker processed
-an archive file exactly once, and in order.  As @command{ld} read each object
+@file{tsort} exists because very early versions of the Unix linker processed
+an archive file exactly once, and in order.  As @file{ld} read each object
 in the archive, it decided whether it was needed in the program based on
 whether it defined any symbols which were undefined at that point in
 the link.
@@ -3795,16 +3794,16 @@
 
 The way to address this problem was to first generate a set of
 dependencies of one object file on another.  This was done by a shell
-script called @command{lorder}.  The GNU tools don't provide a version of
+script called @file{lorder}.  The GNU tools don't provide a version of
 lorder, as far as I know, but you can still find it in BSD
 distributions.
 
-Then you ran @command{tsort} over the @command{lorder} output, and you used the
+Then you ran @file{tsort} over the @file{lorder} output, and you used the
 resulting sort to define the order in which you added objects to the archive.
 
 This whole procedure has been obsolete since about 1980, because
 Unix archives now contain a symbol table (traditionally built by
-@command{ranlib}, now generally built by @command{ar} itself), and the Unix
+@file{ranlib}, now generally built by @file{ar} itself), and the Unix
 linker uses the symbol table to effectively make multiple passes over
 an archive file.
 
@@ -3816,11 +3815,11 @@
 
 
 @node ptx invocation
-@section @command{ptx}: Produce permuted indexes
+@section @file{ptx}: Produce permuted indexes
 
 @pindex ptx
 
-@command{ptx} reads a text file and essentially produces a permuted index, with
+@file{ptx} reads a text file and essentially produces a permuted index, with
 each keyword in its context.  The calling sketch is either one of:
 
 @example
@@ -3828,11 +3827,11 @@
 ptx -G [@var{option} @dots{}] [@var{input} [@var{output}]]
 @end example
 
-The @option{-G} (or its equivalent: @option{--traditional}) option disables
+The @code{-G} (or its equivalent: @code{--traditional}) option disables
 all @sc{gnu} extensions and reverts to traditional mode, thus introducing some
 limitations and changing several of the program's default option values.
-When @option{-G} is not specified, @sc{gnu} extensions are always enabled.
-@sc{gnu} extensions to @command{ptx} are documented wherever appropriate in this
+When @code{-G} is not specified, @sc{gnu} extensions are always enabled.
+@sc{gnu} extensions to @file{ptx} are documented wherever appropriate in this
 document.  For the full list, see @xref{Compatibility in ptx}.
 
 Individual options are explained in the following sections.
@@ -3856,7 +3855,7 @@
 respectively the name of the @var{input} file to read and the name of
 the @var{output} file to produce.  @emph{Be very careful} to note that,
 in this case, the contents of file given by the second parameter is
-destroyed.  This behavior is dictated by System V @command{ptx}
+destroyed.  This behavior is dictated by System V @file{ptx}
 compatibility; @sc{gnu} Standards normally discourage output parameters not
 introduced by an option.
 
@@ -3887,7 +3886,7 @@
 @item -G
 @itemx --traditional
 As already explained, this option disables all @sc{gnu} extensions to
-@command{ptx} and switches to traditional mode.
+@file{ptx} and switches to traditional mode.
 
 @item --help
 Print a short help on standard output, then exit without further
@@ -3909,8 +3908,8 @@
 As it is set up now, the program assumes that the input file is coded
 using 8-bit ISO 8859-1 code, also known as Latin-1 character set,
 @emph{unless} it is compiled for MS-DOS, in which case it uses the
-character set of the IBM-PC.  (@sc{gnu} @command{ptx} is not known to work on
-smaller MS-DOS machines anymore.)  Compared to 7-bit @acronym{ASCII}, the set
+character set of the IBM-PC.  (@sc{gnu} @file{ptx} is not known to work on
+smaller MS-DOS machines anymore.)  Compared to 7-bit @b{ASCII}, the set
 of characters which are letters is different; this alters the behavior
 of regular expression matching.  Thus, the default regular expression
 for a keyword allows foreign or diacriticized letters.  Keyword sorting,
@@ -3934,13 +3933,13 @@
 @item -b @var{file}
 @item --break-file=@var{file}
 
-This option provides an alternative (to @option{-W}) method of describing
+This option provides an alternative (to @code{-W}) method of describing
 which characters make up words.  It introduces the name of a
 file which contains a list of characters which can@emph{not} be part of
 one word; this file is called the @dfn{Break file}.  Any character which
 is not part of the Break file is a word constituent.  If both options
-@option{-b} and @option{-W} are specified, then @option{-W} has precedence and
-@option{-b} is ignored.
+@code{-b} and @code{-W} are specified, then @code{-W} has precedence and
+@code{-b} is ignored.
 
 When @sc{gnu} extensions are enabled, the only way to avoid newline as a
 break character is to write all the break characters in the file with no
@@ -3955,9 +3954,9 @@
 never be taken as keywords in concordance output.  It is called the
 @dfn{Ignore file}.  The file contains exactly one word in each line; the
 end of line separation of words is not subject to the value of the
-@option{-S} option.
+@code{-S} option.
 
-There is a default Ignore file used by @command{ptx} when this option is
+There is a default Ignore file used by @file{ptx} when this option is
 not specified, usually found in @file{/usr/local/lib/eign} if this has
 not been changed at installation time.  If you want to deactivate the
 default Ignore file, specify @code{/dev/null} instead.
@@ -3969,7 +3968,7 @@
 be retained in concordance output; any word not mentioned in this file
 is ignored.  The file is called the @dfn{Only file}.  The file contains
 exactly one word in each line; the end of line separation of words is
-not subject to the value of the @option{-S} option.
+not subject to the value of the @code{-S} option.
 
 There is no default for the Only file.  When both an Only file and an
 Ignore file are specified, a word is considered a keyword only
@@ -3982,12 +3981,12 @@
 taken to be a reference that has the purpose of identifying this input
 line in the resulting permuted index.  For more information about reference
 production, see @xref{Output formatting in ptx}.
-Using this option changes the default value for option @option{-S}.
+Using this option changes the default value for option @code{-S}.
 
 Using this option, the program does not try very hard to remove
 references from contexts in output, but it succeeds in doing so
 @emph{when} the context ends exactly at the newline.  If option
-@option{-r} is used with @option{-S} default value, or when @sc{gnu} extensions
+@code{-r} is used with @code{-S} default value, or when @sc{gnu} extensions
 are disabled, this condition is always met and references are completely
 excluded from the output contexts.
 
@@ -3998,7 +3997,7 @@
 line or the end of a sentence.  In fact, this regular expression is not
 the only distinction between end of lines or end of sentences, and input
 line boundaries have no special significance outside this option.  By
-default, when @sc{gnu} extensions are enabled and if @option{-r} option is not
+default, when @sc{gnu} extensions are enabled and if @code{-r} option is not
 used, end of sentences are used.  In this case, this @var{regex} is
 imported from @sc{gnu} Emacs:
 
@@ -4006,7 +4005,7 @@
 [.?!][]\"')@}]*\\($\\|\t\\|  \\)[ \t\n]*
 @end example
 
-Whenever @sc{gnu} extensions are disabled or if @option{-r} option is used, end
+Whenever @sc{gnu} extensions are disabled or if @code{-r} option is used, end
 of lines are used; in this case, the default @var{regexp} is just:
 
 @example
@@ -4016,7 +4015,7 @@
 Using an empty @var{regexp} is equivalent to completely disabling end of
 line or end of sentence recognition.  In this case, the whole file is
 considered to be a single big line or sentence.  The user might want to
-disallow all truncation flag generation as well, through option @option{-F
+disallow all truncation flag generation as well, through option @code{-F
 ""}.  @xref{Regexps, , Syntax of Regular Expressions, emacs, The GNU Emacs
 Manual}.
 
@@ -4032,7 +4031,7 @@
 
 As a matter of convenience to the user, many usual backslashed escape
 sequences from the C language are recognized and converted to the
-corresponding characters by @command{ptx} itself.
+corresponding characters by @file{ptx} itself.
 
 @item -W @var{regexp}
 @itemx --word-regexp=@var{regexp}
@@ -4049,7 +4048,7 @@
 
 As a matter of convenience to the user, many usual backslashed escape
 sequences, as found in the C language, are recognized and converted to
-the corresponding characters by @command{ptx} itself.
+the corresponding characters by @file{ptx} itself.
 
 @end table
 
@@ -4057,15 +4056,15 @@
 @node Output formatting in ptx
 @subsection Output formatting
 
-Output format is mainly controlled by the @option{-O} and @option{-T} options
-described in the table below.  When neither @option{-O} nor @option{-T} are
+Output format is mainly controlled by the @code{-O} and @code{-T} options
+described in the table below.  When neither @code{-O} nor @code{-T} are
 selected, and if @sc{gnu} extensions are enabled, the program chooses an
 output format suitable for a dumb terminal.  Each keyword occurrence is
 output to the center of one line, surrounded by its left and right
 contexts.  Each field is properly justified, so the concordance output
 can be readily observed.  As a special feature, if automatic
-references are selected by option @option{-A} and are output before the
-left context, that is, if option @option{-R} is @emph{not} selected, then
+references are selected by option @code{-A} and are output before the
+left context, that is, if option @code{-R} is @emph{not} selected, then
 a colon is added after the reference; this nicely interfaces with @sc{gnu}
 Emacs @code{next-error} processing.  In this default output format, each
 white space character, like newline and tab, is merely changed to
@@ -4089,7 +4088,7 @@
 
 Select the maximum output width of each final line.  If references are
 used, they are included or excluded from the maximum output width
-depending on the value of option @option{-R}.  If this option is not
+depending on the value of option @code{-R}.  If this option is not
 selected, that is, when references are output before the left context,
 the maximum output width takes into account the maximum length of all
 references.  If this option is selected, that is, when references are
@@ -4103,21 +4102,21 @@
 Select automatic references.  Each input line will have an automatic
 reference made up of the file name and the line ordinal, with a single
 colon between them.  However, the file name will be empty when standard
-input is being read.  If both @option{-A} and @option{-r} are selected, then
+input is being read.  If both @code{-A} and @code{-r} are selected, then
 the input reference is still read and skipped, but the automatic
 reference is used at output time, overriding the input reference.
 
 @item -R
 @itemx --right-side-refs
 
-In the default output format, when option @option{-R} is not used, any
-references produced by the effect of options @option{-r} or @option{-A} are
+In the default output format, when option @code{-R} is not used, any
+references produced by the effect of options @code{-r} or @code{-A} are
 placed to the far right of output lines, after the right context.  With
-default output format, when the @option{-R} option is specified, references
+default output format, when the @code{-R} option is specified, references
 are rather placed at the beginning of each output line, before the left
-context.  For any other output format, option @option{-R} is
-ignored, with one exception:  with @option{-R} the width of references
-is @emph{not} taken into account in total output width given by @option{-w}.
+context.  For any other output format, option @code{-R} is
+ignored, with one exception:  with @code{-R} the width of references
+is @emph{not} taken into account in total output width given by @code{-w}.
 
 This option is automatically selected whenever @sc{gnu} extensions are
 disabled.
@@ -4128,32 +4127,32 @@
 This option will request that any truncation in the output be reported
 using the string @var{string}.  Most output fields theoretically extend
 towards the beginning or the end of the current line, or current
-sentence, as selected with option @option{-S}.  But there is a maximum
-allowed output line width, changeable through option @option{-w}, which is
+sentence, as selected with option @code{-S}.  But there is a maximum
+allowed output line width, changeable through option @code{-w}, which is
 further divided into space for various output fields.  When a field has
 to be truncated because it cannot extend beyond the beginning or the end of
 the current line to fit in, then a truncation occurs.  By default,
-the string used is a single slash, as in @option{-F /}.
+the string used is a single slash, as in @code{-F /}.
 
-@var{string} may have more than one character, as in @option{-F ...}.
-Also, in the particular case when @var{string} is empty (@option{-F ""}),
+@var{string} may have more than one character, as in @code{-F ...}.
+Also, in the particular case when @var{string} is empty (@code{-F ""}),
 truncation flagging is disabled, and no truncation marks are appended in
 this case.
 
 As a matter of convenience to the user, many usual backslashed escape
 sequences, as found in the C language, are recognized and converted to
-the corresponding characters by @command{ptx} itself.
+the corresponding characters by @file{ptx} itself.
 
 @item -M @var{string}
 @itemx --macro-name=@var{string}
 
 Select another @var{string} to be used instead of @samp{xx}, while
-generating output suitable for @command{nroff}, @command{troff} or @TeX{}.
+generating output suitable for @file{nroff}, @file{troff} or @TeX{}.
 
 @item -O
 @itemx --format=roff
 
-Choose an output format suitable for @command{nroff} or @command{troff}
+Choose an output format suitable for @file{nroff} or @file{troff}
 processing.  Each output line will look like:
 
 @smallexample
@@ -4162,13 +4161,13 @@
 
 so it will be possible to write a @samp{.xx} roff macro to take care of
 the output typesetting.  This is the default output format when @sc{gnu}
-extensions are disabled.  Option @option{-M} can be used to change
+extensions are disabled.  Option @code{-M} can be used to change
 @samp{xx} to another macro name.
 
 In this output format, each non-graphical character, like newline and
 tab, is merely changed to exactly one space, with no special attempt to
 compress consecutive spaces.  Each quote character: @kbd{"} is doubled
-so it will be correctly processed by @command{nroff} or @command{troff}.
+so it will be correctly processed by @file{nroff} or @file{troff}.
 
 @item -T
 @itemx --format=tex
@@ -4183,9 +4182,9 @@
 @noindent
 so it will be possible to write a @code{\xx} definition to take care of
 the output typesetting.  Note that when references are not being
-produced, that is, neither option @option{-A} nor option @option{-r} is
+produced, that is, neither option @code{-A} nor option @code{-r} is
 selected, the last parameter of each @code{\xx} call is inhibited.
-Option @option{-M} can be used to change @samp{xx} to another macro
+Option @code{-M} can be used to change @samp{xx} to another macro
 name.
 
 In this output format, some special characters, like @kbd{$}, @kbd{%},
@@ -4197,7 +4196,7 @@
 @code{~\@{ @}} respectively.  Other diacriticized characters of the
 underlying character set produce an appropriate @TeX{} sequence as far
 as possible.  The other non-graphical characters, like newline and tab,
-and all other characters which are not part of @acronym{ASCII}, are merely
+and all other characters which are not part of @b{ASCII}, are merely
 changed to exactly one space, with no special attempt to compress
 consecutive spaces.  Let me know how to improve this special character
 processing for @TeX{}.
@@ -4206,46 +4205,46 @@
 
 
 @node Compatibility in ptx
-@subsection The @sc{gnu} extensions to @command{ptx}
+@subsection The @sc{gnu} extensions to @file{ptx}
 
-This version of @command{ptx} contains a few features which do not exist in
-System V @command{ptx}.  These extra features are suppressed by using the
-@option{-G} command line option, unless overridden by other command line
+This version of @file{ptx} contains a few features which do not exist in
+System V @file{ptx}.  These extra features are suppressed by using the
+@code{-G} command line option, unless overridden by other command line
 options.  Some @sc{gnu} extensions cannot be recovered by overriding, so the
-simple rule is to avoid @option{-G} if you care about @sc{gnu} extensions.
-Here are the differences between this program and System V @command{ptx}.
+simple rule is to avoid @code{-G} if you care about @sc{gnu} extensions.
+Here are the differences between this program and System V @file{ptx}.
 
 @itemize @bullet
 
 @item
 This program can read many input files at once, it always writes the
 resulting concordance on standard output.  On the other hand, System V
-@command{ptx} reads only one file and sends the result to standard output
+@file{ptx} reads only one file and sends the result to standard output
 or, if a second @var{file} parameter is given on the command, to that
 @var{file}.
 
 Having output parameters not introduced by options is a dangerous
-practice which @sc{gnu} avoids as far as possible.  So, for using @command{ptx}
+practice which @sc{gnu} avoids as far as possible.  So, for using @file{ptx}
 portably between @sc{gnu} and System V, you should always use it with a
 single input file, and always expect the result on standard output.  You
-might also want to automatically configure in a @option{-G} option to
-@command{ptx} calls in products using @command{ptx}, if the configurator finds
-that the installed @command{ptx} accepts @option{-G}.
+might also want to automatically configure in a @code{-G} option to
+@file{ptx} calls in products using @file{ptx}, if the configurator finds
+that the installed @file{ptx} accepts @code{-G}.
 
 @item
-The only options available in System V @command{ptx} are options @option{-b},
-@option{-f}, @option{-g}, @option{-i}, @option{-o}, @option{-r}, @option{-t} and
-@option{-w}.  All other options are @sc{gnu} extensions and are not repeated in
+The only options available in System V @file{ptx} are options @code{-b},
+@code{-f}, @code{-g}, @code{-i}, @code{-o}, @code{-r}, @code{-t} and
+@code{-w}.  All other options are @sc{gnu} extensions and are not repeated in
 this enumeration.  Moreover, some options have a slightly different
 meaning when @sc{gnu} extensions are enabled, as explained below.
 
 @item
-By default, concordance output is not formatted for @command{troff} or
-@command{nroff}.  It is rather formatted for a dumb terminal.  @command{troff}
-or @command{nroff} output may still be selected through option @option{-O}.
+By default, concordance output is not formatted for @file{troff} or
+@file{nroff}.  It is rather formatted for a dumb terminal.  @file{troff}
+or @file{nroff} output may still be selected through option @code{-O}.
 
 @item
-Unless @option{-R} option is used, the maximum reference width is
+Unless @code{-R} option is used, the maximum reference width is
 subtracted from the total output line width.  With @sc{gnu} extensions
 disabled, width of references is not taken into account in the output
 line width computations.
@@ -4253,12 +4252,12 @@
 @item
 All 256 characters, even @kbd{NUL}s, are always read and processed from
 input file with no adverse effect, even if @sc{gnu} extensions are disabled.
-However, System V @command{ptx} does not accept 8-bit characters, a few
+However, System V @file{ptx} does not accept 8-bit characters, a few
 control characters are rejected, and the tilde @kbd{~} is also rejected.
 
 @item
 Input line length is only limited by available memory, even if @sc{gnu}
-extensions are disabled.  However, System V @command{ptx} processes only
+extensions are disabled.  However, System V @file{ptx} processes only
 the first 200 characters in each line.
 
 @item
@@ -4269,13 +4268,13 @@
 
 @item
 The program makes better use of output line width.  If @sc{gnu} extensions
-are disabled, the program rather tries to imitate System V @command{ptx},
+are disabled, the program rather tries to imitate System V @file{ptx},
 but still, there are some slight disposition glitches this program does
 not completely reproduce.
 
 @item
 The user can specify both an Ignore file and an Only file.  This is not
-allowed with System V @command{ptx}.
+allowed with System V @file{ptx}.
 
 @end itemize
 
@@ -4291,10 +4290,10 @@
 
 
 @node cut invocation
-@section @command{cut}: Print selected parts of lines
+@section @file{cut}: Print selected parts of lines
 
 @pindex cut
-@command{cut} writes to standard output selected parts of each line of each
+@file{cut} writes to standard output selected parts of each line of each
 input file, or standard input if no files are given or for a file name of
 @samp{-}.  Synopsis:
 
@@ -4306,7 +4305,7 @@
 and @var{field-list} are one or more numbers or ranges (two numbers
 separated by a dash) separated by commas.  Bytes, characters, and
 fields are numbered starting at 1.  Incomplete ranges may be
-given: @option{-@var{m}} means @samp{1-@var{m}}; @samp{@var{n}-} means
+given: @code{-@var{m}} means @samp{1-@var{m}}; @samp{@var{n}-} means
 @samp{@var{n}} through end of line or last field.  The list elements
 can be repeated, can overlap, and can be specified in any order; but
 the selected input is written in the same order that it is read, and
@@ -4324,7 +4323,7 @@
 Print only the bytes in positions listed in @var{byte-list}.  Tabs and
 backspaces are treated like any other character; they take up 1 byte.
 If an output delimiter is specified, (see the description of
-@option{--output-delimiter}), then output that string between
+@code{--output-delimiter}), then output that string between
 ranges of selected bytes.
 
 @item -c @var{character-list}
@@ -4332,11 +4331,11 @@
 @opindex -c
 @opindex --characters
 Print only characters in positions listed in @var{character-list}.
-The same as @option{-b} for now, but internationalization will change
+The same as @code{-b} for now, but internationalization will change
 that.  Tabs and backspaces are treated like any other character; they
 take up 1 character.
 If an output delimiter is specified, (see the description of
-@option{--output-delimiter}), then output that string between
+@code{--output-delimiter}), then output that string between
 ranges of selected bytes.
 
 @item -f @var{field-list}
@@ -4346,13 +4345,13 @@
 Print only the fields listed in @var{field-list}.  Fields are
 separated by a TAB character by default.
 Also print any line that contains no delimiter character, unless
-the @option{--only-delimited} (@option{-s}) option is specified
+the @code{--only-delimited} (@code{-s}) option is specified
 
 @item -d @var{input_delim_byte}
 @itemx --delimiter=@var{input_delim_byte}
 @opindex -d
 @opindex --delimiter
-For @option{-f}, fields are separated in the input by the first character
+For @code{-f}, fields are separated in the input by the first character
 in @var{input_delim_byte} (default is TAB).
 
 @item -n
@@ -4363,14 +4362,14 @@
 @itemx --only-delimited
 @opindex -s
 @opindex --only-delimited
-For @option{-f}, do not print lines that do not contain the field separator
+For @code{-f}, do not print lines that do not contain the field separator
 character.  Normally, any line without a field separator is printed verbatim.
 
 @itemx --output-delimiter=@var{output_delim_string}
 @opindex --output-delimiter
-With @option{-f}, output fields are separated by @var{output_delim_string}.
-The default with @option{-f} is to use the input delimiter.
-When using @option{-b} or @option{-c} to select ranges of byte or
+With @code{-f}, output fields are separated by @var{output_delim_string}.
+The default with @code{-f} is to use the input delimiter.
+When using @code{-b} or @code{-c} to select ranges of byte or
 character offsets (as opposed to ranges of fields),
 output @var{output_delim_string} between ranges of selected bytes.
 
@@ -4381,12 +4380,12 @@
 
 
 @node paste invocation
-@section @command{paste}: Merge lines of files
+@section @file{paste}: Merge lines of files
 
 @pindex paste
 @cindex merging files
 
-@command{paste} writes to standard output lines consisting of sequentially
+@file{paste} writes to standard output lines consisting of sequentially
 corresponding lines of each given file, separated by a TAB character.
 Standard input is used for a file name of @samp{-} or if no input files
 are given.
@@ -4451,12 +4450,12 @@
 
 
 @node join invocation
-@section @command{join}: Join lines on a common field
+@section @file{join}: Join lines on a common field
 
 @pindex join
 @cindex common field, joining on
 
-@command{join} writes to standard output a line for each pair of input
+@file{join} writes to standard output a line for each pair of input
 lines that have identical join fields.  Synopsis:
 
 @example
@@ -4469,10 +4468,10 @@
 sorted on the join fields.
 
 Normally, the sort order is that of the
-collating sequence specified by the @env{LC_COLLATE} locale.  Unless
-the @option{-t} option is given, the sort comparison ignores blanks at
+collating sequence specified by the @var{LC_COLLATE} locale.  Unless
+the @code{-t} option is given, the sort comparison ignores blanks at
 the start of the join field, as in @code{sort -b}.  If the
-@option{--ignore-case} option is given, the sort comparison ignores
+@code{--ignore-case} option is given, the sort comparison ignores
 the case of characters in the join field, as in @code{sort -f}.
 
 However, as a GNU extension, if the input has no unpairable lines the
@@ -4532,7 +4531,7 @@
 Join on field @var{field} (a positive integer) of file 2.
 
 @item -j @var{field}
-Equivalent to @option{-1 @var{field} -2 @var{field}}.
+Equivalent to @code{-1 @var{field} -2 @var{field}}.
 
 @item -o @var{field-list}
 Construct each output line according to the format in @var{field-list}.
@@ -4544,10 +4543,10 @@
 In most cases, the functionality of the @samp{0} field spec
 may be reproduced using the explicit @var{m.n} that corresponds
 to the join field.  However, when printing unpairable lines
-(using either of the @option{-a} or @option{-v} options), there is no way
+(using either of the @code{-a} or @code{-v} options), there is no way
 to specify the join field using @var{m.n} in @var{field-list}
 if there are unpairable lines in both files.
-To give @command{join} that functionality, @acronym{POSIX} invented the @samp{0}
+To give @file{join} that functionality, @b{POSIX} invented the @samp{0}
 field specification notation.
 
 The elements in @var{field-list}
@@ -4565,8 +4564,8 @@
 
 @end table
 
-In addition, when @sc{gnu} @command{join} is invoked with exactly one argument,
-the @option{--help} and @option{--version} options are recognized.
+In addition, when @sc{gnu} @file{join} is invoked with exactly one argument,
+the @code{--help} and @code{--version} options are recognized.
 @xref{Common options}.
 
 @exitstatus
@@ -4587,7 +4586,7 @@
 
 
 @node tr invocation
-@section @command{tr}: Translate, squeeze, and/or delete characters
+@section @file{tr}: Translate, squeeze, and/or delete characters
 
 @pindex tr
 
@@ -4597,7 +4596,7 @@
 tr [@var{option}]@dots{} @var{set1} [@var{set2}]
 @end example
 
-@command{tr} copies standard input to standard output, performing
+@file{tr} copies standard input to standard output, performing
 one of the following operations:
 
 @itemize @bullet
@@ -4613,8 +4612,8 @@
 
 The @var{set1} and (if given) @var{set2} arguments define ordered
 sets of characters, referred to below as @var{set1} and @var{set2}.  These
-sets are the characters of the input that @command{tr} operates on.
-The @option{--complement} (@option{-c}) option replaces @var{set1} with its
+sets are the characters of the input that @file{tr} operates on.
+The @code{--complement} (@code{-c}) option replaces @var{set1} with its
 complement (all of the characters that are not in @var{set1}).
 
 @exitstatus
@@ -4677,7 +4676,7 @@
 collate before @var{n}; if it doesn't, an error results.  As an example,
 @samp{0-9} is the same as @samp{0123456789}.
 
-@sc{gnu} @command{tr} does not support the System V syntax that uses square
+@sc{gnu} @file{tr} does not support the System V syntax that uses square
 brackets to enclose ranges.  Translations specified in that format
 sometimes work as expected, since the brackets are often transliterated
 to themselves.  However, they should be avoided because they sometimes
@@ -4685,10 +4684,10 @@
 as well as digits.
 
 Many historically common and even accepted uses of ranges are not
-portable.  For example, on @acronym{EBCDIC} hosts using the @samp{A-Z}
+portable.  For example, on @b{EBCDIC} hosts using the @samp{A-Z}
 range will not do what most would expect because @samp{A} through @samp{Z}
-are not contiguous as they are in @acronym{ASCII}.
-If you can rely on a @acronym{POSIX} compliant version of @command{tr}, then
+are not contiguous as they are in @b{ASCII}.
+If you can rely on a @b{POSIX} compliant version of @file{tr}, then
 the best way to work around this is to use character classes (see below).
 Otherwise, it is most portable (and most ugly) to enumerate the members
 of the ranges.
@@ -4709,8 +4708,8 @@
 The notation @samp{[:@var{class}:]} expands to all of the characters in
 the (predefined) class @var{class}.  The characters expand in no
 particular order, except for the @code{upper} and @code{lower} classes,
-which expand in ascending order.  When the @option{--delete} (@option{-d})
-and @option{--squeeze-repeats} (@option{-s}) options are both given, any
+which expand in ascending order.  When the @code{--delete} (@code{-d})
+and @code{--squeeze-repeats} (@code{-s}) options are both given, any
 character class can be used in @var{set2}.  Otherwise, only the
 character classes @code{lower} and @code{upper} are accepted in
 @var{set2}, and then only if the corresponding character class
@@ -4765,7 +4764,7 @@
 equivalent to @var{c}, in no particular order.  Equivalence classes are
 a relatively recent invention intended to support non-English alphabets.
 But there seems to be no standard way to define them or determine their
-contents.  Therefore, they are not fully implemented in @sc{gnu} @command{tr};
+contents.  Therefore, they are not fully implemented in @sc{gnu} @file{tr};
 each character's equivalence class consists only of that character,
 which is of no particular use.
 
@@ -4777,9 +4776,9 @@
 
 @cindex translating characters
 
-@command{tr} performs translation when @var{set1} and @var{set2} are
-both given and the @option{--delete} (@option{-d}) option is not given.
-@command{tr} translates each character of its input that is in @var{set1}
+@file{tr} performs translation when @var{set1} and @var{set2} are
+both given and the @code{--delete} (@code{-d}) option is not given.
+@file{tr} translates each character of its input that is in @var{set1}
 to the corresponding character in @var{set2}.  Characters not in
 @var{set1} are passed through unchanged.  When a character appears more
 than once in @var{set1} and the corresponding characters in @var{set2}
@@ -4791,7 +4790,7 @@
 tr a z
 @end example
 
-A common use of @command{tr} is to convert lowercase characters to
+A common use of @file{tr} is to convert lowercase characters to
 uppercase.  This can be done in many ways.  Here are three of them:
 
 @example
@@ -4803,22 +4802,22 @@
 @noindent
 But note that using ranges like @code{a-z} above is not portable.
 
-When @command{tr} is performing translation, @var{set1} and @var{set2}
+When @file{tr} is performing translation, @var{set1} and @var{set2}
 typically have the same length.  If @var{set1} is shorter than
 @var{set2}, the extra characters at the end of @var{set2} are ignored.
 
 On the other hand, making @var{set1} longer than @var{set2} is not
-portable; @acronym{POSIX} says that the result is undefined.  In this situation,
-BSD @command{tr} pads @var{set2} to the length of @var{set1} by repeating
+portable; @b{POSIX} says that the result is undefined.  In this situation,
+BSD @file{tr} pads @var{set2} to the length of @var{set1} by repeating
 the last character of @var{set2} as many times as necessary.  System V
-@command{tr} truncates @var{set1} to the length of @var{set2}.
+@file{tr} truncates @var{set1} to the length of @var{set2}.
 
-By default, @sc{gnu} @command{tr} handles this case like BSD @command{tr}.
-When the @option{--truncate-set1} (@option{-t}) option is given,
-@sc{gnu} @command{tr} handles this case like the System V @command{tr}
+By default, @sc{gnu} @file{tr} handles this case like BSD @file{tr}.
+When the @code{--truncate-set1} (@code{-t}) option is given,
+@sc{gnu} @file{tr} handles this case like the System V @file{tr}
 instead.  This option is ignored for operations other than translation.
 
-Acting like System V @command{tr} in this case breaks the relatively common
+Acting like System V @file{tr} in this case breaks the relatively common
 BSD idiom:
 
 @example
@@ -4832,7 +4831,7 @@
 
 @noindent
 By the way, the above idiom is not portable because it uses ranges.
-Assuming a @acronym{POSIX} compliant @command{tr}, here is a better way to write it:
+Assuming a @b{POSIX} compliant @file{tr}, here is a better way to write it:
 
 @example
 tr -cs '[:alnum:]' '[\n*]'
@@ -4845,19 +4844,19 @@
 @cindex squeezing repeat characters
 @cindex deleting characters
 
-When given just the @option{--delete} (@option{-d}) option, @command{tr}
+When given just the @code{--delete} (@code{-d}) option, @file{tr}
 removes any input characters that are in @var{set1}.
 
-When given just the @option{--squeeze-repeats} (@option{-s}) option,
-@command{tr} replaces each input sequence of a repeated character that
+When given just the @code{--squeeze-repeats} (@code{-s}) option,
+@file{tr} replaces each input sequence of a repeated character that
 is in @var{set1} with a single occurrence of that character.
 
-When given both @option{--delete} and @option{--squeeze-repeats}, @command{tr}
+When given both @code{--delete} and @code{--squeeze-repeats}, @file{tr}
 first performs any deletions using @var{set1}, then squeezes repeats
 from any remaining characters using @var{set2}.
 
-The @option{--squeeze-repeats} option may also be used when translating,
-in which case @command{tr} first performs translation, then squeezes
+The @code{--squeeze-repeats} option may also be used when translating,
+in which case @file{tr} first performs translation, then squeezes
 repeats from any remaining characters using @var{set2}.
 
 Here are some examples to illustrate various combinations of options:
@@ -4894,7 +4893,7 @@
 by converting each sequence of punctuation and blank characters to a
 single newline.  That puts each ``word'' on a line by itself.
 Next it maps all uppercase characters to lower case, and finally it
-runs @command{uniq} with the @option{-d} option to print out only the words
+runs @file{uniq} with the @code{-d} option to print out only the words
 that were repeated.
 
 @example
@@ -4916,10 +4915,10 @@
 However, when @samp{-} is one of those characters, it can be tricky because
 @samp{-} has special meanings.  Performing the same task as above but also
 removing all @samp{-} characters, we might try @code{tr -d -axM}, but
-that would fail because @command{tr} would try to interpret @option{-a} as
+that would fail because @file{tr} would try to interpret @code{-a} as
 a command-line option.  Alternatively, we could try putting the hyphen
 inside the string, @code{tr -d a-xM}, but that wouldn't work either because
-it would make @command{tr} interpret @code{a-x} as the range of characters
+it would make @file{tr} interpret @code{a-x} as the range of characters
 @samp{a}@dots{}@samp{x} rather than the three.
 One way to solve the problem is to put the hyphen at the end of the list
 of characters:
@@ -4945,17 +4944,17 @@
 @subsection Warning messages
 
 @vindex POSIXLY_CORRECT
-Setting the environment variable @env{POSIXLY_CORRECT} turns off the
+Setting the environment variable @var{POSIXLY_CORRECT} turns off the
 following warning and error messages, for strict compliance with
-@acronym{POSIX}.  Otherwise, the following diagnostics are issued:
+@b{POSIX}.  Otherwise, the following diagnostics are issued:
 
 @enumerate
 
 @item
-When the @option{--delete} option is given but @option{--squeeze-repeats}
-is not, and @var{set2} is given, @sc{gnu} @command{tr} by default prints
+When the @code{--delete} option is given but @code{--squeeze-repeats}
+is not, and @var{set2} is given, @sc{gnu} @file{tr} by default prints
 a usage message and exits, because @var{set2} would not be used.
-The @acronym{POSIX} specification says that @var{set2} must be ignored in
+The @b{POSIX} specification says that @var{set2} must be ignored in
 this case. Silently ignoring arguments is a bad idea.
 
 @item
@@ -4965,21 +4964,21 @@
 
 @end enumerate
 
-@sc{gnu} @command{tr} does not provide complete BSD or System V compatibility.
-For example, it is impossible to disable interpretation of the @acronym{POSIX}
+@sc{gnu} @file{tr} does not provide complete BSD or System V compatibility.
+For example, it is impossible to disable interpretation of the @b{POSIX}
 constructs @samp{[:alpha:]}, @samp{[=c=]}, and @samp{[c*10]}.  Also, @sc{gnu}
-@command{tr} does not delete zero bytes automatically, unlike traditional
+@file{tr} does not delete zero bytes automatically, unlike traditional
 Unix versions, which provide no way to preserve zero bytes.
 
 
 @node expand invocation
-@section @command{expand}: Convert tabs to spaces
+@section @file{expand}: Convert tabs to spaces
 
 @pindex expand
 @cindex tabs to spaces, converting
 @cindex converting tabs to spaces
 
-@command{expand} writes the contents of each given @var{file}, or standard
+@file{expand} writes the contents of each given @var{file}, or standard
 input if none are given or for a @var{file} of @samp{-}, to standard
 output, with tab characters converted to the appropriate number of
 spaces.  Synopsis:
@@ -4988,9 +4987,9 @@
 expand [@var{option}]@dots{} [@var{file}]@dots{}
 @end example
 
-By default, @command{expand} converts all tabs to spaces.  It preserves
+By default, @file{expand} converts all tabs to spaces.  It preserves
 backspace characters in the output; they decrement the column count for
-tab calculations.  The default action is equivalent to @option{-t 8} (set
+tab calculations.  The default action is equivalent to @code{-t 8} (set
 tabs every 8 columns).
 
 The program accepts the following options.  Also see @ref{Common options}.
@@ -5008,10 +5007,10 @@
 last tabstop given with single spaces.  Tabstops can be separated by
 blanks as well as by commas.
 
-On older systems, @command{expand} supports an obsolete option
-@option{-@var{tab1}[,@var{tab2}]@dots{}}, where tabstops must be
-separated by commas.  @acronym{POSIX} 1003.1-2001 (@pxref{Standards
-conformance}) does not allow this; use @option{-t
+On older systems, @file{expand} supports an obsolete option
+@code{-@var{tab1}[,@var{tab2}]@dots{}}, where tabstops must be
+separated by commas.  @b{POSIX} 1003.1-2001 (@pxref{Standards
+conformance}) does not allow this; use @code{-t
 @var{tab1}[,@var{tab2}]@dots{}} instead.
 
 @item -i
@@ -5028,11 +5027,11 @@
 
 
 @node unexpand invocation
-@section @command{unexpand}: Convert spaces to tabs
+@section @file{unexpand}: Convert spaces to tabs
 
 @pindex unexpand
 
-@command{unexpand} writes the contents of each given @var{file}, or
+@file{unexpand} writes the contents of each given @var{file}, or
 standard input if none are given or for a @var{file} of @samp{-}, to
 standard output, with strings of two or more space or tab characters
 converted to as many tabs as possible followed by as many spaces as are
@@ -5042,7 +5041,7 @@
 unexpand [@var{option}]@dots{} [@var{file}]@dots{}
 @end example
 
-By default, @command{unexpand} converts only initial spaces and tabs (those
+By default, @file{unexpand} converts only initial spaces and tabs (those
 that precede all non space or tab characters) on each line.  It
 preserves backspace characters in the output; they decrement the column
 count for tab calculations.  By default, tabs are set at every 8th
@@ -5060,13 +5059,13 @@
 instead of the default 8.  Otherwise, set the tabs at columns
 @var{tab1}, @var{tab2}, @dots{} (numbered from 0), and leave spaces and
 tabs beyond the tabstops given unchanged.  Tabstops can be separated by
-blanks as well as by commas.  This option implies the @option{-a} option.
+blanks as well as by commas.  This option implies the @code{-a} option.
 
-On older systems, @command{unexpand} supports an obsolete option
-@option{-@var{tab1}[,@var{tab2}]@dots{}}, where tabstops must be
-separated by commas.  (Unlike @option{-t}, this obsolete option does
-not imply @option{-a}.)  @acronym{POSIX} 1003.1-2001 (@pxref{Standards
-conformance}) does not allow this; use @option{--first-only -t
+On older systems, @file{unexpand} supports an obsolete option
+@code{-@var{tab1}[,@var{tab2}]@dots{}}, where tabstops must be
+separated by commas.  (Unlike @code{-t}, this obsolete option does
+not imply @code{-a}.)  @b{POSIX} 1003.1-2001 (@pxref{Standards
+conformance}) does not allow this; use @code{--first-only -t
 @var{tab1}[,@var{tab2}]@dots{}} instead.
 
 @item -a
@@ -5084,8 +5083,8 @@
 @node Directory listing
 @chapter Directory listing
 
-This chapter describes the @command{ls} command and its variants @command{dir}
-and @command{vdir}, which list information about files.
+This chapter describes the @file{ls} command and its variants @file{dir}
+and @file{vdir}, which list information about files.
 
 @menu
 * ls invocation::               List directory contents.
@@ -5096,38 +5095,38 @@
 
 
 @node ls invocation
-@section @command{ls}: List directory contents
+@section @file{ls}: List directory contents
 
 @pindex ls
 @cindex directory listing
 
-The @command{ls} program lists information about files (of any type,
+The @file{ls} program lists information about files (of any type,
 including directories).  Options and file arguments can be intermixed
 arbitrarily, as usual.
 
 For non-option command-line arguments that are directories, by default
-@command{ls} lists the contents of directories, not recursively, and
+@file{ls} lists the contents of directories, not recursively, and
 omitting files with names beginning with @samp{.}.  For other non-option
-arguments, by default @command{ls} lists just the file name.  If no
-non-option argument is specified, @command{ls} operates on the current
+arguments, by default @file{ls} lists just the file name.  If no
+non-option argument is specified, @file{ls} operates on the current
 directory, acting as if it had been invoked with a single argument of @samp{.}.
 
 @vindex LC_ALL
 By default, the output is sorted alphabetically, according to the locale
-settings in effect. @footnote{If you use a non-@acronym{POSIX}
-locale (e.g., by setting @env{LC_ALL} to @samp{en_US}), then @command{ls} may
+settings in effect. @footnote{If you use a non-@b{POSIX}
+locale (e.g., by setting @var{LC_ALL} to @samp{en_US}), then @file{ls} may
 produce output that is sorted differently than you're accustomed to.
-In that case, set the @env{LC_ALL} environment variable to @samp{C}.}
+In that case, set the @var{LC_ALL} environment variable to @samp{C}.}
 If standard output is
 a terminal, the output is in columns (sorted vertically) and control
 characters are output as question marks; otherwise, the output is listed
 one per line and control characters are output as-is.
 
-Because @command{ls} is such a fundamental program, it has accumulated many
+Because @file{ls} is such a fundamental program, it has accumulated many
 options over the years.  They are described in the subsections below;
 within each section, options are listed alphabetically (ignoring case).
 The division of options into the subsections is not absolute, since some
-options affect more than one aspect of @command{ls}'s operation.
+options affect more than one aspect of @file{ls}'s operation.
 
 @exitstatus
 
@@ -5147,7 +5146,7 @@
 @node Which files are listed
 @subsection Which files are listed
 
-These options determine which files @command{ls} lists information for.
+These options determine which files @file{ls} lists information for.
 By default, any files and the contents of any directories on the command
 line are shown.
 
@@ -5181,9 +5180,9 @@
 than listing their contents.
 @c The following sentence is the same as the one for -F.
 Do not follow symbolic links listed on the
-command line unless the @option{--dereference-command-line} (@option{-H}),
-@option{--dereference} (@option{-L}), or
-@option{--dereference-command-line-symlink-to-dir} options are specified.
+command line unless the @code{--dereference-command-line} (@code{-H}),
+@code{--dereference} (@code{-L}), or
+@code{--dereference-command-line-symlink-to-dir} options are specified.
 
 @item -H
 @itemx --dereference-command-line
@@ -5201,11 +5200,11 @@
 a directory, show information for that directory rather than for the
 link itself.
 This is the default behavior when no other dereferencing-related
-option has been specified (@option{--classify} (@option{-F}),
-@option{--directory} (@option{-d}),
-(@option{-l}),
-@option{--dereference} (@option{-L}), or
-@option{--dereference-command-line} (@option{-H})).
+option has been specified (@code{--classify} (@code{-F}),
+@code{--directory} (@code{-d}),
+(@code{-l}),
+@code{--dereference} (@code{-L}), or
+@code{--dereference-command-line} (@code{-H})).
 
 @item -I PATTERN
 @itemx --ignore=PATTERN
@@ -5232,7 +5231,7 @@
 @cindex symbolic links, dereferencing
 When showing file information for a symbolic link, show information
 for the file the link references rather than the link itself.
-However, even with this option, @command{ls} still prints the name
+However, even with this option, @file{ls} still prints the name
 of the link itself, not the name of the file that the link points to.
 
 @item -R
@@ -5249,7 +5248,7 @@
 @node What information is listed
 @subsection What information is listed
 
-These options affect the information that @command{ls} displays.  By
+These options affect the information that @file{ls} displays.  By
 default, only file names are shown.
 
 @table @samp
@@ -5266,7 +5265,7 @@
 @opindex -D
 @opindex --dired
 @cindex dired Emacs mode support
-With the long listing (@option{-l}) format, print an additional line after
+With the long listing (@code{-l}) format, print an additional line after
 the main output:
 
 @example
@@ -5279,7 +5278,7 @@
 This makes it easy for Emacs to find the names, even when they contain
 unusual characters such as space or newline, without fancy searching.
 
-If directories are being listed recursively (@option{-R}), output a similar
+If directories are being listed recursively (@code{-R}), output a similar
 line with offsets for each subdirectory name:
 @example
 //SUBDIRED// @var{beg1} @var{end1} @dots{}
@@ -5337,8 +5336,8 @@
 
 Note that although the listing above includes a trailing slash
 for the @samp{deeper} entry, the offsets select the name without
-the trailing slash.  However, if you invoke @command{ls} with @option{--dired}
-along with an option like @option{--escape} (aka @option{-b}) and operate
+the trailing slash.  However, if you invoke @file{ls} with @code{--dired}
+along with an option like @code{--escape} (aka @code{-b}) and operate
 on a file whose name contains special characters, notice that the backslash
 @emph{is} included:
 
@@ -5351,18 +5350,18 @@
 @end example
 
 If you use a quoting style that adds quote marks
-(e.g., @option{--quoting-style=c}), then the offsets include the quote marks.
+(e.g., @code{--quoting-style=c}), then the offsets include the quote marks.
 So beware that the user may select the quoting style via the environment
-variable @env{QUOTING_STYLE}.  Hence, applications using @option{--dired}
-should either specify an explicit @option{--quoting-style=literal} option
-(aka @option{-N} or @option{--literal}) on the command line, or else be
+variable @var{QUOTING_STYLE}.  Hence, applications using @code{--dired}
+should either specify an explicit @code{--quoting-style=literal} option
+(aka @code{-N} or @code{--literal}) on the command line, or else be
 prepared to parse the escaped names.
 
 @item --full-time
 @opindex --full-time
 Produce long format directory listings, and list times in full.  It is
-equivalent to using @option{--format=long} with
-@option{--time-style=full-iso} (@pxref{Formatting file timestamps}).
+equivalent to using @code{--format=long} with
+@code{--time-style=full-iso} (@pxref{Formatting file timestamps}).
 
 @item -g
 @opindex -g
@@ -5373,7 +5372,7 @@
 @opindex -G
 @opindex --no-group
 Inhibit display of group information in a long format directory listing.
-(This is the default in some non-@sc{gnu} versions of @command{ls}, so we
+(This is the default in some non-@sc{gnu} versions of @file{ls}, so we
 provide this option for compatibility.)
 
 @optHumanReadable
@@ -5400,7 +5399,7 @@
 the modification time.
 
 Normally the size is printed as a byte count without punctuation, but
-this can be overridden (@pxref{Block size}).  For example, @option{-h}
+this can be overridden (@pxref{Block size}).  For example, @code{-h}
 prints an abbreviated, human-readable count, and
 @samp{--block-size="'1"} prints a byte count with the thousands
 separator of the current locale.
@@ -5412,9 +5411,9 @@
 The @var{blocks} computed counts each hard link separately;
 this is arguably a deficiency.
 
-@cindex permissions, output by @command{ls}
+@cindex permissions, output by @file{ls}
 The permissions listed are similar to symbolic mode specifications
-(@pxref{Symbolic Modes}).  But @command{ls} combines multiple bits into the
+(@pxref{Symbolic Modes}).  But @file{ls} combines multiple bits into the
 third character of each set of permissions as follows:
 @table @samp
 @item s
@@ -5454,7 +5453,7 @@
 @item -o
 @opindex -o
 Produce long format directory listings, but don't display group information.
-It is equivalent to using @option{--format=long} with @option{--no-group} .
+It is equivalent to using @code{--format=long} with @code{--no-group} .
 
 @item -s
 @itemx --size
@@ -5474,7 +5473,7 @@
 this option reports sizes that are half the correct values.  On HP-UX
 systems, it reports sizes that are twice the correct values for files
 that are NFS-mounted from BSD systems.  This is due to a flaw in HP-UX;
-it also affects the HP-UX @command{ls} program.
+it also affects the HP-UX @file{ls} program.
 
 @optSi
 
@@ -5484,10 +5483,10 @@
 @node Sorting the output
 @subsection Sorting the output
 
-@cindex sorting @command{ls} output
-These options change the order in which @command{ls} sorts the information
+@cindex sorting @file{ls} output
+These options change the order in which @file{ls} sorts the information
 it outputs.  By default, sorting is done by character code
-(e.g., @acronym{ASCII} order).
+(e.g., @b{ASCII} order).
 
 @table @samp
 
@@ -5500,10 +5499,10 @@
 @opindex ctime@r{, printing or sorting by}
 @opindex status time@r{, printing or sorting by}
 @opindex use time@r{, printing or sorting files by}
-If the long listing format (e.g., @option{-l}, @option{-o}) is being used,
+If the long listing format (e.g., @code{-l}, @code{-o}) is being used,
 print the status change time (the @samp{ctime} in the inode) instead of
 the modification time.
-When explicitly sorting by time (@option{--sort=time} or @option{-t})
+When explicitly sorting by time (@code{--sort=time} or @code{-t})
 or when not using a long listing format,
 sort according to the status change time.
 
@@ -5511,10 +5510,10 @@
 @opindex -f
 @cindex unsorted directory listing
 @cindex directory order, listing by
-Primarily, like @option{-U}---do not sort; list the files in whatever
-order they are stored in the directory.  But also enable @option{-a} (list
-all files) and disable @option{-l}, @option{--color}, and @option{-s} (if they
-were specified before the @option{-f}).
+Primarily, like @code{-U}---do not sort; list the files in whatever
+order they are stored in the directory.  But also enable @code{-a} (list
+all files) and disable @code{-l}, @code{--color}, and @code{-s} (if they
+were specified before the @code{-f}).
 
 @item -r
 @itemx --reverse
@@ -5546,26 +5545,26 @@
 @opindex use time@r{, printing or sorting files by}
 @opindex atime@r{, printing or sorting files by}
 @opindex access time@r{, printing or sorting files by}
-If the long listing format (e.g., @option{--format=long}) is being used,
+If the long listing format (e.g., @code{--format=long}) is being used,
 print the last access time (the @samp{atime} in the inode).
-When explicitly sorting by time (@option{--sort=time} or @option{-t})
+When explicitly sorting by time (@code{--sort=time} or @code{-t})
 or when not using a long listing format, sort according to the access time.
 
 @item -U
 @itemx --sort=none
 @opindex -U
 @opindex --sort
-@opindex none@r{, sorting option for @command{ls}}
+@opindex none@r{, sorting option for @file{ls}}
 Do not sort; list the files in whatever order they are
 stored in the directory.  (Do not do any of the other unrelated things
-that @option{-f} does.)  This is especially useful when listing very large
+that @code{-f} does.)  This is especially useful when listing very large
 directories, since not doing any sorting can be noticeably faster.
 
 @item -v
 @itemx --sort=version
 @opindex -v
 @opindex --sort
-@opindex version@r{, sorting option for @command{ls}}
+@opindex version@r{, sorting option for @file{ls}}
 Sort by version name and number, lowest first.  It behaves like a default
 sort, except that each sequence of decimal digits is treated numerically
 as an index/version number.  (@xref{More details about version sort}.)
@@ -5616,8 +5615,8 @@
 This functionality is implemented using the @code{strverscmp} function.
 @xref{String/Array Comparison, , , libc, The GNU C Library Reference Manual}.
 One result of that implementation decision is that @code{ls -v} does not
-use the locale category, @env{LC_COLLATE}.  As a result, non-numeric prefixes
-are sorted as if @env{LC_COLLATE} were set to @code{C}.
+use the locale category, @var{LC_COLLATE}.  As a result, non-numeric prefixes
+are sorted as if @var{LC_COLLATE} were set to @code{C}.
 
 @node General output formatting
 @subsection General output formatting
@@ -5631,7 +5630,7 @@
 @opindex -1
 @opindex --format
 @opindex single-column @r{output of files}
-List one file per line.  This is the default for @command{ls} when standard
+List one file per line.  This is the default for @file{ls} when standard
 output is not a terminal.
 
 @item -C
@@ -5640,9 +5639,9 @@
 @opindex --format
 @opindex vertical @r{sorted files in columns}
 List files in columns, sorted vertically.  This is the default for
-@command{ls} if standard output is a terminal.  It is always the default
-for the @command{dir} and @command{d} programs.
-@sc{gnu} @command{ls} uses variable width columns to display as many files as
+@file{ls} if standard output is a terminal.  It is always the default
+for the @file{dir} and @file{d} programs.
+@sc{gnu} @file{ls} uses variable width columns to display as many files as
 possible in the fewest lines.
 
 @item --color [=@var{when}]
@@ -5662,10 +5661,10 @@
 @vindex always @r{color option}
 - Always use color.
 @end itemize
-Specifying @option{--color} and no @var{when} is equivalent to
-@option{--color=always}.
-Piping a colorized listing through a pager like @command{more} or
-@command{less} usually produces unreadable results.  However, using
+Specifying @code{--color} and no @var{when} is equivalent to
+@code{--color=always}.
+Piping a colorized listing through a pager like @file{more} or
+@file{less} usually produces unreadable results.  However, using
 @code{more -f} does seem to work.
 
 @item -F
@@ -5682,9 +5681,9 @@
 @samp{|} for FIFOs, @samp{=} for sockets, and nothing for regular files.
 @c The following sentence is the same as the one for -d.
 Do not follow symbolic links listed on the
-command line unless the @option{--dereference-command-line} (@option{-H}),
-@option{--dereference} (@option{-L}), or
-@option{--dereference-command-line-symlink-to-dir} options are specified.
+command line unless the @code{--dereference-command-line} (@code{-H}),
+@code{--dereference} (@code{-L}), or
+@code{--dereference-command-line-symlink-to-dir} options are specified.
 
 @item --indicator-style=@var{word}
 @opindex --indicator-style
@@ -5696,18 +5695,18 @@
 @item file-type
 Append @samp{/} for directories, @samp{@@} for symbolic links, @samp{|}
 for FIFOs, @samp{=} for sockets, and nothing for regular files.  This is
-the same as the @option{-p} or @option{--file-type} option.
+the same as the @code{-p} or @code{--file-type} option.
 @item classify
 Append @samp{*} for executable regular files, otherwise behave as for
-@samp{file-type}.  This is the same as the @option{-F} or
-@option{--classify} option.
+@samp{file-type}.  This is the same as the @code{-F} or
+@code{--classify} option.
 @end table
 
 @item -k
 @opindex -k
 Print file sizes in 1024-byte blocks, overriding the default block
 size (@pxref{Block size}).
-This option is equivalent to @option{--block-size=1K}.
+This option is equivalent to @code{--block-size=1K}.
 
 @item -m
 @itemx --format=commas
@@ -5724,7 +5723,7 @@
 @opindex --indicator-style
 @cindex file type, marking
 Append a character to each file name indicating the file type. This is
-like @option{-F}, except that executables are not marked.
+like @code{-F}, except that executables are not marked.
 
 @item -x @var{format}
 @itemx --format=across
@@ -5740,7 +5739,7 @@
 @opindex -T
 @opindex --tabsize
 Assume that each tabstop is @var{cols} columns wide.  The default is 8.
-@command{ls} uses tabs where possible in the output, for efficiency.  If
+@file{ls} uses tabs where possible in the output, for efficiency.  If
 @var{cols} is zero, do not use tabs at all.
 
 @item -w
@@ -5750,7 +5749,7 @@
 @vindex COLUMNS
 Assume the screen is @var{cols} columns wide.  The default is taken
 from the terminal settings if possible; otherwise the environment
-variable @env{COLUMNS} is used if it is set; otherwise the default
+variable @var{COLUMNS} is used if it is set; otherwise the default
 is 80.
 
 @end table
@@ -5761,7 +5760,7 @@
 
 By default, file timestamps are listed in abbreviated form.  Most
 locales use a timestamp like @samp{2002-03-30 23:45}.  However, the
-default @acronym{POSIX} locale uses a date like @samp{Mar 30@ @ 2002}
+default @b{POSIX} locale uses a date like @samp{Mar 30@ @ 2002}
 for non-recent timestamps, and a date-without-year and time like
 @samp{Mar 30 23:45} for recent timestamps.
 
@@ -5769,7 +5768,7 @@
 months old, and is not dated in the future.  If a timestamp dated
 today is not listed in recent form, the timestamp is in the future,
 which means you probably have clock skew problems which may break
-programs like @command{make} that rely on file timestamps.
+programs like @file{make} that rely on file timestamps.
 
 The following option changes how file timestamps are printed.
 
@@ -5784,11 +5783,11 @@
 @item +@var{format}
 @vindex LC_TIME
 List timestamps using @var{format}, where @var{format} is interpreted
-like the format argument of @command{date} (@pxref{date invocation}).
-For example, @option{--time-style="+%Y-%m-%d %H:%M:%S"} causes
-@command{ls} to list timestamps like @samp{2002-03-30 23:45:56}.  As
-with @command{date}, @var{format}'s interpretation is affected by the
-@env{LC_TIME} locale category.
+like the format argument of @file{date} (@pxref{date invocation}).
+For example, @code{--time-style="+%Y-%m-%d %H:%M:%S"} causes
+@file{ls} to list timestamps like @samp{2002-03-30 23:45:56}.  As
+with @file{date}, @var{format}'s interpretation is affected by the
+@var{LC_TIME} locale category.
 
 If @var{format} contains two format strings separated by a newline,
 the former is used for non-recent files and the latter for recent
@@ -5796,30 +5795,30 @@
 spaces in one of the two formats.
 
 @item full-iso
-List timestamps in full using @acronym{ISO} 8601 date, time, and time zone
+List timestamps in full using @b{ISO} 8601 date, time, and time zone
 format with nanosecond precision, e.g., @samp{2002-03-30
 23:45:56.477817180 -0700}.  This style is equivalent to
 @samp{+%Y-%m-%d %H:%M:%S.%N %z}.
 
 This is useful because the time output includes all the information that
 is available from the operating system.  For example, this can help
-explain @command{make}'s behavior, since @acronym{GNU} @command{make}
+explain @file{make}'s behavior, since @b{GNU} @file{make}
 uses the full timestamp to determine whether a file is out of date.
 
 @item long-iso
-List @acronym{ISO} 8601 date and time in minutes, e.g.,
+List @b{ISO} 8601 date and time in minutes, e.g.,
 @samp{2002-03-30 23:45}.  These timestamps are shorter than
 @samp{full-iso} timestamps, and are usually good enough for everyday
 work.  This style is equivalent to @samp{%Y-%m-%d %H:%M}.
 
 @item iso
-List @acronym{ISO} 8601 dates for non-recent timestamps (e.g.,
-@samp{2002-03-30@ }), and @acronym{ISO} 8601 month, day, hour, and
+List @b{ISO} 8601 dates for non-recent timestamps (e.g.,
+@samp{2002-03-30@ }), and @b{ISO} 8601 month, day, hour, and
 minute for recent timestamps (e.g., @samp{03-30 23:45}).  These
 timestamps are uglier than @samp{long-iso} timestamps, but they carry
 nearly the same information in a smaller space and their brevity helps
-@command{ls} output fit within traditional 80-column output lines.
-The following two @command{ls} invocations are equivalent:
+@file{ls} output fit within traditional 80-column output lines.
+The following two @file{ls} invocations are equivalent:
 
 @example
 newline='
@@ -5837,10 +5836,10 @@
 are harder for programs to parse because locale conventions vary so
 widely, but they are easier for many people to read.
 
-The @env{LC_TIME} locale category specifies the timestamp format.  The
-default @acronym{POSIX} locale uses timestamps like @samp{Mar 30@
+The @var{LC_TIME} locale category specifies the timestamp format.  The
+default @b{POSIX} locale uses timestamps like @samp{Mar 30@
 @ 2002} and @samp{Mar 30 23:45}; in this locale, the following two
-@command{ls} invocations are equivalent:
+@file{ls} invocations are equivalent:
 
 @example
 newline='
@@ -5850,27 +5849,27 @@
 @end example
 
 Other locales behave differently.  For example, in a German locale,
-@option{--time-style="locale"} might be equivalent to
-@option{--time-style="+%e. %b %Y $newline%e. %b %H:%M"}
+@code{--time-style="locale"} might be equivalent to
+@code{--time-style="+%e. %b %Y $newline%e. %b %H:%M"}
 and might generate timestamps like @samp{30. M@"ar 2002@ } and
 @samp{30. M@"ar 23:45}.
 
 @item posix-@var{style}
 @vindex LC_TIME
-List @acronym{POSIX}-locale timestamps if the @env{LC_TIME} locale
-category is @acronym{POSIX}, @var{style} timestamps otherwise.  For
+List @b{POSIX}-locale timestamps if the @var{LC_TIME} locale
+category is @b{POSIX}, @var{style} timestamps otherwise.  For
 example, the default style, which is @samp{posix-long-iso}, lists
 timestamps like @samp{Mar 30@ @ 2002} and @samp{Mar 30 23:45} when in
-the @acronym{POSIX} locale, and like @samp{2002-03-30 23:45} otherwise.
+the @b{POSIX} locale, and like @samp{2002-03-30 23:45} otherwise.
 @end table
 @end table
 
 @vindex TIME_STYLE
-You can specify the default value of the @option{--time-style} option
-with the environment variable @env{TIME_STYLE}; if @env{TIME_STYLE} is not set
-the default style is @samp{posix-long-iso}.  @acronym{GNU} Emacs 21 and
-later can parse @acronym{ISO} dates, but older Emacs versions do not, so if
-you are using an older version of Emacs and specify a non-@acronym{POSIX}
+You can specify the default value of the @code{--time-style} option
+with the environment variable @var{TIME_STYLE}; if @var{TIME_STYLE} is not set
+the default style is @samp{posix-long-iso}.  @b{GNU} Emacs 21 and
+later can parse @b{ISO} dates, but older Emacs versions do not, so if
+you are using an older version of Emacs and specify a non-@b{POSIX}
 locale, you may need to set @samp{TIME_STYLE="locale"}.
 
 
@@ -5905,7 +5904,7 @@
 @opindex --hide-control-chars
 Print question marks instead of nongraphic characters in file names.
 This is the default if the output is a terminal and the program is
-@command{ls}.
+@file{ls}.
 
 @item -Q
 @itemx --quote-name
@@ -5924,24 +5923,24 @@
 be one of the following:
 @table @samp
 @item literal
-Output strings as-is; this is the same as the @option{-N} or
-@option{--literal} option.
+Output strings as-is; this is the same as the @code{-N} or
+@code{--literal} option.
 @item shell
 Quote strings for the shell if they contain shell metacharacters or would
 cause ambiguous output.
-The quoting is suitable for @acronym{POSIX}-compatible shells like
-@command{bash}, but it does not always work for incompatible shells
-like @command{csh}.
+The quoting is suitable for @b{POSIX}-compatible shells like
+@file{bash}, but it does not always work for incompatible shells
+like @file{csh}.
 @item shell-always
 Quote strings for the shell, even if they would normally not require quoting.
 @item c
 Quote strings as for C character string literals, including the
 surrounding double-quote characters; this is the same as the
-@option{-Q} or @option{--quote-name} option.
+@code{-Q} or @code{--quote-name} option.
 @item escape
 Quote strings as for C character string literals, except omit the
 surrounding double-quote
-characters; this is the same as the @option{-b} or @option{--escape} option.
+characters; this is the same as the @code{-b} or @code{--escape} option.
 @item clocale
 Quote strings as for C character string literals, except use
 surrounding quotation marks appropriate for the
@@ -5954,8 +5953,8 @@
 this"} in the default C locale.  This looks nicer on many displays.
 @end table
 
-You can specify the default value of the @option{--quoting-style} option
-with the environment variable @env{QUOTING_STYLE}.  If that environment
+You can specify the default value of the @code{--quoting-style} option
+with the environment variable @var{QUOTING_STYLE}.  If that environment
 variable is not set, the default value is @samp{literal}, but this
 default may change to @samp{shell} in a future version of this package.
 
@@ -5963,59 +5962,59 @@
 @opindex --show-control-chars
 Print nongraphic characters as-is in file names.
 This is the default unless the output is a terminal and the program is
-@command{ls}.
+@file{ls}.
 
 @end table
 
 
 @node dir invocation
-@section @command{dir}: Briefly list directory contents
+@section @file{dir}: Briefly list directory contents
 
 @pindex dir
 @cindex directory listing, brief
 
-@command{dir} (also installed as @command{d}) is equivalent to @code{ls -C
+@file{dir} (also installed as @file{d}) is equivalent to @code{ls -C
 -b}; that is, by default files are listed in columns, sorted vertically,
 and special characters are represented by backslash escape sequences.
 
-@xref{ls invocation, @command{ls}}.
+@xref{ls invocation, @file{ls}}.
 
 
 @node vdir invocation
-@section @command{vdir}: Verbosely list directory contents
+@section @file{vdir}: Verbosely list directory contents
 
 @pindex vdir
 @cindex directory listing, verbose
 
-@command{vdir} (also installed as @command{v}) is equivalent to @code{ls -l
+@file{vdir} (also installed as @file{v}) is equivalent to @code{ls -l
 -b}; that is, by default files are listed in long format and special
 characters are represented by backslash escape sequences.
 
 @node dircolors invocation
-@section @command{dircolors}: Color setup for @command{ls}
+@section @file{dircolors}: Color setup for @file{ls}
 
 @pindex dircolors
 @cindex color setup
 @cindex setup for color
 
-@command{dircolors} outputs a sequence of shell commands to set up the
-terminal for color output from @command{ls} (and @command{dir}, etc.).
+@file{dircolors} outputs a sequence of shell commands to set up the
+terminal for color output from @file{ls} (and @file{dir}, etc.).
 Typical usage:
 
 @example
 eval `dircolors [@var{option}]@dots{} [@var{file}]`
 @end example
 
-If @var{file} is specified, @command{dircolors} reads it to determine which
+If @var{file} is specified, @file{dircolors} reads it to determine which
 colors to use for which file types and extensions.  Otherwise, a
 precompiled database is used.  For details on the format of these files,
 run @samp{dircolors --print-database}.
 
 @vindex LS_COLORS
 @vindex SHELL @r{environment variable, and color}
-The output is a shell command to set the @env{LS_COLORS} environment
+The output is a shell command to set the @var{LS_COLORS} environment
 variable.  You can specify the shell syntax to use on the command line,
-or @command{dircolors} will guess it from the value of the @env{SHELL}
+or @file{dircolors} will guess it from the value of the @var{SHELL}
 environment variable.
 
 The program accepts the following options.  Also see @ref{Common options}.
@@ -6028,8 +6027,8 @@
 @opindex --sh
 @opindex --bourne-shell
 @cindex Bourne shell syntax for color setup
-@cindex @command{sh} syntax for color setup
-Output Bourne shell commands.  This is the default if the @env{SHELL}
+@cindex @file{sh} syntax for color setup
+Output Bourne shell commands.  This is the default if the @var{SHELL}
 environment variable is set and does not end with @samp{csh} or
 @samp{tcsh}.
 
@@ -6040,9 +6039,9 @@
 @opindex --csh
 @opindex --c-shell
 @cindex C shell syntax for color setup
-@cindex @command{csh} syntax for color setup
+@cindex @file{csh} syntax for color setup
 Output C shell commands.  This is the default if @code{SHELL} ends with
-@command{csh} or @command{tcsh}.
+@file{csh} or @file{tcsh}.
 
 @item -p
 @itemx --print-database
@@ -6079,14 +6078,14 @@
 
 
 @node cp invocation
-@section @command{cp}: Copy files and directories
+@section @file{cp}: Copy files and directories
 
 @pindex cp
 @cindex copying files and directories
 @cindex files, copying
 @cindex directories, copying
 
-@command{cp} copies files (or, optionally, directories).  The copy is
+@file{cp} copies files (or, optionally, directories).  The copy is
 completely independent of the original.  You can either copy one file to
 another, or copy arbitrarily many files to a destination directory.
 Synopsis:
@@ -6096,37 +6095,37 @@
 cp [@var{option}]@dots{} @var{source}@dots{} @var{directory}
 @end example
 
-If the last argument names an existing directory, @command{cp} copies each
+If the last argument names an existing directory, @file{cp} copies each
 @var{source} file into that directory (retaining the same name).
 Otherwise, if only two files are given, it copies the first onto the
 second.  It is an error if the last argument is not a directory and more
 than two non-option arguments are given.
 
 Generally, files are written just as they are read.  For exceptions,
-see the @option{--sparse} option below.
+see the @code{--sparse} option below.
 
-By default, @command{cp} does not copy directories.  However, the
-@option{-R}, @option{-a}, and @option{-r} options cause @command{cp} to
+By default, @file{cp} does not copy directories.  However, the
+@code{-R}, @code{-a}, and @code{-r} options cause @file{cp} to
 copy recursively by descending into source directories and copying files
 to corresponding destination directories.
 
-By default, @command{cp} follows symbolic links only when not copying
+By default, @file{cp} follows symbolic links only when not copying
 recursively.  This default can be overridden with the
-@option{--archive} (@option{-a}), @option{-d}, @option{--dereference}
-(@option{-L}), @option{--no-dereference} (@option{-P}), and
-@option{-H} options.  If more than one of these options is specified,
+@code{--archive} (@code{-a}), @code{-d}, @code{--dereference}
+(@code{-L}), @code{--no-dereference} (@code{-P}), and
+@code{-H} options.  If more than one of these options is specified,
 the last one silently overrides the others.
 
-By default, @command{cp} copies the contents of special files only
+By default, @file{cp} copies the contents of special files only
 when not copying recursively.  This default can be overridden with the
-@option{--copy-contents} option.
+@code{--copy-contents} option.
 
 @cindex self-backups
 @cindex backups, making only
-@command{cp} generally refuses to copy a file onto itself, with the
-following exception: if @option{--force --backup} is specified with
+@file{cp} generally refuses to copy a file onto itself, with the
+following exception: if @code{--force --backup} is specified with
 @var{source} and @var{dest} identical, and referring to a regular file,
-@command{cp} will make a backup file, either regular or numbered, as
+@file{cp} will make a backup file, either regular or numbered, as
 specified in the usual ways (@pxref{Backup options}).  This is useful when
 you simply want to make a backup of an existing file before changing it.
 
@@ -6141,7 +6140,7 @@
 original files in the copy (but do not attempt to preserve internal
 directory structure; i.e., @samp{ls -U} may list the entries in a copied
 directory in a different order).
-Equivalent to @option{-dpPR}.
+Equivalent to @code{-dpPR}.
 
 @item -b
 @itemx @w{@kbd{--backup}[=@var{method}]}
@@ -6151,7 +6150,7 @@
 @cindex backups, making
 @xref{Backup options}.
 Make a backup of each file that would otherwise be overwritten or removed.
-As a special case, @command{cp} makes a backup of @var{source} when the force
+As a special case, @file{cp} makes a backup of @var{source} when the force
 and backup options are given and @var{source} and @var{dest} are the same
 name for an existing, regular file.  One useful application of this
 combination of options is this tiny Bourne shell script:
@@ -6188,19 +6187,19 @@
 @cindex hard links, preserving
 Copy symbolic links as symbolic links rather than copying the files that
 they point to, and preserve hard links between source files in the copies.
-Equivalent to @option{--no-dereference --preserve=links}.
+Equivalent to @code{--no-dereference --preserve=links}.
 
 @item -f
 @itemx --force
 @opindex -f
 @opindex --force
 When copying without this option and an existing destination file cannot
-be opened for writing, the copy fails.  However, with @option{--force}),
-when a destination file cannot be opened, @command{cp} then unlinks it and
+be opened for writing, the copy fails.  However, with @code{--force}),
+when a destination file cannot be opened, @file{cp} then unlinks it and
 tries to open it again.  Contrast this behavior with that enabled by
-@option{--link} and @option{--symbolic-link}, whereby the destination file
+@code{--link} and @code{--symbolic-link}, whereby the destination file
 is never opened but rather is unlinked unconditionally.  Also see the
-description of @option{--remove-destination}.
+description of @code{--remove-destination}.
 
 @item -H
 @opindex -H
@@ -6265,8 +6264,8 @@
 @c Mention ACLs here.
 @end table
 
-Using @option{--preserve} with no @var{attribute_list} is equivalent
-to @option{--preserve=mode,ownership,timestamps}.
+Using @code{--preserve} with no @var{attribute_list} is equivalent
+to @code{--preserve=mode,ownership,timestamps}.
 
 In the absence of this option, each destination file is created with the
 permissions of the corresponding source file, minus the bits set in the
@@ -6275,14 +6274,14 @@
 @itemx @w{@kbd{--no-preserve}=@var{attribute_list}}
 @cindex file information, preserving
 Do not preserve the specified attributes.  The @var{attribute_list}
-has the same form as for @option{--preserve}.
+has the same form as for @code{--preserve}.
 
 @itemx --parents
 @opindex --parents
-@cindex parent directories and @command{cp}
+@cindex parent directories and @file{cp}
 Form the name of each destination file by appending to the target
 directory a slash and the specified name of the source file.  The last
-argument given to @command{cp} must be the name of an existing directory.
+argument given to @file{cp} must be the name of an existing directory.
 For example, the command:
 
 @example
@@ -6296,12 +6295,12 @@
 @itemx @w{@kbd{--reply}[=@var{how}]}
 @opindex --reply
 @cindex interactivity
-Using @option{--reply=yes} makes @command{cp} act as if @samp{yes} were
+Using @code{--reply=yes} makes @file{cp} act as if @samp{yes} were
 given as a response to every prompt about a destination file.  That effectively
-cancels any preceding @option{--interactive} or @option{-i} option.
-Specify @option{--reply=no} to make @command{cp} act as if @samp{no} were
+cancels any preceding @code{--interactive} or @code{-i} option.
+Specify @code{--reply=no} to make @file{cp} act as if @samp{no} were
 given as a response to every prompt about a destination file.
-Specify @option{--reply=query} to make @command{cp} prompt the user
+Specify @code{--reply=query} to make @file{cp} prompt the user
 about each existing destination file.
 
 @item -R
@@ -6315,22 +6314,22 @@
 @cindex recursively copying directories
 @cindex non-directories, copying as special files
 Copy directories recursively.  Symbolic links are not followed by
-default; see the @option{--archive} (@option{-a}), @option{-d},
-@option{--dereference} (@option{-L}), @option{--no-dereference}
-(@option{-P}), and @option{-H} options.  Special files are copied by
+default; see the @code{--archive} (@code{-a}), @code{-d},
+@code{--dereference} (@code{-L}), @code{--no-dereference}
+(@code{-P}), and @code{-H} options.  Special files are copied by
 creating a destination file of the same type as the source; see the
-@option{--copy-contents} option.  It is not portable to use
-@option{-r} to copy symbolic links or special files.  On some
-non-@sc{gnu} systems, @option{-r} implies the equivalent of
-@option{-L} and @option{--copy-contents} for historical reasons.
-Also, it is not portable to use @option{-R} to copy symbolic links
-unless you also specify @option{-P}, as @acronym{POSIX} allows
+@code{--copy-contents} option.  It is not portable to use
+@code{-r} to copy symbolic links or special files.  On some
+non-@sc{gnu} systems, @code{-r} implies the equivalent of
+@code{-L} and @code{--copy-contents} for historical reasons.
+Also, it is not portable to use @code{-R} to copy symbolic links
+unless you also specify @code{-P}, as @b{POSIX} allows
 implementations that dereference symbolic links by default.
 
 @item --remove-destination
 @opindex --remove-destination
 Remove each existing destination file before attempting to open it
-(contrast with @option{-f} above).
+(contrast with @code{-f} above).
 
 @item --sparse=@var{when}
 @opindex --sparse=@var{when}
@@ -6341,7 +6340,7 @@
 does not occupy any physical disk blocks; the @samp{read} system call
 reads these as zeroes.  This can both save considerable disk space and
 increase speed, since many binary files contain lots of consecutive zero
-bytes.  By default, @command{cp} detects holes in input source files via a crude
+bytes.  By default, @file{cp} detects holes in input source files via a crude
 heuristic and makes the corresponding output file sparse as well.
 Only regular files may be sparse.
 
@@ -6361,11 +6360,11 @@
 (for example,  @samp{efs} filesystems in SGI IRIX 5.3 and earlier),
 but the output file is on a type of filesystem that does support them.
 Holes may be created only in regular files, so if the destination file
-is of some other type, @command{cp} does not even try to make it sparse.
+is of some other type, @file{cp} does not even try to make it sparse.
 
 @item never
 Never make the output file sparse.
-This is useful in creating a file for use with the @command{mkswap} command,
+This is useful in creating a file for use with the @file{mkswap} command,
 since such a file must not have any holes.
 @end table
 
@@ -6408,12 +6407,12 @@
 
 
 @node dd invocation
-@section @command{dd}: Convert and copy a file
+@section @file{dd}: Convert and copy a file
 
 @pindex dd
 @cindex converting while copying a file
 
-@command{dd} copies a file (from standard input to standard output, by
+@file{dd} copies a file (from standard input to standard output, by
 default) with a changeable I/O block size, while optionally performing
 conversions on it.  Synopsis:
 
@@ -6429,7 +6428,7 @@
 @samp{w}=2, @samp{x@var{m}}=@var{m}, or any of the
 standard block size suffixes like @samp{k}=1024 (@pxref{Block size}).
 
-Use different @command{dd} invocations to use different block sizes for
+Use different @file{dd} invocations to use different block sizes for
 skipping and I/O.  For example, the following shell commands copy data
 in 512 KiB blocks between a disk and a tape, but do not save or restore a
 4 KiB label at the start of the disk:
@@ -6445,13 +6444,13 @@
 (dd bs=4k seek=1 count=0 && dd bs=512k) <$tape >$disk
 @end example
 
-Note that sending a @samp{SIGUSR1} signal to a running @command{dd}
+Note that sending a @samp{SIGUSR1} signal to a running @file{dd}
 process makes it print to standard error the number of records read
 and written so far, then to resume copying.  In the example below,
-@command{dd} is run in the background to copy 10 million blocks.
-The @command{kill} command makes it output the first pair of
+@file{dd} is run in the background to copy 10 million blocks.
+The @file{kill} command makes it output the first pair of
 intermediate record counts,
-and when @command{dd} completes, it outputs the final pair.
+and when @file{dd} completes, it outputs the final pair.
 
 @example
 $ dd if=/dev/zero of=/dev/null count=10M & pid=$!
@@ -6472,7 +6471,7 @@
 @item of=@var{file}
 @opindex of
 Write to @var{file} instead of standard output.  Unless
-@samp{conv=notrunc} is given, @command{dd} truncates @var{file} to zero
+@samp{conv=notrunc} is given, @file{dd} truncates @var{file} to zero
 bytes (or the size specified with @samp{seek=}).
 
 @item ibs=@var{bytes}
@@ -6523,15 +6522,15 @@
 
 @item ascii
 @opindex ascii@r{, converting to}
-Convert @acronym{EBCDIC} to @acronym{ASCII}.
+Convert @b{EBCDIC} to @b{ASCII}.
 
 @item ebcdic
 @opindex ebcdic@r{, converting to}
-Convert @acronym{ASCII} to @acronym{EBCDIC}.
+Convert @b{ASCII} to @b{EBCDIC}.
 
 @item ibm
 @opindex alternate ebcdic@r{, converting to}
-Convert @acronym{ASCII} to alternate @acronym{EBCDIC}.
+Convert @b{ASCII} to alternate @b{EBCDIC}.
 
 @item block
 @opindex block @r{(space-padding)}
@@ -6554,7 +6553,7 @@
 @item swab
 @opindex swab @r{(byte-swapping)}
 @cindex byte-swapping
-Swap every pair of input bytes.  @sc{gnu} @command{dd}, unlike others, works
+Swap every pair of input bytes.  @sc{gnu} @file{dd}, unlike others, works
 when an odd number of bytes are read---the last byte is simply copied
 (since there is nothing to swap it with).
 
@@ -6581,12 +6580,12 @@
 
 
 @node install invocation
-@section @command{install}: Copy files and set attributes
+@section @file{install}: Copy files and set attributes
 
 @pindex install
 @cindex copying files and setting attributes
 
-@command{install} copies files while setting their permission modes and, if
+@file{install} copies files while setting their permission modes and, if
 possible, their owner and group.  Synopses:
 
 @example
@@ -6601,7 +6600,7 @@
 (and any missing parent directories) is created.
 
 @cindex Makefiles, installing programs in
-@command{install} is similar to @command{cp}, but allows you to control the
+@file{install} is similar to @file{cp}, but allows you to control the
 attributes of destination files.  It is typically used in Makefiles to
 copy programs into their destination directories.  It refuses to copy
 files onto themselves.
@@ -6614,7 +6613,7 @@
 
 @item -c
 @opindex -c
-Ignored; for compatibility with old Unix versions of @command{install}.
+Ignored; for compatibility with old Unix versions of @file{install}.
 
 @item -d
 @itemx --directory
@@ -6626,7 +6625,7 @@
 Create each given directory and any missing parent directories, setting
 the owner, group and mode as given on the command line or to the
 defaults.  It also gives any parent directories it creates those
-attributes.  (This is different from the SunOS 4.x @command{install}, which
+attributes.  (This is different from the SunOS 4.x @file{install}, which
 gives directories that it creates the default attributes.)
 
 @item -g @var{group}
@@ -6645,7 +6644,7 @@
 @cindex permissions of installed files, setting
 Set the permissions for the installed file or directory to @var{mode},
 which can be either an octal number, or a symbolic mode as in
-@command{chmod}, with 0 as the point of departure (@pxref{File
+@file{chmod}, with 0 as the point of departure (@pxref{File
 permissions}).  The default mode is @samp{u=rwx,go=rx}---read, write,
 and execute for the owner, and read and execute for group and other.
 
@@ -6656,7 +6655,7 @@
 @cindex ownership of installed files, setting
 @cindex appropriate privileges
 @vindex root @r{as default owner}
-If @command{install} has appropriate privileges (is run as root), set the
+If @file{install} has appropriate privileges (is run as root), set the
 ownership of installed files or directories to @var{owner}. The default
 is @code{root}.  @var{owner} may be either a user name or a numeric user
 ID.
@@ -6700,27 +6699,27 @@
 
 
 @node mv invocation
-@section @command{mv}: Move (rename) files
+@section @file{mv}: Move (rename) files
 
 @pindex mv
 
-@command{mv} moves or renames files (or directories).  Synopsis:
+@file{mv} moves or renames files (or directories).  Synopsis:
 
 @example
 mv [@var{option}]@dots{} @var{source} @var{dest}
 mv [@var{option}]@dots{} @var{source}@dots{} @var{directory}
 @end example
 
-If the last argument names an existing directory, @command{mv} moves each
+If the last argument names an existing directory, @file{mv} moves each
 other given file into a file with the same name in that directory.
 Otherwise, if only two files are given, it renames the first as
 the second.  It is an error if the last argument is not a directory
 and more than two files are given.
 
-@command{mv} can move any type of file from one filesystem to another.
+@file{mv} can move any type of file from one filesystem to another.
 Prior to version @code{4.0} of the fileutils,
-@command{mv} could move only regular files between filesystems.
-For example, now @command{mv} can move an entire directory hierarchy
+@file{mv} could move only regular files between filesystems.
+For example, now @file{mv} can move an entire directory hierarchy
 including special device files from one partition to another.  It first
 uses some of the same code that's used by @code{cp -a} to copy the
 requested directories and files, then (assuming the copy succeeded)
@@ -6731,15 +6730,15 @@
 the destination partition and the second and third would be left on the
 original partition.
 
-@cindex prompting, and @command{mv}
+@cindex prompting, and @file{mv}
 If a destination file exists but is normally unwritable, standard input
-is a terminal, and the @option{-f} or @option{--force} option is not given,
-@command{mv} prompts the user for whether to replace the file.  (You might
+is a terminal, and the @code{-f} or @code{--force} option is not given,
+@file{mv} prompts the user for whether to replace the file.  (You might
 own the file, or have write permission on its directory.)  If the
 response does not begin with @samp{y} or @samp{Y}, the file is skipped.
 
 @emph{Warning}: If you try to move a symlink that points to a directory,
-and you specify the symlink with a trailing slash, then @command{mv}
+and you specify the symlink with a trailing slash, then @file{mv}
 doesn't move the symlink but instead moves the directory referenced
 by the symlink.  @xref{Trailing slashes}.
 
@@ -6768,10 +6767,10 @@
 @itemx @w{@kbd{--reply}[=@var{how}]}
 @opindex --reply
 @cindex interactivity
-Specifying @option{--reply=yes} is equivalent to using @option{--force}.
-Specify @option{--reply=no} to make @command{mv} act as if @samp{no} were
+Specifying @code{--reply=yes} is equivalent to using @code{--force}.
+Specify @code{--reply=no} to make @file{mv} act as if @samp{no} were
 given as a response to every prompt about a destination file.
-Specify @option{--reply=query} to make @command{mv} prompt the user
+Specify @code{--reply=query} to make @file{mv} prompt the user
 about each existing destination file.
 
 @item -u
@@ -6802,28 +6801,28 @@
 
 
 @node rm invocation
-@section @command{rm}: Remove files or directories
+@section @file{rm}: Remove files or directories
 
 @pindex rm
 @cindex removing files or directories
 
-@command{rm} removes each given @var{file}.  By default, it does not remove
+@file{rm} removes each given @var{file}.  By default, it does not remove
 directories.  Synopsis:
 
 @example
 rm [@var{option}]@dots{} [@var{file}]@dots{}
 @end example
 
-@cindex prompting, and @command{rm}
-If a file is unwritable, standard input is a terminal, and the @option{-f}
-or @option{--force} option is not given, or the @option{-i} or
-@option{--interactive} option @emph{is} given, @command{rm} prompts the user
+@cindex prompting, and @file{rm}
+If a file is unwritable, standard input is a terminal, and the @code{-f}
+or @code{--force} option is not given, or the @code{-i} or
+@code{--interactive} option @emph{is} given, @file{rm} prompts the user
 for whether to remove the file.  If the response does not begin with
 @samp{y} or @samp{Y}, the file is skipped.
 
-@emph{Warning}: If you use @command{rm} to remove a file, it is usually
+@emph{Warning}: If you use @file{rm} to remove a file, it is usually
 possible to recover the contents of that file.  If you want more assurance
-that the contents are truly unrecoverable, consider using @command{shred}.
+that the contents are truly unrecoverable, consider using @file{shred}.
 
 The program accepts the following options.  Also see @ref{Common options}.
 
@@ -6841,7 +6840,7 @@
 don't require a directory to be empty before trying to unlink it.  This works
 only if you have appropriate privileges and if your operating system supports
 @code{unlink} for directories.  Because unlinking a directory causes any files
-in the deleted directory to become unreferenced, it is wise to @command{fsck}
+in the deleted directory to become unreferenced, it is wise to @file{fsck}
 the filesystem after doing this.
 
 @item -f
@@ -6849,7 +6848,7 @@
 @opindex -f
 @opindex --force
 Ignore nonexistent files and never prompt the user.
-Ignore any previous @option{--interactive} (@option{-i}) option.
+Ignore any previous @code{--interactive} (@code{-i}) option.
 
 @item -i
 @itemx --interactive
@@ -6857,20 +6856,20 @@
 @opindex --interactive
 Prompt whether to remove each file.  If the response does not begin
 with @samp{y} or @samp{Y}, the file is skipped.
-Ignore any previous @option{--force} (@option{-f}) option.
+Ignore any previous @code{--force} (@code{-f}) option.
 
 @itemx --preserve-root
 @opindex --preserve-root
 @cindex root directory, disallow recursive destruction
 Fail upon any attempt to remove the filesystem root, @file{/},
-when used with the @option{--recursive} option.
-Without @option{--recursive}, this option has no effect.
+when used with the @code{--recursive} option.
+Without @code{--recursive}, this option has no effect.
 @xref{Treating / specially}.
 
 @itemx --no-preserve-root
 @opindex --no-preserve-root
 @cindex root directory, allow recursive destruction
-Cancel the effect of any preceding @option{--preserve-root} option.
+Cancel the effect of any preceding @code{--preserve-root} option.
 @xref{Treating / specially}.
 
 @item -r
@@ -6893,7 +6892,7 @@
 @cindex files beginning with @samp{-}, removing
 @cindex @samp{-}, removing files beginning with
 One common question is how to remove files whose names begin with a
-@samp{-}.  @sc{gnu} @command{rm}, like every program that uses the @code{getopt}
+@samp{-}.  @sc{gnu} @file{rm}, like every program that uses the @code{getopt}
 function to parse its arguments, lets you use the @samp{--} option to
 indicate that all following arguments are non-options.  To remove a file
 called @file{-f} in the current directory, you could type either:
@@ -6909,21 +6908,21 @@
 rm ./-f
 @end example
 
-@opindex - @r{and Unix @command{rm}}
-The Unix @command{rm} program's use of a single @samp{-} for this purpose
+@opindex - @r{and Unix @file{rm}}
+The Unix @file{rm} program's use of a single @samp{-} for this purpose
 predates the development of the getopt standard syntax.
 
 @exitstatus
 
 
 @node shred invocation
-@section @command{shred}: Remove files more securely
+@section @file{shred}: Remove files more securely
 
 @pindex shred
 @cindex data, erasing
 @cindex erasing data
 
-@command{shred} overwrites devices or files, to help prevent even
+@file{shred} overwrites devices or files, to help prevent even
 very expensive hardware from recovering the data.
 
 Ordinarily when you remove a file (@pxref{rm invocation}), the data is
@@ -6946,7 +6945,7 @@
 The best way to remove something irretrievably is to destroy the media
 it's on with acid, melt it down, or the like.  For cheap removable media
 like floppy disks, this is the preferred method.  However, hard drives
-are expensive and hard to melt, so the @command{shred} utility tries
+are expensive and hard to melt, so the @file{shred} utility tries
 to achieve a similar effect non-destructively.
 
 This uses many overwrite passes, with the data patterns chosen to
@@ -6958,7 +6957,7 @@
 California, 22--25 July, 1996).  The paper is also available online
 @url{http://www.cs.auckland.ac.nz/~pgut001/pubs/secure_del.html}.
 
-@strong{Please note} that @command{shred} relies on a very important assumption:
+@strong{Please note} that @file{shred} relies on a very important assumption:
 that the filesystem overwrites data in place.  This is the traditional
 way to do things, but many modern filesystem designs do not satisfy this
 assumption.  Exceptions include:
@@ -6992,12 +6991,12 @@
 since this bypasses the problem of filesystem design mentioned above.
 However, even shredding devices is not always completely reliable.  For
 example, most disks map out bad sectors invisibly to the application; if
-the bad sectors contain sensitive data, @command{shred} won't be able to
+the bad sectors contain sensitive data, @file{shred} won't be able to
 destroy it.
 
-@command{shred} makes no attempt to detect or report this problem, just as
+@file{shred} makes no attempt to detect or report this problem, just as
 it makes no attempt to do anything about backups.  However, since it is
-more reliable to shred devices than files, @command{shred} by default does
+more reliable to shred devices than files, @file{shred} by default does
 not truncate or remove the output file.  This default is more suitable
 for devices, which typically cannot be truncated and should not be
 removed.
@@ -7006,7 +7005,7 @@
 File system backups and remote mirrors may contain copies of the
 file that cannot be removed, and that will allow a shredded file
 to be recovered later.  So if you keep any data you may later want
-to destroy using @command{shred}, be sure that it is not backed up or mirrored.
+to destroy using @file{shred}, be sure that it is not backed up or mirrored.
 
 @example
 shred [@var{option}]@dots{} @var{file}[@dots{}]
@@ -7029,7 +7028,7 @@
 @opindex -n @var{NUMBER}
 @opindex --iterations=@var{NUMBER}
 @cindex iterations, selecting the number of
-By default, @command{shred} uses 25 passes of overwrite.  This is enough
+By default, @file{shred} uses 25 passes of overwrite.  This is enough
 for all of the useful overwrite patterns to be used at least once.
 You can reduce this to save time, or increase it if you have a lot of
 time to waste.
@@ -7061,9 +7060,9 @@
 @itemx --exact
 @opindex -x
 @opindex --exact
-By default, @command{shred} rounds the size of a regular file up to the next
+By default, @file{shred} rounds the size of a regular file up to the next
 multiple of the filesystem block size to fully erase the last block of the file.
-Use @option{--exact} to suppress that behavior.
+Use @code{--exact} to suppress that behavior.
 Thus, by default if you shred a 10-byte regular file on a system with 512-byte
 blocks, the resulting file will be 512 bytes long.  With this option,
 shred does not increase the apparent size of the file.
@@ -7072,12 +7071,12 @@
 @itemx --zero
 @opindex -z
 @opindex --zero
-Normally, the last pass that @command{shred} writes is made up of
+Normally, the last pass that @file{shred} writes is made up of
 random data.  If this would be conspicuous on your hard drive (for
 example, because it looks like encrypted data), or you just think
-it's tidier, the @option{--zero} option adds an additional overwrite pass with
+it's tidier, the @code{--zero} option adds an additional overwrite pass with
 all zero bits.  This is in addition to the number of passes specified
-by the @option{--iterations} option.
+by the @code{--iterations} option.
 
 @item -
 @opindex -
@@ -7101,7 +7100,7 @@
 
 Note that the shell command @samp{shred - >file} does not shred the
 contents of @var{file}, since it truncates @var{file} before invoking
-@command{shred}.  Use the command @samp{shred file} or (if using a
+@file{shred}.  Use the command @samp{shred file} or (if using a
 Bourne-compatible shell) the command @samp{shred - 1<>file} instead.
 
 @end table
@@ -7132,7 +7131,7 @@
 @cindex file types, special
 
 This chapter describes commands which create special types of files (and
-@command{rmdir}, which removes directories, one special file type).
+@file{rmdir}, which removes directories, one special file type).
 
 @cindex special file types
 @cindex file types
@@ -7161,14 +7160,14 @@
 
 
 @node link invocation
-@section @command{link}: Make a hard link via the link syscall
+@section @file{link}: Make a hard link via the link syscall
 
 @pindex link
 @cindex links, creating
 @cindex hard links, creating
 @cindex creating links (hard only)
 
-@command{link} creates a single hard link at a time.
+@file{link} creates a single hard link at a time.
 It is a minimalist interface to the system-provided
 @code{link} function.  @xref{Hard Links, , , libc,
 The GNU C Library Reference Manual}.
@@ -7180,14 +7179,14 @@
 
 @var{filename} must specify an existing file, and @var{linkname}
 must specify a nonexistent entry in an existing directory.
-@command{link} simply calls @code{link (@var{filename}, @var{linkname})}
+@file{link} simply calls @code{link (@var{filename}, @var{linkname})}
 to create the link.
 
 @exitstatus
 
 
 @node ln invocation
-@section @command{ln}: Make links between files
+@section @file{ln}: Make links between files
 
 @pindex ln
 @cindex links, creating
@@ -7196,8 +7195,8 @@
 @cindex creating links (hard or soft)
 
 @cindex filesystems and hard links
-@command{ln} makes links between files.  By default, it makes hard links;
-with the @option{-s} option, it makes symbolic (or @dfn{soft}) links.
+@file{ln} makes links between files.  By default, it makes hard links;
+with the @code{-s} option, it makes symbolic (or @dfn{soft}) links.
 Synopses:
 
 @example
@@ -7207,21 +7206,21 @@
 
 @itemize @bullet
 
-@item If the last argument names an existing directory, @command{ln} creates a
+@item If the last argument names an existing directory, @file{ln} creates a
 link to each @var{target} file in that directory, using the
 @var{target}s' names.  (But see the description of the
-@option{--no-dereference} option below.)
+@code{--no-dereference} option below.)
 
-@item If two filenames are given, @command{ln} creates a link from the
+@item If two filenames are given, @file{ln} creates a link from the
 second to the first.
 
-@item If one @var{target} is given, @command{ln} creates a link to that
+@item If one @var{target} is given, @file{ln} creates a link to that
 file in the current directory.
 
 @item It is an error if the last argument is not a directory and more
-than two files are given.  Without @option{-f} or @option{-i} (see below),
-@command{ln} will not remove an existing file.  Use the @option{--backup}
-option to make @command{ln} rename existing files.
+than two files are given.  Without @code{-f} or @code{-i} (see below),
+@file{ln} will not remove an existing file.  Use the @code{--backup}
+option to make @file{ln} rename existing files.
 
 @end itemize
 
@@ -7233,7 +7232,7 @@
 file---indeed, it is not incorrect to say that the inode @emph{is} the
 file.  On all existing implementations, you cannot make a hard link to
 a directory, and hard links cannot cross filesystem boundaries.  (These
-restrictions are not mandated by @acronym{POSIX}, however.)
+restrictions are not mandated by @b{POSIX}, however.)
 
 @cindex dereferencing symbolic links
 @cindex symbolic link, defined
@@ -7274,7 +7273,7 @@
 @itemx --interactive
 @opindex -i
 @opindex --interactive
-@cindex prompting, and @command{ln}
+@cindex prompting, and @file{ln}
 Prompt whether to remove existing destination files.
 
 @item -n
@@ -7287,10 +7286,10 @@
 When the destination is an actual directory (not a symlink to one),
 there is no ambiguity.  The link is created in that directory.
 But when the specified destination is a symlink to a directory,
-there are two ways to treat the user's request.  @command{ln} can
+there are two ways to treat the user's request.  @file{ln} can
 treat the destination just as it would a normal directory and create
 the link in it.  On the other hand, the destination can be viewed as a
-non-directory---as the symlink itself.  In that case, @command{ln}
+non-directory---as the symlink itself.  In that case, @file{ln}
 must delete or backup that symlink before creating the new link.
 The default is to treat a destination that is a symlink to a directory
 just like a directory.
@@ -7328,24 +7327,24 @@
 
 
 @node mkdir invocation
-@section @command{mkdir}: Make directories
+@section @file{mkdir}: Make directories
 
 @pindex mkdir
 @cindex directories, creating
 @cindex creating directories
 
-@command{mkdir} creates directories with the specified names.  Synopsis:
+@file{mkdir} creates directories with the specified names.  Synopsis:
 
 @example
 mkdir [@var{option}]@dots{} @var{name}@dots{}
 @end example
 
-If a @var{name} is an existing file but not a directory, @command{mkdir} prints
+If a @var{name} is an existing file but not a directory, @file{mkdir} prints
 a warning message on stderr and will exit with a status of 1 after
 processing any remaining @var{name}s.  The same is done when a @var{name} is an
 existing directory and the -p option is not given.  If a @var{name} is an
-existing directory and the -p option is given, @command{mkdir} will ignore it.
-That is, @command{mkdir} will not print a warning, raise an error, or change
+existing directory and the -p option is given, @file{mkdir} will ignore it.
+That is, @file{mkdir} will not print a warning, raise an error, or change
 the mode of the directory (even if the -m option is given), and will
 move on to processing any remaining @var{name}s.
 
@@ -7359,7 +7358,7 @@
 @opindex --mode
 @cindex modes of created directories, setting
 Set the mode of created directories to @var{mode}, which is symbolic as
-in @command{chmod} and uses @samp{a=rwx} (read, write and execute allowed for
+in @file{chmod} and uses @samp{a=rwx} (read, write and execute allowed for
 everyone) minus the bits set in the umask for the point of the
 departure.  @xref{File permissions}.
 
@@ -7377,21 +7376,21 @@
 @opindex -v
 @opindex --verbose
 Print a message for each created directory.  This is most useful with
-@option{--parents}.
+@code{--parents}.
 @end table
 
 @exitstatus
 
 
 @node mkfifo invocation
-@section @command{mkfifo}: Make FIFOs (named pipes)
+@section @file{mkfifo}: Make FIFOs (named pipes)
 
 @pindex mkfifo
 @cindex FIFOs, creating
 @cindex named pipes, creating
 @cindex creating FIFOs (named pipes)
 
-@command{mkfifo} creates FIFOs (also called @dfn{named pipes}) with the
+@file{mkfifo} creates FIFOs (also called @dfn{named pipes}) with the
 specified names.  Synopsis:
 
 @example
@@ -7413,7 +7412,7 @@
 @opindex --mode
 @cindex modes of created FIFOs, setting
 Set the mode of created FIFOs to @var{mode}, which is symbolic as in
-@command{chmod} and uses @samp{a=rw} (read and write allowed for everyone) minus
+@file{chmod} and uses @samp{a=rw} (read and write allowed for everyone) minus
 the bits set in the umask for the point of departure.  @xref{File permissions}.
 
 @end table
@@ -7422,13 +7421,13 @@
 
 
 @node mknod invocation
-@section @command{mknod}: Make block or character special files
+@section @file{mknod}: Make block or character special files
 
 @pindex mknod
 @cindex block special files, creating
 @cindex character special files, creating
 
-@command{mknod} creates a FIFO, character special file, or block special
+@file{mknod} creates a FIFO, character special file, or block special
 file with the specified name.  Synopsis:
 
 @example
@@ -7442,7 +7441,7 @@
 file} has a technical meaning on Unix: something that can generate or
 receive data.  Usually this corresponds to a physical piece of hardware,
 e.g., a printer or a disk.  (These files are typically created at
-system-configuration time.)  The @command{mknod} command is what creates
+system-configuration time.)  The @file{mknod} command is what creates
 files of this type.  Such devices can be read either a character at a
 time or a ``block'' (many characters) at a time, hence we say there are
 @dfn{block special} files and @dfn{character special} files.
@@ -7484,7 +7483,7 @@
 @opindex -m
 @opindex --mode
 Set the mode of created files to @var{mode}, which is symbolic as in
-@command{chmod} and uses @samp{a=rw} minus the bits set in the umask as the point
+@file{chmod} and uses @samp{a=rw} minus the bits set in the umask as the point
 of departure.  @xref{File permissions}.
 
 @end table
@@ -7493,24 +7492,24 @@
 
 
 @node readlink invocation
-@section @command{readlink}: Print the referent of a symbolic link
+@section @file{readlink}: Print the referent of a symbolic link
 
 @pindex readlink
 @cindex displaying value of a symbolic link
 
-@command{readlink} may work in one of two supported modes:
+@file{readlink} may work in one of two supported modes:
 
 @table @samp
 
 @item Readlink mode
 
-@command{readlink} outputs the value of the given symbolic link.
-If @command{readlink} is invoked with an argument other than the pathname
+@file{readlink} outputs the value of the given symbolic link.
+If @file{readlink} is invoked with an argument other than the pathname
 of a symbolic link, it produces no output and exits with a nonzero exit code.
 
 @item Canonicalize mode
 
-@command{readlink} outputs the absolute name of the given file which contains
+@file{readlink} outputs the absolute name of the given file which contains
 no `.', `..' components nor any repeated path separators (`/') or symlinks.
 If any path component is missing or unavailable,
 it produces no output and exits with a nonzero exit code.
@@ -7521,7 +7520,7 @@
 readlink [@var{option}] @var{file}
 @end example
 
-By default, @command{readlink} operates in readlink mode.
+By default, @file{readlink} operates in readlink mode.
 
 The program accepts the following options.  Also see @ref{Common options}.
 
@@ -7557,19 +7556,19 @@
 
 @end table
 
-The @command{readlink} utility first appeared in OpenBSD 2.1.
+The @file{readlink} utility first appeared in OpenBSD 2.1.
 
 @exitstatus
 
 
 @node rmdir invocation
-@section @command{rmdir}: Remove empty directories
+@section @file{rmdir}: Remove empty directories
 
 @pindex rmdir
 @cindex removing empty directories
 @cindex directories, removing empty
 
-@command{rmdir} removes empty directories.  Synopsis:
+@file{rmdir} removes empty directories.  Synopsis:
 
 @example
 rmdir [@var{option}]@dots{} @var{directory}@dots{}
@@ -7596,8 +7595,8 @@
 Remove @var{directory}, then try to remove each component of @var{directory}.
 So, for example, @samp{rmdir -p a/b/c} is similar to @samp{rmdir a/b/c a/b a}.
 As such, it fails if any of those directories turns out not to be empty.
-Use the @option{--ignore-fail-on-non-empty} option to make it so such
-a failure does not evoke a diagnostic and does not cause @command{rmdir} to
+Use the @code{--ignore-fail-on-non-empty} option to make it so such
+a failure does not evoke a diagnostic and does not cause @file{rmdir} to
 exit unsuccessfully.
 
 @item -v
@@ -7616,12 +7615,12 @@
 
 
 @node unlink invocation
-@section @command{unlink}: Remove files via the unlink syscall
+@section @file{unlink}: Remove files via the unlink syscall
 
 @pindex unlink
 @cindex removing files or directories (via the unlink syscall)
 
-@command{unlink} deletes a single specified file name.
+@file{unlink} deletes a single specified file name.
 It is a minimalist interface to the system-provided
 @code{unlink} function.  @xref{Deleting Files, , , libc,
 The GNU C Library Reference Manual}.  Synopsis:
@@ -7634,10 +7633,10 @@
 directory.  On others, it can be used that way only by a privileged user.
 In the GNU system @code{unlink} can never delete the name of a directory.
 
-By default, @command{unlink} honors the @option{--help} and @option{--version}
+By default, @file{unlink} honors the @code{--help} and @code{--version}
 options.  That makes it a little harder to remove files named
-@option{--help} and @option{--version}, so when the environment variable
-@env{POSIXLY_CORRECT} is set, @command{unlink} treats such a command line
+@code{--help} and @code{--version}, so when the environment variable
+@var{POSIXLY_CORRECT} is set, @file{unlink} treats such a command line
 arguments not as an option, but as an operand.
 
 @exitstatus
@@ -7668,7 +7667,7 @@
 
 
 @node chown invocation
-@section @command{chown}: Change file owner and group
+@section @file{chown}: Change file owner and group
 
 @pindex chown
 @cindex file ownership, changing
@@ -7676,7 +7675,7 @@
 @cindex changing file ownership
 @cindex changing group ownership
 
-@command{chown} changes the user and/or group ownership of each given @var{file}
+@file{chown} changes the user and/or group ownership of each given @var{file}
 to @var{new-owner} or to the user and group of an existing reference file.
 Synopsis:
 
@@ -7712,27 +7711,27 @@
 @itemx @samp{:}group
 If the colon and following @var{group} are given, but the owner
 is omitted, only the group of the files is changed; in this case,
-@command{chown} performs the same function as @command{chgrp}.
+@file{chown} performs the same function as @file{chgrp}.
 
 @end table
 
 Some older scripts may still use @samp{.} in place of the @samp{:} separator.
-@acronym{POSIX} 1003.1-2001 (@pxref{Standards conformance}) does not
-require support for that, but for backward compatibility @acronym{GNU}
-@command{chown} supports @samp{.} so long as no ambiguity results.
+@b{POSIX} 1003.1-2001 (@pxref{Standards conformance}) does not
+require support for that, but for backward compatibility @b{GNU}
+@file{chown} supports @samp{.} so long as no ambiguity results.
 New scripts should avoid the use of @samp{.} because it is not
 portable, and because it has undesirable results if the entire
 @var{owner@samp{.}group} happens to identify a user whose name
 contains @samp{.}.
 
-Warning: The @command{chown} command may clear the set-user-ID or
-set-group-ID bits on some systems.  The @command{chown} command is
+Warning: The @file{chown} command may clear the set-user-ID or
+set-group-ID bits on some systems.  The @file{chown} command is
 dependent upon the policy and functionality of the underlying system
 which may make system-dependent file mode modifications outside the
-control of the @command{chown} command.  On some systems (e.g., Linux)
-the @command{chown} command clears the set-UID and set-GID bits because
+control of the @file{chown} command.  On some systems (e.g., Linux)
+the @file{chown} command clears the set-UID and set-GID bits because
 the underlying, system @code{chown} function clears them.  On other
-systems (e.g., HP-UX and Solaris) the @command{chown} command does not
+systems (e.g., HP-UX and Solaris) the @file{chown} command does not
 affect those bits when operated as the superuser.  On systems which allow
 non-privileged use of chown those bits are always cleared by the system.
 When in doubt, check the underlying system behavior.
@@ -7774,8 +7773,8 @@
 find / -owner OLDUSER -print0 | xargs -0 chown NEWUSER
 @end smallexample
 
-But that is dangerous because the interval between when the @command{find}
-tests the existing file's owner and when the @command{chown} is actually run
+But that is dangerous because the interval between when the @file{find}
+tests the existing file's owner and when the @file{chown} is actually run
 may be quite large.
 One way to narrow the gap would be to invoke chown for each file
 as it is found:
@@ -7808,22 +7807,22 @@
 This is the default.
 This mode relies on the @code{lchown} system call.
 On systems that do not provide the @code{lchown} system call,
-@command{chown} fails when a file specified on the command line
+@file{chown} fails when a file specified on the command line
 is a symbolic link.
 By default, no diagnostic is issued for symbolic links encountered
-during a recursive traversal, but see @option{--verbose}.
+during a recursive traversal, but see @code{--verbose}.
 
 @itemx --preserve-root
 @opindex --preserve-root
 @cindex root directory, disallow recursive modification
 Fail upon any attempt to recursively change the filesystem root, @file{/}.
-Without @option{--recursive}, this option has no effect.
+Without @code{--recursive}, this option has no effect.
 @xref{Treating / specially}.
 
 @itemx --no-preserve-root
 @opindex --no-preserve-root
 @cindex root directory, allow recursive modification
-Cancel the effect of any preceding @option{--preserve-root} option.
+Cancel the effect of any preceding @code{--preserve-root} option.
 @xref{Treating / specially}.
 
 @item --reference=@var{ref_file}
@@ -7839,7 +7838,7 @@
 @opindex --verbose
 Output a diagnostic for every file processed.
 If a symbolic link is encountered during a recursive traversal
-on a system without the @code{lchown} system call, and @option{--no-dereference}
+on a system without the @code{lchown} system call, and @code{--no-dereference}
 is in effect, then issue a diagnostic saying neither the symbolic link nor
 its referent is being changed.
 
@@ -7865,13 +7864,13 @@
 
 
 @node chgrp invocation
-@section @command{chgrp}: Change group ownership
+@section @file{chgrp}: Change group ownership
 
 @pindex chgrp
 @cindex group ownership, changing
 @cindex changing group ownership
 
-@command{chgrp} changes the group ownership of each given @var{file}
+@file{chgrp} changes the group ownership of each given @var{file}
 to @var{group} (which can be either a group name or a numeric group id)
 or to the group of an existing reference file.  Synopsis:
 
@@ -7917,22 +7916,22 @@
 This is the default.
 This mode relies on the @code{lchown} system call.
 On systems that do not provide the @code{lchown} system call,
-@command{chgrp} fails when a file specified on the command line
+@file{chgrp} fails when a file specified on the command line
 is a symbolic link.
 By default, no diagnostic is issued for symbolic links encountered
-during a recursive traversal, but see @option{--verbose}.
+during a recursive traversal, but see @code{--verbose}.
 
 @itemx --preserve-root
 @opindex --preserve-root
 @cindex root directory, disallow recursive modification
 Fail upon any attempt to recursively change the filesystem root, @file{/}.
-Without @option{--recursive}, this option has no effect.
+Without @code{--recursive}, this option has no effect.
 @xref{Treating / specially}.
 
 @itemx --no-preserve-root
 @opindex --no-preserve-root
 @cindex root directory, allow recursive modification
-Cancel the effect of any preceding @option{--preserve-root} option.
+Cancel the effect of any preceding @code{--preserve-root} option.
 @xref{Treating / specially}.
 
 @item --reference=@var{ref_file}
@@ -7947,7 +7946,7 @@
 @opindex --verbose
 Output a diagnostic for every file processed.
 If a symbolic link is encountered during a recursive traversal
-on a system without the @code{lchown} system call, and @option{--no-dereference}
+on a system without the @code{lchown} system call, and @code{--no-dereference}
 is in effect, then issue a diagnostic saying neither the symbolic link nor
 its referent is being changed.
 
@@ -7973,26 +7972,26 @@
 
 
 @node chmod invocation
-@section @command{chmod}: Change access permissions
+@section @file{chmod}: Change access permissions
 
 @pindex chmod
 @cindex changing access permissions
 @cindex access permissions, changing
 @cindex permissions, changing access
 
-@command{chmod} changes the access permissions of the named files.  Synopsis:
+@file{chmod} changes the access permissions of the named files.  Synopsis:
 
 @example
 chmod [@var{option}]@dots{} @{@var{mode} | --reference=@var{ref_file}@} @var{file}@dots{}
 @end example
 
 @cindex symbolic links, permissions of
-@command{chmod} never changes the permissions of symbolic links, since
-the @command{chmod} system call cannot change their permissions.
+@file{chmod} never changes the permissions of symbolic links, since
+the @file{chmod} system call cannot change their permissions.
 This is not a problem since the permissions of symbolic links are
 never used.  However, for each symbolic link listed on the command
-line, @command{chmod} changes the permissions of the pointed-to file.
-In contrast, @command{chmod} ignores symbolic links encountered during
+line, @file{chmod} changes the permissions of the pointed-to file.
+In contrast, @file{chmod} ignores symbolic links encountered during
 recursive directory traversals.
 
 If used, @var{mode} specifies the new permissions.
@@ -8023,13 +8022,13 @@
 @opindex --preserve-root
 @cindex root directory, disallow recursive modification
 Fail upon any attempt to recursively change the filesystem root, @file{/}.
-Without @option{--recursive}, this option has no effect.
+Without @code{--recursive}, this option has no effect.
 @xref{Treating / specially}.
 
 @itemx --no-preserve-root
 @opindex --no-preserve-root
 @cindex root directory, allow recursive modification
-Cancel the effect of any preceding @option{--preserve-root} option.
+Cancel the effect of any preceding @code{--preserve-root} option.
 @xref{Treating / specially}.
 
 @item -v
@@ -8058,38 +8057,38 @@
 
 
 @node touch invocation
-@section @command{touch}: Change file timestamps
+@section @file{touch}: Change file timestamps
 
 @pindex touch
 @cindex changing file timestamps
 @cindex file timestamps, changing
 @cindex timestamps, changing file
 
-@command{touch} changes the access and/or modification times of the
+@file{touch} changes the access and/or modification times of the
 specified files.  Synopsis:
 
 @example
 touch [@var{option}]@dots{} @var{file}@dots{}
 @end example
 
-On older systems, @command{touch} supports an obsolete syntax, as follows.
-If the first @var{file} would be a valid argument to the @option{-t}
-option and no timestamp is given with any of the @option{-d}, @option{-r},
-or @option{-t} options and the @samp{--} argument is not given, that
+On older systems, @file{touch} supports an obsolete syntax, as follows.
+If the first @var{file} would be a valid argument to the @code{-t}
+option and no timestamp is given with any of the @code{-d}, @code{-r},
+or @code{-t} options and the @samp{--} argument is not given, that
 argument is interpreted as the time for the other files instead of
-as a file name.  @acronym{POSIX} 1003.1-2001 (@pxref{Standards conformance})
-does not allow this; use @option{-t} instead.
+as a file name.  @b{POSIX} 1003.1-2001 (@pxref{Standards conformance})
+does not allow this; use @code{-t} instead.
 
 @cindex empty files, creating
 Any @var{file} that does not exist is created empty.
 
 @cindex permissions, for changing file timestamps
 If changing both the access and modification times to the current
-time, @command{touch} can change the timestamps for files that the user
+time, @file{touch} can change the timestamps for files that the user
 running it does not own but has write permission for.  Otherwise, the
 user must own the files.
 
-Although @command{touch} provides options for changing two of the times --
+Although @file{touch} provides options for changing two of the times --
 the times of last access and modification -- of a file, there is actually
 a third one as well: the inode change time.  This is often referred to
 as a file's @code{ctime}.
@@ -8136,8 +8135,8 @@
 
 @item -f
 @opindex -f
-@cindex BSD @command{touch} compatibility
-Ignored; for compatibility with BSD versions of @command{touch}.
+@cindex BSD @file{touch} compatibility
+Ignored; for compatibility with BSD versions of @file{touch}.
 
 @item -m
 @itemx --time=mtime
@@ -8153,8 +8152,8 @@
 @opindex -r
 @opindex --reference
 Use the times of the reference @var{file} instead of the current time.
-If this option is combined with the @option{--date=@var{time}}
-(@option{-d @var{time}}) option, the reference @var{file}'s time is
+If this option is combined with the @code{--date=@var{time}}
+(@code{-d @var{time}}) option, the reference @var{file}'s time is
 the origin for any relative @var{time}s given, but is otherwise ignored.
 For example, @samp{-r foo -d '-5 seconds'} specifies a time stamp
 equal to five seconds before the corresponding time stamp for @file{foo}.
@@ -8180,7 +8179,7 @@
 No disk can hold an infinite amount of data.  These commands report on
 how much disk storage is in use or available.  (This has nothing much to
 do with how much @emph{main memory}, i.e., RAM, a program is using when
-it runs; for that, you want @command{ps} or @command{pstat} or @command{swap}
+it runs; for that, you want @file{ps} or @file{pstat} or @file{swap}
 or some such command.)
 
 @menu
@@ -8192,21 +8191,21 @@
 
 
 @node df invocation
-@section @command{df}: Report filesystem disk space usage
+@section @file{df}: Report filesystem disk space usage
 
 @pindex df
 @cindex filesystem disk usage
 @cindex disk usage by filesystem
 
-@command{df} reports the amount of disk space used and available on
+@file{df} reports the amount of disk space used and available on
 filesystems.  Synopsis:
 
 @example
 df [@var{option}]@dots{} [@var{file}]@dots{}
 @end example
 
-With no arguments, @command{df} reports the space used and available on all
-currently mounted filesystems (of all types).  Otherwise, @command{df}
+With no arguments, @file{df} reports the space used and available on all
+currently mounted filesystems (of all types).  Otherwise, @file{df}
 reports on the filesystem containing each argument @var{file}.
 
 Normally the disk space is printed in units of
@@ -8216,9 +8215,9 @@
 @cindex disk device file
 @cindex device file, disk
 If an argument @var{file} is a disk device file containing a mounted
-filesystem, @command{df} shows the space available on that filesystem
+filesystem, @file{df} shows the space available on that filesystem
 rather than on the filesystem containing the device node (i.e., the root
-filesystem).  @sc{gnu} @command{df} does not attempt to determine the disk usage
+filesystem).  @sc{gnu} @file{df} does not attempt to determine the disk usage
 on unmounted filesystems, because on most kinds of systems doing so
 requires extremely nonportable intimate knowledge of filesystem
 structures.
@@ -8245,13 +8244,13 @@
 @opindex --block-size
 @cindex filesystem sizes
 Scale sizes by @var{size} before printing them (@pxref{Block size}).
-For example, @option{-BG} prints sizes in units of 1,073,741,824 bytes.
+For example, @code{-BG} prints sizes in units of 1,073,741,824 bytes.
 
 @optHumanReadable
 
 @item -H
 @opindex -H
-Equivalent to @option{--si}.
+Equivalent to @code{--si}.
 
 @item -i
 @itemx --inodes
@@ -8267,7 +8266,7 @@
 @cindex kibibytes for filesystem sizes
 Print sizes in 1024-byte blocks, overriding the default block size
 (@pxref{Block size}).
-This option is equivalent to @option{--block-size=1K}.
+This option is equivalent to @code{--block-size=1K}.
 
 @item -l
 @itemx --local
@@ -8281,7 +8280,7 @@
 @opindex --no-sync
 @cindex filesystem space, retrieving old data more quickly
 Do not invoke the @code{sync} system call before getting any usage data.
-This may make @command{df} run significantly faster on systems with many
+This may make @file{df} run significantly faster on systems with many
 disks, but on some systems (notably SunOS) the results may be slightly
 out of date.  This is the default.
 
@@ -8290,10 +8289,10 @@
 @opindex -P
 @opindex --portability
 @cindex one-line output format
-@cindex @acronym{POSIX} output format
+@cindex @b{POSIX} output format
 @cindex portable output format
 @cindex output format, portable
-Use the @acronym{POSIX} output format.  This is like the default format except
+Use the @b{POSIX} output format.  This is like the default format except
 for the following:
 
 @enumerate
@@ -8304,7 +8303,7 @@
 some network mounts), the columns are misaligned.
 
 @item
-The labels in the header output line are changed to conform to @acronym{POSIX}.
+The labels in the header output line are changed to conform to @b{POSIX}.
 @end enumerate
 
 @optSi
@@ -8314,7 +8313,7 @@
 @cindex filesystem space, retrieving current data more slowly
 Invoke the @code{sync} system call before getting any usage data.  On
 some systems (notably SunOS), doing this yields more up to date results,
-but in general this option makes @command{df} much slower, especially when
+but in general this option makes @file{df} much slower, especially when
 there are many or very busy filesystems.
 
 @item -t @var{fstype}
@@ -8323,7 +8322,7 @@
 @opindex --type
 @cindex filesystem types, limiting output to certain
 Limit the listing to filesystems of type @var{fstype}.  Multiple
-filesystem types can be specified by giving multiple @option{-t} options.
+filesystem types can be specified by giving multiple @code{-t} options.
 By default, nothing is omitted.
 
 @item -T
@@ -8332,7 +8331,7 @@
 @opindex --print-type
 @cindex filesystem types, printing
 Print each filesystem's type.  The types printed here are the same ones
-you can include or exclude with @option{-t} and @option{-x}.  The particular
+you can include or exclude with @code{-t} and @code{-x}.  The particular
 types printed are whatever is supported by the system.  Here are some of
 the common names (this list is certainly not exhaustive):
 
@@ -8377,10 +8376,10 @@
 @opindex --exclude-type
 Limit the listing to filesystems not of type @var{fstype}.
 Multiple filesystem types can be eliminated by giving multiple
-@option{-x} options.  By default, no filesystem types are omitted.
+@code{-x} options.  By default, no filesystem types are omitted.
 
 @item -v
-Ignored; for compatibility with System V versions of @command{df}.
+Ignored; for compatibility with System V versions of @file{df}.
 
 @end table
 
@@ -8388,20 +8387,20 @@
 
 
 @node du invocation
-@section @command{du}: Estimate file space usage
+@section @file{du}: Estimate file space usage
 
 @pindex du
 @cindex file space usage
 @cindex disk usage for files
 
-@command{du} reports the amount of disk space used by the specified files
+@file{du} reports the amount of disk space used by the specified files
 and for each subdirectory (of directory arguments).  Synopsis:
 
 @example
 du [@var{option}]@dots{} [@var{file}]@dots{}
 @end example
 
-With no arguments, @command{du} reports the disk space for the current
+With no arguments, @file{du} reports the disk space for the current
 directory.  Normally the disk space is printed in units of
 1024 bytes, but this can be overridden (@pxref{Block size}).
 Non-integer quantities are rounded up to the next higher unit.
@@ -8444,7 +8443,7 @@
 @opindex --block-size
 @cindex file sizes
 Scale sizes by @var{size} before printing them (@pxref{Block size}).
-For example, @option{-BG} prints sizes in units of 1,073,741,824 bytes.
+For example, @code{-BG} prints sizes in units of 1,073,741,824 bytes.
 
 @item -c
 @itemx --total
@@ -8468,23 +8467,23 @@
 
 @item -H
 @opindex -H
-Currently, @option{-H} is the same as @option{--si},
-except that @option{-H} evokes a warning.
+Currently, @code{-H} is the same as @code{--si},
+except that @code{-H} evokes a warning.
 This option will be changed to be equivalent to
-@option{--dereference-args} (@option{-D}).
+@code{--dereference-args} (@code{-D}).
 
 @item -k
 @opindex -k
 @cindex kibibytes for file sizes
 Print sizes in 1024-byte blocks, overriding the default block size
 (@pxref{Block size}).
-This option is equivalent to @option{--block-size=1K}.
+This option is equivalent to @code{--block-size=1K}.
 
 @item -l
 @itemx --count-links
 @opindex -l
 @opindex --count-links
-@cindex hard links, counting in @command{du}
+@cindex hard links, counting in @file{du}
 Count the size of all files, even if they have appeared already (as a
 hard link).
 
@@ -8492,7 +8491,7 @@
 @itemx --dereference
 @opindex -L
 @opindex --dereference
-@cindex symbolic links, dereferencing in @command{du}
+@cindex symbolic links, dereferencing in @file{du}
 Dereference symbolic links (show the disk space used by the file
 or directory that the link points to instead of the space used by
 the link).
@@ -8501,13 +8500,13 @@
 @itemx --no-dereference
 @opindex -P
 @opindex --no-dereference
-@cindex symbolic links, dereferencing in @command{du}
-For each symbolic links encountered by @command{du},
+@cindex symbolic links, dereferencing in @file{du}
+For each symbolic links encountered by @file{du},
 consider the disk space used by the symbolic link.
 
 @item --max-depth=@var{DEPTH}
 @opindex --max-depth=@var{DEPTH}
-@cindex limiting output of @command{du}
+@cindex limiting output of @file{du}
 Show the total for each directory (and file if --all) that is at
 most MAX_DEPTH levels down from the root of the hierarchy.  The root
 is at level 0, so @code{du --max-depth=0} is equivalent to @code{du -s}.
@@ -8518,7 +8517,7 @@
 @opindex --null
 @cindex output NUL-terminated lines
 Output the zero byte (@kbd{NUL}) at the end of each line, rather than a newline.
-This option enables other programs to parse the output of @command{du}
+This option enables other programs to parse the output of @file{du}
 even when that output would contain file names with embedded newlines.
 
 @itemx --si
@@ -8526,7 +8525,7 @@
 @cindex SI output
 Append an SI-style abbreviation to each size, such as @samp{MB} for
 megabytes.  Powers of 1000 are used, not 1024; @samp{MB} stands for
-1,000,000 bytes.  Use the @option{-h} or @option{--human-readable} option if
+1,000,000 bytes.  Use the @code{-h} or @code{--human-readable} option if
 you prefer powers of 1024.
 
 @item -s
@@ -8546,13 +8545,13 @@
 @itemx --one-file-system
 @opindex -x
 @opindex --one-file-system
-@cindex one filesystem, restricting @command{du} to
+@cindex one filesystem, restricting @file{du} to
 Skip directories that are on different filesystems from the one that
 the argument being processed is on.
 
 @item --exclude=@var{PATTERN}
 @opindex --exclude=@var{PATTERN}
-@cindex excluding files from @command{du}
+@cindex excluding files from @file{du}
 When recursing, skip subdirectories or files matching @var{PATTERN}.
 For example, @code{du --exclude='*.o'} excludes files whose names
 end in @samp{.o}.
@@ -8561,39 +8560,39 @@
 @itemx --exclude-from=@var{FILE}
 @opindex -X @var{FILE}
 @opindex --exclude-from=@var{FILE}
-@cindex excluding files from @command{du}
-Like @option{--exclude}, except take the patterns to exclude from @var{FILE},
+@cindex excluding files from @file{du}
+Like @code{--exclude}, except take the patterns to exclude from @var{FILE},
 one per line.  If @var{FILE} is @samp{-}, take the patterns from standard
 input.
 
 @end table
 
 @cindex NFS mounts from BSD to HP-UX
-On BSD systems, @command{du} reports sizes that are half the correct
+On BSD systems, @file{du} reports sizes that are half the correct
 values for files that are NFS-mounted from HP-UX systems.  On HP-UX
 systems, it reports sizes that are twice the correct values for
 files that are NFS-mounted from BSD systems.  This is due to a flaw
-in HP-UX; it also affects the HP-UX @command{du} program.
+in HP-UX; it also affects the HP-UX @file{du} program.
 
 @exitstatus
 
 
 @node stat invocation
-@section @command{stat}: Report file or filesystem status
+@section @file{stat}: Report file or filesystem status
 
 @pindex stat
 @cindex file status
 @cindex filesystem status
 
-@command{stat} displays information about the specified file(s).  Synopsis:
+@file{stat} displays information about the specified file(s).  Synopsis:
 
 @example
 stat [@var{option}]@dots{} [@var{file}]@dots{}
 @end example
 
-With no option, @command{stat} reports all information about the given files.
+With no option, @file{stat} reports all information about the given files.
 But it also can be used to report the information of the filesystems the
-given files are located on. If the files are links, @command{stat} can
+given files are located on. If the files are links, @file{stat} can
 also give information about the files the links point to.
 
 
@@ -8611,11 +8610,11 @@
 @itemx --dereference
 @opindex -L
 @opindex --dereference
-@cindex symbolic links, dereferencing in @command{stat}
-Change how @command{stat} treats symbolic links.
-With this option, @command{stat} acts on the file referenced
+@cindex symbolic links, dereferencing in @file{stat}
+Change how @file{stat} treats symbolic links.
+With this option, @file{stat} acts on the file referenced
 by each symbolic link argument.
-Without it, @command{stat} acts on any symbolic link argument directly.
+Without it, @file{stat} acts on any symbolic link argument directly.
 
 @item -t
 @itemx --terse
@@ -8680,27 +8679,27 @@
 
 
 @node sync invocation
-@section @command{sync}: Synchronize data on disk with memory
+@section @file{sync}: Synchronize data on disk with memory
 
 @pindex sync
 @cindex synchronize disk and memory
 
 @cindex superblock, writing
 @cindex inodes, written buffered
-@command{sync} writes any data buffered in memory out to disk.  This can
+@file{sync} writes any data buffered in memory out to disk.  This can
 include (but is not limited to) modified superblocks, modified inodes,
 and delayed reads and writes.  This must be implemented by the kernel;
-The @command{sync} program does nothing but exercise the @code{sync} system
+The @file{sync} program does nothing but exercise the @code{sync} system
 call.
 
 @cindex crashes and corruption
 The kernel keeps data in memory to avoid doing (relatively slow) disk
 reads and writes.  This improves performance, but if the computer
 crashes, data may be lost or the filesystem corrupted as a
-result. @command{sync} ensures everything in memory is written to disk.
+result. @file{sync} ensures everything in memory is written to disk.
 
-Any arguments are ignored, except for a lone @option{--help} or
-@option{--version} (@pxref{Common options}).
+Any arguments are ignored, except for a lone @code{--help} or
+@code{--version} (@pxref{Common options}).
 
 @exitstatus
 
@@ -8721,7 +8720,7 @@
 
 
 @node echo invocation
-@section @command{echo}: Print a line of text
+@section @file{echo}: Print a line of text
 
 @pindex echo
 @cindex displaying text
@@ -8729,7 +8728,7 @@
 @cindex text, displaying
 @cindex arbitrary text, displaying
 
-@command{echo} writes each given @var{string} to standard output, with a
+@file{echo} writes each given @var{string} to standard output, with a
 space between each and a newline after the last one.  Synopsis:
 
 @example
@@ -8769,7 +8768,7 @@
 @item \\
 backslash
 @item \@var{nnn}
-the character whose @acronym{ASCII} code is @var{nnn} (octal); if @var{nnn} is not
+the character whose @b{ASCII} code is @var{nnn} (octal); if @var{nnn} is not
 a valid octal number, it is printed literally.
 @end table
 
@@ -8779,28 +8778,28 @@
 
 
 @node printf invocation
-@section @command{printf}: Format and print data
+@section @file{printf}: Format and print data
 
 @pindex printf
-@command{printf} does formatted printing of text. Synopsis:
+@file{printf} does formatted printing of text. Synopsis:
 
 @example
 printf @var{format} [@var{argument}]@dots{}
 @end example
 
-@command{printf} prints the @var{format} string, interpreting @samp{%}
-directives and @samp{\} escapes in the same way as the C @command{printf}
+@file{printf} prints the @var{format} string, interpreting @samp{%}
+directives and @samp{\} escapes in the same way as the C @file{printf}
 function.  The @var{format} argument is re-used as necessary to convert
 all of the given @var{argument}s.
 
 @vindex LC_NUMERIC
 A floating-point argument must use a period before any fractional
-digits, but is printed according to the @env{LC_NUMERIC} category of the
+digits, but is printed according to the @var{LC_NUMERIC} category of the
 current locale.  For example, in a locale whose radix character is a
 comma, the command @samp{printf %g 3.14} outputs @samp{3,14} whereas
 the command @samp{printf %g 3,14} is an error.
 
-@command{printf} has one additional directive, @samp{%b}, which prints its
+@file{printf} has one additional directive, @samp{%b}, which prints its
 argument string with @samp{\} escapes interpreted in the same way as in
 the @var{format} string, except that octal escapes are of the form
 @samp{\0@var{ooo}} where @var{ooo} is 0 to 3 octal digits.
@@ -8808,20 +8807,20 @@
 @kindex \@var{ooo}
 @kindex \x@var{hh}
 
-@command{printf} interprets @samp{\@var{ooo}} in @var{format} as an octal number
+@file{printf} interprets @samp{\@var{ooo}} in @var{format} as an octal number
 (if @var{ooo} is 1 to 3 octal digits) specifying a character to print,
 and @samp{\x@var{hh}} as a hexadecimal number (if @var{hh} is 1 to 2 hex
 digits) specifying a character to print.
 
 @kindex \uhhhh
 @kindex \Uhhhhhhhh
-@command{printf} interprets two character syntaxes introduced in ISO C 99:
+@file{printf} interprets two character syntaxes introduced in ISO C 99:
 @samp{\u} for 16-bit Unicode characters, specified as 4 hex digits
 @var{hhhh}, and @samp{\U} for 32-bit Unicode characters, specified as 8 hex
-digits @var{hhhhhhhh}. @command{printf} outputs the Unicode characters
+digits @var{hhhhhhhh}. @file{printf} outputs the Unicode characters
 according to the LC_CTYPE part of the current locale, i.e. depending
-on the values of the environment variables @env{LC_ALL}, @env{LC_CTYPE},
-@env{LANG}.
+on the values of the environment variables @var{LC_ALL}, @var{LC_CTYPE},
+@var{LANG}.
 
 The processing of @samp{\u} and @samp{\U} requires a full-featured
 @code{iconv} facility. It is activated on systems with glibc 2.2 (or newer),
@@ -8829,11 +8828,11 @@
 use of @samp{\u} and @samp{\U} will give an error message.
 
 @kindex \c
-An additional escape, @samp{\c}, causes @command{printf} to produce no
+An additional escape, @samp{\c}, causes @file{printf} to produce no
 further output.
 
-The only options are a lone @option{--help} or
-@option{--version}.  @xref{Common options}.
+The only options are a lone @code{--help} or
+@code{--version}.  @xref{Common options}.
 
 The Unicode character syntaxes are useful for writing strings in a locale
 independent way. For example, a string containing the Euro currency symbol
@@ -8853,12 +8852,12 @@
 @noindent
 will be output correctly in all Chinese locales (GB2312, BIG5, UTF-8, etc).
 
-Note that in these examples, the full pathname of @command{printf} has been
+Note that in these examples, the full pathname of @file{printf} has been
 given, to distinguish it from the GNU @code{bash} builtin function
-@command{printf}.
+@file{printf}.
 
 For larger strings, you don't need to look up the hexadecimal code
-values of each character one by one. @acronym{ASCII} characters mixed with \u
+values of each character one by one. @b{ASCII} characters mixed with \u
 escape sequences is also known as the JAVA source file encoding. You can
 use GNU recode 3.5c (or newer) to convert strings to this encoding. Here
 is how to convert a piece of text into a shell script which will output
@@ -8876,18 +8875,18 @@
 
 
 @node yes invocation
-@section @command{yes}: Print a string until interrupted
+@section @file{yes}: Print a string until interrupted
 
 @pindex yes
 @cindex repeated output of a string
 
-@command{yes} prints the command line arguments, separated by spaces and
+@file{yes} prints the command line arguments, separated by spaces and
 followed by a newline, forever until it is killed.  If no arguments are
 given, it prints @samp{y} followed by a newline forever until killed.
 
-Upon a write error, @command{yes} exits with status @samp{1}.
+Upon a write error, @file{yes} exits with status @samp{1}.
 
-The only options are a lone @option{--help} or @option{--version}.
+The only options are a lone @code{--help} or @code{--version}.
 @xref{Common options}.
 
 
@@ -8912,56 +8911,56 @@
 
 
 @node false invocation
-@section @command{false}: Do nothing, unsuccessfully
+@section @file{false}: Do nothing, unsuccessfully
 
 @pindex false
 @cindex do nothing, unsuccessfully
 @cindex failure exit status
-@cindex exit status of @command{false}
+@cindex exit status of @file{false}
 
-@command{false} does nothing except return an exit status of 1, meaning
+@file{false} does nothing except return an exit status of 1, meaning
 @dfn{failure}.  It can be used as a place holder in shell scripts
 where an unsuccessful command is needed.
 
-By default, @command{false} honors the @option{--help} and @option{--version}
-options.  However, that is contrary to @acronym{POSIX}, so when the environment
-variable @env{POSIXLY_CORRECT} is set, @command{false} ignores @emph{all}
-command line arguments, including @option{--help} and @option{--version}.
+By default, @file{false} honors the @code{--help} and @code{--version}
+options.  However, that is contrary to @b{POSIX}, so when the environment
+variable @var{POSIXLY_CORRECT} is set, @file{false} ignores @emph{all}
+command line arguments, including @code{--help} and @code{--version}.
 
-This version of @command{false} is implemented as a C program, and is thus
+This version of @file{false} is implemented as a C program, and is thus
 more secure and faster than a shell script implementation, and may safely
 be used as a dummy shell for the purpose of disabling accounts.
 
-Note that @command{false} (unlike all other programs documented herein)
+Note that @file{false} (unlike all other programs documented herein)
 exits unsuccessfully, even when invoked with
-@option{--help} or @option{--version}.
+@code{--help} or @code{--version}.
 
 
 @node true invocation
-@section @command{true}: Do nothing, successfully
+@section @file{true}: Do nothing, successfully
 
 @pindex true
 @cindex do nothing, successfully
 @cindex no-op
 @cindex successful exit
-@cindex exit status of @command{true}
+@cindex exit status of @file{true}
 
-@command{true} does nothing except return an exit status of 0, meaning
+@file{true} does nothing except return an exit status of 0, meaning
 @dfn{success}.  It can be used as a place holder in shell scripts
 where a successful command is needed, although the shell built-in
 command @code{:} (colon) may do the same thing faster.
-In most modern shells, @command{true} is a built-in command, so when
+In most modern shells, @file{true} is a built-in command, so when
 you use @samp{true} in a script, you're probably using the built-in
 command, not the one documented here.
 
-By default, @command{true} honors the @option{--help} and @option{--version}
-options.  However, that is contrary to @acronym{POSIX}, so when the environment
-variable @env{POSIXLY_CORRECT} is set, @command{true} ignores @emph{all}
-command line arguments, including @option{--help} and @option{--version}.
-
-Note, however, that it is possible to cause @command{true}
-to exit with nonzero status: when invoked in non-@acronym{POSIX} mode,
-with the @option{--help} or @option{--version} option, and with standard
+By default, @file{true} honors the @code{--help} and @code{--version}
+options.  However, that is contrary to @b{POSIX}, so when the environment
+variable @var{POSIXLY_CORRECT} is set, @file{true} ignores @emph{all}
+command line arguments, including @code{--help} and @code{--version}.
+
+Note, however, that it is possible to cause @file{true}
+to exit with nonzero status: when invoked in non-@b{POSIX} mode,
+with the @code{--help} or @code{--version} option, and with standard
 output already closed or redirected to a file that evokes an I/O error.
 For example, using a Bourne-compatible shell:
 
@@ -8972,26 +8971,26 @@
 ./true: write error: No space left on device
 @end example
 
-This version of @command{true} is implemented as a C program, and is thus
+This version of @file{true} is implemented as a C program, and is thus
 more secure and faster than a shell script implementation, and may safely
 be used as a dummy shell for the purpose of disabling accounts.
 
 @node test invocation
-@section @command{test}: Check file types and compare values
+@section @file{test}: Check file types and compare values
 
 @pindex test
 @cindex check file types
 @cindex compare values
 @cindex expression evaluation
 
-@command{test} returns a status of 0 (true) or 1 (false) depending on the
+@file{test} returns a status of 0 (true) or 1 (false) depending on the
 evaluation of the conditional expression @var{expr}.  Each part of the
 expression must be a separate argument.
 
-@command{test} has file status checks, string operators, and numeric
+@file{test} has file status checks, string operators, and numeric
 comparison operators.
 
-@command{test} has an alternate form that uses opening and closing
+@file{test} has an alternate form that uses opening and closing
 square brackets instead a leading @samp{test}.  For example, instead
 of @samp{test -d /}, you can write @samp{[ -d / ]}.  The square
 brackets must be separate arguments; for example, @samp{[-d /]} does
@@ -9006,14 +9005,14 @@
 different functionality than that described here.
 
 Besides the options below, a single argument is also allowed:
-@command{test} returns true if the argument is not null.  The argument
+@file{test} returns true if the argument is not null.  The argument
 can be any string, including strings like @samp{-d}, @samp{-1},
 @samp{--}, @samp{--help}, and @samp{--version} that most other
 programs would treat as options.  To get help and version information,
 invoke the commands @samp{[ --help} and @samp{[ --version}, without
 the usual closing brackets.  @xref{Common options}.
 
-@cindex exit status of @command{test}
+@cindex exit status of @file{test}
 Exit status:
 
 @display
@@ -9187,7 +9186,7 @@
 @cindex string tests
 
 These options test string characteristics.  Strings are not quoted for
-@command{test}, though you may need to quote them to protect characters
+@file{test}, though you may need to quote them to protect characters
 with special meaning to the shell, e.g., spaces.
 
 @table @samp
@@ -9259,7 +9258,7 @@
 
 
 @node Connectives for test
-@subsection Connectives for @command{test}
+@subsection Connectives for @file{test}
 
 @cindex logical connectives
 @cindex connectives, logical
@@ -9288,34 +9287,34 @@
 
 
 @node expr invocation
-@section @command{expr}: Evaluate expressions
+@section @file{expr}: Evaluate expressions
 
 @pindex expr
 @cindex expression evaluation
 @cindex evaluation of expressions
 
-@command{expr} evaluates an expression and writes the result on standard
+@file{expr} evaluates an expression and writes the result on standard
 output.  Each token of the expression must be a separate argument.
 
 Operands are either integers or strings.  Integers consist of one or
 more decimal digits, with an optional leading @samp{-}.
-@command{expr} converts
+@file{expr} converts
 anything appearing in an operand position to an integer or a string
 depending on the operation being applied to it.
 
-Strings are not quoted for @command{expr} itself, though you may need to
+Strings are not quoted for @file{expr} itself, though you may need to
 quote them to protect characters with special meaning to the shell,
 e.g., spaces.  However, regardless of whether it is quoted, a string
-operand should not be a parenthesis or any of @command{expr}'s
+operand should not be a parenthesis or any of @file{expr}'s
 operators like @code{+}, so you cannot safely pass an arbitrary string
 @code{$str} to expr merely by quoting it to the shell.  One way to
-work around this is to use the @command{GNU} extension @code{+},
+work around this is to use the @file{GNU} extension @code{+},
 (e.g., @code{+ "$str" = foo}); a more portable way is to use
 @code{@w{" $str"}} and to adjust the rest of the expression to take
 the leading space into account (e.g., @code{@w{" $str" = " foo"}}).
 
 You should not pass a negative integer or a string with leading
-@samp{-} as @command{expr}'s first argument, as it might be
+@samp{-} as @file{expr}'s first argument, as it might be
 misinterpreted as an option; this can be avoided by parenthesization.
 Also, portable scripts should not use a string operand that happens to
 take the form of an integer; this can be worked around by inserting
@@ -9327,7 +9326,7 @@
 parentheses and many operators to avoid the shell evaluating them,
 however.
 
-@cindex exit status of @command{expr}
+@cindex exit status of @file{expr}
 Exit status:
 
 @display
@@ -9351,7 +9350,7 @@
 @cindex string expressions
 @cindex expressions, string
 
-@command{expr} supports pattern matching and other string operators.  These
+@file{expr} supports pattern matching and other string operators.  These
 have lower precedence than both the numeric and relational operators (in
 the next sections).
 
@@ -9383,8 +9382,8 @@
 @kindex \| @r{regexp operator}
 In the regular expression, @code{\+}, @code{\?}, and @code{\|} are
 operators which respectively match one or more, zero or one, or separate
-alternatives.  SunOS and other @command{expr}'s treat these as regular
-characters.  (@acronym{POSIX} allows either behavior.)
+alternatives.  SunOS and other @file{expr}'s treat these as regular
+characters.  (@b{POSIX} allows either behavior.)
 @xref{Top, , Regular Expression Library, regex, Regex}, for details of
 regular expression syntax.  Some examples are in @ref{Examples of expr}.
 
@@ -9421,7 +9420,7 @@
 
 @end table
 
-To make @command{expr} interpret keywords as strings, you must use the
+To make @file{expr} interpret keywords as strings, you must use the
 @code{quote} operator.
 
 
@@ -9431,7 +9430,7 @@
 @cindex numeric expressions
 @cindex expressions, numeric
 
-@command{expr} supports the usual numeric operators, in order of increasing
+@file{expr} supports the usual numeric operators, in order of increasing
 precedence.  The string operators (previous section) have lower precedence,
 the connectives (next section) have higher.
 
@@ -9459,13 +9458,13 @@
 
 
 @node Relations for expr
-@subsection Relations for @command{expr}
+@subsection Relations for @file{expr}
 
 @cindex connectives, logical
 @cindex logical connectives
 @cindex relations, numeric or string
 
-@command{expr} supports the usual logical connectives and relations.  These
+@file{expr} supports the usual logical connectives and relations.  These
 are higher precedence than either the string or numeric operators
 (previous sections).  Here is the list, lowest-precedence operator first.
 
@@ -9498,18 +9497,18 @@
 @cindex comparison operators
 @vindex LC_COLLATE
 Compare the arguments and return 1 if the relation is true, 0 otherwise.
-@code{==} is a synonym for @code{=}.  @command{expr} first tries to convert
+@code{==} is a synonym for @code{=}.  @file{expr} first tries to convert
 both arguments to integers and do a numeric comparison; if either
 conversion fails, it does a lexicographic comparison using the character
-collating sequence specified by the @env{LC_COLLATE} locale.
+collating sequence specified by the @var{LC_COLLATE} locale.
 
 @end table
 
 
 @node Examples of expr
-@subsection Examples of using @command{expr}
+@subsection Examples of using @file{expr}
 
-@cindex examples of @command{expr}
+@cindex examples of @file{expr}
 Here are a few examples, including quoting for shell metacharacters.
 
 To add 1 to the shell variable @code{foo}, in Bourne-compatible shells:
@@ -9558,14 +9557,14 @@
 
 
 @node tee invocation
-@section @command{tee}: Redirect output to multiple files
+@section @file{tee}: Redirect output to multiple files
 
 @pindex tee
 @cindex pipe fitting
 @cindex destinations, multiple output
 @cindex read from stdin and write to stdout and files
 
-The @command{tee} command copies standard input to standard output and also
+The @file{tee} command copies standard input to standard output and also
 to any files given as arguments.  This is useful when you want not only
 to send some data down a pipe, but also to save a copy.  Synopsis:
 
@@ -9575,7 +9574,7 @@
 
 If a file being written to does not already exist, it is created.  If a
 file being written to already exists, the data it previously contained
-is overwritten unless the @option{-a} option is used.
+is overwritten unless the @code{-a} option is used.
 
 The program accepts the following options.  Also see @ref{Common options}.
 
@@ -9615,7 +9614,7 @@
 
 
 @node basename invocation
-@section @command{basename}: Strip directory and suffix from a file name
+@section @file{basename}: Strip directory and suffix from a file name
 
 @pindex basename
 @cindex strip directory and suffix from file names
@@ -9624,7 +9623,7 @@
 @cindex file names, stripping directory and suffix
 @cindex leading directory components, stripping
 
-@command{basename} removes any leading directory components from
+@file{basename} removes any leading directory components from
 @var{name}.  Synopsis:
 
 @example
@@ -9632,54 +9631,54 @@
 @end example
 
 If @var{suffix} is specified and is identical to the end of @var{name},
-it is removed from @var{name} as well.  @command{basename} prints the
+it is removed from @var{name} as well.  @file{basename} prints the
 result on standard output.
 
-The only options are @option{--help} and @option{--version}.  @xref{Common
+The only options are @code{--help} and @code{--version}.  @xref{Common
 options}.
 
 @exitstatus
 
 
 @node dirname invocation
-@section @command{dirname}: Strip non-directory suffix from a file name
+@section @file{dirname}: Strip non-directory suffix from a file name
 
 @pindex dirname
 @cindex directory components, printing
 @cindex stripping non-directory suffix
 @cindex non-directory suffix, stripping
 
-@command{dirname} prints all but the final slash-delimited component of
+@file{dirname} prints all but the final slash-delimited component of
 a string (presumably a filename).  Synopsis:
 
 @example
 dirname @var{name}
 @end example
 
-If @var{name} is a single component, @command{dirname} prints @samp{.}
+If @var{name} is a single component, @file{dirname} prints @samp{.}
 (meaning the current directory).
 
-The only options are @option{--help} and @option{--version}.  @xref{Common
+The only options are @code{--help} and @code{--version}.  @xref{Common
 options}.
 
 @exitstatus
 
 
 @node pathchk invocation
-@section @command{pathchk}: Check file name portability
+@section @file{pathchk}: Check file name portability
 
 @pindex pathchk
 @cindex file names, checking validity and portability
 @cindex valid file names, checking for
 @cindex portable file names, checking for
 
-@command{pathchk} checks portability of filenames.  Synopsis:
+@file{pathchk} checks portability of filenames.  Synopsis:
 
 @example
 pathchk [@var{option}]@dots{} @var{name}@dots{}
 @end example
 
-For each @var{name}, @command{pathchk} prints a message if any of
+For each @var{name}, @file{pathchk} prints a message if any of
 these conditions is true:
 @enumerate
 @item
@@ -9704,12 +9703,12 @@
 @opindex --portability
 Instead of performing length checks on the underlying filesystem,
 test the length of each file name and its components against the
-@acronym{POSIX} minimum limits for portability.  Also check that the file
+@b{POSIX} minimum limits for portability.  Also check that the file
 name contains no characters not in the portable file name character set.
 
 @end table
 
-@cindex exit status of @command{pathchk}
+@cindex exit status of @file{pathchk}
 Exit status:
 
 @display
@@ -9737,15 +9736,15 @@
 
 
 @node pwd invocation
-@section @command{pwd}: Print working directory
+@section @file{pwd}: Print working directory
 
 @pindex pwd
 @cindex print name of current directory
 @cindex current working directory, printing
 @cindex working directory, printing
 
-@cindex symbolic links and @command{pwd}
-@command{pwd} prints the fully resolved name of the current directory.
+@cindex symbolic links and @file{pwd}
+@file{pwd} prints the fully resolved name of the current directory.
 That is, all components of the printed name will be actual directory
 names---none will be symbolic links.
 
@@ -9755,21 +9754,21 @@
 unadorned command name in a script or interactively may get you
 different functionality than that described here.
 
-The only options are a lone @option{--help} or
-@option{--version}.  @xref{Common options}.
+The only options are a lone @code{--help} or
+@code{--version}.  @xref{Common options}.
 
 @exitstatus
 
 
 @node stty invocation
-@section @command{stty}: Print or change terminal characteristics
+@section @file{stty}: Print or change terminal characteristics
 
 @pindex stty
 @cindex change or print terminal settings
 @cindex terminal settings
 @cindex line settings of terminal
 
-@command{stty} prints or changes terminal characteristics, such as baud rate.
+@file{stty} prints or changes terminal characteristics, such as baud rate.
 Synopses:
 
 @example
@@ -9777,14 +9776,14 @@
 stty [@var{option}]
 @end example
 
-If given no line settings, @command{stty} prints the baud rate, line
+If given no line settings, @file{stty} prints the baud rate, line
 discipline number (on systems that support it), and line settings
 that have been changed from the values set by @samp{stty sane}.
 By default, mode reading and setting are performed on the tty line
 connected to standard input, although this can be modified by the
-@option{--file} option.
+@code{--file} option.
 
-@command{stty} accepts many non-option arguments that change aspects of
+@file{stty} accepts many non-option arguments that change aspects of
 the terminal line operation, as described below.
 
 The program accepts the following options.  Also see @ref{Common options}.
@@ -9803,8 +9802,8 @@
 @opindex --file
 Set the line opened by the filename specified in @var{device} instead of
 the tty line connected to standard input.  This option is necessary
-because opening a @acronym{POSIX} tty requires use of the @code{O_NONDELAY} flag to
-prevent a @acronym{POSIX} tty from blocking until the carrier detect line is high if
+because opening a @b{POSIX} tty requires use of the @code{O_NONDELAY} flag to
+prevent a @b{POSIX} tty from blocking until the carrier detect line is high if
 the @code{clocal} flag is not set.  Hence, it is not always possible
 to allow the shell to open the device in the traditional manner.
 
@@ -9812,9 +9811,9 @@
 @itemx --save
 @opindex -g
 @opindex --save
-@cindex machine-readable @command{stty} output
+@cindex machine-readable @file{stty} output
 Print all current settings in a form that can be used as an argument to
-another @command{stty} command to restore the current settings.  This option
+another @file{stty} command to restore the current settings.  This option
 may not be used in combination with any line settings.
 
 @end table
@@ -9825,9 +9824,9 @@
 case, that is, when @emph{not} negated (unless stated otherwise,
 of course).
 
-Some settings are not available on all @acronym{POSIX} systems, since they use
-extensions.  Such arguments are marked below with ``Non-@acronym{POSIX}'' in their
-description.  On non-@acronym{POSIX} systems, those or other settings also may not
+Some settings are not available on all @b{POSIX} systems, since they use
+extensions.  Such arguments are marked below with ``Non-@b{POSIX}'' in their
+description.  On non-@b{POSIX} systems, those or other settings also may not
 be available, but it's not feasible to document all the variations: just
 try it and see.
 
@@ -9897,7 +9896,7 @@
 @cindex hardware flow control
 @cindex flow control, hardware
 @cindex RTS/CTS flow control
-Enable RTS/CTS flow control.  Non-@acronym{POSIX}.  May be negated.
+Enable RTS/CTS flow control.  Non-@b{POSIX}.  May be negated.
 @end table
 
 
@@ -9971,19 +9970,19 @@
 @item iuclc
 @opindex iuclc
 @cindex uppercase, translating to lowercase
-Translate uppercase characters to lowercase.  Non-@acronym{POSIX}.  May be
+Translate uppercase characters to lowercase.  Non-@b{POSIX}.  May be
 negated.
 
 @item ixany
 @opindex ixany
 Allow any character to restart output (only the start character
-if negated).  Non-@acronym{POSIX}.  May be negated.
+if negated).  Non-@b{POSIX}.  May be negated.
 
 @item imaxbel
 @opindex imaxbel
 @cindex beeping at input buffer full
 Enable beeping and not flushing input buffer if a character arrives
-when the input buffer is full.  Non-@acronym{POSIX}.  May be negated.
+when the input buffer is full.  Non-@b{POSIX}.  May be negated.
 @end table
 
 
@@ -10001,74 +10000,74 @@
 @item olcuc
 @opindex olcuc
 @cindex lowercase, translating to output
-Translate lowercase characters to uppercase.  Non-@acronym{POSIX}.  May be
+Translate lowercase characters to uppercase.  Non-@b{POSIX}.  May be
 negated.
 
 @item ocrnl
 @opindex ocrnl
 @cindex return, translating to newline
-Translate carriage return to newline.  Non-@acronym{POSIX}.  May be negated.
+Translate carriage return to newline.  Non-@b{POSIX}.  May be negated.
 
 @item onlcr
 @opindex onlcr
 @cindex newline, translating to crlf
-Translate newline to carriage return-newline.  Non-@acronym{POSIX}.  May be
+Translate newline to carriage return-newline.  Non-@b{POSIX}.  May be
 negated.
 
 @item onocr
 @opindex onocr
-Do not print carriage returns in the first column.  Non-@acronym{POSIX}.
+Do not print carriage returns in the first column.  Non-@b{POSIX}.
 May be negated.
 
 @item onlret
 @opindex onlret
-Newline performs a carriage return.  Non-@acronym{POSIX}.  May be negated.
+Newline performs a carriage return.  Non-@b{POSIX}.  May be negated.
 
 @item ofill
 @opindex ofill
 @cindex pad instead of timing for delaying
-Use fill (padding) characters instead of timing for delays.  Non-@acronym{POSIX}.
+Use fill (padding) characters instead of timing for delays.  Non-@b{POSIX}.
 May be negated.
 
 @item ofdel
 @opindex ofdel
 @cindex pad character
-Use delete characters for fill instead of null characters.  Non-@acronym{POSIX}.
+Use delete characters for fill instead of null characters.  Non-@b{POSIX}.
 May be negated.
 
 @item nl1
 @itemx nl0
 @opindex nl@var{n}
-Newline delay style.  Non-@acronym{POSIX}.
+Newline delay style.  Non-@b{POSIX}.
 
 @item cr3
 @itemx cr2
 @itemx cr1
 @itemx cr0
 @opindex cr@var{n}
-Carriage return delay style.  Non-@acronym{POSIX}.
+Carriage return delay style.  Non-@b{POSIX}.
 
 @item tab3
 @itemx tab2
 @itemx tab1
 @itemx tab0
 @opindex tab@var{n}
-Horizontal tab delay style.  Non-@acronym{POSIX}.
+Horizontal tab delay style.  Non-@b{POSIX}.
 
 @item bs1
 @itemx bs0
 @opindex bs@var{n}
-Backspace delay style.  Non-@acronym{POSIX}.
+Backspace delay style.  Non-@b{POSIX}.
 
 @item vt1
 @itemx vt0
 @opindex vt@var{n}
-Vertical tab delay style.  Non-@acronym{POSIX}.
+Vertical tab delay style.  Non-@b{POSIX}.
 
 @item ff1
 @itemx ff0
 @opindex ff@var{n}
-Form feed delay style.  Non-@acronym{POSIX}.
+Form feed delay style.  Non-@b{POSIX}.
 @end table
 
 
@@ -10090,7 +10089,7 @@
 
 @item iexten
 @opindex iexten
-Enable non-@acronym{POSIX} special characters.  May be negated.
+Enable non-@b{POSIX} special characters.  May be negated.
 
 @item echo
 @opindex echo
@@ -10124,12 +10123,12 @@
 @cindex case translation
 Enable input and output of uppercase characters by preceding their
 lowercase equivalents with @samp{\}, when @code{icanon} is set.
-Non-@acronym{POSIX}.  May be negated.
+Non-@b{POSIX}.  May be negated.
 
 @item tostop
 @opindex tostop
 @cindex background jobs, stopping at terminal write
-Stop background jobs that try to write to the terminal.  Non-@acronym{POSIX}.
+Stop background jobs that try to write to the terminal.  Non-@b{POSIX}.
 May be negated.
 
 @item echoprt
@@ -10137,7 +10136,7 @@
 @opindex echoprt
 @opindex prterase
 Echo erased characters backward, between @samp{\} and @samp{/}.
-Non-@acronym{POSIX}.  May be negated.
+Non-@b{POSIX}.  May be negated.
 
 @item echoctl
 @itemx ctlecho
@@ -10146,7 +10145,7 @@
 @cindex control characters, using @samp{^@var{c}}
 @cindex hat notation for control characters
 Echo control characters in hat notation (@samp{^@var{c}}) instead
-of literally.  Non-@acronym{POSIX}.  May be negated.
+of literally.  Non-@b{POSIX}.  May be negated.
 
 @item echoke
 @itemx crtkill
@@ -10154,7 +10153,7 @@
 @opindex crtkill
 Echo the @code{kill} special character by erasing each character on
 the line as indicated by the @code{echoprt} and @code{echoe} settings,
-instead of by the @code{echoctl} and @code{echok} settings.  Non-@acronym{POSIX}.
+instead of by the @code{echoctl} and @code{echok} settings.  Non-@b{POSIX}.
 May be negated.
 @end table
 
@@ -10220,7 +10219,7 @@
 
 @item cbreak
 @opindex cbreak
-Same as @option{-icanon}.  May be negated.  If negated, same as
+Same as @code{-icanon}.  May be negated.  If negated, same as
 @code{icanon}.
 
 @item pass8
@@ -10231,23 +10230,23 @@
 
 @item litout
 @opindex litout
-Same as @option{-parenb -istrip -opost cs8}.  May be negated.
+Same as @code{-parenb -istrip -opost cs8}.  May be negated.
 If negated, same as @code{parenb istrip opost cs7}.
 
 @item decctlq
 @opindex decctlq
-Same as @option{-ixany}.  Non-@acronym{POSIX}.  May be negated.
+Same as @code{-ixany}.  Non-@b{POSIX}.  May be negated.
 
 @item tabs
 @opindex tabs
-Same as @code{tab0}.  Non-@acronym{POSIX}.  May be negated.  If negated, same
+Same as @code{tab0}.  Non-@b{POSIX}.  May be negated.  If negated, same
 as @code{tab3}.
 
 @item lcase
 @itemx LCASE
 @opindex lcase
 @opindex LCASE
-Same as @code{xcase iuclc olcuc}.  Non-@acronym{POSIX}.  May be negated.
+Same as @code{xcase iuclc olcuc}.  Non-@b{POSIX}.  May be negated.
 
 @item crt
 @opindex crt
@@ -10275,9 +10274,9 @@
 @cindex disabling special characters
 @kindex u@r{, and disabling special characters}
 For GNU stty, giving a value of @code{^-} or @code{undef} disables that
-special character.  (This is incompatible with Ultrix @command{stty},
+special character.  (This is incompatible with Ultrix @file{stty},
 which uses  a value of @samp{u} to disable a special character.  GNU
-@command{stty} treats a value @samp{u} like any other, namely to set that
+@file{stty} treats a value @samp{u} like any other, namely to set that
 special character to @key{U}.)
 
 @table @samp
@@ -10308,11 +10307,11 @@
 
 @item eol2
 @opindex eol2
-Alternate character to end the line.  Non-@acronym{POSIX}.
+Alternate character to end the line.  Non-@b{POSIX}.
 
 @item swtch
 @opindex swtch
-Switch to a different shell layer.  Non-@acronym{POSIX}.
+Switch to a different shell layer.  Non-@b{POSIX}.
 
 @item start
 @opindex start
@@ -10328,20 +10327,20 @@
 
 @item dsusp
 @opindex dsusp
-Send a terminal stop signal after flushing the input.  Non-@acronym{POSIX}.
+Send a terminal stop signal after flushing the input.  Non-@b{POSIX}.
 
 @item rprnt
 @opindex rprnt
-Redraw the current line.  Non-@acronym{POSIX}.
+Redraw the current line.  Non-@b{POSIX}.
 
 @item werase
 @opindex werase
-Erase the last word typed.  Non-@acronym{POSIX}.
+Erase the last word typed.  Non-@b{POSIX}.
 
 @item lnext
 @opindex lnext
 Enter the next character typed literally, even if it is a special
-character.  Non-@acronym{POSIX}.
+character.  Non-@b{POSIX}.
 @end table
 
 
@@ -10354,12 +10353,12 @@
 @item min @var{n}
 @opindex min
 Set the minimum number of characters that will satisfy a read until
-the time value has expired, when @option{-icanon} is set.
+the time value has expired, when @code{-icanon} is set.
 
 @item time @var{n}
 @opindex time
 Set the number of tenths of a second before reads time out if the minimum
-number of characters have not been read, when @option{-icanon} is set.
+number of characters have not been read, when @code{-icanon} is set.
 
 @item ispeed @var{n}
 @opindex ispeed
@@ -10371,13 +10370,13 @@
 
 @item rows @var{n}
 @opindex rows
-Tell the tty kernel driver that the terminal has @var{n} rows.  Non-@acronym{POSIX}.
+Tell the tty kernel driver that the terminal has @var{n} rows.  Non-@b{POSIX}.
 
 @item cols @var{n}
 @itemx columns @var{n}
 @opindex cols
 @opindex columns
-Tell the kernel that the terminal has @var{n} columns.  Non-@acronym{POSIX}.
+Tell the kernel that the terminal has @var{n} columns.  Non-@b{POSIX}.
 
 @item size
 @opindex size
@@ -10385,13 +10384,13 @@
 @vindex COLUMNS
 Print the number of rows and columns that the kernel thinks the
 terminal has.  (Systems that don't support rows and columns in the kernel
-typically use the environment variables @env{LINES} and @env{COLUMNS}
-instead; however, GNU @command{stty} does not know anything about them.)
-Non-@acronym{POSIX}.
+typically use the environment variables @var{LINES} and @var{COLUMNS}
+instead; however, GNU @file{stty} does not know anything about them.)
+Non-@b{POSIX}.
 
 @item line @var{n}
 @opindex line
-Use line discipline @var{n}.  Non-@acronym{POSIX}.
+Use line discipline @var{n}.  Non-@b{POSIX}.
 
 @item speed
 @opindex speed
@@ -10405,31 +10404,31 @@
 of: 0 50 75 110 134 134.5 150 200 300 600 1200 1800 2400 4800 9600
 19200 38400 @code{exta} @code{extb}.  @code{exta} is the same as
 19200; @code{extb} is the same as 38400.  0 hangs up the line if
-@option{-clocal} is set.
+@code{-clocal} is set.
 @end table
 
 
 @node printenv invocation
-@section @command{printenv}: Print all or some environment variables
+@section @file{printenv}: Print all or some environment variables
 
 @pindex printenv
 @cindex printing all or some environment variables
 @cindex environment variables, printing
 
-@command{printenv} prints environment variable values.  Synopsis:
+@file{printenv} prints environment variable values.  Synopsis:
 
 @example
 printenv [@var{option}] [@var{variable}]@dots{}
 @end example
 
-If no @var{variable}s are specified, @command{printenv} prints the value of
+If no @var{variable}s are specified, @file{printenv} prints the value of
 every environment variable.  Otherwise, it prints the value of each
 @var{variable} that is set, and nothing for those that are not set.
 
-The only options are a lone @option{--help} or @option{--version}.
+The only options are a lone @code{--help} or @code{--version}.
 @xref{Common options}.
 
-@cindex exit status of @command{printenv}
+@cindex exit status of @file{printenv}
 Exit status:
 
 @display
@@ -10440,13 +10439,13 @@
 
 
 @node tty invocation
-@section @command{tty}: Print file name of terminal on standard input
+@section @file{tty}: Print file name of terminal on standard input
 
 @pindex tty
 @cindex print terminal file name
 @cindex terminal file name, printing
 
-@command{tty} prints the file name of the terminal connected to its standard
+@file{tty} prints the file name of the terminal connected to its standard
 input.  It prints @samp{not a tty} if standard input is not a terminal.
 Synopsis:
 
@@ -10468,7 +10467,7 @@
 
 @end table
 
-@cindex exit status of @command{tty}
+@cindex exit status of @file{tty}
 Exit status:
 
 @display
@@ -10499,14 +10498,14 @@
 
 
 @node id invocation
-@section @command{id}: Print real and effective uid and gid
+@section @file{id}: Print real and effective uid and gid
 
 @pindex id
 @cindex real uid and gid, printing
 @cindex effective uid and gid, printing
 @cindex printing real and effective uid and gid
 
-@command{id} prints information about the given user, or the process
+@file{id} prints information about the given user, or the process
 running it if no user is specified.  Synopsis:
 
 @example
@@ -10520,7 +10519,7 @@
 Each of these numeric values is preceded by an identifying string and
 followed by the corresponding user or group name in parentheses.
 
-The options cause @command{id} to print only part of the above information.
+The options cause @file{id} to print only part of the above information.
 Also see @ref{Common options}.
 
 @table @samp
@@ -10541,14 +10540,14 @@
 @opindex -n
 @opindex --name
 Print the user or group name instead of the ID number.  Requires
-@option{-u}, @option{-g}, or @option{-G}.
+@code{-u}, @code{-g}, or @code{-G}.
 
 @item -r
 @itemx --real
 @opindex -r
 @opindex --real
 Print the real, instead of effective, user or group id.  Requires
-@option{-u}, @option{-g}, or @option{-G}.
+@code{-u}, @code{-g}, or @code{-G}.
 
 @item -u
 @itemx --user
@@ -10562,7 +10561,7 @@
 
 
 @node logname invocation
-@section @command{logname}: Print current login name
+@section @file{logname}: Print current login name
 
 @pindex logname
 @cindex printing user's login name
@@ -10572,41 +10571,41 @@
 @flindex /etc/utmp
 @flindex utmp
 
-@command{logname} prints the calling user's name, as found in the file
+@file{logname} prints the calling user's name, as found in the file
 @file{/etc/utmp}, and exits with a status of 0.  If there is no
-@file{/etc/utmp} entry for the calling process, @command{logname} prints
+@file{/etc/utmp} entry for the calling process, @file{logname} prints
 an error message and exits with a status of 1.
 
-The only options are @option{--help} and @option{--version}.  @xref{Common
+The only options are @code{--help} and @code{--version}.  @xref{Common
 options}.
 
 @exitstatus
 
 
 @node whoami invocation
-@section @command{whoami}: Print effective user id
+@section @file{whoami}: Print effective user id
 
 @pindex whoami
 @cindex effective UID, printing
 @cindex printing the effective UID
 
-@command{whoami} prints the user name associated with the current
+@file{whoami} prints the user name associated with the current
 effective user id.  It is equivalent to the command @samp{id -un}.
 
-The only options are @option{--help} and @option{--version}.  @xref{Common
+The only options 