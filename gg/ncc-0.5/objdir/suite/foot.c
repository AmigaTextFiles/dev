#include <stdio.h>
#include <string.h>

int x, *y, d [32];

int foobar (int x, int y);

typedef int *zoo;

zoo pa, *faa;

struct foo {
	int x, y;
	int **z[24];
	zoo asa [12] [64];
	struct hoola_hoop hh;
} pollartek, *ppoo;

typedef struct hoola_hoop {
	int akja, aks;
} foo;

typedef foo **ctdef;

ctdef cocacola [32];

ctdef funcar (void);


int main ()
{
	char *fname;
	int i;
	
	if (!*fname || !strcmp (fname, "-")) i=0;
}