
int sboom ()
{


	int x, y, i, z;
	float f, ff, fff;
	int *xp, **xpp;
	int *cp;
	char *ccp;

	f + (float) (3+1);
	x + (float) (int) 1.01;
	x + (float) (int) f;

	(short*) x + 10;
	ccp + 10;

	x*y/z;
	x==1 || y==2 || x==20;
	x - x + y * f / (fff + ff + i * z) + (xp - cp);
	x + y*i;
	x+y;
	x + y * f;
	f==0.1 || i!=32 && i==1.0;
	f || z;
//	x + 0.1 + *xp + **xpp;
	f + 0.1;	
	i == 1.0;
	f == 0.1;

	xp - 1;
		
//	1 + *(*xpp + 1);
	x==10|| y==0&&x==1 || x!= 12;
	
	xp + 1;

}

#if 0
struct tzoo { int x; };

int lool ()
{
	int x, y[20], e, i;
	struct lala {
		int x [27];
		struct {
			char y [12];
		} info;
	} o, *ep;
	
	sizeof x;
	sizeof y;
	sizeof y[0];
	sizeof o;
	sizeof o.x;
	sizeof ep->x;
	sizeof ep->x[0];
	sizeof ep->info.y;
	sizeof o.info.y[3];

	
	x + y[1] + x * e / i;
x * i + e * i;
	if (3 < 1) x=2;
	
}

int lool ();
#endif
#if 0
int kkfoo ()
{
struct barra;
	x = 1;
	x = (int)2;
	x = (int*)2;
	x = (struct barra*) 2;


	foo (x + y, x = 3);
	-1, ((void*)0), 1;
	foobar (-1, ((void*)0), 1);
	
	waitpid (-1, ((void*) 0), 1) > 0;
	x [z.e [1<<12]] = ~4 + 1<<12;
	x [43/2-1];
	x = 43+43+ 43;
	x [y [z]];
	foo (32+32, 12-15%6, 12/10);
	if (x) x = 0;
	else return;

	y = (char*)4 + 2 + x;
	foo (1+2, 2+3, 3+4*2);
	y = z [32+43] = (int*) x.e = (32-21 != 32-21) ? 1 : 0;
	
	x = (float) y;

	x = (float)1 + (float)2;

;;;;;;;;;;;;
	x = 3 + (1, 2, 3);

}
#endif
#if 0
int bar [] = { 1, 2, 3, 4 };
typedef struct { int x, y; } hoo;

hoo loo [] = { 1, 2, 3, 4 };

int x;

int foo (int y, int z)
{
	int e;
	struct e {
		int x, y, z, f, e;
	} papahoo;
	
	if (x) {
		int f;
		f = e + x * y [z] + sizeof (int**);
	}
}

struct foo {
	int a, b, c;
};
int bara (int x, void *y, struct foo *hoola)
{
sizeof (int) + sizeof (long long) + sizeof (long int)
+ sizeof (float) + sizeof (int *[20]);
}

int lolal ()
{
	bara (x, y, e*d, foo (3, x[27]++), (t=3, t + 2));
return xaxax;

}

int zpp (void, char*);

extern int taa [5];

int taa [] = { 1, 2, 3, 4, 5 };

typedef int (*ff) (int, char[], int);

void f1 (void);

void f1 (void)
{
	t = 2, t + 3;
	x = y;
}

int (*f) (void);

#endif
