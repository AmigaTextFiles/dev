.KEY MUINTFILE
.BRA {
.KET }

;     SCRIPT FILENAME: L.LV.scp            
;     DATE           :  
;     AUTHOR         :
;     DESCRIPTION    : Executes a MUInt .def file   


;====================================================================
;  Do some directory changing to keep the PATH straight
;====================================================================

IF EXISTS ENV:LV

     SET homedir `CD`
     SET localdir `pathpart  {muintfile}`
     
     CD $localdir
     
     EXECUTE {muintfile}
     
     CD $homedir

     QUIT

ELSE

     rtEZrequest "ERROR:" "You must select something from the ListView"
     QUIT

ENDIF
