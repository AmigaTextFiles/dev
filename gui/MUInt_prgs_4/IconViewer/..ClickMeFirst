IF EXISTS MUI:

  IF $KICKSTART EQ "39.106"
     ADVICE "IconViewer" ICONVIEWER.TXT 
  ELSE
     RTEZREQUEST TITLE "ERROR MESSAGE:" BODY "You must have KICKSTART 39.106 (3.0)\nor greater to use IconViewer"
  ENDIF

ELSE

  RTEZREQUEST TITLE "ERROR MESSAGE:" BODY "You must have MUI installed to use\nIconViewer"

ENDIF

;DOESN'T CHECK FOR WB 3.1 