alias endcli "echo The Git Desktop shell cannot be exited manually"
alias endshell "echo The Git Desktop shell cannot be exited manually"

alias ll "list"

Prompt "%S> "

; GG environment setup
if exists SDK:Startup NOREQ
	assign gg: SDK:GG
	assign bin: gg:bin
	path gg:bin add
endif
