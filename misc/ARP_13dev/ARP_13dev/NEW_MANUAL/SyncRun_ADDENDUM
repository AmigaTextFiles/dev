


     SyncRun(39.0)	  ARP Programmers Manual	 SyncRun(39.0)



     ADDENDUM FOR
	  SyncRun -- Run a process and wait for	its completion.

     FUNCTION
	  In V39.0 of ArpLib, the SyncRun() function has been revised
	  so that it will work with BCPL programs, and it may also
	  be called from a WorkBench process, which will create a CLI
	  environment for the program being run and then will wait for
	  the launched program terminate.
