
class stringArray
{
    Object class system.mem speicher

    long MaxIndex

    public getAddr()
     {
        long mem
        
        mem=speicher.getAddr()
        
        return mem
          
     }

    public getString(long Number)
     {
      
       long returnvalue

       if number<0
        {
            return -1
        }

       returnvalue=speicher.getAddr()
       returnvalue==returnvalue[number]

       return returnvalue
       
     }

    public setString(long Number,string StringObject)
     {
       long returnvalue
       
       if StringObject=-1
        {
          return 0
        }
       if number>maxindex
        {
          return 0
        }
       if number<0
        {
          return 0
        }

       returnvalue=speicher.getAddr()
       returnvalue[number]==StringObject
       
       return 1
     }

    public addString(string StringObject)
     {
       long returnvalue number
       
       if StringObject=0
        {
          return 0
        }

       number==MaxIndex*4+4
       speicher.getMem(Number) 
       returnvalue=speicher.getAddr()
       returnvalue[MaxIndex]==StringObject
       MaxIndex==MaxIndex+1
       return MaxIndex
     }

    public getSize()
     {
       return MaxIndex
     }
    
    public clearArray()
    {
       long mem I 

       mem=speicher.getAddr()

       I==0

       Repeat

        mem[I]==0
        I==I+1

       until i=MaxIndex
    }


    public DeConstructor()
    {
       long mem I 
       Object void
        
       mem=speicher.getAddr()

       I==0

       Repeat

        void==mem[I]
        mem[I]==0
        I==I+1
        del(void)

       until i=MaxIndex
    }
       
}
