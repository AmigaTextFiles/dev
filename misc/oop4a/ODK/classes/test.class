
/*
                                   Testclass 

                                - StringArray -

                 This software is part of the ODK distribution.

    (c) 2001-2002 Cyborg

    This software is provided as is. No warrenty of any kind.

    It`s free to use while it`s usage is private only. 
    
*/    

    

class test
{

    // 

    object class system.stringarray array
    object class system.system system
    object class com.amiga.system.doswrapper dos

    public Main()
    {
        object String1 String2 String3
        long Index mem mem1 mem2 mem3

        Index=array.getSize()
        dos.printf("Einträge=%ld\n",Index)

        String1=new("system/string")
        String2=new("system/string")
        String3=new("system/string")
        
        String1.addString("String1")
        String2.addString("String2")
        String3.addString("String3")
        
        Index=array.addString(String1)
        dos.printf("String1 inserted. New index = %ld\n",Index)
        Index=array.addString(String2)
        dos.printf("String2 inserted. New index = %ld\n",Index)
        Index=array.addString(String3)
        dos.printf("String3 inserted. New index = %ld\n",Index)
        Index=array.getSize()
        dos.printf("MaxIndex %ld\n",Index)
        
        mem=array.getAddr()
        
        mem->(mem1,mem2,mem3)

        dos.printf("String=$%lx $%lx $%lx $%lx\n",mem,mem1,mem2,mem3)

        mem1=array.getString(0)
        mem2=array.getString(1)
        mem3=array.getString(2)

        system.println(mem1)
        system.println(mem2)
        system.println(mem3)
        
    }
}