.key PATTERN/A,DESTDIR/A
.bra {
.ket }

; This script can convert many AIFF files to Studio 16 files based
; on pattern matching.
;
; $VER: ConvertAIFF2Studio 1.0 (11.11.97)
; By Kenneth "Kenny" Nilsen (kenny@bgnett.no)
;
; USAGE: ConvertAIFF2Studio <pattern> <destdir/>
;
; NOTE: The commands must be in the command search path. To be safe
; copy the commands in C: or add the path where you keep the commands to
; the global search path (startup-sequence: path <path> ADD).

;-- convert files

list {pattern} files lformat="echo *"Processing file '%N'...*"*nAIFF2Studio %P%N {DESTDIR}%M" >t:tmp.16

echo "Converting AIFF files to Studio 16 files.. please wait!"

execute t:tmp.16
if WARN
	echo "Error occured while converting files!"
	endif

echo "Done"

lab Final
