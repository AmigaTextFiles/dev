# makefile for GNU make (automatically generated by makemake)
# NOTE: Command lines in GNU makefiles must start with a TAB !

EXE = MapTest.exe

all : $(EXE) 

example.o : example.c
	gcc $(shell gccprefs) -c -o example.o example.c

$(EXE) : example.o
	gcc $(shell gccprefs) -o $(EXE) example.o
