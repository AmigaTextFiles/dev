# 

SHELL=/bin/sh

prefix=/usr/local
exec_prefix=${prefix}
subdirs= cobpp

all:
	@for i in ${subdirs}; do \
		echo Making all in $$i ; \
		(cd $$i; ${MAKE} ) ; \
	done
#		(cd $$i; ${MAKE} all) ; 

clean:
	@for i in ${subdirs}; do \
		echo Cleaning in $$i ; \
		(cd $$i; ${MAKE} clean) ; \
	done

