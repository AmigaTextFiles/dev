/*   $VER: FetchQuit.amitex 1.0 (Samedi 15 Février 1997)
**
**   Script ARexx pour quitter FetchRefs.
*/

signal on syntax

if show(p, "FETCHREFS") then do
    ADDRESS 'FETCHREFS'
    FR_QUIT
end
exit

/* Traitement des erreurs, interruption du programme */
syntax:
erreur=RC
'MESSAGE("Erreur de syntaxe"+CHR(10)+"en ligne 'SIGL'"+CHR(10)+"'errortext(erreur)'")'
exit
