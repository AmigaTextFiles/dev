
All: Benchmark BusyCPU BusyDISK

BenchMark: Benchmark.c
	sc BenchMark.c link lib=tdlib:toolbox.lib math=s opt opttime nodebug\
		optinline CPU=68000

BusyCPU: BusyCPU.c
	sc BusyCPU.c link opt opttime nodebug optinline CPU=68000

BusyDISK: BusyDISK.c
	sc BusyDISK.c link opt opttime nodebug optinline CPU=68000

cleanup:
	delete Benchmark.(lnk|o) BusyCPU.(lnk|o) BusyDisk.(lnk|o)