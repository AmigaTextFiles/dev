CC=ppc-amigaos-gcc
CFLAGS= -D__USE_INLINE__ -O2 -Wall
LDFLAGS= -lauto
NAME=spatch.os4

$(NAME) : $(NAME).db
	strip -R.comment $< -o $@

$(NAME).db: spatch.c
	$(CC) $(CFLAGS) -o $@ $<

archive: $(NAME)
	-delete spatch-68k.lnk spatch.o
	lha -r u RAM:spatch // spatch
