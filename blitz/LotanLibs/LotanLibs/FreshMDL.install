; FreshMDL Installer Script

(set @default-dest
    (askdir
        (prompt   "Where would you like to copy FreshMDL ?")
        (help     @askdir-help)
        (default  "C:")
    )
)

(copyfiles (source "Tools/FreshMDL") (dest @default-dest) )

(exit)
