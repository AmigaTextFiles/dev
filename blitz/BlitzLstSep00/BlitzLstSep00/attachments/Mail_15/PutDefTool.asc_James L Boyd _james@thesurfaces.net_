
; change filename below - make sure your file actually HAS
; a default tool first!

d$="ram:rogobox.guide" ; default tool of this is "sys:utilities/more"

*do.DiskObject=GetDiskObject_(&d$) ; or GetDiskObjectNew_ ()

If *do

  ; dt$=Peek$(Peek.l(*do+50))
  dt$="SYS:Utilities/Multiview"

  Poke$ *do+50,dt$

  PutDiskObject_ &d$,*do

  FreeDiskObject_(*do)

Else Request "","Didn't get icon...","OK"
EndIf

NPrint dt$

MouseWait

End

