WBStartup

XINCLUDE "blitzincs:libraries/openurl.bb2"

DEFTYPE.Library *OpenURLBase

*OpenURLBase=OpenLibrary_("openurl.library",0)

If *OpenURLBase=0
 Print "Unable to open openurl.library"+Chr$(10)+"Is it installed?"+Chr$(10)
 Goto endit
EndIf

InitTagList 1,2

Use TagList 1

AddTags #URL_Launch,True
AddTags #URL_Show,True

If NumPars<1
  Print "No URL supplied"+Chr$(10)
  Goto endit
EndIf

Print Par$(1)+Chr$(10)

url$=Par$(1)

If URL_OpenA_(&url$,TagList(1))
  Print "Sent URL to browser"+Chr$(10)
Else
  Print "Couldn't send URL"+Chr$(10)+"Are your prefs set properly?"+Chr$(10)
EndIf


.endit

If *OpenURLBase<>0
CloseLibrary_(*OpenURLBase)
EndIf

End


