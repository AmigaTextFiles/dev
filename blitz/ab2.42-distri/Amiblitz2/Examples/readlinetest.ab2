If ReadFile(0,"s:user-startup")
Repeat
     FileInput 0
     Runerrsoff ;need because no window is open (for debugger only )
     a$=Edit$(200)
     Runerrson  ;need because no window is open (for debugger only)
     NPrint a$
Until  Eof(0)<>0
CloseFile 0
End If
MouseWait

