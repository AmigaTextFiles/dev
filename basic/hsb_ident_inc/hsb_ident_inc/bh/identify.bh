' identify BASIC header

REM $INCLUDE identify.bc
LIBRARY DECLARE "identify.library"

DECLARE FUNCTION IdExpansion& LIBRARY		'(VARPTR(TagList))
DECLARE FUNCTION IdHardware& LIBRARY		'(Type&, VARPTR(TagList))
DECLARE FUNCTION IdAlert& LIBRARY			'(ID&, VARPTR(TagList))
DECLARE FUNCTION IdFunction& LIBRARY		'(SADD(LibName$), Offset&, VARPTR(TagList))
DECLARE FUNCTION IdHardwareNum& LIBRARY		'(Type&, VARPTR(TagList))
DECLARE SUB IdHardwareUpdate LIBRARY
