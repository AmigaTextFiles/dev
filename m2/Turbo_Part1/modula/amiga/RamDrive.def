DEFINITION FOR AMIGALIB MODULE RamDrive ;

FROM SYSTEM IMPORT STRING, ADDRESS ;

VAR
  RamdriveDevice : ADDRESS ;

(*--- functions in V34 or higher (Release 1.3) ---*)

PROCEDURE KillRAD0( ) : STRING ;

(*--- functions in V36 or higher (Release 2.0) ---*)

PROCEDURE KillRAD( unit : LONGINT ) : STRING ;

END RamDrive.
