DEFINITION FOR C MODULE OpenLib ;

PROCEDURE OpenLib( name : STRING ; version : LONGINT ) : ADDRESS ;
(* Like the Exec version but the library is automatically closed at program  *)
(* exit.								     *)

END OpenLib.
