DEFINITION MODULE Template;

FROM Intuition IMPORT ScreenPtr, WindowPtr, MenuPtr;

VAR
   TheScreen  : ScreenPtr;
   TheWindow  : WindowPtr;
   TheMenu    : MenuPtr;

   PROCEDURE CreateScreenWindowAndMenus () : BOOLEAN;
 
   PROCEDURE ProcessIntuiMessages (VAR finished : BOOLEAN);
 
   PROCEDURE DestroyScreenWindowAndMenus;

END Template.
