Compiler Version.: 2.1b
Art .............: Wieviele Smiley-Gesichter kann man zeichnen
Autor ...........: Daniel van Gerpen


    Beschreibung von Side-Faces
    ---------------------------

    Ihr kennt Sie doch, die vielen Smilys (:-)) die so in den
    Schreiben stehen, die wir uns alle so zuschicken.

    Also was kann man daraus machen? Richtig, ein Programm in E schreiben,
    daß ALLE möglichen Kombinationen, die es gibt, einmal ausdruckt.

    So ein Programm habe ich noch nicht gesehen.

    Über Sinn oder Unsinn möchte ich mich mit keinem streiten. Ich
    finde das Ding aber wirklich toll. Nur was sagen viele der kleinen
    Gesichter aus? Vielleicht wagt sich ja mal einer an eine Beschreibung
    ran.

    Jörg.

    Side-Faces
    ----------

Zeigt dir die wundervolle Welt der 8)'s,ß^o's und :^}'s...

Anwerfen, sehen, staunen .....

Wer schaft mehr als 210 (!!) Stück???



DEF i,j,k,c=0

PROC main()
 FOR i:=1 TO 5
    FOR j:=1 TO 3
          FOR k:=1 TO 14
           draw()
          ENDFOR
    ENDFOR
 ENDFOR
ENDPROC


PROC draw()
   SELECT i
    CASE 1;   WriteF(':')
    CASE 2;   WriteF('8')
    CASE 3;   WriteF(';')
    CASE 4;   WriteF('|')
    CASE 5;   WriteF('ß')

   ENDSELECT
   SELECT j
    CASE 1;   WriteF('-')
    CASE 2;   WriteF('')
    CASE 3;   WriteF('^')
   ENDSELECT
   SELECT k
    CASE 1;   WriteF(')  ')
    CASE 2;   WriteF('(  ')
    CASE 3;   WriteF('|  ')
    CASE 4;   WriteF('>  ')
    CASE 5;   WriteF('<  ')
    CASE 6;   WriteF('}  ')
    CASE 7;   WriteF('{  ')
    CASE 8;   WriteF(']  ')
    CASE 9;   WriteF('[  ')
    CASE 10;  WriteF('+  ')
    CASE 11;  WriteF('*  ')
    CASE 12;  WriteF('X  ')
    CASE 13;  WriteF('C  ')
    CASE 14;  WriteF('7  ')
   ENDSELECT
 IF c++>3
    WriteF('\n\n')
    c:=0
 ENDIF
ENDPROC

