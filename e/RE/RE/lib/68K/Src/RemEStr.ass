; a1 - estring

	xdef	_RemEStr

_RemEStr	move.l	a6,-(a7)
	moveq	#0,d0
	move.w	(-4,a1),d0	; block size
	addq.l	#4,d0
	subq	#4,a1		; real memory block address
	movea.l	$4.w,a6
	jsr	(-210,a6)		; FreeMem
.finish	move.l	(a7)+,a6
	rts
