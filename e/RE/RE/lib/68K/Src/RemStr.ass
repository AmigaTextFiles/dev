; a1 - NewStr() allocated string

	xdef	_RemStr

_RemStr	movem.l	d1/a6,-(a7)
	moveq	#0,d0
	move.w	(-2,a1),d0	; block size
	addq.l	#2,d0
	subq	#2,a1		; real memory block address
	movea.l	$4.w,a6
	jsr	(-210,a6)		; FreeMem
.finish	movem.l	(a7)+,d1/a6
	rts
