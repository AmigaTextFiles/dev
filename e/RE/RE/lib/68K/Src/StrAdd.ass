; a0 - dest str
; a1 - src str
; d0 - length=-1

	xdef	_StrAdd

_StrAdd
	move.l	a0,d1
.find	tst.b	(a0)+		; find end
	bne.s	.find
	subq.l	#1,a0		; jump on zero byte
.loop	move.b	(a1)+,(a0)+	; add str
	beq.s	.finish
	dbf.s	d0,.loop
	clr.b	(-1,a0)
.finish	move.l	d1,d0
	rts
