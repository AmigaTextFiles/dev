/* This document describes the current Audiostream file support*/

--------------
SAMPLE FORMATS
--------------
<import>:

IFF-8SVX	PCM8 signed only, extracts only sample data, ONLY MONO AT 
		THIS POINT!
IFF-16SV	PCM16 signed only, extracts only sample data, ONLY MONO AT 
		THIS POINT!
IFF-AIFF	PCM8,PCM16 signed, mono, stereo (2chan max)
IFF-MAUD	PCM8 unsigned, PCM16 signed, mono, stereo
RIFF-WAVE	PCM8 unsigned, PCM16 signed low-endian,mono,stereo (2chan max)		
RAW		PCM8,PCM16 signed only,mono,stereo


<export>: