##stringtype E
OPT MODULE

MODULE 'class/catalog'

-> Generated automatically with %fv from "%f0"
EXPORT OBJECT cat_%b OF catalog_obj; ENDOBJECT

EXPORT CONST %i=%d

EXPORT PROC open(name=NIL, lang=NIL, locale=NIL) OF cat_%b
  SUPER self.open(
    IF name THEN name ELSE '%b.catalog',
    IF lang THEN lang ELSE %l,
    locale
  )
  SUPER self.def([
    %d, %s%(,)
  ])
ENDPROC
