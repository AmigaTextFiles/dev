This is a program to list disk files that don't have their archive
bits set.  unrkv ? for usage.  Criticism welcome.  Not sure what the
group will decide on encoding/archiving/ascii, but this one is 1/3
smaller this way, so here goes.
