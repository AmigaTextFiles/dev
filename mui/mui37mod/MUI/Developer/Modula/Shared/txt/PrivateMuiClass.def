DEFINITION MODULE 低lassNameClass; (* 06-Jan-96 *)

(* Written by S霵ke Tesch.
 * Got suggestions or problems? Contact me at <soenke.tesch@elmshorn.netsurf.de>
 *)

(* replace.......with
 * 低lassName  = name of this class
 * 低lassVar   = name of the VAR that contains this classes address
 *
 * Both will have "Class" appended.
 * So if you replace 低lassName with Test and 低lassVar with test this modules`
 * name will be TestClass and the IClassPtr for it will be testClass.
 *)

FROM IntuitionD IMPORT IClassPtr
;

VAR 低lassVarClass : IClassPtr;


(* your tags and methods
 * Both start at 80430000H, MUI uses values between 80420000H and 8042FFFFH.
 * I normaly use these TYPEs since they are faster/easier to define and import
 * than CONSTants. However, you`ll have to CAST them to LONGCARD/LONGINT if
 * you use a Set() or Get().
 *)

TYPE 低lassNameTags = (低lassNameTagFirst:=080430000H,
                       低lassNameTagLast
                      );

TYPE 低lassNameMethods = (低lassNameMethodFirst:=08043F000H,
                          低lassNameMethodLast
                         );

(* init/dispose our private class
 * To use your private class, first call Create低lassNameClass(). After that,
 * create objects with intuition.NewObjectA(低lassVar,NIL,tags..).
 * On application exit, call Delete低lassNameClass() after mDisposeObject().
 *)

(* 06-Jan-96----------------------------------------------------------------------- *)
PROCEDURE Create低lassNameClass();

(* 06-Jan-96----------------------------------------------------------------------- *)
PROCEDURE Delete低lassNameClass();

END 低lassNameClass.def
