;
; AmigaDOS script script to change all MUI autodocs to HTML
; Output will be sent to RAM:
;

;
; build a list of all MUI autodocs and convert them
;
list >ram:xme MUI:Developer/Autodocs/#?.doc LFORMAT="mui2html %s%s RAM:%s.html"
execute ram:xme
delete ram:xme

;
; print message about file names
;
echo " "
echo "Script completed."
echo "Note that the filenames are goofy.  You will need to rename files from"
echo "#?.doc.html to #?.html, if you choose.  Some command shells make this"
echo "easier than others. ;-)"
echo " "
echo "Directory Opus makes it easiest of all!!!  Can you say 'GUI'?"
echo " "

; end
Ask "Hit RETURN to exit>"
