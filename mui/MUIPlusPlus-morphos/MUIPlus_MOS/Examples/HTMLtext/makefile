# makefile for GNU make (automatically generated by makemake)
# NOTE: Command lines in GNU makefiles must start with a TAB !

EXE = testhtm 

all : $(EXE) 

HTMLtext.o : HTMLtext.cpp 
	gcc $(shell gccprefs) -noixemul -c -o HTMLtext.o HTMLtext.cpp 

$(EXE) : HTMLtext.o 
	gcc  $(shell gccprefs) -noixemul -o $(EXE) HTMLtext.o muipp.a
