# makefile for GNU make (automatically generated by makemake)
# NOTE: Command lines in GNU makefiles must start with a TAB !

EXE = TNListView 

all : $(EXE) 

TNListview.o : TNListview.cpp SDI_hook.h SDI_compiler.h 
	g++ $(shell gccprefs) -noixemul -c -o TNListview.o TNListview.cpp

$(EXE) : TNListview.o 
	g++ $(shell gccprefs) -noixemul -o $(EXE) TNListview.o muipp.a
