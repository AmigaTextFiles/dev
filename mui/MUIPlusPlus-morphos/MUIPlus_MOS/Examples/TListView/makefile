# makefile for GNU make (automatically generated by makemake)
# NOTE: Command lines in GNU makefiles must start with a TAB !

EXE = TListview 

all : $(EXE) 

# Warning: SDI_hook.h can not be located !
TListview.o : TListview.cpp 
	g++ $(shell gccprefs) -noixemul -frepo -c -o TListview.o TListview.cpp

$(EXE) : TListview.o 
	g++ $(shell gccprefs) -noixemul -frepo -o $(EXE) TListview.o muipp.a
