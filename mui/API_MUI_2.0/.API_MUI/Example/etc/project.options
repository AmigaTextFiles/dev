; project configuration (do not delete)

(compiler

    (alias "gcc/classic/stable")

    (makefile "makefiles/gcc-classic-stable/makefile")

    (make "make -f %makefile %target")

    (executable "bin/gcc-classic-stable/Example")

    (stack "65536")

    (target "dist")

    (options

        "-Wall"

        "-O2"

        "-noixemul"

        "-m68020"
    )
)

(compiler

    (alias "gcc/morphos/stable")

    (options

        "-Wall"

        "-O2"

        "-noixemul"

        "-mcpu=powerpc"
    )

    (makefile "makefiles/gcc-morphos-stable/makefile")

    (make "make -f %makefile %target")

    (executable "bin/gcc-morphos-stable/Example")

    (stack "65536")

    (target "strip")
)

; project configuration (do not delete)

(compiler

    (alias "gcc/classic/stable")

    (makefile "makefiles/gcc-classic-stable/makefile")

    (make "make -f %makefile %target")

    (executable "bin/gcc-classic-stable/Example")

    (stack "65536")

    (target "")

    (options

        "-Wall"

        "-O2"

        "-noixemul"

        "-m68020"
    )
)

(compiler

    (alias "gcc/morphos/stable")

    (options

        "-Wall"

        "-O2"

        "-noixemul"

        "-mcpu=powerpc"
    )
)

(compiler

    (alias "gcc/morphos")

    (options

        "-Wall"

        "-O2"

        "-noixemul"

        "-mcpu=powerpc"
    )

    (makefile "makefiles/gcc-morphos/makefile")

    (make "make -f %makefile %target")

    (executable "bin/gcc-morphos/Example")

    (stack "65536")

    (target "")
)

; project configuration (do not delete)

(compiler

    (alias "gcc/classic/stable")

    (makefile "makefiles/gcc-classic-stable/makefile")

    (make "make -f %makefile %target")

    (executable "bin/gcc-classic-stable/Example")

    (stack "65536")

    (target "")

    (options

        "-Wall"

        "-O2"

        "-noixemul"

        "-m68020"
    )
)

(compiler

    (alias "gcc/morphos/stable")

    (options

        "-Wall"

        "-O2"

        "-noixemul"

        "-mcpu=powerpc"
    )

    (makefile "makefiles/gcc-morphos-stable/makefile")

    (make "make -f %makefile %target")

    (executable "bin/gcc-morphos-stable/Example")

    (stack "65536")

    (target "")
)

; project configuration (do not delete)

(compiler

    (alias "gcc/classic/stable")

    (makefile "makefiles/gcc-classic-stable/makefile")

    (make "make -f %makefile %target")

    (executable "bin/gcc-classic-stable/Example")

    (stack "65536")

    (target "")

    (options

        "-Wall"

        "-O2"

        "-noixemul"

        "-m68020"
    )
)

(compiler

    (alias "gcc/morphos/stable")

    (options

        "-Wall"

        "-O2"

        "-noixemul"

        "-mcpu=powerpc"
    )
)

(compiler

    (alias "gcc/morphos")

    (options

        "-Wall"

        "-O2"

        "-noixemul"

        "-mcpu=powerpc"
    )
)
