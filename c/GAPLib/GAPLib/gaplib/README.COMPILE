
To compile manually, perform the following steps:

1) Compile and link mkver.c

2) Run mkver like this : "mkver c >VString"

3) Compile but do not link all other .c files except "increv.c".

4) Add the object files created in (3) to the a library with a suitable tool
   like ar (unix).

