Q:

The Page Tabs are draw wrong.

A:

ALl Storm Wizard libs need at least in hgroup VBorder 3 and in BVOffset 1 for the group that
draw the Tab

Q:

On AFA the GUI look wrong

A:

You have zune_lib.exe not installed or your zune_lib.exe Version is too old.
If you dont want install it, you must create the envvar wizardstyle with content system,
so the new features are not support
