#!/bin/sh
#$(SHLIB.cc) -o $@ $^ $(LIBS)

shift
lib=$1
shift

files=" "

for i in $*; do
  case $i in
    *.o) files="${files} $i";;
    *) ;;
  esac
  shift
done
rm -f ${lib}
ar rcv ${lib} ${files}
ranlib ${lib}
