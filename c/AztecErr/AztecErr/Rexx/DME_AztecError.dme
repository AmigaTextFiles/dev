# Dies ist ein Beispiel für die Benutzung von AztecErr von DME aus
map (sA-n) ( RX "address AZTECERROR next" )
map (sA-p) ( nexterr p )
map (sA-f) ( nexterr f )
map (sA-l) ( nexterr l )
map (sA-q) ( nexterr q )
map (sA-b) ( nexterr b )
