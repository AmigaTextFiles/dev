
cmple of two constants, determine at compile time
change exit code back to 0 from 1
move $5,$5 can be removed
Dead code eliminator
Constant folding *before* reg-alloc but after copy propogate

see OPTIMIZATION_TODO

add "free"'s back to reg_alloc

Convert samples to use just 32 bits, to make gcc comparison more fair.

minimal preparser, at least #include and #define

Keep track of char constants and pass them to the assembler as is.

char bob[]="Hello World\n";

pointers

boolean expressions

fix sizeof()

proper pre/postfix support


load store just one value, with new "size" field?
