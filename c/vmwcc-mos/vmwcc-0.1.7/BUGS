
+ We use parameter regs as spill regs.  Fix it to auto allocate.
+ We possibly put parameters on the stack in reverse order.
+ "bm0.c" causes the register allocator to get stuck somehow.

+ linking against C library, works fine printing to stdout
  but redirecting to file the output gets lost??
  
